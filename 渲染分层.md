你观察到的“整个纹理变白”问题，根源确实是渲染层次不分，所有效果混乱叠加。要实现工业级的分层互不干扰，你需要把颜色计算拆解为基础色、光照、边缘光、扰动等独立层，最后用合理的混合方式组合。下面给出具体分析和修改方案。

一、当前代码的问题
在你的 FluidSimulatorAdapter.ts 片段着色器中：

glsl
vec3 color = baseColor * diff;               // 漫反射结果
color += flowColor;                          // 流动扰动直接累加
color += edgeColor * edgeIntensity;          // 边缘光直接累加
弊端：

所有效果直接累加到 color，导致颜色越来越亮，整体发白。

边缘光使用了固定的亮色 vec3(0.3,0.5,0.8)，在界面处显著提高亮度。

流动扰动也是固定颜色累加，没有受光照影响，显得不自然。

高光部分虽然算出来了，但未与漫反射分离，无法单独控制强度。

二、分层渲染的正确做法
按照物理正确的渲染管线，应该这样分层：

层名	内容	混合方式	说明
Base Color	水体的基础颜色（不受光照影响）	直接输出	通常使用无光照的颜色纹理，但这里可以用深度渐变
Diffuse Lighting	漫反射（法线与入射光夹角）	添加到 Base	决定水体明暗，但不应该添加额外颜色
Specular Lighting	高光（镜面反射）	最后添加	通常白色或菲涅尔相关
Emissive / Edge Glow	自发光/边缘光	最后叠加	不应该参与漫反射，单独控制强度
Detail (Flow)	流动扰动	作为细节噪点	不应该大面积改变色调
最终颜色 = BaseColor × (Ambient + Diffuse) + Specular + Emissive + Detail

而且每一步的强度都应该是可独立调节的参数。

三、修改 FluidSimulatorAdapter.ts 的片段着色器
下面给出一个完全重写的片段着色器，实现清晰的分层：

glsl
uniform sampler2D phiTex;
uniform sampler2D velTex;
uniform float time;
uniform vec2 resolution;
uniform vec3 lightDir;
uniform vec3 waterColor;
uniform vec3 deepColor;
uniform float edgeWidth;
uniform float edgeIntensity;      // 边缘光强度
uniform float specularIntensity;  // 高光强度（新增）
uniform float flowIntensity;      // 流动扰动强度（新增）

varying vec2 vUv;

vec3 computeNormal(vec2 uv, float eps) {
    // ...（保持不变）
}

void main() {
    float phi = texture2D(phiTex, vUv).r;
    if (phi > 0.001) discard;   // 空气透明

    float eps = 1.0 / resolution.x;
    vec3 normal = computeNormal(vUv, eps);
    vec3 viewDir = vec3(0.0, 0.0, 1.0);
    vec3 lightDirNorm = normalize(lightDir);

    // ========== 1. 基础颜色层 ==========
    float depth = clamp(-phi * 2.0, 0.0, 1.0);
    vec3 baseColor = mix(waterColor, deepColor, depth);

    // ========== 2. 漫反射光照层 ==========
    float diff = max(0.1, dot(normal, lightDirNorm));
    vec3 diffuse = baseColor * diff;

    // ========== 3. 高光层（独立） ==========
    vec3 halfDir = normalize(lightDirNorm + viewDir);
    float spec = pow(max(dot(normal, halfDir), 0.0), 64.0);
    vec3 specular = vec3(1.0) * spec * specularIntensity;

    // ========== 4. 边缘发光层（独立） ==========
    float edge = 1.0 - smoothstep(0.0, edgeWidth, abs(phi));
    vec3 glowColor = vec3(0.2, 0.6, 1.0);   // 淡蓝色发光
    vec3 emissive = glowColor * edge * edgeIntensity;

    // ========== 5. 流动扰动层（细节） ==========
    vec2 vel = texture2D(velTex, vUv).rg;
    float flow = length(vel) * 0.5;
    vec3 flowColor = vec3(0.15, 0.25, 0.35) * flow * flowIntensity;

    // ========== 最终合成 ==========
    // 颜色 = 漫反射 + 高光 + 自发光 + 流动扰动
    vec3 color = diffuse + specular + emissive + flowColor;

    // 透明度基于 phi（边缘可稍透明）
    float alpha = clamp(0.6 - phi * 2.0, 0.2, 0.9);

    gl_FragColor = vec4(color, alpha);
}
同时需要修改 FluidSimulatorAdapter 构造函数中的 uniforms，增加 specularIntensity 和 flowIntensity：

typescript
uniforms: {
    // ... 原有
    specularIntensity: { value: 0.5 },
    flowIntensity: { value: 0.3 },
    // ... 其余
}
并且允许用户通过适配器方法调整这两个参数。

四、分层渲染的优势
互不干扰：漫反射、高光、发光互相独立，修改边缘光强度不会影响光照强度。

易于调试：可以暂时关闭某一层来观察其它层效果。

可扩展：未来增加屏幕空间反射、折射等效果，只需额外添加层，不影响已有层。

性能不变：依然是一次绘制，只是计算逻辑更清晰。

五、如果需要高级分层（多 Pass 渲染）
如果追求极致控制，可以使用 EffectComposer 多个 Pass：

GBuffer Pass：输出 BaseColor、Normal、Depth 等。

Lighting Pass：计算光照贡献，输出到另一纹理。

Composite Pass：合并所有层，加入边缘光、雾效等。

但考虑到你的项目规模，上述单材质分层已经足够解决“变白”问题。

六、总结
白色化的原因：边缘光、流动扰动直接累加到漫反射颜色，导致过曝。

解决方案：将光照（漫反射+高光）、自发光、细节三部分分离计算，最终叠加。

效果：水体颜色纯正，边缘柔和发光，光照独立，可单独调节各层强度。

按照上述修改你的 FluidSimulatorAdapter.ts，即可实现工业级的分层渲染，互不干扰。如果你需要具体的完整文件代码（包含 uniform 定义和着色器字符串），我可以帮你生成。

