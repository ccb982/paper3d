# 2026-09-18

## 舰内昼夜时钟冻结（用户提问「舰船内能固定时间吗」）

结论：**改之前不能，改之后能。**

- 世界逻辑本来就是冻结的：`WorldMode.update` 在 `phase === 'interior'` 分支直接
  `return`（只驱动 `shipInterior.update(dt)`），所以实体/敌人/定时遗物全停。
- 但**昼夜照跑**：`main.ts:448` 主循环无条件调 `renderManager.update(dt)` →
  `SunCycle.update(dt)`，`DAY_SECONDS = 900`（现实 15 分钟 = 游戏 24h，即 ~37.5s/游戏小时）。
  进舱 10 分钟出来天色黑掉 16 小时，而且舱内看不到天空 = 静默流失。

### 改动（tsc 通过）

1. `src/services/render/RenderManager.ts`
   - 新增 `private clockPaused = false` + `setClockPaused(v)`
   - `update()`: `if (!this.clockPaused) this.sunCycle.update(dt * ts);`
   - 只停昼夜，**不动 scaledDt**（房间行走/UI 仍用真实 dt）；云/月亮本来就被
     `setFlightMode(true)` 停了，没动。
2. `src/modes/WorldMode.ts` 三处接线：
   - `enter()`：`setClockPaused(false)`（每次进世界复位，防跨局残留）
   - `enterShipInterior()`：`setClockPaused(true)`
   - `exitShipInterior()`：`setClockPaused(false)`

### 备注

- 基地（BaseMode）里太阳也照走，但**不影响**：`WorldMode.enter()` 里 `renderManager.resetDay()`
  每次出击都把时间重置到 `START_HOUR = 6`，所以基地停留多久都无所谓。
- `resetDay()` 不动 clockPaused（冻结与否是场景状态，不是时间值），复位靠 WorldMode.enter。

## 追加：用户定调「不允许 敌人不动 + 时间照走」（2026-09-18）

用户明确二选一：舰内时间暂停（已选），或敌人 AI 照常跑 + 时间照常。**禁止中间态**。
→ 于是把「世界冻结」的地方全部同步冻结昼夜：

1. `shipDestroyed = true`（结算/复活面板等待，`update` 在 1196 直接 return）→ `setClockPaused(true)`
2. `reviveShip()` 里 `shipDestroyed = false` → `setClockPaused(false)`

一致态清单（以后加新的"世界 return"分支必须同步问一遍）：

| 场景 | 世界（敌人 AI） | 昼夜 | 一致 |
|---|---|---|---|
| 舰内 interior | 停 | 停 | ✅ |
| 舰船被毁等待复活 | 停 | 停 | ✅ |
| 对话中 / 面板打开 | 跑（只锁输入+指针） | 跑 | ✅ |
| 顿帧 hitstop | 缩 | 缩（同一 ts） | ✅ |

tsc EXIT=0。

## 水里持续游动音效降速（用户「降低在水里持续游动的音频播放速度」）

根因不是"触发太频繁"，是**素材时长 vs 节流间隔倒挂**：
`涉水.mp3` = **0.73s**，而 `playSfx('waterWade', 330)` 间隔 330ms → 每 0.33s 起一个新实例，
上层还没播完就叠下一个（≈2.2 层重叠）→ 听着像急促的连续噪声。
（`入水.mp3` = 1.54s / 节流 400ms，同样倒挂，但用户没提，未动。）

改动（4 处，tsc EXIT=0）：
1. `platform/PlatformAdapter.ts`：`playSfx(src, rate?)`
2. `platform/WebAdapter.ts`：`new Audio` 后 `a.playbackRate = rate`（**必须在 play() 之前设**）
3. `services/audio/Sfx.ts`：`playSfx(id, minGapMs, rate?)` → 透传
4. `WorldMode.updateWaterEntry`：`playSfx('waterWade', 700, 0.75)`
   —— 慢放 0.75（时长 0.73→0.97s，音调降） + 间隔 330→700ms。
   ★ 只改了音效节流，**没动水面泛波的 gap**（340 - speed*28），视觉波纹节奏不变。

### 后续（同一轮）：用户要「慢放但**连续**，慢放比例可随机」→ 改循环轨

点播再怎么拉间隔都是一段一段的，游动是持续状态 → 改成**循环轨**：

1. **素材**：ffmpeg 加工 `public/sfx/涉水_loop.mp3`（0.63s，5.2KB，64k 单声道）：
   主体 `atrim 0:0.59` + 尾 `atrim 0.59:0.73,afade=t=out:d=0.14` → `amix=normalize=0` 叠回开头
   （0.73s 原素材直接 loop 会有接缝咔哒）。
2. `config/sfx.ts`：`LOOP_SFX` 加 `waterSwim`；**删掉**一次性 `SFX.waterWade`（不再点播，免留死配置）。
3. `WebAdapter`：**loopTrack 单槽 → `loopTracks: Map<src, LoopTrack>`**，多条循环轨可同时响
   （引擎 + 涉水互不顶掉）；`playLoopSfx(src, {rate?, volume?})`，`stopLoopSfx(src?)`（不传 = 全停）。
   ★ `playbackRate` 必须在 `play()` 之前设；已在册的轨换 rate 直接改属性（不改循环性、不重头播）。
4. `WorldMode.updateWadeLoop(dt)`（每帧裁决，`updateWaterEntry` 之后调用）：
   在水里 + 位移速度 > 0.3 → 起轨；停 / 出水 / 非 explore / 死亡 → `stopWadeLoop()`。
   ★ **随机慢放**：起轨时 `0.70 + random()*0.15`，**同一条轨内固定**（每次入水变一次，不逐帧抖）；音量 0.35。
   停止点：`enterShipInterior()`（interior 时 update 直接 return）、`dispose()`。
5. 水面泛波视觉节奏不变；入水瞬间的 `waterEnter` 一次性音保留。

tsc EXIT=0；`npm run build` EXIT=0（17.11s），dist 已带上 `涉水_loop.mp3`。

### 再后续：用户反馈「水里持续游动声音不明显」→ 选段错了 + 音量给低了

排查用数据，不靠猜（脚本跑完已删）：

| 素材 | I (LUFS) | peak | 40ms 窗平均 RMS | 判读 |
|---|---|---|---|---|
| 涉水.mp3 | -16.5 | -2.3 | ≈-22 | 一声"哗"的瞬态，crest 15dB |
| 涉水_loop 旧版 | -18.2 | -1.2 | **-22.0** | 循环后更轻、听着"一下一下" |
| 入水.mp3 | -16.0 | +3.1 | 前 0.4s 冲击(-6dB) + **0.45~1.25s 平坦持续段(-20~-24)** | ★ 尾段才是"人在水里"的声音 |
| 涉水_loop 新版 | -18.8 | -2.4 | **-19.8，起伏 6.5dB** | 用入水尾段重做 |

两个真因：
1. **选段错**：持续声要选**平坦段**（起伏小），不能用瞬态素材（`涉水.mp3`）去 loop —— 后者 crest 15dB，平均能量低，循环起来就是"一下一下"。
   重做：`atrim 0.45:1.25`（入水尾段）+ 尾 0.16s 淡出 `amix` 叠回 → 归一 -1.5dBFS。
2. **音量给低**：一次性音效 `new Audio()` 的 volume 默认 **1.0**，循环轨我给了 0.35 → 比脚步声
   （`stepGrass` I=-22.9 @1.0）还轻 5dB。→ 改 **0.65**（等效 ≈ -22.9，与脚步持平）。
   慢放范围 0.70~0.85 → **0.80~0.92**（<0.8 明显发闷，反而更听不清）。

踩坑：
- **这版 ffmpeg 的 `acompressor` 对短素材提不了响度**：threshold=0.08/ratio=14 把整体压掉
  （I 反而 -19.5→-28.7）；改 threshold=0.35/ratio=6 只换来 +1.3dB。
  **peak 已经贴顶（≈-1.2dBFS）时，压缩不是解药 —— 要么换选段，要么调播放音量。**
- 判断"循环素材像不像持续声"看**40ms 窗 RMS 包络的起伏**（新版 6.5dB ✓），
  单看 LUFS/peak 看不出来（旧版 LUFS 甚至更高）。

tsc EXIT=0；build EXIT=0（23.66s），dist 已更新。

### 最终：用户拍板「换素材」→ 换成了真·游动素材

**Mixkit 直链抓取法（可复用，已验证）**：
- 分类页 `https://mixkit.co/free-sound-effects/water/` 抓 HTML，
  用**标题文字的位置往前回溯最近的 `active_storage/sfx/<id>/`** 拿 id
  （校验：Water splash→1311、Jump into the water→1180，与 来源.txt 完全一致 ✓）
- 下载：`https://assets.mixkit.co/active_storage/sfx/<id>/<id>-preview.mp3`（preview = 完整素材）

候选实测（0.25s 窗包络）：

| 素材 | id | 时长 | I (LUFS) | 判读 |
|---|---|---|---|---|
| Sea swimming loop | 1181 | 10.4s | -17.1 | 有划水节奏但起伏 25dB，含 -41dB 静音谷 → 单用会"断" |
| Water flowing ambience loop | 3126 | 51s | -25.3 | 太平太轻（RMS -30），救不回来 |
| **River water flowing** | **2454** | 3:00 | **-11.9** | 极平坦（±1.5dB）、够响 → 主层 |

**成品 `涉水_loop.mp3`**：river(2454) 20~22s 主层 + sea swimming(1181) 7~9s 压 6dB 动作层
→ amix → 主体 0:1.7s + 尾 0.3s 淡出叠回（无缝）→ 归一 -1.5dBFS。
**1.75s / 13KB**；40ms 窗平均 RMS **-18.2dB**（旧版 -19.8）、起伏 8.6dB、无静音谷。
`来源.txt` 已补两行来源与加工链。build EXIT=0（17.91s），dist 已更新。

播放参数保持：volume 0.65、慢放 0.80~0.92（等效 ≈ 脚步响度 -22.9 LUFS）。

### ★★ 真 bug：循环轨淡入被每帧打断 →「在水里移动几乎没声」

用户反馈「几乎没声，不如最一开始的（点播版），但那个不连续」。查代码而不是继续调素材，找到：

- `updateWadeLoop` **每帧**调 `playLoopSfx`；`playLoopSfx` 在册分支里无条件 `fadeTo(vol)`；
- `fadeTo` 会先 `clearInterval` 上一个计时器、再从**当前音量**重新起一段 **BGM_FADE_MS=700ms** 淡入；
- 结果：淡入每帧被重置，音量按指数逼近，约 **1.5~2s** 才到目标 → 游一下就出水 = 等于没声。
  （引擎轨只在 phase 变化时调用，所以这个 bug 从来没暴露。）

修法（`WebAdapter`）：
1. `LoopTrack` 加 `target` 字段 → **只有目标音量真的变了才重新 fade**（每帧调用不再打断）。
2. `fadeTo` 加可选 `ms` 参数（默认仍 BGM_FADE_MS）→ 循环轨用 **`LOOP_FADE_MS = 220`**
   （循环轨跟的是秒级动作，700ms 淡入来不及）。
3. 起轨时 `target` 一并记录；`stopLoopSfx`（单轨 + 全停）也走 220ms。
   ★ 回归检查：play() 被自动播放拦截时 `el.paused` 仍为 true → 下一帧走 paused 分支重试，不会卡死在 0。

### 素材比例也反了 → 动作层改主层

第一版「河流主 + 划水压 6dB 次」太平坦没冲击。重做：
`Sea swimming loop`(1181) **6.0~7.5s 主层**(0dB，首尾都在响区) + `River water flowing`(2454)
20~21.5s **压 6dB 垫底**（填浪间隙的静音谷）→ amix → 主体 1.25s + 尾 0.25s 淡出叠回 → 归一 -1.5dBFS。

**成品**：1.28s / 10KB，I=**-16.5 LUFS**（与最初点播的 涉水.mp3 持平，比中间那版 -17.3 好），
40ms 窗平均 RMS **-17.9dB**、最大 **-14.1dB**（有冲击）、起伏 7.7dB、无静音谷。
播放音量 0.65 → **0.85**（等效 ≈ -17.9 LUFS，比脚步 -22.9 明显一档）。

**教训**：做"持续声"循环时，连续层只负责填空隙，**冲击/动作层必须是主层** ——
否则得到一段平坦底噪，在数据上达标（LUFS/RMS 都不差）但人耳完全注意不到。
build EXIT=0（15.91s）。

### 收尾：音量再小点 + 音量也随机（2026-09-18 用户定稿）

`updateWadeLoop` 起轨时随机两件事，都**缓存在字段里**（轨内固定）：
- `wadeRate = 0.80 + random()*0.12`（0.80~0.92 慢放）
- `wadeVol  = 0.45 + random()*0.17`（0.45~0.62）

★ 音量也必须缓存：`updateWadeLoop` 每帧调 `playLoopSfx`，若音量逐帧变会不停触发
fadeTo（配合 LoopTrack.target 的"目标变了才淡"逻辑 = 每帧都淡）→ 音量抖动。
等效响度 ≈ -21~-24 LUFS，与脚步（stepGrass -22.9）同档。
tsc EXIT=0 / build EXIT=0（15.99s）。

## 架构体检（用户：「不想项目无止境无序膨胀」）

跑量化统计（临时脚本已删），结论写进了 MEMORY.md 的「架构体检基线」一节。要点：
- 240 文件 / 66,643 行 / **中位数 153 行** → 整体粒度健康、分层清楚；
- 头部 4 个文件占 16%（WorldMode 4129 / ChunkManager 3480 / GachaOverlay 1760 / FluidSolver 1504）；
- **膨胀机制不是行数，是"人肉同步"**：phase 5 处赋值 × 每处要手动跟
  syncSceneBgm(10) / setClockPaused(5) / setFlightMode(5) → 每加一条不变量就再 ×N。
  今天补昼夜冻结正是又加了 3 处，所以用户这个担心是对的。
- 给了 P0/P1/P2 治理清单，**未执行**（大重构需用户同意），已用选项形式问他选哪些。

## 执行：用户选了 P0-a（阶段副作用收口）+ P2（死代码清理 + 护栏）

### P0-a 完成：`WorldMode.setPhase()` 唯一入口

新增 `setPhase(next)`，phase 绑定的副作用全部收进去（环境 / 飞行模式 / 昼夜冻结 /
水面 / 粗块 LOD / 涉水轨 / BGM）。5 个赋值点改为调用它，删掉散落的重复调用。

| 指标 | 改前 | 改后 |
|---|---|---|
| `this.phase =` 直接赋值 | 5 处 | **1 处**（setPhase 内部） |
| `syncSceneBgm()` 调用 | 10 处 | 6 处（定义+setPhase+3 处 groupWarn 翻转） |
| `setClockPaused` / `setFlightMode` | 各 5 处 | 各 1 处 |

细节：
- 新增 `envKind` 字段做幂等（`setEnvironment('world')` 内部会重置云场，不能每帧调）。
- `enter()` 里改成显式 `setEnvironment('world')`（修掉一个潜在 bug：上一局在舰内退出，
  新一局 skyDome 还藏着 → 没天空）。
- 顺手修 `syncSceneBgm`：`stopLoopSfx()` 无参 = **全停**，会连涉水轨一起停 →
  改为 `stopLoopSfx('shipEngine')` 只停引擎轨。

### P2 完成

1. 删 `PRESERVER_AI`（aiconfig.ts，零引用；敌人 AI 已走 mobAI 数据驱动模板）
2. 删 `drainInteractions`（InputActions.ts，零调用；队列由 DesktopBinding 自己 push+清）
   ★ **项目没有 git** → 两段原文备份在 `.workbuddy/deadcode/2026-09-18.txt`
3. 新增 `scripts/arch-guard.mjs` + `npm run guard`：
   - 文件行数 > 1200（6 个已知债务进白名单，只警告）
   - **不变量退化检查**：phase 直接赋值必须 = 1、setPhase ≥ 3、syncSceneBgm ≤ 8
   - 配置同步：遗物三处登记（relics / FTX_ICON_SOURCES + registerDynamicIcon / gachaPool）、
     BGM 表 vs 手写 BgmKey

护栏首次运行就抓出我自己的两处正则假警报（图标表格式是 `key:'/fx/..'` 不是 `id:'..'`；
priestess 走 registerDynamicIcon 动态图标；抽卡池 id 是嵌套的）—— 已修正，现在 exit 0。

tsc EXIT=0 / guard EXIT=0 / build EXIT=0。

---

## P1：WorldMode 刷怪/波次子系统拆出（完成）

**WorldMode 4158 → 3563 行（-595）**，新增 `src/systems/spawn/WorldSpawner.ts`（724 行）。

### 搬走的东西

18 个方法（原 1910..2471 连续段）：`scanAndSpawnWaves / spawnAtRandomPointInChunk /
refreshEnemyScale / spawnBoss / onBossDefeated / spawnDirectorWave / spawnWaveNear /
pickMob / demoteFarEnemies / updateShipGroupWarning / agentMelee / nearestTauntSentinel /
spawnStressAgents / mobAgentStats / spawnOne / quotaAllows / promoteAgent / createEnemyEntity`。

连同私有状态一起搬：`spawnedChunks` / `_tauntScratch` / `cullAccum` /
`groupWarnAccum` / `groupWarnShown` / 全部刷怪常量（MAX_ALIVE / AMBIENT_PRELOAD_RATIO /
ENEMY_CULL_RADIUS / ENEMY_CULL_INTERVAL / SHIP_GROUP_* / SHIP_INTENT_*）。
模块级 `AGENT_SOURCE`、`SENTINEL_TAUNT_RADIUS`、`interface MobDef` 一并迁出并 export。

### 桥接方式（关键设计）

`SpawnDeps` 接口 + **getter/setter 桥**：WorldMode 持有状态，Spawner 通过
`get enemies(){return self.enemies}` 读实时值、`set bossEntity(v)` 写回。
这样 WorldMode 侧 `this.enemies = []` 之类的重新赋值不会让 Spawner 拿到旧引用。
回调三项：`showFloatingAt / syncSceneBgm / returnToBase`。
★ 构造必须放在 `enter()` **最开头**（第 573 行）—— 放在蜂群回调绑定处（原 782）会晚于
`setPhase('sail')`（657）→ `syncSceneBgm` 读 `this.spawner.warnShown` 直接 TypeError。

### 降格节拍收进子系统

WorldMode.update 里原本手写的
`cullAccum += dt; if (>= INTERVAL) { cullAccum=0; demoteFarEnemies(...) }`
→ 一行 `this.spawner.tickDemote(dt, pp.x, pp.y);`

### 验证方式（下次照做）

1. **逐方法文本比对**：备份 `WorldMode.bak.ts` 的 18 个方法体 vs WorldSpawner 的，
   归一化（`this.deps.`→`this.`、`WorldSpawner.`→`WorldMode.`、去 `private`、
   `returnToBase()`→`onReturn?.()`）后 difflib 逐行比对 → **18/18 一致**。
2. tsc EXIT=0、build EXIT=0、guard EXIT=0。

### 踩的坑

- **段边界用 `strip()=="}"` 定位是错的**：多行签名里的对象类型 `{ count: number; ... }`
  会提前配平（把 562 行的段判成 36 行）。正确做法：以方法声明行 + **2 空格缩进的 `  }`**
  作为成员结束。
- 生成代码时会漏掉**声明在原类里、但引用在被搬段内**的字段（`spawnedChunks`/`_tauntScratch`）。
- `worldUIManager` 在 WorldMode 是 `!:` 非空 → `SpawnDeps` 里也要声明成非空，否则 TS2531。
- 迁移脚本必须**带断言**（每个待删区间的首尾行文本），否则行号一漂就误删。

### 护栏加了两条

- **硬天花板 3800 行**（KNOWN_BIG 也照样 FAIL）—— 欠债可以，继续长不行。
- **迁出方法回潮检查**：WorldMode 里再出现那 18 个方法定义 → FAIL。

### 清理

根目录 6 个更早会话的临时迁移脚本（`_mig.py ~ _mig4.py` / `_migrate.py` / `_types.py`，
共 142 行）已删；**删前备份**到 `.workbuddy/tmp/legacy-scripts-2026-09-18/`
（项目没有 git，删前一律先备份）。根目录现已无 `.py` 残留。

### 遗留

WorldMode 3563 行仍是全项目最大，下一刀建议拆 `ChunkManager.ts`（3481）。

---

## ★★ 假回归排查：「敌人直接不生成」= 当天配额闸门（不是 P1）

用户报「敌人直接不生成了」，**删档后恢复正常** → 确认与 P1 拆分无关。

### 排查路径（下次照走，别再从代码猜）

1. **先证伪拆分本身**：对 P1 前备份做全文件 difflib → 只有预期的 3 类改动
   （调用点改名 / 降格块替换 / 大块搬移），`ChunkManager` 等无意外。
   + 18/18 方法体逐行一致 → 搬移无误。
   **拆分已被机器验证等价，就别再怀疑它**，转去查玩法闸门。
2. **顺着生成链找闸门**：`scanAndSpawnWaves` 早退条件 → `spawnOne` →
   **`quotaAllows()` → `remainingQuota() = quota − spawned`**。
3. **`spawned` 是当天累计生成数，只增不减、跨出击累计**，
   只有 `everDeparted !== meta.day`（换日）才 `resetDayQuota`。
   顶到 `quota` 后 → 当天一只都不再生成（**设计如此**）。
   ★ 注意：`meta.day++` 只在 `onReturn` 里发生 → **刷新/强退不会换日**，
   配额当天打满后，不返回基地就一直没敌人。

### 顺手修掉的真隐患（已改）

`quotaAllows()` 里加兜底：`quota <= 0` 视为**从未初始化** → 放行 + `console.warn`。

原因：`ensureDayQuota` 只对 `quota<=0` 算一次；若它没跑到（`this.threat` 为空 /
enter 中断），`quota` 就停在 0 → `remainingQuota = 0 − spawned = 0`
→ **永久一只都不刷，换日也救不回来**（`resetDayQuota` 只是再清成 0）。
这是异常态，不该由闸门背锅；正常路径（quota 已按威胁档位预计算）完全不受影响。

验证：build EXIT=0 / guard EXIT=0。

### 配额耗尽提示（用户要求「可以加提示」，已做）

问题：当天配额打满后野外一只敌人都没有，**表现跟"敌人不生成"的 bug 一模一样**
（2026-09-18 就是在这上面白查一轮）。必须让状态可见。

三层提示，都只在 **explore 段**、**每局一次**（`quotaExhaustedShown`，`reset()` 清）：
1. **常驻**：顶部档位标签改为「敌军攻势：已肃清」（`setThreatLabel('…', '#9fe6b0')`）
   —— 玩家随时能看到"为什么没敌人"，这条最重要。
2. **一次性横幅**：「今日敌军已肃清 · 可返回基地」（9s 后自动淡出）
3. **头顶浮空字**：「今日敌军已肃清」（就在眼前，不会看漏）

判定：`notifyQuotaExhausted(dt)`（WorldMode.update 的 explore 块每帧调，内部
延迟 1.5s 再判、一次性）。口径 = `quota > 0 && remainingQuota <= 0`。

**UI 侧新增独立通道**：`WorldUIManager.showNotice(text, seconds)` ——
青色横幅 top:124px，**与敌袭横幅(top:38) / 访客提示(top:84) 三通道互不覆盖**。
生命周期照 `showVisitorNotice` 的写法（`noticeTimer` 在 update 里递减、最后 1s 渐隐、
dispose 里 remove）。

顺带两处：
- `quotaAllows()` 的兜底 warn 加 `quotaInitWarned` **一次性**标志
  （否则每帧 warn 刷屏）。
- `scanAndSpawnWaves` 开头加 `if (!this.quotaAllows()) return;`
  —— 配额耗尽时省掉每帧 24 个 chunk 的扫描。语义等价（耗尽时 spawnOne 必被拦下）。

验证：tsc / build / guard 全 EXIT=0；
`verify-move.py` 报 **16/18 一致**，2 个差异正是上面这两处有意改动。
