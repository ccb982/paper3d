# 2026-09-16

## 背包「遗物」区块改为独立滚动区（隐藏滚动条）

**问题**：`CharacterStatsPanel` 把每条遗物（图标 + 名称×件数 + 多行 description）
直接 append 进 `root`（`flex:0 0 232px` 纵向 flex 列），遗物越多左栏越高 →
撑大 `ui-panel-inner` → 撑大整个背包页面。

**改动**：
- `src/ui/shared/CharacterStatsPanel.ts`
  - 新增常量 `RELIC_SCROLL_MAX_HEIGHT_PX = 188`
  - 新增私有方法 `private scrollBox(maxHeight: number): HTMLDivElement`
    —— `max-height` + `overflow-y:auto` + `scrollbar-width:none` +
    `-ms-overflow-style:none` + 内联 `<style>` 注入 `.ui-relic-scroll::-webkit-scrollbar{display:none}` +
    `overscroll-behavior:contain` + 滚轮 `stopPropagation`（被动监听）
  - `render()` 的遗物区块：`total === 0` 时保持原样（仅「尚未拥有遗物」提示）；
    否则把「遗物」+「BOSS」两个分区**一起**塞进同一个 `scrollBox`
  - 顺手修掉既有笔误 `this.    hpFill` → `this.hpFill`
- `src/ui/shared/ui-styles.css`
  - `.ui-panel-inner` 后追加 `.ui-relic-scroll` 及其 `::-webkit-scrollbar` 兜底规则

**关键设计点**：
- 用 `max-height` 而非 `height` → 遗物少时容器自适应，不浪费空间
- 滚轮 `stopPropagation` 是必须的：外层 `gridRoot` 和弹窗 wrapper 都可纵向滚动
- 「遗物」与「BOSS」合用一个滚动区（否则两个滚动条视觉很碎）

**两处宿主自动受益**（都调 `characterStatsPanel.render`，无需改）：
- `WorldUIManager.renderInventoryPanel()`（局内背包）
- `ShipUIManager.toggleInventoryPage()` / `refreshInventoryStats()`（基地背包）

`tsc --noEmit` 通过，0 错误。

**注意**：另一处遗物入口 `ShipPanels.ts` 的 `OperatorPanel`（标题「遗物」）
未改动——那是独立的遗物管理侧边面板，不走 `CharacterStatsPanel`。

---

## 地形破坏 / 水体解耦（用户定调）

**问题**：打「有水 chunk」时坑洞更新极慢。根因——`PatchCompute.computeTableGeometry`
里 `buildWaterSurface(...)` 与 top/wall 几何**串行在同一个 terrainPatch worker 调用内**，
水体求解（initSolve/updateSolve + 5cm 边界探针点阵采样）拖慢整个几何返回；
且无水 chunk 也白走一遍（patch 分支会建 `waterStateFor` 状态）。

**用户设计（原话）**：「如果坑洞涉及区域没有水，就别更新水，更新水也是要先更新坑洞，
额外的 worker 慢慢算水」。

**改动**：
1. `PatchCompute.ts`
   - `computeTableGeometry` 新增末位参数 `waterMode: 'full' | 'none'`（默认 full）
     —— `'none'` 跳过 `buildWaterSurface`，返回 `water: null`
   - `PatchGeomRaw.water` 类型 → `WaterSurfaceRaw | null`
   - 新增导出 `computeWaterOnly(readChunk, seed, cx, cz, levels, dirty, levelAt)`
     —— 复用 `getRefinedSource` 缓存，只跑水体，不碰 top/wall/物理
2. 新文件 `waterSolve.worker.ts` —— 只算水的专用 worker
3. 新文件 `WaterSolve.ts` —— 主线程客户端，**单 worker 串行队列**
   - 每 chunk 终态收敛：新请求覆盖旧的（dirty 取并集），旧 promise 以 null 结清
   - worker 不可用 → 主线程同步 `computeWaterOnly` 回退
4. `terrainPatch.worker.ts` / `coarsePatch.worker.ts`：透传 waterMode，transferOf 判空
5. `TerrainPatch.ts`：compute 请求加 waterMode，透传主线程回退与 postMessage
6. `ChunkManager.ts`
   - `waterMaskCache` + `chunkWaterMask(cx,cz)`：4m 块液体位图（缓存）
   - `dirtyNearWater(cx,cz,dirty)`：dirty 块 ±4 块（16m）内是否有液体
   - `decideWaterMode()` → `'none'`（跳过）/ `'defer'`（异步慢算）
   - `patchRebuildChunk`：几何恒传 `waterMode:'none'`；defer 时投 `waterSolve.compute`
   - `replaceWaterMesh` 改**三态**：`undefined`=保持旧水面 / `null`或0-quad=拆掉 / 有值=换
   - 新增 `applyWaterResult`（只换水网格）、`stashWaterMesh`/`restoreWaterMesh`
     （全量换装前保住旧水，防水面闪没）
   - 切风格/dispose 两处加 `waterSolve.clearCaches()` + `waterMaskCache.clear()`
   - `digPerf` 新增 `waterSkip` / `waterDefer` / `waterApplied`

**关键设计点**：
- **单 worker 而非池**：`WaterSurface.waterStates` 是模块级 Map（每 worker 一份增量
  状态：occ/连通分量/边界探针/床面点阵）。同 chunk 跨 worker 漂移 → 状态丢失 →
  退化 initSolve 全量重解（更慢）。单 worker = 状态亲缘稳定 = 走 updateSolve 增量。
- **首建仍 waterMode:'full'**（`finishStandardChunk` 不传 = 默认 full）：首建没有
  「旧水面」可保持，必须同步出水面。waterMode:'none' 只用于破坏重建。
- **levels 双拷贝**：几何那份被 transfer 掉，水任务必须另拷一份
  （`new Uint8Array(levelsArr)` 两次），否则拿到 neutered 数组。
- 跨 chunk 场景自洽：水体状态 per-chunk，含水块的那个 chunk 会因跨块联动
  （`enqueuePatch`）自己重建并 defer，所以边界水也会更新。

**验证**：`tsc --noEmit` 0 错误；`vite build` 通过，`waterSolve.worker` 正确产出
独立 chunk（35.85 kB）。未做游戏内实测（避免与用户同开实例抢 GPU）。

---

## 架构评审结论（用户问「架构很好了是吧」）

**骨架确实好**（有据可依，不是客套）：
- 三层分离干净：数据面 `RasterMap` / 计算面 Worker 纯函数 / 装配面主线程预算化
- 字节一致性靠「同函数同输入」保证，不靠人肉约定 → worker 挂了回退主线程结果也一致
- 增量几何 + Tier A/B 原地更新 + 物理分区，每层都有明确降级路径
- `digPerf` 常驻分项埋点（含本次新增 waterSkip/waterDefer/waterApplied）
- 每处优化留日期+原因（2026-09-15 装饰降级明显是实测后回退的产物）

**唯一明确腐化点**：`ChunkManager.ts` 2900+ 行 —— 命中解析 / 破坏重建 / 装饰 /
物理 / 粗块 LOD / 封存 / 装配预算 / 风格切换全塞一起。为加 4 个方法翻了很久。

**重复模式已到第 3 次，该抽了**：
- `platformFlagCache` → `waterMaskCache`：「扫 256 块 + 缓存 flag」同款，应抽
  `chunkFlagCache(key, predicate)`
- `makeLevelAt`：`TerrainPatch.ts` 一份 + `WaterSolve.ts` 一份（本次复制）

**注释误导（已修 2026-09-16）**：`TerrainPatch.ts` 头部原称「无 hash 亲缘…
退化为全量重解，可接受」。实际 `pickLeastBusy()` 在 cost 平局时取**最小编号** →
单发恒落 worker#0，亲缘性远好于注释所述；只有并发多 chunk 重建时才散到 #1/#2。
已重写该段（含亲缘性澄清 + 水体已不在本池持有），并同步修正了 compute() 的
`@param req.dirty` 语义与「流程」树里的「归属 worker」措辞（本就是非固定归属）。

## ★ 实测结论（用户 2026-09-16 反馈）

用户实测后原话：「现在水体一点也不卡了，我非常满意」。
**水体解耦方案验证通过** —— 「坑洞几何恒走 waterMode:'none' + 专用水 worker
异步慢算」这条路线是对的，无需回退。

## 架构文档同步（2026-09-16）

更新 `全新的游戏/地形与渲染管线架构.md`（2900+ 行，唯一权威档）：
- 标题加「2026-09-16 破坏/水体解耦定稿」；目录第二/四部分加解耦提示
- §14.10b：补「水体已移出本池」+ **亲缘性澄清**（pickLeastBusy 平局取最小编号）
- §17 标题、§17.3（水三态）、§17.4（水体分支判定树）
- **新增 §17.13**（坑洞优先 + 专用水 worker；含单 worker 硬约束、踩过的坑、验收）
- 水体章 §0 一句话、§3 生命周期契约、**新增 §3.2**（放在 §3.1 之后）
- 水体章 §6 文件落点：原「src/services/water/ 规划目录」实际未实施 →
  改为实际落点 `services/map/`（WaterSurface / WaterMaterial / WaterSolve /
  waterSolve.worker / PatchCompute）
- 水体章 §7 开放项 +2（水面迟到窗口观感、水 worker 冷启动）
- 水体章 沿革 + 2026-09-16 条目（含实测通过结论）
- **附录 A 新增铁律 13**：坑洞优先、水面不进关键路径

**踩坑**：插入 §3.2 时一度插到 §3.1 之前（编号顺序颠倒），已修正。
教训：往带编号章节插新小节后应 grep `^#### 3\.` 确认顺序。

## 新增访客「普瑞印刷」（§19.8）

用户要求新增一名访客，纹理 `/characters/protagonist/普瑞印刷.ftx3.gz` 已自备。

**接入只需改 2 个配置文件**（名册驱动，管线零改动 —— 已 grep `yjwangzi`
验证：整个 src 下只出现在 visitors.ts 与 dialogues.json 两个 config 文件）：
1. `src/config/visitors.ts`：`VISITORS` 追加
   `{ id:'puruiyinshua', name:'普瑞印刷', moveSpeed:3.0, model:{ height:3.2 } }`
2. `src/config/dialogues.json`：`trees.visitor_puruiyinshua`（印刷/油墨梗，
   奖励 `polyester`×1 + `met_puruiyinshua`；`polyester`=聚酸酯，见 sixBrothers.ts）

**校验方式**（可复用）：node 脚本读 dialogues.json → ① JSON.parse 合法性
② start/next/choices[].next **悬空引用扫描**（全库 9 棵树 NONE）
③ 给 item 但无 met_ flag 的节点告警（只有 base_supply 命中，语义正确：可重复领）。
注意顶层 key 是 **`trees`**（不是 dialogues），别查错。

**顺带修文档漂移**：§19.8 三处仍写「5 名 / 五棵树」，实际加"鹰叫王子"时没同步
（早已是 6 名）。已全部改为 7 名 / 7 棵访客树，并把「新增访客流程 + 校验清单」
沉淀进文档，下次加访客不用再摸代码。

---

## 顶部状态条「敌人数量 + 舰船生命」+ 击杀统计架构（新建）

**用户需求原话**：
> 「这个图放在最顶部就行，我仿照的明日方舟的ui即可 / 我需要做击杀统计，放在敌人的基类里吧」
> 「当每天产生的敌人数量都是有限的，而且动态调控，因为离得远被清除的敌人不算击杀，
>   那个ui左边是当前击杀数/当天敌人总数，右边是红字舰船生命，舰船生命不要血条了，
>   只显示一个数字，生命90%以上白色数字，30%以上黄色数字，低了用红橙色」
> 「等比缩小居中，明日方舟的ui没这么大，用我ftx纹理的比例」

### 素材与实测锚点

`public/ui/敌人数量和舰船生命.ftx3.gz` → 实测解码 **644×68**（宽高比 9.4706）。
**两段式镜像对称**版面，逐列统计饱和度/亮度实测出的落位：

| 元素 | x 范围 | 归一化中心 |
|---|---|---|
| 竖线①（左端装饰） | ≈18 | — |
| ENEMY 橙色准星图标 | 65~137 | 0.157 |
| **左段留白（击杀数落位）** | 140~330 | **0.365** |
| 竖线②（中缝） | ≈332 | — |
| 舰船蓝色图标 | 401~463 | 0.671 |
| **右段留白（生命数字落位）** | 470~627 | **0.852** |

### 新增文件

- **`src/services/ui/EnemyKillHud.ts`** —— 顶部状态条
  - `HUD_TARGET_HEIGHT_PX = 46`（**只定高度**，宽度 = 46 × 9.4706 ≈ 436px 自动推导）
  - `ANCHOR_KILL = 0.365` / `ANCHOR_SHIP = 0.852`
  - `left:50% + translateX(-50%)` **水平居中**，`background-size:100% 100%`
    （容器尺寸已严格等于素材比例 → 不会变形）
  - `applySize()`：高度定死，宽度按 `h × ratio` 算；字号 `max(11, round(h*0.30))`
    同步缩放 → 改目标高度时观感一致
  - `shipHealthColor(ratio)`：≥0.9 白 `#ffffff` / ≥0.3 黄 `#ffd24a` / 更低红橙 `#ff5a2b`；
    低血额外加强辉光
  - 降级：素材加载失败 → `console.warn` + 保留纯色占位，不留空白
- **`src/systems/combat/KillCounter.ts`** —— 击杀统计 + 每日配额
  - `estimateDailyQuota(threat, expectedMinutes=12)`：按威胁档位估算当日**击杀可达上限**
    （`assaults × waves × count × PACK_MULT(1.35)` + `ambientSpawns`，下限 8）
  - `DayEnemyProgress { quota, kills, recalled }` 挂在 `session.dayProgress.enemies`

### ★ 核心口径设计（易错点）

1. **分母 = 击杀可达上限，不是生成总数**。
2. **「远距清除不算击杀」的落地 = 分子不加 + 分母扣 1**（`recordRecall`）。
   只做"分子不加"的话分母永远打不满，进度条永远差一截。
3. **双路径严格互斥**：实体侧走 `EnemyBase.dispose()` → `eventBus.emit('enemy_killed')`；
   代理侧走 `swarmHooks.onAgentKilled` 直接记数。两条路绝不能同时触发。
4. **`killedByCombat` 标记**（`EntityBase` 新增）：默认 `true`；远距回收/降格路径
   **先置 `false` 再 `dispose()`** → 即使走 `dispose()` 也不上报。
5. **`spawnOne()` 是唯一收口点**：导演波次 / 扫描波次 / 压测全经过它，
   配额闸门只加这一处 → `quotaAllows()` =
   `alive < max(0, total - kills)`（alive = 实体数 + 代理数）。
6. **`everDeparted` 防同日重复清零**：存"已为哪一天初始化过配额"，
   否则同日第二次出击会把当天击杀进度清掉。

### 改动的既有文件

- `src/core/Session.ts`：`dayProgress` 加 `enemies?` + `everDeparted?`
- `src/entity/EntityBase.ts`：新增 `killedByCombat = true`
- `src/entity/EnemyBase.ts`：`override dispose()` 里上报（含 `deathReported` 去重）；
  `override onDeath(source)` 记录 `deathSource`；补 `import type { EntityBase }`
- `src/core/EventBus.ts`：新增 `enemy_killed` 事件
- `src/systems/swarm/SwarmSystem.ts`：新增 `onAgentRecalled?` 回调；
  L1 半径外回收处 `recalled++`，帧末统一 `hooks.onAgentRecalled?.(recalled)`
- `src/modes/WorldMode.ts`：接线 `onAgentKilled` / `onAgentRecalled` /
  订阅 `enemy_killed` / 出击时 `resetDayQuota` + `ensureDayQuota` /
  HUD 0.1s 节拍刷新 / `spawnOne` 配额闸门 / `demoteFarEnemies` 置 `killedByCombat=false`
- `src/ui/world/WorldUIManager.ts`：`setShipStatus(hp, maxHp, kills, total)` 转调
  `EnemyKillHud.update()`；**删掉旧的 `shipStatusEl` 整段实现**（原「舰船 HP / 油量」单行文本）

### 验证

`tsc --noEmit` **0 错误**；`vite build` 通过（15.32s，266 modules，
`waterSolve.worker` 正常产出 35.85 kB）。
**未做游戏内实测**（避免与用户同开实例抢 GPU）。
浏览器预览里生成过 4 张对照图（尺寸对照 / 高·中·低血三种配色），
实测参数：`RATIO=9.4706 targetH=46 width=436 fontSize=14`。

### 待办

- HUD 观感待用户确认；微调只改 `HUD_TARGET_HEIGHT_PX`（高度）或
  `ANCHOR_KILL` / `ANCHOR_SHIP`（文字落位），无需动其他文件
- 前序遗留（低风险）：抽 `chunkFlagCache(key, predicate)` 统一
  `platformFlagCache` / `waterMaskCache`；合并 `TerrainPatch.ts` 与
  `WaterSolve.ts` 各一份的 `makeLevelAt`

---

## ★ 修正：分母（当天敌人总数）会自己变 —— 口径推翻重做

**用户反馈**：「敌人总数怎么还会变的？我的设计是当天的敌人会预计算好会有多少人，
和难度曲线动态调整有关」

### 根因：分母被三处地方改写

1. **远距回收扣分母**（前一版的错误设计）。我当时把"不算击杀"落地成
   「分子不加 + 分母扣 1」，结果玩家一跑动，HUD 上「当天总数」就自己往下掉。
   **这直接违背用户设计** —— 总数是开战前定好的计划值，不该被玩家行为改写。
2. **闸门用 `alive` 判定**：`alive < total - kills`。敌人被远距回收 → `alive` 掉下来
   → 立刻腾出名额 → 补刷 → 等于**无限刷**，分母永远追不上。
3. **`refHp`/`refAtk` 变化会改威胁档位**（仅存疑，非主因）：`computeThreat` 依赖
   参考属性，若中途重算会改 `ambientInterval` → 估算式结果不同。

### 修正口径（★ 定稿）

| 量 | 语义 | 是否冻结 |
|---|---|---|
| `quota` | 当天敌人总数（预计算） | **冻结**，换日才重算 |
| `kills` | 真击杀 | 只增 |
| `recalled` | 远距回收消失数 | 只增，**不影响 quota** |
| `spawned` | 当天累计生成过多少只 | 只增，**闸门依据** |

- **闸门改为 `quota − spawned > 0`**（原来是 `alive < total − kills`）。
  只看"生成过多少"，玩家跑动/回收都不影响 → 稳定，且不会无限刷。
- **回收只记 `recalled`，绝不碰分母**。分母冻结 = 用户要的"预计算好"。
- 新增 `remainingQuota(session)`（= quota − spawned）与
  `effectiveTotal(session)`（= quota − recalled，给进度条类 UI 用；
  HUD 默认仍展示原始 quota）。
- `ensureDayQuota` **幂等**：`quota > 0` 就原样返回、绝不重算 → 这才是"总数不变"的保证。
- 旧存档迁移：`ensureDayQuota` / `recordSpawn` 里补 `spawned ?? 0`。

### 分工（关键概念澄清）

- **定死的一份** = `quota`，只当分母，全天不变 → HUD 稳定、玩家知道还剩多少。
- **动态的一份** = 刷怪**节奏**（何时刷/刷多快），由 `ambientTarget`（存活数自适应）
  与导演 `budget` 限流 → 玩家清得快就补得快，但**不影响当天总数**。
  → 用户说的"动态调整"指的是节奏，不是总数。

### 改动文件

- `src/systems/combat/KillCounter.ts` —— 重写。`DayEnemyProgress` 加 `spawned`；
  新增 `recordSpawn` / `remainingQuota` / `effectiveTotal`；
  `recordRecall` 改为纯记账；文件头写清"为什么要冻结"的完整推导。
- `src/core/Session.ts` —— `enemies` 类型加 `spawned`，注释改为"预计算后冻结"。
- `src/modes/WorldMode.ts` ——
  - import 加 `recordSpawn, remainingQuota`
  - `quotaAllows()` 改为 `remainingQuota(s) > 0`（弃用 alive 判定）
  - `spawnOne()` 内每个成功生成的个体 `recordSpawn(this.session)`
    （在 `pack` 循环里、`idx >= 0` 分支 → 粒度 = 个体，正确）
  - 出击初始化注释改为"预计算并冻结"
  - swarmHooks `onAgentRecalled` 注释更新

### 验证

`tsc --noEmit` **0 错误**；`vite build` 通过（15.46s，266 modules，
`waterSolve.worker` 正常）。未做游戏内实测。

### 遗留待确认

- ~~HUD 进度可能"打不满"~~ → **已定调（用户 2026-09-16）**：
  「让角色打满是刷怪的部分负责的」→ HUD 分母**保持冻结的原始 quota**，
  绝不用 `effectiveTotal()` 做补偿。打满的责任在**刷怪**，不在 UI。

---

## ★ 补上"打满"这一环：刷怪负责把 spawned 送到 quota

**用户定调**：「让角色打满是刷怪的部分负责的」

既然总数定死、分母冻结，那 `kills` 要能追平 `quota`，就必须保证
**刷怪最终会把 quota 只全都刷出来**。查下来有三处会让刷怪提前停：

1. **导演计划用尽**：`beginDay` 排定 n 场袭击，`plan` 走完就再无袭击，
   只剩环境散兵慢慢磨 → 磨不满。
2. **环境怪被存活数闸住**：`alive < ambientTarget` 才补 → 场上够多就
   不再生成新个体 → `spawned` 卡住。
3. **玩家低血停刷**：`playerHpRatio < 0.3` 完全不补（这条**保留**，
   是合理的低血保护）。

### 改动

- `src/systems/swarm/Director.ts`
  - `DirectorInputs` 新增 `quotaLeft?: number`（= quota − spawned；
    缺省 undefined → 按旧行为，兼容未接线调用方）
  - `calm` 阶段补怪逻辑改为：
    ```ts
    const left = inp.quotaLeft ?? 0;
    const backfill = left > 0;
    // 配额没刷完 → 节拍缩到 1/3（下限 1.2s），把缺口补上
    this.spawnTimer = backfill
      ? Math.max(1.2, this.threat.ambientInterval / 3)
      : this.threat.ambientInterval;
    // ★ alive 只在"配额已刷完"时才是硬闸；没刷完 → 允许越过 ambientTarget
    const aliveOk = backfill || inp.alive < this.threat.ambientTarget;
    if (aliveOk && inp.playerHpRatio >= 0.3) { ... }
    ```
  - 关键：**`alive` 从"硬闸"降级为"节奏闸"**。旧逻辑把它当硬闸 →
    场上够多就永远补不满，这正是打不满的根因之一。
- `src/modes/WorldMode.ts` —— `swarmDirector.update()` 调用处传入
  `quotaLeft: remainingQuota(this.session)`

### 为什么这样能自洽（三条路径的配合）

- **quota** 冻结 → 分母稳定，HUD 不跳
- **spawned** 只增 → 闸门稳定，不会因回收腾名额而无限刷
- **配额没刷完 → 环境补怪提速并越过 alive 闸** → `spawned` 必达 quota
- **`spawnWaveNear` 没放下的**（`want*10` 次尝试仍失败）→ 缺口留在
  `spawned` 的欠账里，由环境补怪兜底 → 两条路径天然互补
- lull（袭击后 18s 间歇）不补刷 —— 故意的呼吸感，时间很短，不构成缺口

### 验证

`tsc --noEmit` **0 错误**；`vite build` 通过（14.94s，266 modules，
`waterSolve.worker` 正常）。未做游戏内实测。

### 仍需实测确认（下次跑游戏时看）

- 一天打满后 HUD 是否稳定显示 `N / N`
- 配额未满时是否会看到"怪持续补上"的观感（而不是突然一批）
- 低血（<30%）时停刷 → 回血后是否恢复补刷

---

## 访客模型替换（进行中）：先解耦，再做确认页

**用户定调**：「你自己找合适模型替换，我不要太精细的，我要维持包体，
或者你另起一个展示网页，我确认模型可用，再正式替换」
→ 选了**两条一起做**：先解耦代码 + 另起确认页，等用户点头再正式替换。

### ★ 换模型的三处硬耦合（原来全按 Kenney 写死，会**静默失效**）

| 耦合点 | 原实现 | 换了模型的表现 |
|---|---|---|
| 动画名 | `clip.name === 'idle'/'walk'/'sprint'` 逐字匹配 | 三个 action 全 null → **人站着不动，不报错** |
| 头网格 | `getObjectByName('head-mesh')` | 找不到 → 换脸整段跳过 → 7 人全用模型自带脸 |
| 脸区 UV | `FACE_U0/U1/V0/V1` 按那张 512 贴图实测标定 | 取到乱码区 → **脸上是花的** |

**已实测证实**：候选模型 Quaternius Cube Guy 的动画名是
`CharacterArmature|CharacterArmature|CharacterArmature|Idle`，逐字匹配必然全挂。

### 解耦改动（`src/services/render/VisitorModelRenderer.ts`）

1. **动画名**：新增 `ANIM_CANDIDATES` 候选表 + `pickClipName()`
   - 优先级：`style.animNames` 显式覆盖 → 候选表**后缀**匹配 → 候选表子串匹配
   - 候选表：idle `['idle','stand','breathing',...]`、walk `['walk','walking',...]`、
     sprint `['sprint','run','jog','running',...]`（sprint 优先 run，walk 避开 walk_hold）
   - **兜底**：一条都没匹配上 → 用第 0 条循环 + `console.warn` 打出可用名列表
     → 至少不是"站着不动"；缺 walk/sprint 时用 idle 顶替
2. **头网格**：新增 `findHeadMesh()` 三级推断
   - ① `style.headMeshName` 精确 → ② 名字含 `head`/`skull`/`头` →
     ③ **几何推断：所有 SkinnedMesh 里局部包围盒最高的那个**（头在人体最高处）
   - 找不到时 `console.warn` 提示可用 `headMeshName` 指定（原来是静默 return）
3. **脸区 UV / 底色采样点**：改为 `style.faceRect` / `style.skinUV` 可覆盖，
   缺省仍用原 Kenney 实测值 → **老模型行为完全不变**

`VisitorModelStyle` 新增 4 个可选字段：`animNames` / `headMeshName` /
`faceRect` / `skinUV`。**解耦后换模型 = 只改 `visitors.ts` 一行 url**。

### 确认页（新建 `模型确认页/`）

- `模型确认页/index.html` —— 自包含 Three.js 查看器（CDN importmap，无需构建）
- 左右并排：左 = 当前 Kenney（241KB）／右 = 候选 Quaternius Cube Guy（334KB）
- 两卡都归一化到 **3.2m** + 脚底贴地 → 直接比比例/剪影
- 功能：切 idle/walk/run（0.2s 交叉淡化）· 自动旋转 · 参考网格 · 身高标尺线
- 材质统一成 `MeshToonMaterial`（与游戏内一致）；用 `pickClipName` 同源逻辑
- 每卡显示：动画条数 / 三角面数 / 缩放系数 / **动画匹配结果**（便于发现匹配失败）

**启动方式**：`python -m http.server 8777`（已跑起来并在内置浏览器打开过）

### 候选模型评估

| 模型 | 大小 | 三角面 | 动画 | 授权 | 结论 |
|---|---|---|---|---|---|
| Kenney Mini Characters（现用） | 241 KB | 低 | 32 条 | CC0 | 基准 |
| **Quaternius Cube Guy** | 334 KB | 3.1k | 18 条 | CC0 | **首选**（体量接近，无脸=抽象符号化） |
| Quaternius Universal Base Characters | — | 13.7k | 需配动画库 | CC0 | ❌ 太重，超包体预算 |
| Quaternius Animated Action Figure | 2.16 MB | 13.7k | 45 条 | CC BY | ❌ 太重 |

Cube Guy 特点：**无脸/块状抽象**，正好匹配用户"拉开与主角立绘距离 + 抽象符号化"
的要求；且无脸意味着**换脸那套逻辑可能不需要**（`swapFace:false` 即可）。

### 状态

- 解耦：`tsc --noEmit` **0 错误**
- 确认页：已验证 HTML/两个 GLB 均 200 且字节数正确
- **尚未正式替换** —— 等用户在确认页拍板
- 候选模型暂存于 `public/models/visitors/candidate_cubeguy.glb`（未接线，不影响构建）
- `visitors.ts` **未改**（7 名访客仍用 Kenney）
- 临时文件已全部清理（桌面 + 项目 root 均为 0）

---

## ★ 访客模型正式替换完成：Kenney → Quaternius Cube Guy

**用户定调**：「可以了，新模型更自然一些，把脸削平然后使用新模型吧」

### 关键发现：Cube Guy 的脸**本身就是平的**，不存在"需要削平的东西"

用户要求"削平脸"，但实测解析这个模型后发现**没有可削的东西**。证据链：

1. **全身是一整块 `Character` 蒙皮网格**（3122 三角面、单张 32×32 内嵌 Atlas），
   **没有独立头部网格**。骨骼里有 `Head` 骨，但没有 `head-mesh` 这样的独立对象。
   → 旧那套"删掉头正面小面片 + 补一块平板"的手术**在此模型上不可用**：
   `findHeadMesh()` 会退到几何推断、选中唯一的 SkinnedMesh（=全身），
   然后按"三顶点都在前平面"筛三角形 → **会把整个身体正面挖空**。危险操作，必须禁用。

2. **脸部所有顶点的 UV 收敛到同一个点**。取 Head 骨骼主导（权重>0.5）且 z 最大的
   那一层前脸顶点，202 个顶点 UV **全部等于 (0.0665, 0.3119)** → 贴图第 (2,22) 像素。
   即整个脸是一块**纯色平面**，贴图上没有任何五官。

3. **五官靠独立几何表达**：头部特写渲染可见脸平面是干净的肤色方块，
   眼睛是两个**独立的小面片**（不在脸平面上）。所以它看起来有表情，
   但"脸"确实已经是平的。

**结论**：`swapFace: false`。不需要任何几何手术，直接用模型自带眼睛
（更抽象，正合"拉开与主角立绘距离"的目标）。

### 换模型踩到的 5 个坑（都修了，值得记）

| # | 坑 | 表现 | 修法 |
|---|---|---|---|
| 1 | **`SkeletonUtils` 没有具名导出** | `does not provide an export named 'SkeletonUtils'` → 整个 module 崩，两个模型都不加载 | 该文件导出的是 `clone`，改 `import { clone as skeletonClone }` |
| 2 | **`file://` 下 CDN 模块被 CORS 拦** | `Unsafe attempt to load URL ... 'file:' URLs are treated as unique security origins` | 单文件页必须**把 three.js 一起打包内嵌**（esbuild bundle，0 外部引用） |
| 3 | **`String.replace` 会把 bundle 里的 `$&` 当替换模式** | 内嵌后占位符没被替换 / 内容错乱 | 一律用**函数式** `replace(pattern, () => value)` |
| 4 | **`setFromObject` 依赖 `matrixWorld`** | 模型挂到场景**之前**量包围盒 → 量到塌陷尺寸 → 缩放系数算成 0.03 → 人小到看不见 | 先 `add` 再 `updateMatrixWorld(true)` 再量。已同步修 `VisitorModelRenderer.setup()` |
| 5 | **贴图可以是外部 URI** | 单文件页里 Kenney 白模（GLB 里 `"uri":"Textures/colormap.png"`，图不在 GLB 内） | 写 `embed_tex.mjs` 把外部 PNG 塞进 BIN chunk + 注册 bufferView，再改 `images[]` 为内嵌引用 |

**坑 5 是个隐藏的选型优势**：Cube Guy 的贴图是 `"bufferView":338,"mimeType":"image/png"`
（**自包含**），而 Kenney 指向外部文件。自包含模型在单文件分发/离线场景更省事。

### 归一化测量的实证结论（避免以后再走弯路）

写 `measure_test.mjs` 对两个模型做了**三法对照**（`setFromObject` / 骨骼世界坐标 /
逐顶点 `applyBoneTransform` 蒙皮真值）：

| 模型 | setFromObject | 骨骼跨度 | **蒙皮真值** |
|---|---|---|---|
| Kenney | 0.671 | 0.343 | **0.671** ✅ |
| Cube Guy | 2.707 | 2.203 | **2.707** ✅ |

**`Box3.setFromObject()` 与蒙皮真值完全吻合，骨骼跨度反而漏掉头顶/脚底而偏小。**
→ 我之前一度改成"量骨骼"，那是错的（把 Kenney 从 ×4.77 弄成过缩放）。
**已回退为 `setFromObject`**，并把结论写进代码注释。

### 动画名匹配（解耦成果验证）

Cube Guy 是三层嵌套名 `CharacterArmature|CharacterArmature|CharacterArmature|Idle`。
用真实 clip 列表跑 `pickClipName` 验证：`idle→Idle`、`walk→Walk`、`sprint→Run`
**全部正确**，且 `endsWith` 优先正确避开了 `Idle_Attack` / `Walk_Hold` / `Run_Attack`。
→ **`visitors.ts` 里无需写 `animNames` 覆盖**，之前那轮解耦直接生效。

### 最终改动

- `src/config/visitors.ts` —— `VISITOR_MODEL_URL` 改指
  `/models/visitors/visitor_cubeguy.glb`；抽出共用的 `VISITOR_MODEL` 常量
  （含 `swapFace: false`）；7 名访客 `model: { ...VISITOR_MODEL, height }`；
  文件头写清换模型的三处差异与"为什么 swapFace:false"
- `src/services/render/VisitorModelRenderer.ts` —— 修 `setup()` 里
  `add`/`updateMatrixWorld` 与量包围盒的顺序（坑 4）
- `public/models/visitors/visitor_cubeguy.glb` —— 新增，334.3 KB（原 Kenney
  `visitor.glb` 241.1 KB 保留未删，便于回退）

### 验证

- `tsc --noEmit` **0 错误**
- `vite build` 通过（16.47s，266 modules，`waterSolve.worker` 正常产出 35.85 kB）
- 渲染验证：7 名访客全身预览图（各自身高 2.9~3.4m + Toon 材质 + 三态动画）
  全部正确显示，剪影干净抽象
- **未做游戏内实测**（避免与用户同开实例抢 GPU）

### 复用工具（留在 `模型确认页/`）

`glbinfo.mjs`（裸解析 GLB 结构）/ `measure_test.mjs`（三法对照量身高）/
`faceuv.mjs`（按骨骼权重定位脸部 UV 区域）/ `extract_tex.mjs`（导出内嵌贴图）/
`animcheck.mjs`（动画名匹配自检）/ `build.mjs`（打包成离线单文件页）/
`snap.mjs`（无头 Chrome 截图）/ `gallery.mjs`（按游戏内身高批量预览）

---

## ★★ 推翻上一条结论：Cube Guy 必须换脸，且脸在 **-y**

**用户反馈原话**：「旧模型删掉，脸上要削平贴纹理啊」
→ 我上一条写的「脸已经是平的、`swapFace:false`、无需手术」被否掉。
用户要的是**把各自立绘贴到脸上**，不是"用模型自带的小眼睛"。

### 我先做错了一步（记下来）

第一版判定"脸在 `+y`"，理由是按 three 常识"y 是前后、面朝 +y"。
**用户看到渲染结果直接指出：「你弄反了，纹理全在后脑勺了」。**

根因：**GLB 网格节点的 `rotation` 会整体旋转局部轴。** Cube Guy 的 mesh 节点带
`rotation = [-0.7071, 0, 0, 0.7071]`（绕 X 轴 −90°）→ **局部 `-y` 才映射到世界 `+z`**。
而 `Box3`/UV 都在**局部坐标系**里算，所以符号必须按局部轴判。

**教训**：前向轴的符号**不能靠常识猜**，必须"渲染 ±两向对比图，挑出能看到五官的那张"。
配置类型也改成带符号联合类型 `'+x'|'-x'|'+y'|'-y'|'+z'|'-z'`，逼自己写清方向。

### 三个硬事实（都是实测出来的，不是推测）

**① 局部轴：`z` = 高度、`y` = 前后、`x` = 左右**
- 脚 `z = 0.00003` → 头顶 `z = 0.02839`（跨度最大的才是"高"轴 —— 别用常识）
- `y` 只有 `±0.00571`
- 判据：**统计全模型三轴跨度，最大的是高度轴**

**② 这是"调色板贴图"，整个模型只有 8 个 UV**
- 贴图 **32×32 / 8bit / RGB**，**1794 个顶点只采样 8 个不同 UV**
- 8 个 v 全挤在 `0.307..0.329` 窄带
- 后果：`diag4` 画棋盘格渲染成**一整片灰**（每片只读单 texel）；
  **任何局部染色都做不到**（同色点共享 texel，会整身变色）
- **一条命令识别**：对 uv 去重计数 `new Set(...)` → 数量远小于顶点数就是调色板贴图

**③ 安全贴图区 = `u 0..6/32, v 23/32..1`**（px `0,368`–`96,512`）
- 全模型无任何顶点命中这块 → 可自由画画
- **但 UV 是双线性插值**，边缘会插到邻居 → 必须内缩半 texel（`PAD = 0.5/32`）

### 最终实现：`remapFaceUV`（不增删几何，只改 UV）

替代旧的 `flattenFace`（单块网格上会挖空整个身体正面，已确认禁用）。
`VisitorModelStyle` 新增 4 字段：
`faceMode: 'flatten' | 'remap'` / `faceFacingMin` / `frontAxis`(带符号) / `upAxis`。

算法：头部主导顶点（`主导关节==Head && w>0.5`）→ 法线 `* fSign > faceFacingMin` 取正脸
→ (左右轴, 高度轴) 包围盒归一化 → 写入脸区 UV（`v=0` 是图顶，故 `1 - tu`）。
**只改本实例 `geo.clone()` 出来的几何**，共享模板不动。

### 正脸判定阈值实测定标

| 阈值 | 命中顶点 | 正脸 bbox | 结论 |
|---|---|---|---|
| 0.50 | 192 | 0.01365 × 0.01246 | 带到侧脑/头顶 |
| 0.90 | 68 | 0.01297 × 0.01147 | 接近 |
| **0.95** | **67** | **0.01297 × 0.01147** | **最佳，宽高比 1.13** |
| 1.00 | 33 | 0.01296 × 0.00593 | 太窄，脸缩水 |

### `visitors.ts` 定稿

```ts
faceMode: 'remap', frontAxis: '-y', upAxis: 'z',
faceFacingMin: 0.95,
faceRect: { u0: 0/32, u1: 6/32, v0: 23/32, v1: 1 },
```
文件头注释已重写（删掉"脸已经是平的、无需削平"那套过时结论）。

### 验证

- `tsc --noEmit` **0 错误**；`vite build` 通过（20.46s，266 modules）
- **验收页 `swapfinal.html`**：内嵌 GLB + 7 个真实 `.ftx3.gz` 立绘，
  用游戏真实管线（`FtxAsset.load` + `compositeFrameToCanvas`）合成脸图，
  再套用与生产**逐行一致**的 `remapFaceUV`（含 `fSign=-1`）
  → 截图确认 **7 名访客立绘都在正脸、各贴各脸、没有串到后脑勺**
- 桌面成品：`C:\Users\22641\Desktop\访客换脸确认.html`（旧 `访客模型确认.html` 已删）
- **未做游戏内实测**（避免与用户同开实例抢 GPU）

### 本轮踩到的工具坑

- **`swapfinal` TDZ**：`paintFace()` 回调里调 `render()`，而 `const r = new WebGLRenderer(...)`
  声明在下方 → `Cannot access 'r' before initialization`。
  修法：把 相机/渲染器/`render` 声明块**提到回调定义之前**。
- **确认页要"照抄实现"而不是 import 生产模块**：`route2.mjs` 里 esbuild 报
  `Could not resolve "@game/vendor/player/FtxAsset"`（alias `@game` 指向游戏根，
  但源码在 `src/` 下）→ 正确路径是 `@game/src/vendor/player/FtxAsset`。
  最终改成逐行复制 `remapFaceUV` + 同源常量，反而更稳。
- **`asset.resolver.resolveFrame` 不存在**：`resolveFrame` 是 **`FtxAsset` 自身**的方法；
  `asset.resolver` 是 `FrameResolver`，其方法名是 `resolve`。
- 临时产物已清理，`模型确认页/` 只留 `swapfinal.html/.png` + `snap.mjs` + `candidate_cubeguy.glb`。

### 待办

- **游戏内实测**：7 名访客实际观感、动画时脸是否跟随（未做，避免抢 GPU）
- `public/models/visitors/candidate_cubeguy.glb`（候选副本）是否删 —— 未定
- 技能 `threejs-glb-model-swap` 已补入本轮 4 个坑（调色板贴图 / 前向轴带符号 /
  单块网格必须 remap / 中文路径截图）

---

## ★★ 访客换脸第 4 轮（最终）：'plate' 压平几何 + 删眼睛 —— 收敛并清理

**用户 4 轮纠正的完整轨迹**（每一轮都是我被证伪）：
1. 「你弄反了，纹理全在后脑勺了」→ 投影方向写反，选中后脑层
2. 「没削平前脸」→ 只改 UV 做不出"平"，几何还是凸的
3. 「你把头顶头发弄没了，而且纹理绘制在前面头发上，是不是分不清头顶头发和脸」
   → ① `z >= 0.026036 当头发` 把**整个头顶**削了（头是封闭盒！）
     ② "最靠前那一层"是**左侧鬓角 + 额前刘海**（L 形），**根本不含眼睛**
4. （拍板）「整个前脸片全削平再贴」+「扩大脸面到整个头正面」+「头发只去掉靠近脸的」
   + 「**把眼睛删了就行了**」

### 最终方案（`faceMode: 'plate'`，唯一策略）

`VisitorModelRenderer.flattenFacePlate()`：
- 选点：`isHeadDom(i) ∧ z ∈ [0.014933, 0.026036] ∧ proj = -y >= 0.0038` → 命中 **319/1794**
- 压平：顶点前向坐标统一设为 `-0.00585`（fSign=-1）；法线拉直成 `(0,-1,0)`
- UV：按 (左右, 高度) 归一化映射到 `faceRect u 0..0.4434 / v 0.62..1`
- 删眼：`z ∈ [0.0208,0.0223] ∧ |x| ∈ [0.0017,0.0060] ∧ proj >= 0.0040`，三顶点全中才删
  → 命中 **100 面**；只重排索引（不动顶点缓冲）→ 蒙皮零错位
- 头发**完全不动**

### 探针链（probe17-26，全在 `模型确认页/`）

| 探针 | 结论 |
|---|---|
| probe18 | "最靠前层" = L 形鬓角+刘海，连通块 1 个，**不含眼睛** |
| probe21 | 前脸区 (x,z) 网格：52.5% 处 88 面最密 = 眼睛；87.5% 处 115 面 = 发际线 |
| probe22 | 头发区连通块 **2 个**（352 面薄圈带 + 110 面顶隆起） |
| probe23 | 模拟删前刘海 → 删 384 面、留 **448 条边界边**（挖大洞，方案否决） |
| **probe24** | ★ **射线 441 点覆盖脸区，头发遮挡 0 个** ⇒ **不存在"垂在脸前的刘海"** |
| probe25 | 压平条件命中 319 顶点；眼睛 108/108 会压平；headDom 挡掉 0 个 |
| probe26 | UV 映射反推像素：方向正确，但旧 faceRect 只用 36% 高度（比例踩坑） |

### 两条通用铁律（本轮最有价值的产出）

1. **判断"脸平不平"必须用射线探针**（Möller–Trumbore），
   不能用"正脸顶点 bbox 跨度"——0.0038 的"起伏"是把耳朵算进正脸的统计口径错误，
   脸面层自己是**严格共面**（y 跨度 3e-9）。
2. **立绘区宽高比必须与脸面宽高比一致**。
   脸 1.169:1 vs 立绘区 0.625:1 → 走"横铺+纵裁"只用了 36% 高度，
   看起来像"没贴上"。现已按比例重选 faceRect，去掉裁剪逻辑，改 contain（不变形）。

### 本轮收尾：清理废弃路径（用户拍板后）

`faceMode` 从 3 值（`'flatten'|'remap'|'plate'`）收敛为 **1 值**，删掉：
- `remapFaceUV()`（只改 UV，做不出"平"）
- `stripFaceDetails()`（"z>=X 当头发"会削掉整个头顶）
- `flattenFace()`（Kenney 手术，在单块全身网格上会挖空整个身体正面）
- 常量 `FRONT_EPS` / `CG_FACE_Z_TOP` / `CG_FACE_U0..V1` / `stripFaceDetails` /
  `flattenFacePlate` / `faceFacingMin` 字段

⚠️ **清理时的自伤**：`resolveAxes` / `makeHeadDomTest` 两个辅助方法藏在被删区段里
（它们原本是 `flattenFacePlate` 与 `stripFaceDetails` 共用，物理位置在 remapFaceUV
之后），一并删掉 → `tsc` 报 TS2339。**修复**：把它们从 `private` 方法改成
**模块级函数**，放回 `findHeadMesh` 之后（它们不依赖实例状态，本来就不该是方法）。

### 验证

- `tsc --noEmit` **exit 0**
- `vite build` **exit 0**（20.99s，266 modules，`waterSolve.worker` 正常 35.85 kB）
- 验收页 `模型确认页/plate.mjs` 重建 + 截图：日志
  `压平顶点 319/1794 Head关节=8 宽高比 1.169 删眼睛 100 面 UV u 0.0156..0.4277 v 0.6356..0.9844`
  → ②④ 两格（压平版）可见**紫底 + 白框 + 黄/绿/红横条**（立绘确实贴上脸面）、
  侧脸是**竖直平面**、头发完整
- **未做游戏内实测**（避免与用户同开实例抢 GPU）

### 待办

- **游戏内实测**：7 名访客在真实 `.ftx3.gz` 立绘下的观感、动画时脸是否跟随
- 脸面上半被头发边缘遮住的观感是否可接受（属模型真实形状，可调 `CG_PLATE_Z1` / `flatProj`）

---

## 新遗物「喜羊羊」（闪灵的遗物）+ 抽卡分档概率重定

**用户定调**：「新建一个遗物叫喜羊羊，和闪灵有关」→ 追问后明确
**「喜羊羊是闪灵的外号」**（《明日方舟》闪灵的角/发型梗），走**治疗/守护系**，**5★**。

### 遗物配置（`src/config/relics.ts`）

```ts
xiyangyang: {
  id: 'xiyangyang', name: '喜羊羊', rarity: 5,
  texture: '/fx/喜羊羊.ftx3.gz',
  effects: [
    { type: 'regen', base: 1.2, perCopy: 0.6 },                              // 每秒回血
    { type: 'stat_multiplier', perDay: 1.08, perDayStep: 0.01, scope: 'defense' }, // 防御每日复利
    { type: 'respawn_time', base: 0.2, perCopy: 0.02 },                       // 复活加速
  ],
}
```

顺带为它**扩了 `stat_multiplier` 的 scope 分档**（`RelicEffects.ts`）：
原来只有 `'all'` / `'attack'`，新增 `'defense'` 分支。缺省分支仍是全属性 → 既有行为零变化。

### 抽卡分档概率（用户定调：弹药 35% / 可装备 45% / 遗物 18% / BOSS 2%）

★ **精确命中，非约等**。总刻度 **2000**：

| 档 | 件数 | 每件权重 | 档合计 | 占比 |
|---|---|---|---|---|
| 弹药消耗品 | 2 | 350 | 700 | **35.00%** |
| 可装备道具 | 4 | 225 | 900 | **45.00%** |
| 遗物 | 5 | 72 | 360 | **18.00%** |
| BOSS（独立判定） | 1 | — | (40) | **2.00%** |

**分类口径**（按 `items.json` 自动判定，不手写）：
- **弹药消耗品** = `type:'consumable'` 且无 `deployable` → 古米的蜂蜜糖、霜星的辣味糖
- **可装备道具** = `type:'equip'` **或** `deployable` → 黍姐的XX、逻各斯的圆凳、鱼生萌萌香、可露希尔的无人机
  （★ 无人机是 `consumable + deployable`，语义上属"可部署物"不属弹药 → 归此档）

5★ 遗物**统一权重**（用户要求）：5 件各 72，完全均等 → 每件 3.60%。

★ **BOSS 那 2% 是独立优先判定**（`GachaOverlay.pull` 里的 `bossPity` 递增管线），
不占池权重；配置里登记 `40` 只为让总量刻度自洽、便于校验。
（用户选择了"池内按 98% 归一"方案，但用 2000 刻度可以**同时**让四档精确成立。）

### ★★ 踩坑：遗物图标空白 —— `relics.ts` 的 `texture` 字段是**死数据**

用户反馈「喜羊羊纹理没有使用」。排查结论：

**`RelicItemConfig.texture` 在整个 src 下没有任何消费者。**
（grep `.texture` 命中的全是 Three.js 的 `renderTarget.texture` 之类，与此无关。）

遗物图标的**真实唯一真源**是 `src/services/item/ItemIconRegistry.ts` 里的硬编码表
`FTX_ICON_SOURCES`。新增遗物**必须在那里补一条**，否则图标空白。

**修法**：在 `FTX_ICON_SOURCES` 补 `xiyangyang: '/fx/喜羊羊.ftx3.gz'`。

**★ 顺手尝试"自动派生"失败，已回退**：本想把 `relicFtxSources()` 从
`RELIC_ITEM_CONFIG` 派生以消除双份配置 —— 但 `ItemIconRegistry` 值导入
`config/relics` 会把它拖进 core 层初始化链，风险大于收益。**已回退成显式声明**，
并把这条警告写进该文件的注释（"不要自动派生"）。

### 验证

- 立绘素材：`public/fx/喜羊羊.ftx3.gz`（15.3KB）→ gunzip 出 55164B，
  头部 `FTX3` + ver 3，与「衣服」「魔王的黑冠」**格式完全一致** → 素材合法
- 概率校验脚本：四档精确命中 35/45/18/2（`abs(...-目标) < 1e-9` 全 True）
- `tsc --noEmit` **exit 0**、`vite build` **exit 0**
- **未做游戏内实测**

### 待办

- 游戏内看：抽卡结果卡 / 遗物面板里「喜羊羊」图标是否正常显示（本次修复的直接验收点）
- 三条效果的实战体感（回血 1.2/s、防御复利、复活 -20%）
