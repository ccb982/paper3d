# 专题：架构治理 与 蜂群 v2 设计

> 索引见 `../MEMORY.md`。

## 架构治理

- **头部过重**（上限 1200 行，超出仅 warn，属已知债务）：
  - `WorldMode.ts` 4129 → **3570**（已拆）
  - `ChunkManager.ts` 3481
  - `GachaOverlay.ts` 1763
  - `FluidSolver.ts` 1505
  - **下一刀建议 `ChunkManager.ts`。**
- 进度：✅ P0-a（`setPhase` 收口）/ ✅ P1（拆 `WorldSpawner`）/ ✅ P2（删死代码 + guard）；
  ❌ 未做 P0-b（`BgmKey` 改 `keyof` 派生）。
- ★ **真正让项目膨胀的是「人肉同步」**：不变量靠注释 + 靠人记得改 N 处；
  `WorldMode` 是默认垃圾桶（新逻辑随手往里塞）。
- 拆大类见技能 `god-object-extraction`：
  - ★ 成员起止行**别用花括号配平**；
  - → 写**断言式迁移脚本**；
  - → ★ 验证用 `.py` **方法体归一化 difflib 逐行比对**。
  - **tsc 通过 ≠ 行为一致。**

## 蜂群 v2（设计稿 `全新的游戏/蜂群架构.md` §11~§25，待实施）

- ★ 寻路升级为 **HPA\* + 走廊带**（现 FlowField 只 324m 窗口 —— 这就是「找不到玩家」的根因）。
- 编队层次**正交于 LOD**（两者独立）。
- 降级链**不许**「找不到路就不动」。
- 硬不变量：
  - **两阵营通行掩码**（对己方也要可通行，否则 AI 自锁）；
  - 陷阱**不参与 HPA\***、不标脏 cluster；
  - 飞行兵**不参与地面寻路**（★ 用户主动要的减法：只轰炸、绝不缠斗）。
- ★ **其余细节一律查该文档，勿凭记忆。**
