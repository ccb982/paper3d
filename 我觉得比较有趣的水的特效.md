# 2D流体爆炸特效参数记录

水向外扩散又极剧收缩，但是总水量消失，像泡泡破裂（？）

---

## 爆炸系统配置

| 参数 | 当前值 | 说明 |
|------|--------|------|
| 爆炸触发频率 | 每3帧1次 | 模拟帧计数，非wall-clock时间 |
| 爆炸总次数 | 5次 | 序列结束后自动停止 |
| 爆炸强度数组 | [25000, 20000, 15000, 10000, 5000] | 递减强度 |
| 爆炸半径 | 0.15 | UV空间半径 |
| 爆炸持续时间 | 0.1秒 | 控制包络衰减 |
| **生成水花** | **false** | 当前已停用 |
| 爆炸期间压力迭代 | 200次 | 确保体积守恒 |
| 正常压力迭代 | 50次 | 非爆炸期间 |

---

## 散度源模型

| 参数 | 值 | 说明 |
|------|-----|------|
| 散度公式 | `S = -strength * envelope * mask` | 负散度产生向外膨胀 |
| 时间包络 | `4.0 * u * (1.0 - u)` | 二次抛物线，先升后降 |
| 物理效果 | 向外膨胀 | 负散度 → 流体体积增加 |

---

## 流体基础参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 密度 (ρ) | 1000 kg/m³ | 水密度 |
| 黏度 (μ) | 0.001 Pa·s | 动力黏度 |
| 表面张力 (γ) | 0.0728 N/m | 水-空气界面 |
| 重力 (g) | 9.81 m/s² | 向下 |
| 时间步长 (Δt) | 0.002 秒 | 固定步长 |
| 弹性系数 | 0.3 | 碰撞恢复 |
| 摩擦系数 | 0.95 | 速度衰减 |

---

## 求解器配置

| 参数 | 当前值 | 说明 |
|------|--------|------|
| **压力求解器** | **Jacobi迭代** | PCG已停用 |
| 压力迭代次数 | 50次（正常）/ 200次（爆炸期间） | - |
| Level Set重初始化 | 5次 | 界面恢复 |
| 最大粒子寿命 | 4秒 | 水体消散计时 |

---




这个反而是一个完美的爆炸效果
## 当前小水流 (LightFluidEntity) 参数
```
纹理尺寸：32 × 32
基础半径：baseRadius = sqrt(waterVolume) * texSize * 0.
45

边缘扰动参数：
├── 噪声类型：简化的哈希噪声（伪随机）
├── 八度数：3
├── 各八度频率：1, 2, 4
├── 各八度权重：1/oct (即 1.0, 0.5, 0.33)
├── 扰动强度乘数：2.0
└── 最终半径：baseRadius + perturbation
```
## 核心扰动代码
```
const hash = (x: number, y: number): number => {
    let h = x * 374761393 + y * 668265263;
    h = (h ^ (h >> 13)) * 1274126177;
    return (h ^ (h >> 16)) / 2147483648;
};

let perturbation = 0;
for (let oct = 1; oct <= 3; oct++) {
    const freq = 1 << oct;
    perturbation += (hash((x * freq) % w, (y * freq) 
    % h) - 0.5) * 2.0 / oct;
}
perturbation *= 2.0;  // 扰动强度

const perturbedRadius = baseRadius + perturbation;
data[i] = dist - perturbedRadius;
```