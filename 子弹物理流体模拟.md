# 子弹物理流体模拟实现文档

## 一、实现概述

本项目使用 `@bienehito/fluid-dynamics` 库实现了**子弹侧面视角**的流体拖尾特效，模拟子弹高速飞行时产生的卡门涡街、马赫环等物理现象。

## 二、核心实现配置

### 2.1 FluidDynamics 初始化参数

```typescript
this.fluidDynamics = new FluidDynamics(this.canvas, {
  width: 512,
  height: 512,
  simScale: 1.0,
  dyeScale: 1.2,
  curl: 12,
  velocityDissipation: 0.05,
  dyeDissipation: 0.08,
  pressureIterations: 20
});
```

### 2.2 参数说明

| 参数 | 值 | 作用 |
|-----|---|-----|
| `simScale` | 1.0 | 模拟缩放，保持高精度 |
| `dyeScale` | 1.2 | 染料扩散放大，增强可见性 |
| `curl` | 12 | 高旋转强度，涡旋效果明显 |
| `velocityDissipation` | 0.05 | 中等速度消散，尾气长度适中 |
| `dyeDissipation` | 0.08 | 较快颜色消散，避免拖影残留 |

### 2.3 子弹位置与方向

```typescript
this.bulletPosition.x = 250; // 偏右，留出尾气空间
this.bulletPosition.y = 180; // 偏上，避免贴地
this.bulletAngle = 0; // 固定方向向右
```

## 三、视觉效果结构

### 3.1 子弹侧面视角示意图

```
┌─────────────────────────────────────────────────────────────┐
│                    子弹侧面视图 (512x512)                  │
│                                                           │
│                                          ▓▓▓▓ ▓▓▓▓      │
│                                      ▓▓ 橙色马赫环 ▓▓    │
│                                    ▓▓                    │
│                                  ▓▓                      │
│    ████████████████████████████████                       │
│    ████ 弹头（白色椭圆形）      ↑                        │
│    ████████████████████████████████                       │
│        ↑                      蓝色尾气核心               │
│      弹头位置 x=250            向左延伸200px             │
│                                                           │
│        ○ ←→ ○  第一组涡旋 (粉色/青色)                    │
│                                                           │
│        ○ ←→ ○  第二组涡旋 (紫色/青色)                    │
│                                                           │
│        ○ ←→ ○  第三组涡旋 (紫色/青色)                    │
│                                                           │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 各部分实现代码

#### 弹头高压区（椭圆形）

```typescript
// 弹头高压区（椭圆形）
this.fluidDynamics.setVelocity(x, y, 0, 25, 15, 0, 0);
this.fluidDynamics.setDye(x, y, 0, 20, 12, [1, 1, 0.95]);
```

#### 马赫环（弹头前方橙色区域）

```typescript
// 弹头前方（橙色马赫环）
const aheadX = x + 30;
const aheadY = y;
this.fluidDynamics.setVelocity(aheadX, aheadY, 0, 18, 12, 60, 0);
this.fluidDynamics.setDye(aheadX, aheadY, 0, 15, 10, [1.0, 0.5, 0.2]);
```

#### 尾气核心区（锥形向后延伸）

```typescript
// 尾气核心区（从弹头后方开始的锥形区域）
const trailLength = 200;
const step = 15;
for (let dist = 25; dist <= trailLength; dist += step) {
  const backX = x - dist; // 向左延伸（弹头在右侧）
  const width = 8 + dist * 0.15; // 尾迹逐渐变宽
  const intensity = 1 - (dist / trailLength);

  // 尾气向后流动
  this.fluidDynamics.setVelocity(backX, y, 0, width, width * 0.6,
    -80 * intensity, (Math.random() - 0.5) * 20);
  this.fluidDynamics.setDye(backX, y, 0, width * 0.8, width * 0.5,
    [0.2, 0.4, 1.0]);
}
```

#### 卡门涡街（三组交替涡旋）

```typescript
// 卡门涡街：交替的涡旋在尾迹上下两侧
const vortexOffset = 12;
const vortexStrength = 150;
const alternating = Math.sin(this.elapsedTime * 2) > 0 ? 1 : -1;

// 第一组涡旋
const v1X = x - 50;
this.fluidDynamics.setVelocity(v1X, y + vortexOffset, 0, 15, 15,
  -vortexStrength * alternating, 0);
this.fluidDynamics.setDye(v1X, y + vortexOffset, 0, 12, 12, [1.0, 0.2, 0.6]);

this.fluidDynamics.setVelocity(v1X, y - vortexOffset, 0, 15, 15,
  -vortexStrength * alternating, 0);
this.fluidDynamics.setDye(v1X, y - vortexOffset, 0, 12, 12, [0.1, 0.8, 0.9]);

// 第二组涡旋
const v2X = x - 90;
const alternating2 = Math.sin(this.elapsedTime * 2 + Math.PI) > 0 ? 1 : -1;
this.fluidDynamics.setVelocity(v2X, y + vortexOffset * 1.5, 0, 14, 14,
  -vortexStrength * 0.8 * alternating2, 0);
this.fluidDynamics.setDye(v2X, y + vortexOffset * 1.5, 0, 11, 11, [0.9, 0.3, 1.0]);

this.fluidDynamics.setVelocity(v2X, y - vortexOffset * 1.5, 0, 14, 14,
  -vortexStrength * 0.8 * alternating2, 0);
this.fluidDynamics.setDye(v2X, y - vortexOffset * 1.5, 0, 11, 11, [0.1, 0.9, 0.7]);

// 第三组涡旋（更远，强度更弱）
const v3X = x - 130;
const alternating3 = Math.sin(this.elapsedTime * 2 + Math.PI * 0.5) > 0 ? 1 : -1;
this.fluidDynamics.setVelocity(v3X, y + vortexOffset * 2, 0, 12, 12,
  -vortexStrength * 0.6 * alternating3, 0);
this.fluidDynamics.setDye(v3X, y + vortexOffset * 2, 0, 10, 10, [0.7, 0.2, 0.9]);

this.fluidDynamics.setVelocity(v3X, y - vortexOffset * 2, 0, 12, 12,
  -vortexStrength * 0.6 * alternating3, 0);
this.fluidDynamics.setDye(v3X, y - vortexOffset * 2, 0, 10, 10, [0.2, 0.7, 0.8]);
```

#### 湍流效果（随机扰动）

```typescript
// 湍流效果：添加一些随机扰动
for (let i = 0; i < 5; i++) {
  const turbX = x - 40 - Math.random() * 150;
  const turbY = y + (Math.random() - 0.5) * 60;
  this.fluidDynamics.setVelocity(turbX, turbY, 0, 20, 20,
    (Math.random() - 0.5) * 50, (Math.random() - 0.5) * 50);
}
```

## 四、技术要点总结

### 4.1 子弹侧面视角的关键

1. **固定方向**：子弹方向固定向右（angle=0），尾气向左延伸
2. **弹头偏右**：弹头位置 x=250，为尾气留出约250px的延伸空间
3. **椭圆形弹头**：使用 `setVelocity(x, y, 0, 25, 15, 0, 0)` 形成椭圆形高压区
4. **尾气锥形扩散**：尾气宽度随距离增加而增大（width = 8 + dist * 0.15）

### 4.2 卡门涡街实现

1. **三组涡旋**：在不同距离（50px, 90px, 130px）生成三组涡旋
2. **交替旋转**：每组上下两个涡旋旋转方向相反
3. **强度递减**：远离弹头方向，涡旋强度逐渐减弱（1.0 → 0.8 → 0.6）
4. **颜色变化**：使用高饱和颜色（粉色、洋红、青色、紫色）

### 4.3 消散参数选择

选择 `velocityDissipation: 0.05` 和 `dyeDissipation: 0.08` 是为了：
- 尾气不会过于持久，避免视觉残留
- 保持适当的尾气长度（约200px）
- 让涡旋效果清晰可见但不过分夸张

## 五、文件结构

```
src/
├── systems/textures/
│   ├── TextureManager.ts          # 纹理管理器
│   └── BulletFluidTexture.ts       # 子弹流体纹理生成器（核心实现）
├── effects/
│   └── BulletFluidEffect.ts        # 子弹流体特效（应用纹理到3D平面）
└── core/
    └── EffectManager.ts            # 特效管理器
```

## 六、使用方式

```typescript
// 在场景中添加子弹流体特效
const position = new THREE.Vector3(5, 5, 0); // 位置（抬高到空中）
EffectManager.getInstance().playBulletFluidEffect(position, 60, 8);
// 参数：位置、持续时间(秒)、平面大小(单位)
```

## 七、效果特点

| 特点 | 描述 |
|-----|-----|
| **视角** | 子弹侧面视图，弹头在右，尾气在左 |
| **弹头** | 白色椭圆形高压区 |
| **马赫环** | 弹头前方的橙色区域 |
| **尾气** | 蓝色锥形，向后逐渐变宽，约200px长 |
| **涡旋** | 三组交替旋转的卡门涡街 |
| **湍流** | 随机扰动让尾气更自然 |
| **消散** | 中等速度，尾气长度适中，无明显残留 |