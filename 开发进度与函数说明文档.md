# 开发进度与函数说明文档

## 1. 项目概述

本项目是一个基�?React + Three.js �?3D 纸片风格游戏，核心玩法包�?*日常行动**�?*战斗**两种模式，支�?*锁定�?*�?*自由�?*两种射击系统�?
### 1.1 核心功能

- **页面管理�?*：通过 `gameStore.mode` 控制页面切换，支持菜单、日常、战斗、商店、抽卡、设�?6 种模�?- **双射击系�?*：锁定射击（5 条射线检测�? 秒锁定）+ 自由射击（指针锁定、点击发射）
- **子弹系统**：基于实体系统的子弹实体，支持范围伤害和特效
- **调试模式**：可视化射线、坐标信息、射击方向等调试信息
- **鼠标锁定控制**：根据模式和射击系统自动控制指针锁定
- **实体系统**：基于Entity基类的继承体系，支持角色、静态物品和子弹实体
- **碰撞管理**：统一的碰撞检测系统，支持空间分区优化
- **特效系统**：纯程序化特效，包括命中闪光、枪口闪光、爆炸和爆裂黎明特效
- **动画系统**：支持序列帧动画和按键控�?
### 1.2 技术栈

| 技�?| 用�?|
|------|------|
| React 18 | 前端框架 |
| Three.js | 3D 渲染核心 |
| @react-three/fiber | React Three.js 绑定 |
| @react-three/drei | React 专用 3D 工具�?|
| @react-three/postprocessing | 后处理效�?|
| Zustand | 全量级全局状态管�?|
| Vite | 开发服务器和构建工�?|
| TypeScript | 类型系统 |
| simplex-noise | 噪声生成�?|

---

## 2. 开发进�?
### 2.1 已完成功�?
| 功能模块 | 完成状�?| 最后更新日�?| 描述 |
|---------|---------|-------------|------|
| 地形生成系统 | �?完成 | 2026-04-16 | 基于 Simplex 噪声的多八度地形生成，优化为以平原为主（�?0%左右�?|
| 光线渲染系统 | �?完成 | 2026-04-16 | 基于海拔差的四档光影算法，远处亮度优化，角色周围20格暗部亮度调�?|
| 角色物理系统 | �?完成 | 2026-04-16 | 重力系统，碰撞检测，防止角色穿模，确保角色脚位置不低于地形高�?1.5 |
| 场景元素渲染 | �?完成 | 2026-04-16 | 草丛和靶子的自适应放置，根据地形高度调整位�?|
| 纹理系统 | �?完成 | 2026-04-16 | 地形纹理加载和重复设置（10x10重复），草纹理加�?|
| 辅助光照系统 | �?完成 | 2026-04-16 | 环境光、方向光、背光补光，色调映射（ReinhardToneMapping�?|
| 场景背景 | �?完成 | 2026-04-16 | 蓝白色背景（#87CEEB），模拟白天效果 |
| 后处理效�?| �?完成 | 2026-04-16 | Bloom 模糊效果，为亮部区域添加柔和的光�?|
| 性能优化 | �?完成 | 2026-04-16 | 地形细分度优化（�?4减少�?2），减少计算和渲染开销 |
| 射击系统架构 | �?完成 | 2026-04-18 | 基于接口和管理器模式，支持自由式和锁定式双模式动态切�?|
| 锁定式目标检�?| �?完成 | 2026-04-18 | THREE.Raycaster射线检测，多射线布局（十字形5射线），动态补偿修�?|
| 锁定进度UI | �?完成 | 2026-04-18 | SVG圆环进度显示，CSS动画，锁定倒计时反�?|
| 自由式射线检�?| �?完成 | 2026-04-18 | 相机射线检测实际击中点，子弹精确射向击中位�?|
| 子弹系统 | �?完成 | 2026-04-18 | 基于实体系统的子弹实�?BulletEntity)，碰撞检测和生命周期管理 |
| **页面管理�?* | �?完成 | 2026-04-18 | 通过 gameStore.mode 控制页面切换，支�?种模�?|
| **鼠标锁定控制** | �?完成 | 2026-04-18 | 根据模式和射击系统自动请�?释放指针锁定 |
| **调试模式** | �?完成 | 2026-04-18 | 可视化射线、坐标信息、射击方向控制（默认关闭，按F12切换�?|
| **Canvas常驻架构** | �?完成 | 2026-04-18 | Canvas始终渲染，GameWorld组件承载所�?D逻辑 |
| **射击系统状态管�?* | �?完成 | 2026-04-18 | 射击系统成为唯一数据源，通过回调传递状态给UI |
| **角色位置监听�?* | �?完成 | 2026-04-18 | 使用独立的CharacterPositionStore单例，通过节流更新减少UI重渲�?|
| **实体系统架构** | �?完成 | 2026-04-18 | 实体基类(Entity)、实体管理器(EntityManager)、子弹实�?BulletEntity) |
| **角色实体系统** | �?完成 | 2026-04-18 | CharacterEntity基类、FriendlyEntity友方实体、EnemyEntity敌方实体 |
| **玩家角色管理�?* | �?完成 | 2026-04-18 | PlayerCharacterManager管理当前操控角色，快速访问接�?|
| **角色直面摄像�?* | �?完成 | 2026-04-18 | FriendlyEntity实现角色始终背对摄像机，直面玩家 |
| **敌人 AI** | �?完成 | 2026-04-18 | EnemyEntity实现AI追踪玩家、跟随玩�?|
| **敌人可射击属�?* | �?完成 | 2026-04-18 | 为所有角色添加isShootable属性，敌人可被射击系统识别为目�?|
| **静态物品实体系�?* | �?完成 | 2026-04-18 | 实现StaticEntity基类和TargetEntity靶子实体，支持可射击和命中反�?|
| **碰撞管理�?* | �?完成 | 2026-04-18 | 实现CollisionManager统一管理碰撞检测，支持多种碰撞类型和回�?|
| **空间分区优化** | �?完成 | 2026-04-18 | 实现网格空间分区GridSpatialPartition，支持大规模实体高效碰撞检�?|
| **碰撞冷却机制** | �?完成 | 2026-04-18 | 添加碰撞冷却防止同对物体重复触发，优化碰撞处理频�?|
| **伤害系统** | �?完成 | 2026-04-19 | 实现DamageInfo接口支持多种伤害类型，实体基类添加health和maxHealth属�?|
| **血条UI系统** | �?完成 | 2026-04-19 | 实现EntityHealthBars组件，显示敌人和靶子的实时生命�?|
| **相机存储系统** | �?完成 | 2026-04-19 | 实现CameraStore单例，用于在不同组件间共享相机引�?|
| **敌人AI模式切换** | �?完成 | 2026-04-19 | 敌人在非战斗模式下随机移动，战斗模式下追踪玩�?|
| **非战斗模式攻击禁�?* | �?完成 | 2026-04-19 | 非战斗模式下敌人碰撞不造成伤害，子弹击中敌人自动切换到战斗模式 |
| **子弹伤害修复** | �?完成 | 2026-04-19 | 修复子弹对敌人造成伤害的逻辑，正确处理敌人阵营检�?|
| **敌人血条初始化** | �?完成 | 2026-04-19 | 修复敌人初始血条不满的问题，同时设置health和maxHealth |
| **动画系统** | �?完成 | 2026-04-19 | 实现角色动画系统，支持序列帧动画和按键控�?|
| **按键控制动画** | �?完成 | 2026-04-19 | 按W键播放正面动画，按S键播放后背动�?|
| **特效系统** | �?完成 | 2026-04-19 | 实现纯程序化特效系统，包括命中闪光、枪口闪光、爆炸和环形�?|
| **特效管理�?* | �?完成 | 2026-04-19 | 实现EffectManager单例，统一管理特效生命周期 |
| **爆裂黎明特效** | �?完成 | 2026-04-19 | 实现华丽的爆裂黎明特效，包含核心爆闪、多环冲击波、粒子喷射等多层视觉效果，特效自动朝向相�?|
| **爆裂黎明子弹** | �?完成 | 2026-04-19 | 实现DawnExplosionBulletEntity，支�?0单位半径的范围伤�?|
| **实体目录结构优化** | �?完成 | 2026-04-19 | 优化实体目录结构，将实体按类型分类到不同子文件夹 |
| **子弹火焰精灵特效** | �?完成 | 2026-04-20 | 为爆裂黎明子弹添加动态火焰精灵特效，增强视觉效果 |
| **子弹视觉优化** | �?完成 | 2026-04-20 | 降低子弹移�?0%，增大子弹体积和碰撞半径，提高发光强度，使子弹更易观�?|
| **火焰特效优化** | �?完成 | 2026-04-21 | 实现地图场景火焰特效，支�?D粒子、动态轮廓和填充效果，添加颜色渐变和亮度衰减 |
| **重力系统增强** | �?完成 | 2026-04-21 | 为角色基类添加更完善的重力系统，支持跳跃和与地形的交�?|
| **竖直碰撞检�?* | �?完成 | 2026-04-21 | 实现角色与地形的竖直碰撞检测，确保角色不会穿透地�?|
| **原石虫敌人类** | �?完成 | 2026-04-21 | 创建半椭球形状的原石虫敌人，背上随机生成6-9根红色圆�?|
| **实体位置调整** | �?完成 | 2026-04-21 | 下调所有敌人和火焰实体的生成位置，使其与地形更加协�?|
| **水面实体系统** | �?完成 | 2026-04-24 | 实现WaterEntity水面实体类，支持动态波动效�?|
| **水面波动方程** | �?完成 | 2026-04-24 | 使用波动方程实现水面动态模拟，包含大波和小波叠�?|
| **水面随机扰动** | �?完成 | 2026-04-24 | 实现水面随机涟漪效果，模拟微风和水波扩散 |
| **洪水填充河流生成** | �?完成 | 2026-04-24 | 使用洪水填充算法在地形低洼区域生成完整河�?|
| **子弹速度调整** | �?完成 | 2026-04-24 | 子弹速度�?提升�?5，加快射击节�?|
| **子弹伤害增强** | �?完成 | 2026-04-24 | 基础子弹伤害1�?，爆裂子弹伤�?�?，伤害乘3 |
| **敌人高度拉长** | �?完成 | 2026-04-24 | 敌人mesh高度scale设为1.5倍，增强视觉表现 |
| **敌人碰撞偏移** | �?完成 | 2026-04-24 | 敌人groundOffset设为2.2，实现碰撞位置上�?.1单位 |
| **水面涟漪系统** | �?完成 | 2026-04-25 | 实现角色进入水中时的涟漪效果，支持扩散sin波和随机强度 |
| **水面渲染优化** | �?完成 | 2026-04-25 | 修复水面透明边框问题，优化渲染性能 |
| **静态水面优�?* | �?完成 | 2026-04-25 | 降低静态模式波动参数，使水面更加平静自�?|
| **流体库安�?* | �?完成 | 2026-04-27 | 安装 @bienehito/fluid-dynamics 流体动力学库 |
| **子弹流体纹理** | �?完成 | 2026-04-27 | 实现子弹侧面视角的流体拖尾纹理生成器 |
| **子弹流体特效** | �?完成 | 2026-04-27 | 实现BulletFluidEffect特效类，应用纹理�?D平面 |
| **纹理管理器集�?* | �?完成 | 2026-04-27 | 扩展TextureManager支持BulletFluidTexture注册 |
| **尾气分层上色** | �?完成 | 2026-04-27 | 实现尾气核心蓝色、卡门涡街多色、马赫环橙色的分层效�?|
| **2D流体模拟器重�?* | �?完成 | 2026-05-07 | 重构为自研Level Set流体模拟器，实现完整2D单相自由表面流模�?|
| **流体寿命系统** | �?完成 | 2026-05-07 | 实现水滴追踪的年龄平流系统，支持真实时间计时和流体消�?|
| **爆炸散度源模�?* | �?完成 | 2026-05-08 | 将爆炸视为体积源注入散度，依赖压力求解器产生向外推进的压力梯�?|
| **水花速度同步修复** | �?完成 | 2026-05-08 | 修复新生成水花速度不参与散度计算和压力投影的问�?|
| **重力应用修复** | �?完成 | 2026-05-08 | 确保爆炸使用包含重力的速度场，流体正确下落 |
| **压力求解器符号修�?* | �?完成 | 2026-05-08 | 统一PCG和Jacobi求解器的拉普拉斯符号，确保压力梯度方向正�?|
| **爆炸计时一致�?* | �?完成 | 2026-05-08 | 使用模拟帧数代替wall-clock时间，确保爆炸行为一�?|
| **解耦边界处�?* | �?完成 | 2026-05-08 | 实现边界环阻尼，允许流体越过边界膨胀，避�?碰壁回弹" |
| **各向异性爆炸系�?* | �?完成 | 2026-05-08 | 支持四极子和偶极子模式，实现方向性爆炸效果，包含收缩阻尼 |
| **轻量级流体实�?* | �?完成 | 2026-05-08 | 实现LightFluidEntity类，内部包含32x32小型FluidSimulator，支持EntityManager管理 |
| **液滴创建接口** | �?完成 | 2026-05-08 | EntityManager新增createDroplets/createDroplet异步方法，支持批量创建液�?|
| **外部力接�?* | �?完成 | 2026-05-09 | 实现FluidExternalForce和IFluidForceTarget接口，统一外部力施加方�?|
| **局部速度注入** | �?完成 | 2026-05-09 | 实现addLocalVelocityImpulse方法，支持局部区域速度和最大速度限制 |
| **随机横向力系�?* | �?完成 | 2026-05-09 | �?秒对水流施加随机横向力，每帧增量0.001，最大速度2.0 |
| 流体区域管理�?| �?完成 | 2026-05-09 | 实现FluidRegionManager类，基于距离的LOD控制，支持距�?10单位自动销�?|
| LOD系统 | �?完成 | 2026-05-09 | 四档LOD（HIGH/MEDIUM/LOW/OFF），控制模拟频率和纹理更�?|
| 安全实体删除 | �?完成 | 2026-05-09 | 标记-延迟删除机制，避免迭代期间修改entities映射�?|
| 时间缩放系统 | �?完成 | 2026-05-09 | EntityManager全局timeScale参数，解决游戏时间与真实时间不同步问�?|
| 活跃模拟限制 | �?完成 | 2026-05-09 | maxActiveSimulations=4，限制同时活跃的流体模拟实例�?|
| 累计时间�?| �?完成 | 2026-05-09 | 使用累积时间计算加速度，提高跳帧时的物理精�?|
| GPU质心计算 | �?完成 | 2026-05-11 | 实现computeFluidCentroidGPU()，用归约计算质心替代包围盒计算，避免GPU回读大数�?|
| 模拟器参数优�?| �?完成 | 2026-05-11 | 提高pressureIterations(4)、reinitIterations(1)、surfaceTension(0.1)，增强外力响应灵敏度 |
| 局部速度注入 | �?完成 | 2026-05-11 | processPendingForces改用addLocalVelocityImpulse，产生局部速度梯度 |
| 世界加速度转散�?| �?完成 | 2026-05-11 | 将世界加速度转化为局部散度注入，模拟惯性挤压效�?|
| 外力注水系统 | �?完成 | 2026-05-11 | 在GameWorld中为小液滴添加持续注水，增强水体补充 |
| **水面纹理分裂系统** | �?完成 | 2026-05-11 | 实现FluidFragmentSystem，支持GPU加速分裂、扇形提取、碎片实体化 |
| **流体居中系统** | �?完成 | 2026-05-11 | 实现GPU质心计算与纹理平移，防止流体漂移出边界 |
| **循环依赖修复** | �?完成 | 2026-05-11 | 抽取FluidTypes.ts解决EntityManager与FluidRegionManager循环依赖 |
| **代码重复消除** | �?完成 | 2026-05-11 | 统一液滴创建逻辑到FluidRegionManager，消除重复代码 |

### 2.2 待开发功�?
| 功能模块 | 优先�?| 描述 |
|---------|--------|------|
| 商店功能 | �?| 商品购买、金币消耗界面和逻辑 |
| 抽卡功能 | �?| 随机奖励、卡池管理界面和逻辑 |
| 升级系统 | �?| 角色属性提升界面和逻辑 |
| 动态光源系�?| �?| 支持太阳位置变化，模拟一天中不同时间的光�?|
| 天气系统 | �?| 根据天气状况调整光照参数和场景效�?|
| 阴影映射 | �?| 实现更真实的阴影效果 |
| 角色动画 | �?| 添加角色行走、跳跃等动画效果 |
| 音效系统 | �?| 背景音乐、射击音�?|
| 存档系统 | �?| 本地存储游戏进度 |

---

## 3. 页面管理器架�?
### 3.1 页面模式定义

```typescript
// src/systems/state/gameStore.ts
export enum GameMode {
  MENU = 'menu',       // 主菜�?  DAILY = 'daily',     // 日常行动（派遣、对话、收集资源）
  BATTLE = 'battle',   // 战斗模式
  SHOP = 'shop',       // 商店
  GACHA = 'gacha',     // 抽卡
  SETTING = 'setting', // 设置
}
```

### 3.2 页面渲染规则

| 模式 | 3D Canvas | 调试面板 | Crosshair | 页面组件 |
|------|-----------|----------|-----------|----------|
| MENU | �?渲染（常驻） | �?| �?| MenuPage |
| DAILY | �?渲染（常驻） | �?| �?| DailyPage |
| BATTLE | �?渲染（常驻） | �?| �?| BattleHUD |
| SHOP | �?渲染（常驻） | �?| �?| ShopPage |
| GACHA | �?渲染（常驻） | �?| �?| GachaPage |
| SETTING | �?渲染（常驻） | �?| �?| SettingPage |

> 注意：Canvas 始终渲染（常驻），GameWorld 组件承载所�?3D 内容，根据模式显�?隐藏调试面板和准�?
### 3.3 UI 布局

```
┌─────────────────────────────────────────────────�?�? [调试面板]                                      �?�? 左上�?(调试模式开启时)                          �?�?                                                 �?�?           (3D Canvas 游戏世界)                   �?�?                                                 �?�? [BattleHUD]              [DailyPage]            �?�? 左下�?                 右下�?                  �?└─────────────────────────────────────────────────�?```

### 3.4 模式切换方法

| 方法 | 功能 | 路由 |
|------|------|------|
| `setMode(mode)` | 直接切换页面模式 | - |
| `startBattle()` | 进入战斗模式 | DAILY �?BATTLE |
| `exitBattle()` | 退出战斗，返回上一�?| BATTLE �?previousMode |
| `openDaily()` | 进入日常模式 | MENU �?DAILY |
| `exitDaily()` | 退出日常，返回菜单 | DAILY �?MENU |
| `openShop()` | 进入商店 | MENU �?SHOP |
| `exitShop()` | 退出商�?| SHOP �?previousMode |
| `openGacha()` | 进入抽卡 | MENU �?GACHA |
| `exitGacha()` | 退出抽�?| GACHA �?previousMode |
| `openSettings()` | 进入设置 | MENU �?SETTING |
| `exitSettings()` | 退出设�?| SETTING �?previousMode |

---

## 4. 射击系统架构

### 4.1 系统结构

```
IShootingSystem (接口)
    �?BaseShootingSystem (抽象类，提供通用实现)
    �?    ├── FreeStyleShootingSystem (自由�?
    └── LockonShootingSystem (锁定�?

ShootingSystemManager (管理器，统一调度)
```

### 4.2 射击模式行为

| 模式 | 射击系统 | 鼠标行为 | 指针锁定 |
|------|----------|----------|----------|
| 日常 (DAILY) | 锁定式（**强制**�?| 显示 | �?不需�?|
| 战斗 (BATTLE) - 锁定 | 锁定�?| 显示 | �?不需�?|
| 战斗 (BATTLE) - 自由 | 自由�?| 隐藏 | �?需�?|
| 其他模式 | - | 显示 | �?不需�?|

### 4.3 IShootingSystem 接口

```typescript
export interface IShootingSystem {
  name: string;                    // 系统名称
  initialize(): void;              // 初始�?  update(delta: number): void;     // 每帧更新
  onMouseDown(event: MouseEvent): void;   // 鼠标按下
  onMouseUp(event: MouseEvent): void;     // 鼠标释放
  onMouseMove(event: MouseEvent): void;   // 鼠标移动
  dispose(): void;                 // 销�?  requiresPointerLock(): boolean;  // 是否需要指针锁�?  getRayData(): RayData[];         // 获取射线数据（用于可视化�?  setActive(active: boolean): void; // 设置是否为活跃系�?}

export interface ShootingCallbacks {
  onBulletCreated?: (bullet: BulletData) => void;
  onBulletDestroyed?: (id: number) => void;
  onTargetLocked?: (target: LockTarget | null) => void;
  onLockStateChanged?: (isLocking: boolean, countdown: number) => void;
  onRayDataUpdated?: (rayData: RayData[]) => void;
  onShootDirectionChanged?: (direction: { x, y, z } | null) => void;
}
```

### 4.4 锁定式射�?(LockonShootingSystem)

#### 锁定流程

```
鼠标按下 �?开始锁�?�?检测目�?�?显示锁定进度 �?1秒后锁定成功 �?自动射击
```

#### 多射线布局

```
    [1]
     |
[3]-[0]-[4]
     |
    [2]
```

- `[0]`: 中心射线（朝向相机方向）
- `[1]`: 上方射线
- `[2]`: 下方射线
- `[3]`: 左侧射线
- `[4]`: 右侧射线

#### 动态补偿修�?
```typescript
export function getCorrectedNDC(
  canvas: HTMLCanvasElement,
  clientX: number,
  clientY: number,
  camera: THREE.Camera,
  characterPosition: THREE.Vector3,
  baseCompensation: number = 0.3
): CorrectedNDCResult {
  // 1. 计算基础 NDC 坐标
  const ndcX = ((clientX - rect.left) / rect.width) * 2 - 1;
  const ndcY = -((clientY - rect.top) / rect.height) * 2 + 1;

  // 2. 计算相机仰角
  const pitch = getCameraPitch(camera);

  // 3. 距离补偿：远距离�?25米）完全不修�?  const distanceToCamera = camera.position.distanceTo(characterPosition);
  const distanceCompensation = distanceToCamera > 25 ? 0 : Math.min(1.2, 20 / distanceToCamera);

  // 4. 俯角补偿：俯角越大，补偿系数越大
  const pitchCompensation = 1.0 + Math.max(0, pitch) * 0.5;

  // 5. 综合补偿系数
  const totalCompensation = baseCompensation * distanceCompensation * pitchCompensation;
  const correction = -pitch * totalCompensation;

  // 6. 应用修正
  const correctedNDC = new THREE.Vector2(ndcX, ndcY + correction);
  return { raw: rawNDC, corrected: correctedNDC };
}
```

### 4.5 自由式射�?(FreeStyleShootingSystem)

```typescript
private getRaycastHitPoint(): THREE.Vector3 | null {
  const raycaster = new THREE.Raycaster();
  const direction = new THREE.Vector3();
  this.camera.getWorldDirection(direction);
  raycaster.set(this.camera.position, direction);

  if (this.shootableObjects.length > 0) {
    const intersects = raycaster.intersectObjects(this.shootableObjects, true);
    if (intersects.length > 0) {
      return intersects[0].point;
    }
  }

  // 未击中任何物体时，返回射线最远点
  return this.camera.position.clone().add(direction.multiplyScalar(100));
}
```

### 4.6 鼠标锁定控制

```typescript
// App.tsx - 模式切换时的鼠标锁定处理
useEffect(() => {
  const needsPointerLock = (mode === GameMode.BATTLE && activeShootingSystem === 'freestyle');

  if (needsPointerLock) {
    gameContainer.requestPointerLock();
    gameContainer.style.cursor = 'none';
  } else {
    document.exitPointerLock();
    gameContainer.style.cursor = 'default';
  }
}, [mode, activeShootingSystem]);
```

---

## 5. 角色实体系统

### 5.1 系统架构

```
Entity (基类)
├── CharacterEntity (角色基类)
�?  ├── FriendlyEntity (友方实体)
�?  �?  ├── cameraRef: THREE.Camera | null - 相机引用
�?  �?  ├── setCamera(camera) - 设置相机引用
�?  �?  ├── faceToCamera(delta) - 让角色直面摄像机
�?  �?  └── update() - 重写，从 characterPositionStore 获取位置
�?  └── EnemyEntity (敌方实体)
�?      └── updateAI(delta) - 追踪玩家
├── BulletEntity (子弹实体)
�?  └── DawnExplosionBulletEntity (爆裂黎明子弹)
└── StaticEntity (静态物品基�?
    └── TargetEntity (靶子实体)
```

### 5.2 实体目录结构

```
src/entities/
├── bullets/            // 子弹实体
�?  ├── BulletEntity.ts
�?  └── DawnExplosionBulletEntity.ts
├── characters/         // 角色实体
�?  ├── CharacterEntity.ts
�?  ├── FriendlyEntity.ts
�?  └── EnemyEntity.ts
├── static/             // 静态物品实�?�?  ├── StaticEntity.ts
�?  └── TargetEntity.ts
└── Entity.ts           // 实体基类
```

### 5.3 角色实体基类 (CharacterEntity)

**功能**：所有角色（玩家、敌人、NPC）的父类，提供通用属性和方法

**主要属�?*�?- `faction`: 阵营 ('friendly' �?'enemy')
- `health` / `maxHealth`: 生命�?- `speed`: 移动速度
- `moveDirection`: 移动方向
- `isPlayerControlled`: 是否由玩家控�?- `isMoving`: 是否正在移动
- `attackCooldown` / `attackRate`: 攻击冷却
- `isGrounded` / `verticalVelocity`: 重力状�?
**特殊属�?*�?- `isShootable: true`: 所有角色都可被射击，敌人会被射击系统识别为可射击目�?
**主要方法**�?- `loadTexture(path)`: 异步加载纹理
- `setTexture(path)`: 动态更换贴�?- `update(delta)`: 每帧更新
- `updateAI(delta)`: 更新AI行为（抽象方法）
- `attack(delta)`: 执行攻击
- `applyGravity(delta)`: 应用重力
- `takeDamage(amount)`: 承受伤害
- `onDeath()`: 角色死亡时调�?- `isEnemy(other)`: 判断是否为敌�?
### 5.4 友方实体 (FriendlyEntity)

**功能**：友方角色（玩家控制或友方AI角色�?
**特殊功能**�?- **玩家控制模式**：从 `characterPositionStore` 获取玩家位置
- **AI控制模式**：当 `isPlayerControlled` �?false 时，跟随玩家
- **角色直面摄像�?*：玩家控制的角色始终背对摄像机（直面玩家�?
**核心代码**�?
```typescript
public update(delta: number): void {
  if (!this.isActive) return;

  if (this.isPlayerControlled) {
    // �?characterPositionStore 获取玩家位置
    const storePos = characterPositionStore.position;
    this.position.copy(storePos);
    this.mesh.position.copy(this.position);
    this.isMoving = characterPositionStore.isMoving;

    // 让角色直面摄像机
    this.faceToCamera(delta);
  } else {
    super.update(delta);
  }

  if (this.attackCooldown > 0) {
    this.attackCooldown -= delta;
  } else {
    this.attack(delta);
  }
}

private faceToCamera(delta: number): void {
  if (!this.cameraRef || !this.mesh) return;

  const cameraPos = this.cameraRef.position.clone();
  const characterPos = this.mesh.position.clone();

  // 计算从角色指向相机的水平方向
  const toCamera = new THREE.Vector3().subVectors(cameraPos, characterPos);
  toCamera.y = 0;
  toCamera.normalize();

  // 角色背面朝向相机 �?角色的正面方�?= 相机方向的相反数
  const targetDirection = toCamera.clone().negate();

  // 计算目标旋转（只绕Y轴）
  const targetQuat = new THREE.Quaternion().setFromUnitVectors(
    new THREE.Vector3(0, 0, 1),  // 角色默认正面�?+Z
    targetDirection
  );

  // 平滑旋转
  const rotationSpeed = 8.0;
  this.mesh.quaternion.rotateTowards(targetQuat, rotationSpeed * delta);
  this.mesh.quaternion.normalize();
}
```

### 5.5 敌方实体 (EnemyEntity)

**功能**：敌方AI角色，自动追踪玩�?
**AI行为**�?- 查找 `isPlayerControlled` �?true 的角色作为目�?- 计算到目标的水平方向
- 设置移动方向为朝向目�?
**核心代码**�?
```typescript
protected updateAI(delta: number): void {
  const target = EntityManager.getInstance().getEntitiesByType('character')
    .find(e => (e as CharacterEntity).isPlayerControlled === true) as CharacterEntity;
  if (!target) return;
  const direction = new THREE.Vector3().subVectors(target.position, this.position);
  direction.y = 0;
  if (direction.length() > 0.01) {
    this.moveDirection.copy(direction);
  } else {
    this.moveDirection.set(0, 0, 0);
  }
}
```

### 5.6 玩家角色管理�?(PlayerCharacterManager)

**功能**：专门管�?当前被操控角�?的容器，采用单例模式

**核心方法**�?
| 方法 | 功能 |
|------|------|
| `getInstance()` | 获取单例实例 |
| `setCurrentCharacter(character)` | 设置当前被玩家操控的角色 |
| `getCurrentCharacter()` | 获取当前被玩家操控的角色 |
| `updateCurrentCharacterPosition(x, y, z)` | 更新当前角色的位�?|
| `getCurrentCharacterId()` | 获取当前角色ID |
| `isCurrentCharacter(characterId)` | 判断指定ID是否为当前角�?|

**�?GameWorld 中的集成**�?
```typescript
// 实体创建时注�?useEffect(() => {
  const playerEntity = new FriendlyEntity('player', '/textures/character.png', position);
  entityManager.addEntity(playerEntity);
  playerCharacterManager.setCurrentCharacter(playerEntity);
}, []);

// 每帧更新时传递相机引�?useFrame(({ camera }, delta) => {
  const currentChar = playerCharacterManager.getCurrentCharacter();
  if (currentChar && 'setCamera' in currentChar) {
    (currentChar as FriendlyEntity).setCamera(camera);
  }
  EntityManager.getInstance().update(delta);
});
```

---

## 6. 静态物品实体系�?
### 6.1 系统架构

```
Entity (基类)
└── StaticEntity (静态物品基�?
    └── TargetEntity (靶子实体)
        ├── isShootable: true - 可被射击
        ├── health: number - 生命�?        ├── takeDamage(amount) - 承受伤害
        ├── onHit() - 命中时调用（变色�?        └── onDestroy() - 销毁时调用
```

### 6.2 静态物品基�?(StaticEntity)

**功能**：所有静态物品（靶子、树木、宝箱等）的父类，提供通用属性和方法

**主要属�?*�?- `isShootable: true`: 可被射击
- `health: number`: 生命值（默认1，一击即毁）

**主要方法**�?- `takeDamage(amount)`: 承受伤害
- `onHit()`: 命中时调用（子类可重写）
- `onDestroy()`: 销毁时调用（子类可重写�?
### 6.3 靶子实体 (TargetEntity)

**功能**：可被射击的靶子，命中时会变色，多次命中后会被摧�?
**特殊功能**�?- **命中反馈**：被击中时会从红色变为绿色，0.3秒后恢复
- **可摧�?*：生命值为1，一击即�?
**核心代码**�?
```typescript
export class TargetEntity extends StaticEntity {
  private originalMaterial: THREE.Material;
  private hitMaterial: THREE.Material;

  constructor(id: string, position: THREE.Vector3, size: { width: number; height: number } = { width: 2, height: 3 }) {
    const geometry = new THREE.PlaneGeometry(size.width, size.height);
    const material = new THREE.MeshStandardMaterial({ color: 0xff0000, side: THREE.DoubleSide });
    const mesh = new THREE.Mesh(geometry, material);
    super(id, mesh, position);
    this.originalMaterial = material;
    this.hitMaterial = new THREE.MeshStandardMaterial({ color: 0x00ff00, side: THREE.DoubleSide });
  }

  protected onHit(): void {
    (this.mesh as THREE.Mesh).material = this.hitMaterial;
    setTimeout(() => {
      if (this.isActive) {
        (this.mesh as THREE.Mesh).material = this.originalMaterial;
      }
    }, 300);
  }
}
```

### 6.4 与射击系统集�?
**射线检�?*：射击系统会通过 `mesh.userData.isShootable` 识别靶子

**子弹碰撞**：子弹会与靶子发生碰撞并造成伤害

**使用示例**�?
```typescript
// 创建靶子实体
const targetEntity = new TargetEntity(
  'target-1',
  new THREE.Vector3(10, 5, 10)
);

// 添加到实体管理器
EntityManager.getInstance().addEntity(targetEntity);
```

---

## 7. 碰撞管理�?
### 7.1 系统架构

```
CollisionManager (单例)
├── GridSpatialPartition - 网格空间分区
�?  ├── cellSize - 网格大小（默�?0�?�?  ├── rebuild() - 重建网格索引
�?  └── getPotentialCollisions() - 获取潜在碰撞实体
├── collisionCooldowns - 碰撞冷却Map
├── registerCollision(typeA, typeB, callback) - 注册碰撞回调
├── update() - 每帧检测碰�?└── handleCollision(a, b) - 处理碰撞逻辑
```

### 7.2 核心功能

**碰撞检�?*�?- 基于距离的碰撞检测（球体碰撞�?- 支持所有实体类型之间的碰撞
- 每帧自动检测所有实体对

**空间分区优化**�?- 网格大小�?0x10x10 单位
- 只检测同一网格及相邻网格的实体�?- 大幅减少碰撞检测次�?
**碰撞回调**�?- 支持注册不同类型实体之间的碰撞处�?- 回调函数接收碰撞的两个实体作为参�?- 自动处理碰撞顺序（A-B �?B-A�?
**碰撞冷却机制**�?- 防止同一对物体在100ms内重复触发碰�?- 5秒后自动清理过期的冷却记�?
### 7.3 碰撞类型

| 碰撞类型 | 触发条件 | 处理逻辑 |
|---------|---------|---------|
| 子弹 vs 角色 | 子弹与敌人距�?< 半径之和 | 敌人扣血，子弹销毁，触发特效 |
| 子弹 vs 静态物�?| 子弹与靶子距�?< 半径之和 | 靶子扣血，子弹销毁，触发特效 |
| 角色 vs 角色 | 玩家与敌人距�?< 半径之和 | 双方扣血 |

### 7.4 核心代码

```typescript
// src/core/CollisionManager.ts
class GridSpatialPartition {
  private cellSize: number;
  private grid: Map<string, Entity[]>;

  constructor(cellSize: number = 10) {
    this.cellSize = cellSize;
    this.grid = new Map();
  }

  private getCellKey(x: number, y: number, z: number): string {
    const cellX = Math.floor(x / this.cellSize);
    const cellY = Math.floor(y / this.cellSize);
    const cellZ = Math.floor(z / this.cellSize);
    return `${cellX},${cellY},${cellZ}`;
  }

  private getNeighborKeys(cellKey: string): string[] {
    const [cx, cy, cz] = cellKey.split(',').map(Number);
    const neighbors: string[] = [];
    for (let dx = -1; dx <= 1; dx++) {
      for (let dy = -1; dy <= 1; dy++) {
        for (let dz = -1; dz <= 1; dz++) {
          neighbors.push(`${cx + dx},${cy + dy},${cz + dz}`);
        }
      }
    }
    return neighbors;
  }

  public rebuild(entities: Entity[]): void {
    this.clear();
    for (const entity of entities) {
      if (entity.isActive) {
        this.insert(entity);
      }
    }
  }

  public getPotentialCollisions(entity: Entity): Entity[] {
    const key = this.getCellKey(entity.position.x, entity.position.y, entity.position.z);
    const neighborKeys = this.getNeighborKeys(key);
    const candidates: Entity[] = [];

    for (const neighborKey of neighborKeys) {
      const cellEntities = this.grid.get(neighborKey);
      if (cellEntities) {
        candidates.push(...cellEntities);
      }
    }

    return candidates;
  }
}

export class CollisionManager {
  private static instance: CollisionManager;
  private callbacks: Map<string, CollisionCallback> = new Map();
  private spatialPartition: GridSpatialPartition;
  private collisionCooldowns: Map<string, number>;

  private constructor() {
    this.spatialPartition = new GridSpatialPartition(10);
    this.collisionCooldowns = new Map();
  }

  public update(): void {
    const entities = EntityManager.getInstance().getAllEntities();
    this.spatialPartition.rebuild(entities);

    const checkedPairs = new Set<string>();

    for (const entity of entities) {
      if (!entity.isActive) continue;
      const candidates = this.spatialPartition.getPotentialCollisions(entity);

      for (const other of candidates) {
        if (!other.isActive) continue;
        if (entity === other) continue;

        const pairKey = this.getCollisionKey(entity, other);
        if (checkedPairs.has(pairKey)) continue;
        checkedPairs.add(pairKey);

        const dx = entity.position.x - other.position.x;
        const dy = entity.position.y - other.position.y;
        const dz = entity.position.z - other.position.z;
        const distSq = dx * dx + dy * dy + dz * dz;
        const minDist = entity.radius + other.radius;

        if (distSq < minDist * minDist && this.canCollide(entity, other)) {
          this.handleCollision(entity, other);
        }
      }
    }
  }

  private canCollide(a: Entity, b: Entity): boolean {
    const key = this.getCollisionKey(a, b);
    const lastCollision = this.collisionCooldowns.get(key);
    const now = Date.now();

    if (lastCollision && now - lastCollision < 100) {
      return false;
    }

    this.collisionCooldowns.set(key, now);
    return true;
  }
}
```

### 7.5 性能对比

| 实体数量 | 原始 O(N²) | 空间分区优化 |
|---------|-----------|-------------|
| 10 | 100 次检�?| ~27 次检�?|
| 50 | 2500 次检�?| ~135 次检�?|
| 100 | 10000 次检�?| ~270 次检�?|

### 7.6 注册碰撞回调示例

```typescript
// GameWorld.tsx
const collisionManager = CollisionManager.getInstance();

// 注册子弹 vs 敌人碰撞
collisionManager.registerCollision('bullet', 'character', (bullet, character) => {
  const bulletEntity = bullet as BulletEntity;
  const characterEntity = character as CharacterEntity;
  if (characterEntity.faction === 'enemy') {
    const currentMode = useGameStore.getState().mode;
    if (currentMode !== GameMode.BATTLE) {
      useGameStore.getState().setMode(GameMode.BATTLE);
    }
    // 调用子弹的onHit方法，触发范围伤害和特效
    if (typeof (bulletEntity as any).onHit === 'function') {
      (bulletEntity as any).onHit(characterEntity);
    }
    bulletEntity.isActive = false;
  }
});

// 注册子弹 vs 靶子碰撞
collisionManager.registerCollision('bullet', 'static', (bullet, staticObj) => {
  const staticEntity = staticObj as StaticEntity;
  if (staticEntity.isShootable) {
    staticEntity.takeDamage(1);
    bullet.isActive = false;
  }
});
```

### 7.6 每帧调用

```typescript
// GameWorld.tsx useFrame
useFrame((_, delta) => {
  // 1. 更新所有实体位�?  EntityManager.getInstance().update(delta);
  // 2. 检测碰�?  CollisionManager.getInstance().update();
  // 3. 更新特效
  EffectManager.getInstance().update(delta);
});
```

---

## 8. 子弹系统

### 8.1 子弹实体

**功能**：基于实体系统的子弹实体，支持移动、碰撞检测和生命周期管理

**子弹类型**�?- **基础子弹**：直线飞行，命中目标后销�?- **爆裂黎明子弹**：命中目标后产生范围伤害和华丽特�?
**创建示例**�?
```typescript
// 创建基础子弹
const bulletEntity = new BulletEntity(
  new THREE.Vector3(position.x, position.y, position.z),
  new THREE.Vector3(direction.x, direction.y, direction.z),
  bulletVelocity,
  0xffaa00
);

// 创建爆裂黎明子弹
const dawnBullet = new DawnExplosionBulletEntity(
  new THREE.Vector3(position.x, position.y, position.z),
  new THREE.Vector3(direction.x, direction.y, direction.z),
  bulletVelocity,
  0xffd700
);

// 添加到实体管理器
EntityManager.getInstance().addEntity(bulletEntity);
EntityManager.getInstance().addEntity(dawnBullet);
```

### 8.2 子弹更新流程

```
每帧更新 (EntityManager.update delta)
       �?BulletEntity.update(delta)
       �?更新位置 (position.add(velocity * delta))
       �?碰撞检�?(distance < radius + enemy.radius)
       �?命中目标 �?onHit() �?触发特效 �?应用范围伤害 �?isActive = false
       �?超过生命周期 �?isActive = false �?自动从管理器移除
```

### 8.3 爆裂黎明子弹

**功能**：实现范围伤害和华丽的爆裂黎明特�?
**核心代码**�?
```typescript
// src/entities/bullets/DawnExplosionBulletEntity.ts
export class DawnExplosionBulletEntity extends BulletEntity {
  private explosionRadius: number = 10; // 爆炸范围半径
  private fireSprite: THREE.Sprite | null = null; // 火焰精灵特效
  
  constructor(
    position: THREE.Vector3,
    direction: THREE.Vector3,
    speed: number,
    color: number = 0xffd700
  ) {
    super(position, direction, speed, color);
    this.setDamage(2);
    
    // 创建火焰精灵特效并添加到场景�?    this.fireSprite = createFireSprite(position, 1.5); // 增大火焰精灵大小
    const scene = EntityManager.getInstance().getScene();
    if (this.fireSprite && scene) {
      scene.add(this.fireSprite);
    }
  }
  
  protected onHit(entity: any): void {
    super.onHit(entity);
    EffectManager.getInstance().playDawnExplosion(this.position);
    this.applyAreaDamage();
  }
  
  private applyAreaDamage(): void {
    const allEntities = EntityManager.getInstance().getAllEntities();
    const areaDamage = this.getDamage() * 0.5;
    
    allEntities.forEach((entity) => {
      if (entity.id === this.id || !entity.isActive) return;
      
      const distance = entity.position.distanceTo(this.position);
      if (distance <= this.explosionRadius) {
        if (
          (entity instanceof CharacterEntity && (entity as CharacterEntity).faction === 'enemy') ||
          (entity instanceof StaticEntity && (entity as StaticEntity).isShootable)
        ) {
          const damageMultiplier = 1 - (distance / this.explosionRadius);
          const finalDamage = Math.round(areaDamage * damageMultiplier);
          
          if (finalDamage > 0 && typeof entity.takeDamage === 'function') {
            entity.takeDamage(finalDamage);
          }
        }
      }
    });
  }
  
  public update(delta: number): void {
    super.update(delta);
    // 让火焰精灵随子弹移动
    if (this.fireSprite) {
      this.fireSprite.position.copy(this.position);
    }
  }
  
  public onDestroy(): void {
    super.onDestroy();
    // 清理火焰精灵
    if (this.fireSprite && this.fireSprite.parent) {
      this.fireSprite.parent.remove(this.fireSprite);
    }
  }
}

// 创建火焰精灵特效的辅助函�?function createFireSprite(position: THREE.Vector3, size: number = 0.5): THREE.Sprite {
  const canvas = document.createElement('canvas');
  canvas.width = 128; // 增大画布尺寸
  canvas.height = 128;
  const ctx = canvas.getContext('2d')!;
  
  const texture = new THREE.CanvasTexture(canvas);
  const material = new THREE.SpriteMaterial({ 
    map: texture, 
    blending: THREE.AdditiveBlending,
    transparent: true,
    opacity: 0.9 // 增加不透明�?  });
  const sprite = new THREE.Sprite(material);
  sprite.position.copy(position);
  sprite.scale.set(size, size, 1);
  
  let time = 0;
  function animate() {
    time += 0.05;
    ctx.clearRect(0, 0, 128, 128);
    // 绘制火焰形状（椭圆形，随机的波动�?    const radiusX = 40 + Math.sin(time * 8) * 8; // 增大火焰半径
    const radiusY = 60 + Math.sin(time * 12) * 12;
    const grad = ctx.createRadialGradient(64, 64, 0, 64, 64, radiusX);
    grad.addColorStop(0, '#ffffff'); // 中心更亮
    grad.addColorStop(0.3, '#ffaa33');
    grad.addColorStop(0.6, '#ff4422');
    grad.addColorStop(1, 'rgba(255, 0, 0, 0)');
    ctx.fillStyle = grad;
    ctx.beginPath();
    ctx.ellipse(64, 64, radiusX, radiusY, 0, 0, Math.PI * 2);
    ctx.fill();
    texture.needsUpdate = true;
    requestAnimationFrame(animate);
  }
  animate();
  return sprite;
}
```

---

## 9. 调试模式

### 9.1 调试模式控制

- **默认状�?*：调试模式默认关闭（`isDebug: false`�?- **开启方�?*：按 `F12` 键切换调试模�?- **面板位置**：左上角

### 9.2 调试面板内容

```typescript
// App.tsx 调试面板
{isDebug && (
  <div style={{
    position: 'absolute',
    top: '10px',
    left: '10px',
    backgroundColor: 'rgba(0,0,0,0.7)',
    padding: '10px',
    borderRadius: '5px',
    fontSize: '12px',
    zIndex: 1000
  }}>
    <div>角色 Y: {characterPos.y.toFixed(2)}</div>
    <div>地形 Y: {terrainHeight.toFixed(2)}</div>
    <div>角色�?Y: {(characterPos.y - CHARACTER_HEIGHT / 2).toFixed(2)}</div>
    <div>射击系统: {activeShootingSystem === 'lockon' ? '锁定�? : '自由�?}</div>
    <div>模式: {mode === GameMode.BATTLE ? '战斗' : mode === GameMode.DAILY ? '日常' : mode}</div>
    <div>调试: {isDebug ? '开�? : '关闭'}</div>
  </div>
)}
```

### 9.3 调试可视�?
| 可视化内�?| 说明 |
|-----------|------|
| **绿色射线** | 相机检测射线（`getWorldDirection`�?|
| **红色标记** | 射线命中�?|
| **青色多射�?* | 锁定系统检测射线（`MultiRayVisualizer`�?|
| **红色箭头** | 射击方向（`ShootDirectionVisualizer`�?|

---

## 10. 特效系统

### 10.1 系统架构

```
BaseEffect (基类)
├── HitFlashEffect (命中闪光)
├── MuzzleFlashEffect (枪口闪光)
├── ExplosionEffect (爆炸)
├── RingWaveEffect (环形�?
└── DawnBurstEffect (爆裂黎明)

EffectManager (单例)
├── activeEffects: BaseEffect[] - 活跃特效列表
├── playHitFlash() - 播放命中闪光
├── playMuzzleFlash() - 播放枪口闪光
├── playExplosion() - 播放爆炸
├── playRingWave() - 播放环形�?├── playDawnExplosion() - 播放爆裂黎明
└── update() - 每帧更新特效
```

### 10.2 核心功能

**特效基类**�?- 统一的生命周期管�?- 抽象的更新和销毁方�?- 支持特效完成回调

**特效类型**�?- **命中闪光**：子弹命中目标时的短暂闪�?- **枪口闪光**：武器发射时的枪口火�?- **爆炸**：基础爆炸效果
- **环形�?*：从中心向外扩散的环形波�?- **爆裂黎明**：华丽的多层组合特效

**特效管理�?*�?- 单例模式，统一管理所有特�?- 自动处理特效的创建、更新和销�?- 提供简单的API接口播放各种特效

### 10.3 爆裂黎明特效

**功能**：子弹命中时的华丽特效，包含多层视觉元素

**视觉效果**�?- **核心爆闪**：中心白色闪光球体，快速膨胀并淡�?- **多层尖刺**：内层红色尖刺和外层蓝色尖刺，向四周喷射
- **多环冲击�?*：红、蓝、金三层圆环，从中心向外扩散
- **范围伤害**：爆炸范围内的敌人和静态实体受到伤�?
**核心代码**�?
```typescript
// src/effects/DawnBurstEffect.ts
export class DawnBurstEffect extends BaseEffect {
  private group: THREE.Group;
  private innerSpikes: Spike[] = [];
  private outerSpikes: Spike[] = [];
  private coreFlash: THREE.Mesh | null = null;
  private coreMaterial: THREE.MeshBasicMaterial | null = null;
  private rings: RingEffect[] = [];
  
  constructor(position: THREE.Vector3, duration: number = 4.0) {
    super(duration);
    this.group = new THREE.Group();
    this.group.position.copy(position);
    const scene = EntityManager.getInstance().getScene();
    if (scene) scene.add(this.group);
    
    // 获取相机方向，使特效朝向相机
    const camera = cameraStore.getCamera();
    let targetDir = new THREE.Vector3(0, 1, 0);
    if (camera) {
      targetDir = new THREE.Vector3().subVectors(camera.position, this.group.position).normalize();
      targetDir.y += 0.2;
      targetDir.normalize();
    }
    
    // 创建核心闪光、尖刺和环形�?    // ...
  }
  
  protected onUpdate(delta: number): void {
    // 更新核心闪光、尖刺和环形�?    // ...
  }
  
  public dispose(): void {
    // 清理资源
    // ...
  }
}
```

### 10.4 集成到游戏中

**子弹命中时触�?*�?
```typescript
// GameWorld.tsx 碰撞回调
collisionManager.registerCollision('bullet', 'character', (bullet, character) => {
  const bulletEntity = bullet as BulletEntity;
  const characterEntity = character as CharacterEntity;
  if (characterEntity.faction === 'enemy') {
    // 触发爆裂黎明特效
    EffectManager.getInstance().playDawnExplosion(bulletEntity.position);
    characterEntity.takeDamage(bulletEntity.getDamage() ?? 1);
    bulletEntity.isActive = false;
  }
});
```

**每帧更新**�?
```typescript
// GameWorld.tsx useFrame
useFrame((_, delta) => {
  // 更新特效
  EffectManager.getInstance().update(delta);
});
```

---

## 11. 动画系统

### 11.1 系统架构

```
AnimationClip (动画片段�?
├── frames: THREE.Texture[] - 动画帧纹理数�?├── frameRate: number - 帧率
├── loop: boolean - 是否循环
└── update(delta): THREE.Texture | null - 更新动画并返回当前帧

PaperAnimator (纸片人动画器)
├── frontClip: AnimationClip | null - 正面动画
├── backClip: AnimationClip | null - 背面动画
├── currentClip: AnimationClip | null - 当前播放的动�?├── updateDirection(charPos, cameraPos): void - 根据相机方向切换动画
└── update(delta): void - 更新动画播放

AnimationLoader (动画加载�?
└── loadClip(basePath, frameCount, startIndex, frameRate, loop): Promise<AnimationClip> - 异步加载动画
```

### 11.2 核心功能

**动画片段管理**�?- 支持序列帧动画播�?- 可配置帧率和循环模式
- 自动处理帧切换逻辑

**纸片人动�?*�?- 正面和背面动画切�?- 根据相机方向自动调整
- 支持按键强制切换动画方向

**异步加载**�?- 并行加载多帧纹理
- 自动处理加载失败情况

**按键控制**�?- 按W键播放正面动�?- 按S键播放后背动�?
---

## 12. 火焰特效系统

### 12.1 系统架构

```
ParticleFireEffect (粒子火焰特效)
├── particles: FireParticle[] - 粒子数组
├── geometry: THREE.BufferGeometry - 粒子几何�?├── material: THREE.ShaderMaterial - 粒子材质
├── points: THREE.Points - 粒子系统
└── FlameContour3D (3D火焰轮廓)
    ├── group: THREE.Group - 轮廓�?    ├── line: THREE.LineLoop - 轮廓�?    └── fillMesh: THREE.Mesh - 填充网格

FlameManager (火焰管理�?
├── instance: ParticleFireEffect - 单例火焰实例
├── positions: THREE.Vector3[] - 火焰位置数组
└── addFlamePosition(position): number - 添加火焰位置
```

### 12.2 核心功能

**粒子系统**�?- 基于THREE.js的Points和BufferGeometry实现
- 自定义着色器实现圆形粒子和径向渐�?- 物理模拟实现火焰上升、扩散和湍流

**3D火焰轮廓**�?- 使用LineLoop和CatmullRomCurve3实现平滑轮廓
- 支持侧面/俯视视角自动切换
- 保持轮廓始终面向相机水平方向

**填充效果**�?- 基于粒子颜色的动态填�?- 10分区y轴颜色渐�?- 横向亮度衰减（中心亮，边缘暗�?
**视觉效果**�?- 从中心白色到边缘橙色的平滑渐�?- 顶部亮、底部暗的自然亮度过�?- 中心亮、边缘暗的水平亮度分�?
### 12.3 使用示例

```typescript
// 在地图上创建一个持续时间无限的火焰特效
EffectManager.getInstance().clearAllParticleFireEffects();
const firePosition = new THREE.Vector3(0, 1.5, 10);
EffectManager.getInstance().playParticleFireEffect(firePosition, Infinity);

// 使用火焰管理器创建多个火焰位�?flameManager.initialize(scene, camera);
const flameIndex1 = flameManager.addFlamePosition(new THREE.Vector3(0, 0, 0));
const flameIndex2 = flameManager.addFlamePosition(new THREE.Vector3(1, 0, 1));

// 在每一帧更新火�?flameManager.update();
```

---

## 13. 原石虫敌人类

### 13.1 系统架构

```
StoneBugEnemy (原石虫敌�?
├── extends EnemyEntity - 继承自敌方实�?├── cones: THREE.Mesh[] - 红色圆锥数组
├── createStoneBugModel(): void - 创建原石虫模�?├── addRedCones(): void - 添加红色圆锥
└── update(delta): void - 更新方法
```

### 13.2 核心功能

**半椭球形�?*�?- 使用SphereGeometry创建半个球体
- 调整缩放比例为椭球形
- 使用灰色石头材质增强质感

**红色圆锥**�?- 随机生成6-9根圆�?- 分布在半椭球表面
- 指向半椭球外
- 使用红色发光材质

**动画效果**�?- 圆锥轻微摆动
- 红色圆锥发光效果

**AI行为**�?- 继承EnemyEntity的AI行为
- 追踪玩家
- 跟随玩家移动

---

## 14. 水面实体系统

### 14.1 系统架构

```
WaterEntity (水面实体)
├── extends StaticEntity - 继承自静态实�?├── rtA / rtB: THREE.WebGLRenderTarget - 双缓冲纹�?├── waterMaterial: THREE.ShaderMaterial - 水面材质
├── computeMaterial: THREE.ShaderMaterial - 计算着色器
├── createWaterEffect(): void - 创建水面效果
├── updateWave(): void - 更新水面波动
├── addDisturbance(position, strength): void - 添加扰动
└── addDisturbanceAtWorldPos(worldPos, strength): void - 在世界坐标添加扰�?```

### 14.2 核心功能

**动态水�?*�?- 使用波动方程实现水面物理模拟
- 双缓冲纹理（Ping-Pong Buffer）优化性能
- 支持大波和小波叠加效�?
**涟漪系统**�?- 角色进入水中时产生扩散的sin波涟�?- 涟漪强度随机�?.5-7倍）
- 涟漪触发距离控制�?.5单位�?- Y轴碰撞检测，只有角色底部接触水面才触�?
**渲染优化**�?- 修复水面透明边框问题
- 静态模式波动参数优化（更平静）
- 动态模式波动参数增强（更活跃）

**洪水填充生成**�?- 使用洪水填充算法在地形低洼区域生成河�?- 水面高度随机�?.3-0.6单位�?- 自动适应地形高度

### 14.3 技术实�?
**波动方程**�?```typescript
// 计算着色器中的波动方程实现
float laplacian = (hL + hR + hT + hB - 4.0 * h);
float hNext = 2.0 * h - hPrev + uLambda2 * laplacian;
hNext *= uDamping;
```

**涟漪效果**�?```typescript
// 顶点着色器中的涟漪实现
if (uPlayerStrength > 0.0) {
  vec2 playerWorldPos = uWaterWorldPos + (uPlayerPos - 0.5) * vec2(40.0, 46.0);
  float playerDist = length(vWorldPos.xz - playerWorldPos);
  float rippleRadius = 2.0;
  float rippleWave = sin(playerDist * 15.0 - uTime * 8.0) * 0.15;
  float rippleFade = max(0.0, 1.0 - playerDist / rippleRadius);
  float rippleHeight = rippleWave * rippleFade * uPlayerStrength;
  pos.y += rippleHeight;
  vHeight += rippleHeight;
}
```

**Y轴检�?*�?```typescript
// 角色底部接触水面检�?const waterSurfaceY = meshPos.y;
const playerBottomY = worldPos.y - 1.0;
const touchThreshold = 0.5;
return Math.abs(playerBottomY - waterSurfaceY) < touchThreshold;
```

### 14.4 使用示例

**创建水面实体**�?```typescript
const waterHeight = 0.3 + Math.random() * 0.3; // 随机水面高度 0.3-0.6
const waterEntity = new WaterEntity(
  new THREE.Vector3(28, waterHeight, 25), // 中心位置
  40, // 宽度
  46, // 高度
  256, // 分辨�?  64 // 网格分段�?);

// 添加到实体管理器
EntityManager.getInstance().addEntity(waterEntity);
```

**触发涟漪**�?```typescript
// 当角色进入水面时
if (entity instanceof WaterEntity) {
  const inWater = entity.isInWater(playerPosVec);
  if (inWater) {
    entity.addDisturbanceAtWorldPos(playerPosVec, 3.0);
  }
}
```

### 14.5 性能优化

- **双缓冲纹�?*：使用WebGLRenderTarget实现高效的GPU计算
- **空间分区**：涟漪检测使用距离判断，避免不必要的计算
- **参数优化**：静态模式降低波动参数，减少计算开销
- **渲染设置**：关闭透明模式，避免渲染顺序问�?
### 14.6 视觉效果

| 模式 | 波动频率 | 波动速度 | 波幅大小 | 视觉效果 |
|------|---------|---------|---------|---------|
| 静态模�?| 低（0.1-0.2�?| 慢（0.3-0.5�?| 小（0.1-0.2�?| 平静如水 |
| 动态模�?| 高（0.5-1.0�?| 快（0.9-1.5�?| 大（0.35-0.7�?| 活跃波动 |

**涟漪效果**�?- 向外扩散的sin波形
- 距离衰减的强度变�?- 随机强度�?.5-7倍）
- 明显的Y轴位�?        const backClip = await loader.loadClip(
          '/textures/characters/player/back/back',
          4, // 4�?          0,
          12,
          true
        );

        const newAnimator = new PaperAnimator(frontClip, backClip);
        setAnimator(newAnimator);
        // 初始使用后背0贴图
        if (backClip.frames.length > 0) {
          setCurrentTexture(backClip.frames[0]);
        }
      } catch (error) {
        console.error('Failed to load animations:', error);
      }
    };

    loadAnimations();

    return () => {
      animator?.dispose();
    };
  }, []);

  // 按键控制动画方向
  useEffect(() => {
    const handleKeyDown = (event: KeyboardEvent) => {
      if (animator) {
        if (event.key === 'w' || event.key === 'W') {
          animator.forceDirection('front');
        } else if (event.key === 's' || event.key === 'S') {
          animator.forceDirection('back');
        }
      }
    };

    const handleKeyUp = (event: KeyboardEvent) => {
      if (animator) {
        if (event.key === 'w' || event.key === 'W' || event.key === 's' || event.key === 'S') {
          animator.releaseForceDirection();
        }
      }
    };

    window.addEventListener('keydown', handleKeyDown);
    window.addEventListener('keyup', handleKeyUp);

    return () => {
      window.removeEventListener('keydown', handleKeyDown);
      window.removeEventListener('keyup', handleKeyUp);
    };
  }, [animator]);

  // 每帧更新动画
  useFrame((state, delta) => {
    if (!animator || !camera) return;

    // 获取角色位置
    const characterPosition = characterPositionStore.position;

    // 更新动画方向
    animator.updateDirection(characterPosition, camera.position);

    // 更新动画
    const texture = animator.update(delta);
    if (texture) {
      setCurrentTexture(texture);
    }
  });

  // 渲染角色
  return (
    <mesh
      position={characterPositionStore.position}
      rotation={[0, Math.PI, 0]}
    >
      <planeGeometry args={[2, 2]} />
      <meshStandardMaterial
        map={currentTexture || null}
        side={THREE.DoubleSide}
      />
    </mesh>
  );
};
```

---

## 15. 纹理系统

### 15.1 系统架构

```
ITextureGenerator (接口)
    �?BulletFluidTexture (子弹流体纹理生成�?
    �?TextureManager (纹理管理�?
    �?BulletFluidEffect (子弹流体特效)
    �?EffectManager (特效管理�?
```

### 15.2 ITextureGenerator 接口

```typescript
export interface ITextureGenerator {
  type: 'canvas' | 'shader';
  generate(): THREE.Texture | THREE.Material;
  update(delta?: number): void;
  dispose(): void;
}
```

### 15.3 BulletFluidTexture 核心实现

**功能**：生成子弹侧面视角的流体拖尾纹理

**FluidDynamics 初始化参�?*�?
```typescript
this.fluidDynamics = new FluidDynamics(this.canvas, {
  width: 512,
  height: 512,
  simScale: 1.0,           // 模拟缩放
  dyeScale: 1.2,          // 染料扩散放大
  curl: 12,               // 旋转强度
  velocityDissipation: 0.05, // 速度消散
  dyeDissipation: 0.08,   // 颜色消散
  pressureIterations: 20
});
```

### 15.4 尾气分层上色实现

#### 分层结构

| 层级 | 颜色 | RGB�?| 位置 |
|-----|------|-------|------|
| 马赫�?| 橙色 | `[1.0, 0.5, 0.2]` | 弹头前方 |
| 弹头 | 白色 | `[1, 1, 0.95]` | 弹头中心 |
| 尾气核心 | 蓝色 | `[0.2, 0.4, 1.0]` | 弹头后方锥形区域 |
| 卡门涡街 | 粉色/青色/紫色 | 多色交替 | 尾气上下两侧 |

#### 核心代码

```typescript
// 弹头高压�?this.fluidDynamics.setVelocity(x, y, 0, 25, 15, 0, 0);
this.fluidDynamics.setDye(x, y, 0, 20, 12, [1, 1, 0.95]);

// 马赫�?this.fluidDynamics.setDye(aheadX, aheadY, 0, 15, 10, [1.0, 0.5, 0.2]);

// 尾气核心（锥形扩散）
for (let dist = 25; dist <= trailLength; dist += step) {
  const backX = x - dist;
  const width = 8 + dist * 0.15;
  this.fluidDynamics.setDye(backX, y, 0, width * 0.8, width * 0.5, [0.2, 0.4, 1.0]);
}

// 卡门涡旋（交替方向）
const alternating = Math.sin(this.elapsedTime * 2) > 0 ? 1 : -1;
this.fluidDynamics.setVelocity(v1X, y + vortexOffset, 0, 15, 15,
  -vortexStrength * alternating, 0);
this.fluidDynamics.setDye(v1X, y + vortexOffset, 0, 12, 12, [1.0, 0.2, 0.6]);
```

### 15.5 纹理管理器集�?
```typescript
// TextureManager.ts
public initialize(): void {
  if (this.isInitialized) return;
  this.register('bulletFluid', new BulletFluidTexture());
  this.isInitialized = true;
}

// 获取纹理
const texture = TextureManager.getInstance().getTexture('bulletFluid');

// 更新纹理
TextureManager.getInstance().update('bulletFluid', delta);

// 释放引用
TextureManager.getInstance().release('bulletFluid');
```

### 15.6 使用方式

```typescript
// 播放子弹流体特效
const position = new THREE.Vector3(5, 5, 0);
EffectManager.getInstance().playBulletFluidEffect(position, 60, 8);
// 参数：位置、持续时�?�?、平面大�?单位)
```

### 15.7 文件结构

```
src/
├── systems/textures/
�?  ├── TextureManager.ts          # 纹理管理�?�?  └── BulletFluidTexture.ts      # 子弹流体纹理生成�?├── effects/
�?  ├── BulletFluidEffect.ts       # 子弹流体特效
�?  └── FluidEffect.ts             # 通用流体特效
└── core/
    └── EffectManager.ts           # 特效管理�?```

---

## 16. 2D流体模拟器系�?
### 16.1 系统概述

本系统是一�?*自研�?D单相自由表面流模拟器**，基于Level Set方法实现，替代了原有的第三方流体库。开发周期长�?0天，涉及多个技术难点的攻克�?
### 16.2 核心架构

```
FluidSimulator (核心�?
├── 参数配置 (FluidParams)
├── 纹理缓冲 (双缓�?
�?  ├── velTexA/B        # 速度�?�?  ├── phiTexA/B        # Level Set (符号距离函数)
�?  ├── pressureTexA/B   # 压力�?�?  ├── ageTexA/B        # 年龄�?(寿命系统)
�?  └── divergenceTex    # 散度�?├── 着色器系统
�?  ├── advectionShader          # 速度平流
�?  ├── divergenceShader         # 散度计算
�?  ├── pressureSolverShader     # 压力求解 (PCG)
�?  ├── velocityCorrectShader    # 速度修正
�?  ├── levelSetAdvectionShader  # Level Set平流
�?  ├── levelSetReinitShader     # Level Set重新初始�?�?  ├── ageAdvectionShader       # 年龄平流 (寿命系统)
�?  ├── ageUpdateShader          # 年龄更新 (寿命系统)
�?  └── dissipationShader        # 流体消散 (寿命系统)
└── 分层渲染材质
    └── 漫反射光照、高光、边缘发光、流动扰�?```

### 16.3 核心算法流程

| 步骤 | 着色器 | 输入 | 输出 | 说明 |
|------|--------|------|------|------|
| 1 | 速度平流 | velTex, phiTex | velTex | 反向追踪平流 |
| 2 | 散度计算 | velTex | divergenceTex | ∇·v |
| 3 | 压力求解 | divergenceTex | pressureTex | PCG迭代求解泊松方程 |
| 4 | 速度修正 | velTex, pressureTex | velTex | vel = vel - ∇p/ρ |
| 5 | Level Set平流 | phiTex, velTex | phiTex | 流体界面平流 |
| 6 | Level Set重新初始�?| phiTex | phiTex | 保持窄带符号距离 |
| 7 | 注入处理 | - | phiTex/velTex | 新流体注�?|
| 8 | 寿命系统 | ageTex, phiTex, velTex | ageTex/phiTex | 年龄平流→更新→消散 |
| 9 | 分层渲染 | phiTex, velTex | 最终渲�?| 漫反�?高光+发光+扰动 |

### 16.4 关键技术突�?
#### 16.4.1 Level Set方法

```typescript
// 符号距离函数定义
// phi < 0: 水区�?// phi > 0: 空气区域
// |phi| < 3: 窄带区域
```

#### 16.4.2 压力求解�?(PCG)

```
共轭梯度法预条件求解泊松方程：∇²p = ∇·v
收敛速度�?5~30次迭代达到精度要�?```

#### 16.4.3 双缓冲设�?
```
velTexA ←→ velTexB
phiTexA ←→ phiTexB  
pressureTexA ←→ pressureTexB
ageTexA ←→ ageTexB
每步计算使用不同缓冲，避免读写冲�?```

### 16.5 流体寿命系统 

这是开发过程中的难点，最终实现了**水滴追踪**而非像素追踪�?
#### 三阶段流�?
```
阶段1: 年龄平流
├── 输入: curAgeTex, curVelTex, curPhiTex
├── 输出: ageTexA/B (双缓�?
└── 逻辑: prevPos = uv - vel * dt / resolution
          age_new = texture2D(age, clamp(prevPos, 0, 1)).r

阶段2: 年龄更新
├── 输入: curAgeTex, curPhiTex
├── 输出: ageTexA/B (双缓�?
└── 逻辑: if (phi < 0) age += realDelta  // 使用真实时间
          else age = 0
          if (注入区域) age = 0

阶段3: 流体消散
├── 输入: curPhiTex, curAgeTex
├── 输出: phiTexA/B (双缓�?
└── 逻辑: if (age >= maxLifetime) phi = 0.1  // 水变空气
```

#### 核心设计要点

| 特�?| 实现方式 |
|------|----------|
| 水滴追踪 | 反向速度场追踪，年龄跟随水流移动 |
| 真实秒计�?| 使用 realDelta = performance.now() 差�?|
| 新水重置 | 注入区域检测，新水年龄=0 |
| 边界安全 | clamp(prevPos, 0, 1) 防止越界 |
| 开关控�?| maxLifetime = 0 时禁�?|

#### 关键参数

| 参数 | 类型 | 默认�?| 说明 |
|------|------|--------|------|
| maxLifetime | number | 3.0 | 流体最大寿�?�?�?=禁用 |

### 16.6 分层渲染系统

| 层级 | 效果 | 实现方式 |
|------|------|----------|
| 基础颜色�?| 水深渐变 | mix(waterColor, deepColor, depth) |
| 漫反射光照层 | 光影明暗 | diffuse = baseColor * max(0.1, dot(normal, lightDir)) |
| 高光�?| 镜面反射 | specular = pow(max(dot(normal, halfDir), 0), 64) |
| 边缘发光�?| 边缘辉光 | emissive = glowColor * (1 - smoothstep(0, edgeWidth, abs(phi))) |
| 流动扰动�?| 速度相关扰动 | flowColor = vec3(0.15, 0.25, 0.35) * length(vel) |

### 16.7 文件结构

```
src/lib/fluid-simulator/
├── fluid-simulator/
�?  ├── FluidSimulator.ts       # 核心模拟器类
�?  └── FluidSimulatorParams.ts # 参数定义
├── shaders/
�?  ├── advection.glsl          # 速度平流
�?  ├── divergence.glsl         # 散度计算
�?  ├── pressureSolver.glsl     # 压力求解
�?  ├── velocityCorrect.glsl    # 速度修正
�?  ├── levelSetAdvection.glsl  # Level Set平流
�?  ├── levelSetReinit.glsl     # Level Set重新初始�?�?  ├── ageAdvection.glsl       # 年龄平流
�?  ├── ageUpdate.glsl          # 年龄更新
�?  └── dissipation.glsl        # 流体消散
└── systems/
    └── FluidSimulatorAdapter.ts # 外部适配�?```

### 16.8 开发难点与解决方案

| 难点 | 问题描述 | 解决方案 |
|------|----------|----------|
| 纹理第一帧消�?| 年龄初始化为maxLifetime，导致立即消�?| 修改初始化：水区�?phi<0)年龄设为0 |
| maxLifetime不按秒计�?| 使用固定步长(0.002s)而非真实时间 | 用performance.now()计算realDelta |
| 年龄不跟随水�?| 年龄绑定静态像�?| 添加年龄平流，反向追踪速度�?|
| WebGL反馈循环错误 | Level Set平流读写同一缓冲 | 改为动态双缓冲选择 |
| 性能问题 | 频繁console.log | 添加frameCount�?0限制输出频率 |
| 压力泊松方程符号不一�?| PCG求解器使用负拉普拉斯，Jacobi使用正拉普拉�?| 统一为正拉普拉斯，修正PCG的SpMV着色器 |
| 爆炸后水纹理消失 | 新生成水花继承旧年龄值，立即消散 | 扩展年龄重置区域，移除phi阈值限�?|
| 爆炸计时不一�?| 使用wall-clock时间导致帧率相关的爆炸时长变�?| 改用模拟帧数计数 |
| 爆炸方向错误 | 正散度导致流体收缩而非膨胀 | 散度源取反：S = -strength * envelope * mask |
| 水花速度丢失 | 新生成水花速度不参与后续模拟步�?| 爆炸后同步更新velForDiv引用 |
| 流体不下�?| 爆炸使用不含重力的速度�?| 外力计算后更新curVelTex引用 |

### 16.9 爆炸系统改进（散度源模型�?
#### 核心原理

将爆炸视为体积源，向流场注入散度而非直接修改速度�?
```typescript
// 散度源项计算
// S = -strength * envelope(t) * mask
// 负号确保向外膨胀

// 时间包络（二次抛物线，先升后降）
float u = clamp(t / duration, 0.0, 1.0);
float envelope = 4.0 * u * (1.0 - u);
```

#### 实现步骤

| 步骤 | 操作 | 关键代码 |
|------|------|----------|
| 1 | 清空爆炸散度纹理 | `explosionDivTex.fill(0)` |
| 2 | 累加活跃爆炸的散度贡�?| `S = -strength * envelope * mask` |
| 3 | 同步速度场引�?| `velForDiv = this.curVelTex.texture` |
| 4 | 计算总散�?| `div(u) = ∇·vel + explosionDiv` |
| 5 | 压力求解 | 求解泊松方程 ∇²p = (ρ/Δt)·div |
| 6 | 速度修正 | `vel = vel - (Δt/ρ)·∇p` |

#### 速度场同步修�?
```typescript
// 更新方法中的关键修复
// 4.5 构建爆炸散度源（散度源模型）
const dt = this.params.timeStep;
this.buildExplosionDivergence(dt);

// �?如果爆炸修改了速度场，这里同步更新 velForDiv
velForDiv = this.curVelTex.texture;

// 5. 散度计算 —�?此时使用包含水花速度的速度�?this.divergenceMat.uniforms.velocity.value = velForDiv;
```

#### 重力应用修复

```typescript
// 3. 外力计算（包括重力）
this.externalForcesMat.uniforms.velocity.value = this.curVelTex.texture;
this.externalForcesMat.uniforms.levelset.value = this.curPhiTex.texture;
this.renderFullscreen(this.externalForcesMat, this.forcedVelTex);
this.curVelTex = this.forcedVelTex;  // �?更新为包含重力的速度
```

### 16.10 解耦边界处�?
#### 核心原理

解耦边界处理通过在计算域边缘添加边界环阻尼，允许流体越过边界膨胀，避�?碰壁回弹"的不自然效果�?
#### 参数配置

| 参数 | 类型 | 默认�?| 说明 |
|------|------|--------|------|
| decoupledBoundary | boolean | false | 是否启用解耦边�?|
| boundaryRingWidth | number | 2.0/width | 边界环宽度（�?个像素） |
| boundaryDivDamping | number | 0.5 | 散度阻尼系数 |
| boundaryVelDamping | number | 0.3 | 速度阻尼系数 |

#### 实现方式

**1. 构造函数中存储参数**

```typescript
this.decoupledBoundary = params.decoupledBoundary ?? false;
this.boundaryRingWidth = params.boundaryRingWidth ?? 2.0 / this.width; // 256分辨率下�?.0078
this.boundaryDivDamping = params.boundaryDivDamping ?? 0.5;
this.boundaryVelDamping = params.boundaryVelDamping ?? 0.3;
```

**2. 散度假定着色器修改**

```typescript
// divergenceShader uniforms
uniforms: {
  decoupledBoundary: { value: this.decoupledBoundary ? 1.0 : 0.0 },
  boundaryRingWidth: { value: this.boundaryRingWidth },
  boundaryDivDamping: { value: this.boundaryDivDamping }
}

// fragmentShader 中，计算 div 之后
if (decoupledBoundary > 0.5) {
  float minDist = min(min(uv.x, 1.0 - uv.x), min(uv.y, 1.0 - uv.y));
  float damping = mix(boundaryDivDamping, 1.0, smoothstep(0.0, boundaryRingWidth, minDist));
  div *= damping;
}
```

**3. 速度修正着色器修改**

```typescript
// velocityCorrectShader uniforms  
uniforms: {
  decoupledBoundary: { value: this.decoupledBoundary ? 1.0 : 0.0 },
  boundaryRingWidth: { value: this.boundaryRingWidth },
  boundaryVelDamping: { value: this.boundaryVelDamping }
}

// fragmentShader 中，计算 pressureGrad 之后
if (decoupledBoundary > 0.5) {
  float minDist = min(min(uv.x, 1.0 - uv.x), min(uv.y, 1.0 - uv.y));
  float damping = mix(boundaryVelDamping, 1.0, smoothstep(0.0, boundaryRingWidth, minDist));
  pressureGrad *= damping;
}
```

#### 效果说明

- **边界环内**：散度和压力梯度被削弱，流体可以突破部分无发散约束，保留更大的切向速度
- **边界环外**：正常的无发散约束，保持流体稳定�?- **过渡区域**：使�?smoothstep 实现平滑过渡，避免硬边界

### 16.11 各向异性爆炸系�?
#### 核心原理

通过引入方向调制函数，使爆炸在不同方向产生不同的散度符号和大小，实现方向性爆炸效果�?
#### 各向异性模�?
| 模式 | 名称 | 方向调制函数 | 效果描述 |
|------|------|-------------|---------|
| 0 | 各向同�?| dirMod = 1.0 | 均匀向外膨胀（默认） |
| 1 | 四极�?| dirMod = cos(2*angle) | 两个方向膨胀，两个方向收�?|
| 2 | 偶极�?| dirMod = cos(angle) | 一侧膨胀，对侧收�?|

#### 参数配置

| 参数 | 类型 | 默认�?| 说明 |
|------|------|--------|------|
| anisotropyMode | number | 0 | 各向异性模式（0/1/2�?|
| anisotropyPhase | number | 0.0 | 相位偏移（弧度） |
| anisotropyStrength | number | 0.0 | 各向异性强度（0~1�?|

#### 实现方式

**1. 着色器 uniform 添加**

```typescript
// explosionDivMat uniforms
anisotropyMode: { value: 0 },
anisotropyPhase: { value: 0.0 },
anisotropyStrength: { value: 1.0 }
```

**2. 片段着色器修改**

```glsl
uniform int anisotropyMode;
uniform float anisotropyPhase;
uniform float anisotropyStrength;

// 计算角度
vec2 rel = uv - center;
float angle = atan(rel.y, rel.x) + anisotropyPhase;
float dirMod = 1.0;

if (anisotropyMode == 1) {
  // 四极子：cos(2*angle)
  dirMod = cos(2.0 * angle);
} else if (anisotropyMode == 2) {
  // 偶极子：cos(angle)
  dirMod = cos(angle);
}

// 混合各向异�?float finalMod = mix(1.0, dirMod, anisotropyStrength);
float S = -strength * envelope * mask * finalMod;
```

**3. 接口暴露**

```typescript
public explodeAnisotropic(
  cx: number, cy: number, radius: number, strength: number,
  createWater: boolean, duration: number,
  mode: number = 0, phase: number = 0, anisoStrength: number = 0.0
) {
  this.activeExplosions.push({
    cx, cy, radius, strength, createWater,
    startFrame: this.frameCount,
    durationFrames: Math.ceil(duration / this.params.timeStep),
    noiseOffsetX: Math.random() * 10.0,
    noiseOffsetY: Math.random() * 10.0,
    anisotropyMode: mode,
    anisotropyPhase: phase,
    anisotropyStrength: anisoStrength
  });
}
```

#### 使用示例

```typescript
// 四极子各向异性：左右向外，上下向�?simulator.explodeAnisotropic(0.5, 0.5, 0.15, 8000, true, 0.1, 1, 0.0, 1.0);

// 偶极子：右侧膨胀，左侧收缩（相位旋转90°�?simulator.explodeAnisotropic(0.5, 0.5, 0.15, 8000, true, 0.1, 2, Math.PI/2, 1.0);
```

### 16.12 随机扰动爆炸

#### 核心原理

通过噪声纹理调制爆炸散度源和速度场，产生不规则的波前和涡旋效果�?
#### 参数配置

| 参数 | 类型 | 默认�?| 说明 |
|------|------|--------|------|
| usePerturbation | boolean | false | 是否启用随机扰动 |
| perturbationStrength | number | 0.4 | 扰动强度�?~1�?|
| fragmentCount | number | 1 | 多团块爆炸子团块�?|

#### 噪声纹理生成

```typescript
private generateNoiseTexture(size: number = 64): THREE.DataTexture {
  const data = new Float32Array(size * size * 4);
  for (let y = 0; y < size; y++) {
    for (let x = 0; x < size; x++) {
      const idx = (y * size + x) * 4;
      const hash = (x: number, y: number) => {
        let h = x * 374761393 + y * 668265263;
        h = (h ^ (h >> 13)) * 1274126177;
        return (h ^ (h >> 16)) / 2147483648;
      };
      let val = 0.0;
      for (let oct = 1; oct <= 4; oct++) {
        const freq = 1 << oct;
        val += 0.5 / oct * hash(x * freq % size, y * freq % size);
      }
      data[idx] = val;           // R: 基础扰动
      data[idx + 1] = hash(x + 100, y + 100); // G: 方向扰动
      data[idx + 2] = 0;
      data[idx + 3] = 1;
    }
  }
  const tex = new THREE.DataTexture(data, size, size, THREE.RGBAFormat, THREE.FloatType);
  tex.wrapS = THREE.RepeatWrapping;
  tex.wrapT = THREE.RepeatWrapping;
  tex.minFilter = THREE.LinearFilter;
  tex.magFilter = THREE.LinearFilter;
  tex.needsUpdate = true;
  return tex;
}
```

#### 散度源扰�?
```glsl
uniform sampler2D noiseTex;
uniform float perturbationStrength;

// 在原有中心距离计算之�?float perturb = texture2D(noiseTex, uv * 8.0 + center).r;
perturb = (perturb - 0.5) * 2.0;
float noiseMask = 1.0 + perturb * perturbationStrength;
noiseMask = clamp(noiseMask, 0.0, 1.0);
float mask = (1.0 - smoothstep(0.0, radius, dist)) * noiseMask;
```

#### 切向速度扰动

```glsl
// 在径向速度基础�?vec2 tangent = vec2(dir.y, -dir.x);
float tangentStrength = (texture2D(noiseTex, uv * 6.0).g - 0.5) * 2.0;
vel += tangent * tangentStrength * speed * 0.4;
```

#### 多团块爆�?
```typescript
public explodeFragmented(cx: number, cy: number, radius: number, strength: number, 
                        createWater: boolean, duration: number, fragmentCount: number = 4) {
  for (let i = 0; i < fragmentCount; i++) {
    const angle = Math.random() * Math.PI * 2;
    const offsetR = radius * 0.3 * Math.random();
    const fx = cx + Math.cos(angle) * offsetR;
    const fy = cy + Math.sin(angle) * offsetR;
    const fr = radius * (0.6 + Math.random() * 0.4);
    const fs = strength / fragmentCount * (0.8 + Math.random() * 0.4);
    this.explode(fx, fy, fr, fs, createWater, duration);
  }
}
```

---

## 17. 轻量级流体实体系�?
### 17.1 系统概述

轻量级流体实体（LightFluidEntity）是一种自包含的流体模拟实体，内部包含32x32的小型FluidSimulator，可作为独立实体被EntityManager管理�?
### 17.2 核心架构

```
LightFluidEntity (轻量级流体实�?
├── extends Entity - 继承自实体基�?├── simulator: FluidSimulator - 内部流体模拟�?├── material: THREE.ShaderMaterial - 渲染材质
├── waterVolume: number - 水量占比 (0~1)
├── worldVelocity: THREE.Vector3 - 世界空间速度
└── maxAge: number - 最大存活时�?�?
```

### 17.3 核心实现

```typescript
export class LightFluidEntity extends Entity {
  private simulator: FluidSimulator;
  private material: THREE.ShaderMaterial;
  public waterVolume: number = 0.45;
  public worldVelocity: THREE.Vector3;
  private age: number = 0;
  public maxAge: number = 10;
  
  constructor(
    id: string, 
    renderer: THREE.WebGLRenderer, 
    initialPosition?: THREE.Vector3,
    initialVelocity?: THREE.Vector3,
    scale: number = 0.3
  ) {
    const params: FluidParams = {
      width: 32,
      height: 32,
      density: 1000,
      viscosity: 0.001,
      surfaceTension: 0.0728,
      gravity: 0,           // 内部不做重力，由外部物理控制
      pressureIterations: 8,
      reinitIterations: 2,
      timeStep: 0.002,
      restitution: 0.8,
      friction: 0.95,
      usePCG: false,        // Jacobi 省纹�?      maxLifetime: 0,       // 不自动消�?      decoupledBoundary: false,
      usePerturbation: false,
      injectionEnabled: false,
    };
    
    this.simulator = new FluidSimulator(renderer, params);
    this.setInitialWaterVolume(this.waterVolume);
    this.material = this.simulator.getRenderMaterial();
    
    const geometry = new THREE.PlaneGeometry(1, 1);
    const mesh = new THREE.Mesh(geometry, this.material);
    mesh.scale.set(scale, scale, 1);
    mesh.rotation.z = Math.random() * Math.PI * 2;
    
    super(id, 'lightFluid', mesh);
    
    this.worldVelocity = initialVelocity ?? new THREE.Vector3();
    if (initialPosition) {
      this.mesh.position.copy(initialPosition);
    }
  }
  
  private setInitialWaterVolume(volume: number): void {
    const w = 32, h = 32;
    const data = new Float32Array(w * h * 4);
    const cx = w / 2, cy = h / 2;
    const radius = Math.sqrt(volume) * w * 0.45;
    
    for (let y = 0; y < h; y++) {
      for (let x = 0; x < w; x++) {
        const i = (y * w + x) * 4;
        const dx = x - cx, dy = y - cy;
        const dist = Math.sqrt(dx * dx + dy * dy);
        data[i] = dist - radius;
        data[i+1] = 0;
        data[i+2] = 0;
        data[i+3] = 1;
      }
    }
    
    const tex = new THREE.DataTexture(data, w, h, THREE.RGBAFormat, THREE.FloatType);
    tex.needsUpdate = true;
    this.simulator.setLevelSetTexture(tex);
    tex.dispose();
  }
  
  update(delta: number): void {
    if (!this.isActive) return;
    
    this.age += delta;
    if (this.age > this.maxAge) {
      this.isActive = false;
      return;
    }
    
    this.mesh.position.x += this.worldVelocity.x * delta;
    this.mesh.position.y += this.worldVelocity.y * delta;
    this.mesh.position.z += this.worldVelocity.z * delta;
    this.position.copy(this.mesh.position);
    
    this.simulator.update(delta);
    this.simulator.updateRenderUniforms();
  }
  
  public applyForce(force: THREE.Vector3, delta: number): void {
    this.worldVelocity.add(force.clone().multiplyScalar(delta));
  }
  
  public isEmpty(): boolean {
    return this.waterVolume < 0.05;
  }
  
  onDestroy(): void {
    super.onDestroy();
    this.simulator.dispose();
    this.mesh.geometry.dispose();
  }
}
```

### 17.4 EntityManager 集成

#### 液滴创建接口

```typescript
// EntityManager.ts

public async createDroplet(
  id: string,
  position?: THREE.Vector3,
  velocity?: THREE.Vector3,
  scale: number = 0.2
): Promise<Entity> {
  if (!this.renderer) {
    throw new Error('Renderer not initialized');
  }
  
  const { LightFluidEntity } = await import('@entities/fluid/LightFluidEntity');
  const droplet = new LightFluidEntity(id, this.renderer, position, velocity, scale);
  this.addEntity(droplet);
  
  console.log(`[EntityManager] LightFluidEntity created: ${id}`);
  return droplet;
}

public async createDroplets(
  count: number,
  centerPos: THREE.Vector3,
  spreadRadius: number = 1,
  initialVelocity?: THREE.Vector3,
  scale: number = 0.2
): Promise<Entity[]> {
  const droplets: Entity[] = [];
  
  for (let i = 0; i < count; i++) {
    const offset = new THREE.Vector3(
      (Math.random() - 0.5) * spreadRadius * 2,
      (Math.random() - 0.5) * spreadRadius * 2,
      (Math.random() - 0.5) * spreadRadius * 2
    );
    
    const pos = centerPos.clone().add(offset);
    const vel = initialVelocity?.clone().add(new THREE.Vector3(
      (Math.random() - 0.5) * 0.5,
      (Math.random() - 0.5) * 0.5,
      (Math.random() - 0.5) * 0.5
    ));
    
    const droplet = await this.createDroplet(
      `droplet_${Date.now()}_${i}`,
      pos,
      vel,
      scale
    );
    droplets.push(droplet);
  }
  
  console.log(`[EntityManager] Created ${count} droplets at ${centerPos.x}, ${centerPos.y}, ${centerPos.z}`);
  return droplets;
}
```

### 17.5 使用示例

```typescript
// 创建单个液滴
const droplet = await EntityManager.getInstance().createDroplet(
  'droplet_1',
  new THREE.Vector3(0, 2, 0),
  new THREE.Vector3(0, -1, 0)
);

// 批量创建液滴
const droplets = await EntityManager.getInstance().createDroplets(
  10,
  new THREE.Vector3(5, 3, 5),
  2,
  new THREE.Vector3(0, -2, 0),
  0.25
);

// 每帧更新时应用重�?for (const entity of EntityManager.getInstance().getEntitiesByType('lightFluid')) {
  (entity as LightFluidEntity).applyForce(new THREE.Vector3(0, -9.8, 0), delta);
}
```

### 17.6 实体目录结构

```
src/entities/
├── bullets/            // 子弹实体
�?  ├── BulletEntity.ts
�?  └── DawnExplosionBulletEntity.ts
├── characters/         // 角色实体
�?  ├── CharacterEntity.ts
�?  ├── FriendlyEntity.ts
�?  └── EnemyEntity.ts
├── fluid/              // 流体实体（新增）
�?  ├── LightFluidEntity.ts    // 轻量级流体实�?�?  ├── FluidRegionManager.ts  // 流体区域管理�?�?  └── FluidLOD.ts            // LOD枚举定义
├── static/             // 静态物品实�?�?  ├── StaticEntity.ts
�?  └── TargetEntity.ts
└── Entity.ts           // 实体基类
```

---

## 18. 流体区域管理器系�?
### 18.1 系统概述

流体区域管理器（FluidRegionManager）是EntityManager和LightFluidEntity之间的中间层，负责管理区域性流体实体，实现基于距离的LOD控制和距离超�?0单位时的自动销毁�?
### 18.2 核心架构

```
FluidRegionManager (流体区域管理�?
├── entities: LightFluidEntity[] - 区域内的液滴实体列表
├── currentLOD: FluidLOD - 当前LOD等级
├── markedForRemoval: boolean - 是否标记待销�?├── maxActiveSimulations: number - 最大活跃模拟数（默�?�?└── update(delta, lod): void - 核心更新方法
```

### 18.3 LOD等级定义

```typescript
export enum FluidLOD {
  HIGH = 0,   // 高细节：每帧更新模拟
  MEDIUM = 1, // 中等细节：每2帧更新模�?  LOW = 2,    // 低细节：�?帧更新模拟，冻结纹理
  OFF = 3     // 关闭：完全跳过更新，标记待销�?}
```

### 18.4 距离阈值配�?
| LOD等级 | 距离阈�?| 行为描述 |
|---------|---------|---------|
| HIGH | �?单位 | 最高频率更新，纹理实时更新 |
| MEDIUM | �?单位 | 中等频率更新，纹理正常更�?|
| LOW | �?0单位 | 低频率更新，纹理冻结 |
| OFF | >10单位 | 完全停止更新，区域标记待销�?|

### 18.5 核心实现

```typescript
// src/entities/fluid/FluidRegionManager.ts
export class FluidRegionManager {
  private entities: LightFluidEntity[] = [];
  private currentLOD: FluidLOD = FluidLOD.HIGH;
  public markedForRemoval: boolean = false;
  private maxActiveSimulations: number = 4;

  // 核心更新：由 EntityManager 每帧调用，传�?LOD 等级
  update(delta: number, lod: FluidLOD): void {
    this.currentLOD = lod;
    
    // 收集已失效的液滴（已�?EntityManager 中标记为非活跃的�?    this.entities = this.entities.filter(e => e.isActive);
    
    // 距离角色过远（LOD = OFF），标记整个区域待销�?    if (lod === FluidLOD.OFF) {
      this.markedForRemoval = true;
      // 标记所有液滴为非活跃，由EntityManager安全删除
      for (const droplet of this.entities) {
        droplet.isActive = false;
      }
      return;
    }
    
    // 重置销毁标�?    this.markedForRemoval = false;
    
    // 性能优化：限制同时活跃的模拟实例�?    let activeCount = 0;
    for (const droplet of this.entities) {
      if (droplet.isActive && lod < FluidLOD.OFF) {
        if (activeCount < this.maxActiveSimulations) {
          droplet.lod = lod;
          activeCount++;
        } else {
          // 超出限制的液滴强制休�?          droplet.lod = FluidLOD.OFF;
        }
      } else {
        droplet.lod = lod;
      }
    }
  }

  public createDroplets(count: number, renderer: THREE.WebGLRenderer): void {
    for (let i = 0; i < count; i++) {
      const droplet = new LightFluidEntity(
        `droplet_${Date.now()}_${i}`,
        renderer,
        undefined,
        undefined,
        0.2
      );
      this.entities.push(droplet);
      EntityManager.getInstance().addEntity(droplet);
    }
  }
}
```

### 18.6 LightFluidEntity LOD支持

```typescript
// src/entities/fluid/LightFluidEntity.ts
export class LightFluidEntity extends Entity {
  public lod: FluidLOD = FluidLOD.HIGH;
  private simUpdateAccumulated: number = 0;
  private prevWorldVelocity: THREE.Vector3 = new THREE.Vector3();

  update(delta: number): void {
    if (!this.isActive) return;
    
    // 始终维护生命周期
    this.age += delta;
    
    // 寿命检�?    if (this.age > this.maxAge) {
      this.isActive = false;
      return;
    }
    
    // OFF 级别：完全跳过更�?    if (this.lod === FluidLOD.OFF) {
      this.mesh.visible = false;
      return;
    }
    
    // 位置更新（始终执行，不受LOD影响�?    this.mesh.position.x += this.worldVelocity.x * delta;
    this.mesh.position.y += this.worldVelocity.y * delta;
    this.mesh.position.z += this.worldVelocity.z * delta;
    this.position.copy(this.mesh.position);
    
    if (!this.simulator) return;
    
    // 根据LOD获取模拟更新间隔
    const simInterval = this.getSimInterval();
    
    // 累积时间�?    this.simUpdateAccumulated += delta;
    
    // 仅当累积时间达到间隔时才更新模拟
    if (this.simUpdateAccumulated >= simInterval) {
      // 计算加速度（使用累积时间，提高精度�?      const accel = new THREE.Vector3()
        .subVectors(this.worldVelocity, this.prevWorldVelocity)
        .divideScalar(this.simUpdateAccumulated || delta);
      this.prevWorldVelocity.copy(this.worldVelocity);
      
      const internalForceX = -accel.x * 0.5;
      const internalForceY = -accel.y * 0.5;
      this.simulator.addVelocityImpulse(internalForceX, internalForceY);
      
      // 更新流体模拟器（传递累计时间）
      this.simulator.update(this.simUpdateAccumulated);
      
      // 重置累积时间
      this.simUpdateAccumulated = 0;
    }
    
    // 纹理更新控制
    if (this.lod === FluidLOD.LOW) {
      this.simulator.setTextureUpdateEnabled(false);
    } else {
      this.simulator.setTextureUpdateEnabled(true);
      // 更新材质
      const newMaterial = this.simulator.getRenderMaterial();
      if (this.mesh.material !== newMaterial) {
        if (this.mesh.material instanceof THREE.ShaderMaterial) {
          this.mesh.material.dispose();
        }
        this.mesh.material = newMaterial;
      }
    }
    
    this.mesh.visible = true;
  }

  private getSimInterval(): number {
    switch (this.lod) {
      case FluidLOD.HIGH: return 0;      // 每帧更新
      case FluidLOD.MEDIUM: return 0.033; // 约每2�?      case FluidLOD.LOW: return 0.066;   // 约每4�?      default: return Infinity;
    }
  }
}
```

---

## 19. 时间缩放系统

### 19.1 系统概述

时间缩放系统解决游戏时间与真实时间不同步的问题，通过全局timeScale参数调整delta时间�?
### 19.2 核心实现

```typescript
// src/core/EntityManager.ts
export class EntityManager {
  /**
   * 时间缩放因子，用于加快或减慢游戏内时�?   * 1.0 = 正常速度�?.0 = 两倍速度
   */
  public static timeScale: number = 2.0;

  /**
   * 更新所有实体（每帧调用�?   * @param delta 时间差（秒）
   */
  public update(delta: number): void {
    // 应用时间缩放
    const scaledDelta = delta * EntityManager.timeScale;
    this.updateEntities(scaledDelta);
  }

  /**
   * 更新所有实体（每帧调用，带玩家位置�?   * @param delta 时间差（秒）
   * @param playerPosition 玩家位置（用于计算流体LOD�?   */
  public updateWithPlayer(delta: number, playerPosition?: THREE.Vector3): void {
    // 应用时间缩放
    const scaledDelta = delta * EntityManager.timeScale;
    
    // 1. 更新普通实�?    this.updateEntities(scaledDelta);
    
    // 2. 更新流体区域
    this.updateFluidRegions(scaledDelta, playerPosition);
  }
}
```

### 19.3 使用示例

```typescript
// 加快游戏时间（默�?倍）
EntityManager.timeScale = 2.0;

// 正常速度
EntityManager.timeScale = 1.0;

// 慢动�?EntityManager.timeScale = 0.5;
```

---

### 19.4 外部力接口系�?�?
#### 19.4.1 系统概述

外部力接口（FluidExternalForce）提供统一的外部力施加方式，支持多种力类型：世界加速度、速度注入、散度注入、水体注入�?
#### 19.4.2 接口定义

```typescript
// src/entities/fluid/FluidExternalForce.ts
export interface IFluidForceTarget {
  applyFluidForce(force: FluidExternalForce): void;
}

export interface FluidExternalForce {
  worldAcceleration?: THREE.Vector3;     // 世界空间加速度（重力等�?  velocityInjection?: {                   // 速度注入
    velocity: THREE.Vector2;              // 速度增量（UV空间�?    radius?: number;                     // 影响半径（默�?.2�?    centerUV?: THREE.Vector2;            // 中心位置（默�?.5, 0.5�?  };
  divergenceInjection?: {                  // 散度注入（爆炸等�?    divergence: number;                   // 散度�?    radius?: number;                      // 影响半径
    centerUV?: THREE.Vector2;             // 中心位置
  };
  waterInjection?: {                      // 水体注入
    amount: number;                       // 水量（负值减少）
    radius?: number;                      // 影响半径
    centerUV?: THREE.Vector2;             // 中心位置
  };
}
```

#### 19.4.3 实现�?
| �?| 实现接口 | 说明 |
|---|---------|------|
| `FluidSimulatorAdapter` | IFluidForceTarget | 详细纹理，使用addLocalVelocityImpulse |
| `LightFluidEntity` | IFluidForceTarget | 轻量�?2x32流体，内部调用simulator.addVelocityImpulse |

#### 19.4.4 局部速度注入（addLocalVelocityImpulse�?
```typescript
// FluidSimulator.ts
public addLocalVelocityImpulse(
  dvx: number,       // X方向速度增量
  dvy: number,       // Y方向速度增量
  radius: number = 0.2,    // 影响半径（UV空间�?  cx: number = 0.5,        // 中心X（UV空间�?  cy: number = 0.5,        // 中心Y（UV空间�?  maxSpeed: number = 2.0   // 最大速度上限
): void
```

**实现原理**�?
```glsl
// Shader计算
float d = distance(uv, center);
float mask = 1.0 - smoothstep(0.0, radius, d);  // 渐变遮罩
vec4 oldVel = texture2D(curVel, uv);
vec2 newVel = oldVel.rg + impulse * mask;

// 速度上限
float speed = length(newVel);
if (speed > maxSpeed) {
    newVel = normalize(newVel) * maxSpeed;
}
```

#### 19.4.5 物理时间保护机制 �?新增

为解决外部力注入导致流体数值不稳定（速度累积超过 CFL 条件）的问题，实现了**力缓�?+ 时间步缩�?*机制�?
##### 核心设计要点

| 特�?| 实现方式 |
|------|----------|
| **力缓�?* | `applyFluidForce` 不再立即注入力，而是存入 `pendingForces` 队列 |
| **时间步缩�?* | 速度注入 = velocity(加速度) × dt |
| **与重力一�?* | 外力遵循与重力相同的模型：`vel += acceleration × dt` |
| **双通道支持** | FluidSimulatorAdapter �?LightFluidEntity 均实现此机制 |

##### 实现代码

**FluidSimulatorAdapter 改�?*�?
```typescript
// 新增：待处理的外力列�?private pendingForces: FluidExternalForce[] = [];

applyFluidForce(force: FluidExternalForce): void {
    if (!this.simulator) return;
    // 忽略世界运动力（静态纹理不可移动），但内部力全部缓�?    if (force.velocityInjection || force.divergenceInjection || force.waterInjection) {
        this.pendingForces.push(force);
    }
}

private processPendingForces(dt: number): void {
    const sim = this.simulator;
    if (!sim || this.pendingForces.length === 0) return;

    for (const force of this.pendingForces) {
        if (force.velocityInjection) {
            const inj = force.velocityInjection;
            const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
            const radius = inj.radius ?? 0.2;
            // �?velocity 视为加速度 (uv/s²)，实际增�?= 加速度 * dt
            const dvx = inj.velocity.x * dt;
            const dvy = inj.velocity.y * dt;
            sim.addLocalVelocityImpulse(dvx, dvy, radius, center.x, center.y, 10.0);
        }
        
        if (force.divergenceInjection) {
            const inj = force.divergenceInjection;
            const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
            const radius = inj.radius ?? 0.2;
            sim.addDivergenceImpulse(inj.divergence, radius, center.x, center.y);
        }

        if (force.waterInjection) {
            const inj = force.waterInjection;
            const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
            const radius = inj.radius ?? 0.2;
            sim.addWaterImpulse(inj.amount, radius, center.x, center.y);
        }
    }

    this.pendingForces.length = 0;
}
```

**update 方法集成**�?
```typescript
update(delta?: number): void {
    const now = performance.now();
    const elapsed = now - this.lastUpdateTime;
    this.lastUpdateTime = now;

    const realDelta = (delta !== undefined) ? delta : (elapsed / 1000);
    
    // �?处理缓存的外力（时间保护�?    this.processPendingForces(realDelta);
    
    // ... 原有爆炸逻辑和模拟更�?...
}
```

##### 使用示例（加速度模型�?
```typescript
// 每帧施加持续侧向风力（加速度模型�?const windForce: FluidExternalForce = {
  velocityInjection: {
    velocity: new THREE.Vector2(200, 0),  // 加速度 200 uv/s²
    radius: 1.0,
    centerUV: new THREE.Vector2(0.5, 0.5)
  }
};
adapter.applyFluidForce(windForce);
// adapter.update(dt) 会以 dt 缩放速度增量，平稳驱�?```

##### 参数推荐

| 参数类型 | 推荐范围 | 说明 |
|---------|---------|------|
| 风力加速度 | 50 ~ 500 uv/s² | 重力约为 9.81 uv/s²，风力通常设为重力的数�?|
| 最大速度限制 | 2 ~ 10 uv/s | 防止速度无限累积 |

#### 19.4.6 随机横向力系�?
在GameWorld中实现，�?秒对水流施加随机横向力：

```typescript
// GameWorld.tsx - useFrame�?const currentTime = clock.getElapsedTime();
if (currentTime - lastForceTimeRef.current >= 1.0) {
  lastForceTimeRef.current = currentTime;

  const randomForce: FluidExternalForce = {
    velocityInjection: {
      velocity: new THREE.Vector2(
        (Math.random() - 0.5) * 0.001,  // 每帧极小增量
        0
      ),
      radius: 0.5,                       // 影响半径50%
      centerUV: new THREE.Vector2(0.5, 0.3)  // 中下部位�?    }
  };

  // 施加到详细纹�?  detailSim.applyFluidForce(randomForce);

  // 施加到所有小液滴
  for (const droplet of droplets) {
    droplet.applyFluidForce(randomForce);
  }
}
```

**参数配置**�?
| 参数 | �?| 说明 |
|-----|-----|-----|
| 每帧增量 | 0.001 | 极小速度增量 |
| 最大速度 | 2.0 | 速度上限 |
| 施加间隔 | 1�?| 每秒施加一�?|
| 影响范围 | 半径0.5 | 中下部水�?|

---

## 20. 核心函数说明

### 12.1 地形生成系统

#### `generateTerrain(params: TerrainParams): TerrainData`

**功能**：生成基�?Simplex 噪声的地形数据，优化为以平原为主

**返回�?*：包含地形高度数据和 `getHeightAt` 函数的对�?
**使用场景**：`TerrainRenderer` 中生成地形几何体

#### `getHeightAt(x: number, z: number): number`

**功能**：获取指定坐标的地形高度（使用双线性插值）

**使用场景**�?- 角色碰撞检测时获取地形高度
- 场景元素（草丛、靶子）放置时获取地形高�?
### 12.2 光线渲染系统

#### `updateTerrainColors()`

**功能**：更新地形顶点颜色，实现基于海拔差的四档光影效果

**亮度划分**�?| 分类 | 条件 | 亮度 |
|------|------|------|
| �?| deltaY < -2.0 | 1.0 |
| 较暗 | -2.0 �?deltaY < -0.5 或远�?周围暗部 | 3.0 |
| 较亮 | -0.5 �?deltaY < 2.0 | 5.0 |
| �?| deltaY �?2.0 | 5.8 |

### 12.3 角色物理系统

#### `applyGravityToCharacter(position, velocity, delta, getHeightAt?)`

**功能**：应用重力并处理角色与地形的碰撞检�?
**实现逻辑**�?1. 应用重力，更新角色速度
2. 计算新的角色位置
3. 获取角色当前位置的地形高�?4. 计算角色脚的高度
5. 检测碰撞，确保角色脚不低于地形高度
6. 限制角色下落速度，防止穿�?
### 12.4 射击系统管理�?
#### `ShootingSystemManager`

**功能**：统一管理射击系统的注册、切换和更新

**核心方法**�?| 方法 | 功能 |
|------|------|
| `registerSystem(name, system)` | 注册射击系统 |
| `setActiveSystem(name)` | 切换到指定射击系�?|
| `getActiveSystem()` | 获取当前系统实例 |
| `update(delta)` | 更新当前系统 |
| `onMouseDown/MouseUp/MouseMove` | 转发鼠标事件 |

### 12.5 射击系统状态管�?
**状态管理模�?*：射击系统成为唯一数据源，通过回调传递状态给UI

**回调接口**�?
| 回调 | 功能 | 调用场景 |
|------|------|----------|
| `onLockStateChanged(isLocking, lockCountdown)` | 锁定状态变�?| 锁定过程中状态更�?|
| `onActiveSystemChanged(system)` | 活跃系统变化 | 切换射击系统�?|
| `onBulletCreated(bullet)` | 子弹创建 | 发射子弹�?|
| `onTargetLocked(target)` | 目标锁定 | 锁定成功�?|

**实现方式**�?```typescript
// GameWorld.tsx 中设置回�?shootingManager.setCallbacks({
  onLockStateChanged: (locking, countdown) => {
    setIsLocking(locking);
    setLockCountdown(countdown);
    onLockStateChanged(locking, countdown);
  },
  onActiveSystemChanged: (system) => {
    setActiveShootingSystem(system);
    onActiveSystemChanged(system);
  }
});
```

### 12.6 角色位置监听�?
**实现方式**：使用独立的 `CharacterPositionStore` 单例存储角色位置，通过节流更新减少 UI 重渲染�?
**核心代码**�?
```typescript
// src/systems/character/CharacterPositionStore.ts
export class CharacterPositionStore {
  private static instance: CharacterPositionStore;

  public position: THREE.Vector3 = new THREE.Vector3(0, 0, 0);
  public velocity: THREE.Vector3 = new THREE.Vector3(0, 0, 0);
  public isMoving: boolean = false;

  // 单例模式实现
  public static getInstance(): CharacterPositionStore {
    if (!CharacterPositionStore.instance) {
      CharacterPositionStore.instance = new CharacterPositionStore();
    }
    return CharacterPositionStore.instance;
  }

  // 设置位置
  public setPosition(x: number, y: number, z: number): void {
    this.position.set(x, y, z);
  }

  // 获取位置副本（用�?UI 显示�?  public getPositionCopy(): { x: number; y: number; z: number } {
    return {
      x: this.position.x,
      y: this.position.y,
      z: this.position.z
    };
  }
}

export const characterPositionStore = CharacterPositionStore.getInstance();
```

**UI 更新节流**�?
```typescript
// App.tsx - �?100ms 更新一�?UI 显示
useEffect(() => {
  let animationId: number;
  const throttledUpdate = (timestamp: number) => {
    if (timestamp - lastUpdateRef.current >= 100) {
      setDisplayPos(characterPositionStore.getPositionCopy());
      lastUpdateRef.current = timestamp;
    }
    animationId = requestAnimationFrame(throttledUpdate);
  };
  animationId = requestAnimationFrame(throttledUpdate);
  return () => {
    cancelAnimationFrame(animationId);
  };
}, []);
```

**优势**�?- 避免每帧创建新的位置对象，减少内存分�?- 通过节流更新，减�?UI 组件的重渲染次数
- 集中管理角色位置状态，提高代码可维护�?
### 12.7 特效系统

#### `EffectManager.getInstance().playDawnExplosion(position)`

**功能**：播放爆裂黎明特�?
**参数**�?- `position`: THREE.Vector3 - 特效位置

**使用场景**�?- 子弹命中目标�?- 爆炸效果

#### `EffectManager.getInstance().update(delta)`

**功能**：更新所有活跃特�?
**参数**�?- `delta`: number - 时间增量

**使用场景**�?- 每帧调用，更新特效状�?
### 12.8 动画系统

#### `AnimationLoader.loadClip(basePath, frameCount, startIndex, frameRate, loop)`

**功能**：异步加载动画片�?
**参数**�?- `basePath`: string - 动画帧基础路径
- `frameCount`: number - 帧数
- `startIndex`: number - 起始帧索�?- `frameRate`: number - 帧率
- `loop`: boolean - 是否循环

**返回�?*�?- `Promise<AnimationClip>` - 加载完成的动画片�?
#### `PaperAnimator.updateDirection(charPos, cameraPos)`

**功能**：根据相机方向更新动画方�?
**参数**�?- `charPos`: THREE.Vector3 - 角色位置
- `cameraPos`: THREE.Vector3 - 相机位置

#### `PaperAnimator.forceDirection(direction)`

**功能**：强制设置动画方�?
**参数**�?- `direction`: 'front' | 'back' - 动画方向

---

## 13. 准心组件 (Crosshair)

### 13.1 组件结构

```typescript
interface CrosshairProps {
  isLockMode: boolean;      // 是否为锁定模�?  isLocking?: boolean;      // 是否正在锁定
  lockCountdown?: number;   // 锁定倒计�?(0-1000)
}
```

### 13.2 视觉效果

- **基础准心**�? 条线组成的十�?- **锁定进度**：SVG 圆圈显示锁定倒计�?- **位置追踪**：跟随鼠标移动（`mousemove` 事件�?
### 13.3 锁定进度计算

```typescript
const progress = lockCountdown / 1000;  // 0-1
const circumference = 2 * Math.PI * 15;  // 圆圈周长
const strokeDashoffset = circumference * (1 - progress);  // 进度条偏�?```

---

## 14. 项目结构

```
src/
├── components/           // React 组件
�?  ├── App