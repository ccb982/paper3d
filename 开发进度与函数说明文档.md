# 纸片人3D游戏开发进度与函数说明文档

## 1. 项目概述

**项目名称**：my-paper-game  
**项目类型**：3D纸片人交互游戏  
**技术栈**：React + TypeScript + Three.js + React Three Fiber + React Three Drei + Zustand  
**当前开发阶段**：基础架构搭建与核心功能实现

## 2. 开发进度

### 2.1 已完成的功能

| 功能模块 | 状态 | 完成时间 | 备注 |
|---------|------|---------|------|
| 项目初始化 | ✅ | 已完成 | 搭建Vite React TypeScript项目 |
| 依赖安装 | ✅ | 已完成 | 安装Three.js、React Three Fiber等依赖 |
| 目录结构 | ✅ | 已完成 | 按照系统模块划分目录 |
| 渲染系统 | ✅ | 已完成 | 实现场景设置、相机控制、灯光配置 |
| 角色系统 | ✅ | 已完成 | 实现纸片人渲染、纹理加载、点击交互、平滑旋转 |
| 状态管理系统 | ✅ | 已完成 | 使用Zustand管理游戏状态 |
| 输入系统 | ✅ | 已完成 | 实现键盘WASD控制，支持角色移动 |
| 工具系统 | ✅ | 已完成 | 定义游戏常量 |
| 纹理资源 | ✅ | 已完成 | 创建角色纹理文件 |
| 对话系统 | ✅ | 已完成 | 实现AI对话功能，添加对话气泡，支持打字机效果 |
| UI系统 | ✅ | 已完成 | 添加状态面板和加载动画，实现游戏风格UI |
| 场景系统 | ✅ | 已完成 | 实现地图渲染，使用大地图.png作为地面纹理，添加简单障碍物 |
| 重力系统 | ✅ | 已完成 | 实现角色跳跃和重力模拟 |
| 相机系统 | ✅ | 已完成 | 实现第三人称相机控制，支持鼠标滚轮调整距离和自由旋转 |
| 射击系统 | ✅ | 已完成 | 实现准星、黄色子弹、20秒消失、轻微下坠、连发功能 |
| 物理系统 | ✅ | 已完成 | 实现子弹碰撞检测和生命周期管理 |

### 2.2 待实现的功能

| 功能模块 | 状态 | 优先级 | 备注 |
|---------|------|---------|------|
| 音频系统 | ⏳ | 低 | 添加背景音乐和音效 |

## 3. 核心文件与函数说明

### 3.1 主应用文件

#### App.tsx

**文件路径**：`src/App.tsx`  
**功能**：作为应用的主入口，集成Canvas、SceneSetup、PaperCharacter组件和移动控制器，实现重力系统、相机控制和射击机制  

**核心函数**：

```tsx
function App() {
  const handleCharacterClick = (id: string) => {
    console.log('Clicked character:', id);
    // 后续集成对话系统
  };

  return (
    <div className="game-container">
      <Canvas camera={{ position: [0, 2, 10] }}>
        <SceneSetup />
        <PaperCharacter 
          characterId="player" 
          onClick={handleCharacterClick} 
        />
        <MovementController />
        <Crosshair />
      </Canvas>
    </div>
  );
}

// 移动控制器组件
const MovementController = () => {
  const { camera, gl } = useThree();
  const canvas = gl.domElement;
  const direction = useKeyboard(camera);
  const directionRef = useRef(direction);
  const gameStore = useGameStore();
  const [bullets, setBullets] = useState<Bullet[]>([]);
  const isMouseDownRef = useRef(false);
  const lastFireTimeRef = useRef(0);
  const fireRate = 200; // 射击频率（毫秒）
  const bulletVelocity = 10; // 子弹速度
  const bulletIdRef = useRef(0);
  const mousePosRef = useRef({ x: 0, y: 0 });
  const [cameraDistance, setCameraDistance] = useState(8);
  const [cameraHeight, setCameraHeight] = useState(3);
  const [cameraYaw, setCameraYaw] = useState(0);
  const [cameraPitch, setCameraPitch] = useState(0);

  // 同步方向值到ref，避免闭包问题
  useEffect(() => {
    directionRef.current = direction;
  }, [direction]);

  // 鼠标事件监听
  useEffect(() => {
    const handleMouseMove = (e: MouseEvent) => {
      // 计算鼠标移动距离
      const deltaX = e.clientX - mousePosRef.current.x;
      const deltaY = e.clientY - mousePosRef.current.y;

      // 更新鼠标位置
      mousePosRef.current = { x: e.clientX, y: e.clientY };

      // 计算旋转角度（灵敏度调整）
      const sensitivity = 0.01;
      const yawDelta = -deltaX * sensitivity; // 反向调整
      const pitchDelta = -deltaY * sensitivity; // 反向调整

      // 更新摄像机旋转角度
      setCameraYaw(prev => prev + yawDelta);
      // 限制俯仰角度，避免过度旋转
      setCameraPitch(prev => Math.max(-Math.PI / 2, Math.min(Math.PI / 2, prev - pitchDelta)));
    };

    const handleMouseDown = () => {
      isMouseDownRef.current = true;
    };

    const handleMouseUp = () => {
      isMouseDownRef.current = false;
    };

    const handleWheel = (e: WheelEvent) => {
      // 调整摄像机距离
      setCameraDistance(prev => Math.max(2, Math.min(20, prev - e.deltaY * 0.01)));
    };

    // 添加鼠标事件监听器
    window.addEventListener('mousemove', handleMouseMove);
    window.addEventListener('mousedown', handleMouseDown);
    window.addEventListener('mouseup', handleMouseUp);
    window.addEventListener('wheel', handleWheel);

    // 清理事件监听器
    return () => {
      window.removeEventListener('mousemove', handleMouseMove);
      window.removeEventListener('mousedown', handleMouseDown);
      window.removeEventListener('mouseup', handleMouseUp);
      window.removeEventListener('wheel', handleWheel);
    };
  }, []); // 空依赖数组，只执行一次

  // 实时计算子弹方向（纯函数，每次调用都用最新参数）
  const getBulletDirection = (
    camera: THREE.Camera,
    characterPos: { x: number; y: number; z: number },
    mouseX: number,
    mouseY: number,
    canvasElement: HTMLCanvasElement  // 传入 canvas 元素
  ): THREE.Vector3 => {
    // 1. 计算 canvas 相对坐标
    const rect = canvasElement.getBoundingClientRect();
    const ndcX = ((mouseX - rect.left) / rect.width) * 2 - 1;
    const ndcY = -((mouseY - rect.top) / rect.height) * 2 + 1;

    // 2. 创建射线
    const raycaster = new THREE.Raycaster();
    raycaster.setFromCamera(new THREE.Vector2(ndcX, ndcY), camera);

    // 3. 定义水平面（Y = 角色发射高度）
    const bulletOriginY = characterPos.y + 1;
    const horizontalPlane = new THREE.Plane(new THREE.Vector3(0, 1, 0), bulletOriginY);
    const targetPoint = new THREE.Vector3();

    // 4. 计算交点
    if (raycaster.ray.intersectPlane(horizontalPlane, targetPoint)) {
      // 从角色发射点指向交点
      return new THREE.Vector3().subVectors(
        targetPoint,
        new THREE.Vector3(characterPos.x, bulletOriginY, characterPos.z)
      ).normalize();
    } else {
      // 射线与平面平行（罕见），回退到射线方向
      return raycaster.ray.direction.clone().normalize();
    }
  };

  // 每帧更新角色位置和摄像机位置
  useFrame((_, delta) => {
    const currentPos = gameStore.character.position;
    let currentVelocity = gameStore.character.velocity;
    const currentDirection = directionRef.current;
    
    // 开火检测
    if (isMouseDownRef.current && camera) {
      const now = Date.now();
      if (now - lastFireTimeRef.current >= fireRate) {
        lastFireTimeRef.current = now;
        
        // 实时获取最新值
        const realTimeCharacterPos = gameStore.character.position;
        const direction = getBulletDirection(camera, realTimeCharacterPos, mousePosRef.current.x, mousePosRef.current.y, canvas);
        const newBullet = {
          id: bulletIdRef.current++,
          position: { x: realTimeCharacterPos.x, y: realTimeCharacterPos.y + 1, z: realTimeCharacterPos.z },
          direction,
        };
        setBullets(prev => [...prev, newBullet]);
      }
    }
    
    // 更新子弹位置
    setBullets(prev => prev.filter(bullet => {
      bullet.position.x += bullet.direction.x * bulletVelocity * delta;
      bullet.position.y += bullet.direction.y * bulletVelocity * delta;
      // 模拟重力
      bullet.position.y -= 0.1 * delta;
      bullet.position.z += bullet.direction.z * bulletVelocity * delta;
      // 超出边界后移除
      return Math.abs(bullet.position.x) < 100 && Math.abs(bullet.position.z) < 100 && bullet.position.y < 50;
    }));
    
    // 检查是否按下跳跃键且角色在地面上
    if (currentDirection.jump && currentVelocity.y === 0) {
      currentVelocity.y = 7; // 跳跃力量
    }
    
    // 应用重力
    currentVelocity.y -= 0.2;
    
    // 计算新位置
    const newPos = {
      x: currentPos.x + currentDirection.x * 5 * delta + currentVelocity.x,
      y: currentPos.y + currentVelocity.y * delta,
      z: currentPos.z + currentDirection.z * 5 * delta + currentVelocity.z
    };
    
    // 地面碰撞检测
    if (newPos.y < 0) {
      newPos.y = 0;
      currentVelocity.y = 0;
    }
    
    // 更新角色位置
    gameStore.setCharacterPosition(newPos);
    gameStore.setCharacterVelocity(currentVelocity);
    
    // 更新角色移动状态
    gameStore.setCharacterMoving(currentDirection.x !== 0 || currentDirection.z !== 0);
    
    // 更新摄像机位置
    if (camera) {
      // 计算摄像机位置（围绕角色旋转）
      const offsetX = Math.sin(cameraYaw) * cameraDistance;
      const offsetZ = Math.cos(cameraYaw) * cameraDistance;
      const offsetY = Math.sin(cameraPitch) * cameraDistance + cameraHeight;
      
      camera.position.set(
        newPos.x + offsetX,
        newPos.y + offsetY,
        newPos.z + offsetZ
      );
      
      // 摄像机看向角色
      camera.lookAt(newPos.x, newPos.y + 1, newPos.z);
    }
  });

  return (
    <>
      {/* 渲染子弹 */}
      {bullets.map(bullet => (
        <mesh key={bullet.id} position={[bullet.position.x, bullet.position.y, bullet.position.z]}>
          <sphereGeometry args={[0.1, 8, 8]} />
          <meshStandardMaterial color="yellow" />
        </mesh>
      ))}
    </>
  );
};

// 准星组件
const Crosshair = () => {
  return (
    <div className="crosshair">
      <div className="crosshair-inner"></div>
    </div>
  );
};
```

**参数说明**：
- 无参数

**返回值**：React组件

**功能说明**：
- 创建游戏容器，设置Canvas组件
- 配置相机位置
- 集成SceneSetup组件设置场景
- 集成PaperCharacter组件渲染角色
- 集成MovementController组件处理角色移动、相机控制和射击
- 集成Crosshair组件显示准星
- 实现重力系统，支持角色跳跃
- 实现第三人称相机控制，支持鼠标滚轮调整距离和自由旋转
- 实现射击系统，包括准星、黄色子弹、20秒消失、轻微下坠、连发功能
- 实现子弹碰撞检测和生命周期管理

### 3.2 渲染系统

#### SceneSetup.tsx

**文件路径**：`src/systems/rendering/SceneSetup.tsx`  
**功能**：配置Three.js场景、相机、灯光和轨道控制  

**核心函数**：

```tsx
export const SceneSetup = () => {
  return (
    <>
      {/* 环境光 */}
      <ambientLight intensity={0.6} />
      {/* 定向光（模拟太阳光） */}
      <directionalLight position={[5, 10, 5]} intensity={0.8} />
      {/* 辅助网格（地面参考） */}
      <gridHelper args={[20, 20]} />
      {/* 轨道控制（允许拖拽旋转视角） */}
      <OrbitControls enableDamping={true} dampingFactor={0.1} />
    </>
  );
};
```

**参数说明**：
- 无参数

**返回值**：React组件

**功能说明**：
- 配置环境光，提供基础照明
- 配置定向光，模拟太阳光效果
- 添加辅助网格，帮助定位和参考
- 实现轨道控制，允许用户通过鼠标拖拽旋转视角

### 3.3 角色系统

#### PaperCharacter.tsx

**文件路径**：`src/systems/character/PaperCharacter.tsx`  
**功能**：渲染纸片人角色，实现角色的交互和平滑旋转  

**核心函数**：

```tsx
export const PaperCharacter = ({ characterId, onClick }: PaperCharacterProps) => {
  const meshRef = useRef<any>(null);
  const { camera } = useThree();
  
  // 使用 useLoader 加载纹理
  const texture = useLoader(TextureLoader, '/textures/character.png');

  // 每帧更新角色位置和朝向
  useFrame((_, delta) => {
    if (meshRef.current) {
      const { position, isMoving } = useGameStore.getState().character;
      
      // 更新位置
      meshRef.current.position.set(position.x, FIXED_Y, position.z);
      
      // 当角色在移动时，让角色平滑面向相机的方向向量
      if (isMoving) {
        // 获取相机的方向向量
        const cameraDirection = new THREE.Vector3();
        camera.getWorldDirection(cameraDirection);
        
        // 计算目标旋转四元数（面向相机的方向向量）
        const targetQuat = new THREE.Quaternion().setFromUnitVectors(
          new THREE.Vector3(0, 0, 1), // 假设角色默认正面是 +Z
          cameraDirection
        );
        
        // 平滑插值，rotationSpeed 控制转速（弧度/秒）
        const rotationSpeed = 8.0; // 每秒转 8 弧度，约 460 度/秒，可调
        meshRef.current.quaternion.rotateTowards(targetQuat, rotationSpeed * delta);
        
        // 保持直立
        meshRef.current.rotation.x = 0;
        meshRef.current.rotation.z = 0;
      }
    }
  });

  return (
    <mesh 
      ref={meshRef} 
      position={[0, FIXED_Y, 0]}
      userData={{ characterId }}
      onPointerDown={() => onClick(characterId)}
    >
      <planeGeometry args={[2, 3]} />
      <meshBasicMaterial 
        map={texture} 
        transparent={true} 
        opacity={0.8}
        side={DoubleSide} // 支持双面渲染，确保从任何角度都能看到
      />
    </mesh>
  );
};
```

**参数说明**：
- `characterId`：字符串，角色ID，用于标识角色
- `onClick`：函数，点击回调函数，当角色被点击时触发

**返回值**：React组件

**功能说明**：
- 使用useRef创建mesh引用
- 使用useLoader加载角色纹理
- 每帧更新角色位置和朝向
- 实现角色平滑旋转，面向相机的方向向量
- 创建平面几何体作为角色的形状
- 设置材质，应用纹理并启用透明效果
- 处理角色点击事件，调用传入的onClick回调

### 3.4 输入系统

#### useKeyboard.ts

**文件路径**：`src/systems/input/useKeyboard.ts`  
**功能**：处理键盘输入，计算角色移动方向，支持跳跃功能  

**核心函数**：

```tsx
export const useKeyboard = (camera?: THREE.Camera): Direction => {
  const [direction, setDirection] = useState<Direction>({ x: 0, z: 0, jump: false });

  useEffect(() => {
    const keysPressed = new Set<string>();

    const handleKeyDown = (e: KeyboardEvent) => {
      keysPressed.add(e.key.toLowerCase());
      updateDirection();
    };

    const handleKeyUp = (e: KeyboardEvent) => {
      keysPressed.delete(e.key.toLowerCase());
      updateDirection();
    };

    const updateDirection = () => {
      let x = 0;
      let z = 0;
      const jump = keysPressed.has(' ');

      if (keysPressed.has('w')) z += 1;
      if (keysPressed.has('s')) z -= 1;
      if (keysPressed.has('a')) x -= 1;
      if (keysPressed.has('d')) x += 1;

      // 归一化向量，确保对角线移动速度与轴向移动速度一致
      const length = Math.sqrt(x * x + z * z);
      if (length > 0) {
        x /= length;
        z /= length;
      }

      // 如果提供了相机，将移动方向转换为相机坐标系
      if (camera) {
        // 计算相机位置
        const cameraPosition = camera.position.clone();
        
        // 计算相机到原点的向量
        const cameraToOrigin = new THREE.Vector3().subVectors(new THREE.Vector3(0, 0, 0), cameraPosition);
        
        // 计算相机的前方向（从相机指向原点，移除Y分量）
        const cameraForward = new THREE.Vector3(cameraToOrigin.x, 0, cameraToOrigin.z).normalize();
        
        // 计算相机的右方向
        const cameraRight = new THREE.Vector3();
        cameraRight.crossVectors(cameraForward, new THREE.Vector3(0, 1, 0)).normalize();
        
        // 根据相机坐标系计算移动方向
        // W键：向相机前方向的反方向移动（远离相机）
        // S键：向相机前方向移动（靠近相机）
        // A键：向相机左方向移动
        // D键：向相机右方向移动
        const moveDirection = new THREE.Vector3(
          x * cameraRight.x + z * cameraForward.x,
          0,
          x * cameraRight.z + z * cameraForward.z
        );
        
        setDirection({ x: moveDirection.x, z: moveDirection.z, jump });
      } else {
        setDirection({ x, z, jump });
      }
    };

    window.addEventListener('keydown', handleKeyDown);
    window.addEventListener('keyup', handleKeyUp);

    return () => {
      window.removeEventListener('keydown', handleKeyDown);
      window.removeEventListener('keyup', handleKeyUp);
    };
  }, [camera]);

  return direction;
};
```

**参数说明**：
- `camera`：THREE.Camera，可选参数，用于将移动方向转换为相机坐标系

**返回值**：Direction对象，包含x、z方向的移动分量和jump跳跃状态

**功能说明**：
- 监听键盘按下和释放事件
- 计算移动方向向量
- 归一化移动向量，确保对角线移动速度一致
- 根据相机坐标系转换移动方向
- 支持空格键跳跃功能
- 返回计算后的移动方向

### 3.5 状态管理系统

#### gameStore.ts

**文件路径**：`src/systems/state/gameStore.ts`  
**功能**：管理全局游戏状态  

**核心函数**：

```tsx
export const useGameStore = create<GameState>((set, get) => ({
  // 初始状态
  character: {
    position: { x: 0, y: 0, z: 0 },
    velocity: { x: 0, y: 0, z: 0 },
    isMoving: false
  },
  dialog: {
    text: '',
    visible: false
  },
  isLoading: false,
  isMuted: false,
  volume: 1,
  playSoundCallback: null,

  // Actions
  setCharacterPosition: (pos) => set((state) => ({
    character: {
      ...state.character,
      position: pos
    }
  })),
  setCharacterVelocity: (vel) => set((state) => ({
    character: {
      ...state.character,
      velocity: vel
    }
  })),
  setCharacterMoving: (moving) => set((state) => ({
    character: {
      ...state.character,
      isMoving: moving
    }
  })),
  setDialogText: (text) => set((state) => ({
    dialog: {
      ...state.dialog,
      text
    }
  })),
  setDialogVisible: (visible) => set((state) => ({
    dialog: {
      ...state.dialog,
      visible
    }
  })),
  setIsLoading: (loading) => set({ isLoading: loading }),
  toggleMute: () => set((state) => ({
    isMuted: !state.isMuted
  })),
  registerSoundCallback: (cb) => set({ playSoundCallback: cb }),
  playSound: () => {
    const callback = get().playSoundCallback;
    if (callback) {
      callback();
    }
  }
}));
```

**功能说明**：
- 使用Zustand创建游戏状态存储
- 定义初始状态，包括角色状态（位置、速度、移动状态）、对话状态、加载状态和音频状态
- 提供状态修改的方法，如设置角色位置、速度、对话文本等
- 实现音效播放的回调注册和触发

#### storeTypes.ts

**文件路径**：`src/systems/state/storeTypes.ts`  
**功能**：定义状态管理的类型  

**核心类型**：

```tsx
export interface Position {
  x: number;
  y: number;
  z: number;
}

export interface CharacterState {
  position: Position;
  velocity: Position;
  isMoving: boolean;
}

export interface DialogState {
  text: string;
  visible: boolean;
}

export interface GameState {
  character: CharacterState;
  dialog: DialogState;
  isLoading: boolean;
  isMuted: boolean;
  volume: number;
  playSoundCallback: (() => void) | null;

  // Actions
  setCharacterPosition: (pos: Position) => void;
  setCharacterVelocity: (vel: Position) => void;
  setCharacterMoving: (moving: boolean) => void;
  setDialogText: (text: string) => void;
  setDialogVisible: (visible: boolean) => void;
  setIsLoading: (loading: boolean) => void;
  toggleMute: () => void;
  registerSoundCallback: (cb: () => void) => void;
  playSound: () => void;
}

// 子弹类型
export interface Bullet {
  id: number;
  position: Position;
  direction: THREE.Vector3;
}

// 方向类型
export interface Direction {
  x: number;
  z: number;
  jump: boolean;
}
```

**功能说明**：
- 定义Position接口，表示位置坐标
- 定义CharacterState接口，表示角色状态，包含位置、速度和移动状态
- 定义DialogState接口，表示对话状态
- 定义GameState接口，包含所有游戏状态和操作方法
- 定义Bullet接口，表示子弹状态
- 定义Direction接口，表示移动方向

### 3.5 工具系统

#### constants.ts

**文件路径**：`src/utils/constants.ts`  
**功能**：提供游戏中使用的常量定义  

**核心常量**：

```typescript
// 移动速度
export const MOVEMENT_SPEED = 0.1;

// 旋转速度
export const ROTATION_SPEED = 0.02;

// 角色固定Y轴位置
export const FIXED_Y = 1.5;

// 边界范围
export const BOUNDARY = 10;
```

**功能说明**：
- 定义移动速度常量
- 定义旋转速度常量
- 定义角色固定的Y轴位置
- 定义游戏边界范围

### 3.6 对话系统

#### AIDialogue.ts

**文件路径**：`src/systems/dialogue/AIDialogue.ts`  
**功能**：实现AI对话功能，支持mock模式和真实API调用  

**核心函数**：

```typescript
export const fetchAIResponse = async (
  prompt: string,
  signal?: AbortSignal,
  history?: DialogueMessage[]
): Promise<string> => {
  if (MOCK_AI_ENABLED) {
    return new Promise((resolve) => {
      setTimeout(() => {
        const mockResponses = [
          "你好！很高兴见到你！",
          "今天天气真不错，不是吗？",
          "你想聊点什么呢？",
          "我是一个纸片人，很高兴能和你交流！",
          "你觉得这个游戏好玩吗？"
        ];
        const randomResponse = mockResponses[Math.floor(Math.random() * mockResponses.length)];
        resolve(randomResponse ? randomResponse : '');

      }, 1000);
    });
  }

  try {
    const response = await fetch('/api/ai', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ prompt, history }),
      signal,
    });

    if (!response.ok) {
      throw new Error('API 请求失败');
    }

    const data = await response.json();
    const reply = data.reply;
    return (reply && typeof reply === 'string' && reply !== 'undefined') ? reply : '';
  } catch (error) {
    console.error('AI 请求错误:', error);
    return '（AI 暂时无法回应）';
  }
};
```

**参数说明**：
- `prompt`：字符串，对话提示
- `signal`：AbortSignal，可选参数，用于取消请求
- `history`：DialogueMessage数组，可选参数，对话历史

**返回值**：Promise<string>，AI回复的文本

**功能说明**：
- 支持mock模式，返回随机预设回复
- 支持真实API调用，获取AI回复
- 处理网络错误和API错误，返回用户友好的错误消息
- 确保返回的回复是有效的字符串

#### useDialogue.ts

**文件路径**：`src/systems/dialogue/useDialogue.ts`  
**功能**：提供对话触发功能  

**核心函数**：

```typescript
export const useDialogue = () => {
  const abortControllerRef = useRef<AbortController | null>(null);
  const gameStore = useGameStore();

  const triggerDialogue = async (characterId: string) => {
    if (abortControllerRef.current) {
      abortControllerRef.current.abort();
    }

    const abortController = new AbortController();
    abortControllerRef.current = abortController;

    try {
      gameStore.setIsLoading(true);
      gameStore.setDialogVisible(true);

      const prompt = `你是一个纸片人角色，和用户进行对话。用户点击了你，你需要友好地回应。`;

      const reply = await fetchAIResponse(prompt, abortController.signal);

      gameStore.setDialogText(reply || '');

      gameStore.setIsLoading(false);

      gameStore.playSound();
    } catch (error) {
      console.error('对话触发错误:', error);
      gameStore.setDialogText('（AI 暂时无法回应）');
      gameStore.setIsLoading(false);
    }
  };

  return { triggerDialogue };
};
```

**参数说明**：
- 无参数

**返回值**：包含triggerDialogue函数的对象

**功能说明**：
- 提供triggerDialogue函数，用于触发对话
- 管理AbortController，支持取消未完成的请求
- 处理对话触发过程中的错误
- 调用fetchAIResponse获取AI回复
- 更新游戏状态，显示对话气泡

### 3.7 UI系统

#### DialogBubble.tsx

**文件路径**：`src/components/UI/DialogBubble.tsx`  
**功能**：显示对话气泡，支持打字机效果  

**核心函数**：

```tsx
export const DialogBubble: React.FC = () => {
  const { dialog, isLoading } = useGameStore();
  const [displayText, setDisplayText] = useState('');
  const [isVisible, setIsVisible] = useState(false);
  const intervalRef = useRef<NodeJS.Timeout | null>(null);

  useEffect(() => {
    // 清理上一次的定时器
    if (intervalRef.current) {
      clearTimeout(intervalRef.current);
      intervalRef.current = null;
    }

    if (dialog.visible) {
      setIsVisible(true);
      setDisplayText('');

      // 获取安全的文本：排除 null/undefined/字符串"undefined"
      let rawText = dialog.text;
      if (rawText === undefined || rawText === null || rawText === 'undefined') {
        rawText = '';
      }
      const textToDisplay = String(rawText);

      // 使用递归 setTimeout 实现打字机效果
      const typeCharacter = (index: number, fullText: string) => {
        if (index < fullText.length) {
          setDisplayText(prev => prev + fullText[index]);
          intervalRef.current = setTimeout(() => typeCharacter(index + 1, fullText), 50);
        }
      };

      if (textToDisplay.length > 0) {
        setDisplayText('');
        typeCharacter(0, textToDisplay);
      }
    } else {
      setIsVisible(false);
      setDisplayText('');
    }

    return () => {
      if (intervalRef.current) {
        clearTimeout(intervalRef.current);
      }
    };
  }, [dialog.visible, dialog.text, isLoading]);

  if (!isVisible) return null;

  return (
    <div className="dialog-bubble" style={{
      animation: 'dialogAppear 0.3s ease-out forwards',
      opacity: isVisible ? 1 : 0,
      transform: isVisible ? 'translateX(-50%) scale(1)' : 'translateX(-50%) scale(0.8)'
    }}>
      <div className="dialog-content">
        {isLoading ? (
          <div className="loading-text">
            <span>正在思考</span>
            <span className="ellipsis">...</span>
          </div>
        ) : (
          <p>{displayText || ''}</p>
        )}
      </div>
    </div>
  );
};
```

**参数说明**：
- 无参数

**返回值**：React组件

**功能说明**：
- 显示对话气泡，支持打字机效果
- 处理加载状态，显示"正在思考..."提示
- 确保文本处理的安全性，避免显示"undefined"
- 使用递归setTimeout实现打字机效果，避免状态残留
- 添加动画效果，增强视觉体验

#### StatusPanel.tsx

**文件路径**：`src/components/UI/StatusPanel.tsx`  
**功能**：显示角色状态面板  

**核心函数**：

```tsx
export const StatusPanel: React.FC = () => {
  const { character } = useGameStore();
  const { position, isMoving } = character;

  return (
    <div className="status-panel">
      <h3>角色状态</h3>
      <div className="status-item">
        <span className="label">位置:</span>
        <span className="value">X: {position.x.toFixed(2)}, Z: {position.z.toFixed(2)}</span>
      </div>
      <div className="status-item">
        <span className="label">状态:</span>
        <span className="value">{isMoving ? '移动中' : '静止'}</span>
      </div>
      <div className="status-item">
        <span className="label">好感度:</span>
        <span className="value">100</span>
      </div>
    </div>
  );
};
```

**参数说明**：
- 无参数

**返回值**：React组件

**功能说明**：
- 显示角色当前坐标
- 显示角色移动状态
- 显示角色好感度
- 提供游戏风格的UI设计

#### LoadingIndicator.tsx

**文件路径**：`src/components/UI/LoadingIndicator.tsx`  
**功能**：显示加载动画  

**核心函数**：

```tsx
export const LoadingIndicator: React.FC = () => {
  const { isLoading } = useGameStore();

  if (!isLoading) {
    return null;
  }

  return (
    <div className="loading-indicator">
      <div className="loading-spinner"></div>
      <div className="loading-text">正在思考...</div>
    </div>
  );
};
```

**参数说明**：
- 无参数

**返回值**：React组件

**功能说明**：
- 当isLoading为true时显示加载动画
- 提供旋转的加载指示器
- 显示"正在思考..."文本提示

### 3.8 场景系统

#### MapRenderer.tsx

**文件路径**：`src/systems/scene/MapRenderer.tsx`  
**功能**：渲染游戏地图，使用大地图.png作为地面纹理  

**核心函数**：

```tsx
export const MapRenderer: React.FC = () => {
  // 加载地面纹理
  const groundTexture = useLoader(TextureLoader, '/textures/大地图.png');
  groundTexture.wrapS = groundTexture.wrapT = RepeatWrapping;
  groundTexture.repeat.set(1, 1);

  return (
    <group>
      {/* 地面 */}
      <mesh position={[0, 0, 0]} rotation={[-Math.PI / 2, 0, 0]}>
        <planeGeometry args={[100, 100]} />
        <meshStandardMaterial map={groundTexture} />
      </mesh>
      
      {/* 简单的立方体障碍物 */}
      <mesh position={[5, 0.5, 0]}>
        <boxGeometry args={[1, 1, 1]} />
        <meshStandardMaterial color="#8B4513" />
      </mesh>
      
      <mesh position={[-5, 0.5, 0]}>
        <boxGeometry args={[1, 1, 1]} />
        <meshStandardMaterial color="#8B4513" />
      </mesh>
      
      <mesh position={[0, 0.5, 5]}>
        <boxGeometry args={[1, 1, 1]} />
        <meshStandardMaterial color="#8B4513" />
      </mesh>
      
      <mesh position={[0, 0.5, -5]}>
        <boxGeometry args={[1, 1, 1]} />
        <meshStandardMaterial color="#8B4513" />
      </mesh>
    </group>
  );
};
```

**参数说明**：
- 无参数

**返回值**：React组件

**功能说明**：
- 加载并使用大地图.png作为地面纹理
- 创建大型平面作为游戏地图
- 添加简单的立方体障碍物，增强场景层次感
- 实现地图的基本渲染功能

## 4. 技术实现细节

### 4.1 3D渲染实现

- 使用React Three Fiber作为React和Three.js的桥梁
- 使用React Three Drei提供的Billboard组件实现角色始终面向相机的效果
- 使用useLoader加载纹理资源，确保纹理加载完成后再渲染
- 配置适当的光照，使角色和场景可见

### 4.2 状态管理实现

- 使用Zustand作为轻量级状态管理库
- 采用切片式状态管理，将不同类型的状态分开管理
- 提供清晰的状态更新方法，确保状态更新的一致性
- 支持状态的持久化和响应式更新

### 4.3 纹理加载实现

- 将纹理文件放置在public/textures目录下
- 使用相对路径访问纹理资源
- 实现纹理加载的错误处理，确保组件不会因纹理加载失败而崩溃
- 支持纹理的透明效果，增强视觉表现力

## 5. 项目结构

```
my-paper-game/
├── public/              # 静态资源目录
│   ├── textures/        # 纹理资源
│   │   ├── character.png # 角色纹理
│   │   └── 大地图.png     # 地面纹理
│   ├── favicon.svg      # 网站图标
│   └── icons.svg        # 图标资源
├── src/                 # 源代码目录
│   ├── assets/          # 项目资源
│   ├── components/      # UI组件
│   │   └── UI/          # UI系统组件
│   │       ├── DialogBubble.tsx      # 对话气泡组件
│   │       ├── StatusPanel.tsx       # 状态面板组件
│   │       ├── LoadingIndicator.tsx  # 加载指示器组件
│   │       └── Crosshair.tsx         # 准星组件
│   ├── styles/          # 样式文件
│   │   └── global.css   # 全局样式
│   ├── systems/         # 系统模块
│   │   ├── character/   # 角色系统
│   │   │   ├── CharacterController.ts  # 角色控制器
│   │   │   ├── CharacterTypes.ts       # 角色类型定义
│   │   │   └── PaperCharacter.tsx      # 纸片人组件
│   │   ├── dialogue/     # 对话系统
│   │   │   ├── AIDialogue.ts         # AI对话功能
│   │   │   └── useDialogue.ts        # 对话触发Hook
│   │   ├── input/       # 输入系统
│   │   │   └── useKeyboard.ts          # 键盘输入处理
│   │   ├── projectile/   # 子弹系统
│   │   │   └── Bullet.tsx             # 子弹组件
│   │   ├── rendering/   # 渲染系统
│   │   │   └── SceneSetup.tsx          # 场景设置
│   │   ├── scene/       # 场景系统
│   │   │   └── MapRenderer.tsx         # 地图渲染组件
│   │   └── state/       # 状态管理系统
│   │       ├── gameStore.ts            # 游戏状态存储
│   │       └── storeTypes.ts           # 状态类型定义
│   ├── utils/           # 工具函数
│   │   └── constants.ts                # 常量定义
│   ├── App.tsx          # 主应用组件
│   ├── main.tsx         # 应用入口
│   └── index.css        # 全局CSS
├── package.json         # 项目配置和依赖
├── tsconfig.json        # TypeScript配置
└── vite.config.ts       # Vite配置
```

## 6. 开发环境配置

### 6.1 依赖项

| 依赖 | 版本 | 用途 |
|------|------|------|
| react | ^19.2.4 | 前端框架 |
| react-dom | ^19.2.4 | React DOM |
| three | ^0.183.2 | 3D渲染库 |
| @react-three/fiber | ^9.5.0 | React Three.js 集成 |
| @react-three/drei | ^10.7.7 | React Three Fiber 工具库 |
| zustand | ^5.0.12 | 状态管理库 |
| axios | ^1.15.0 | HTTP客户端 |
| use-sound | ^5.0.0 | 音频播放 |
| typescript | ~6.0.2 | 类型系统 |
| vite | ^8.0.4 | 构建工具 |

### 6.2 开发命令

| 命令 | 功能 |
|------|------|
| npm run dev | 启动开发服务器 |
| npm run build | 构建项目 |
| npm run lint | 代码检查 |
| npm run preview | 预览构建产物 |

## 7. 下一步开发计划

### 7.1 输入系统

- 实现键盘WASD控制，使角色能够移动
- 添加移动动画效果
- 实现角色碰撞检测

### 7.2 对话系统

- 集成AI对话功能
- 添加对话气泡UI
- 实现对话触发机制

### 7.3 UI系统

- 添加状态面板，显示游戏信息
- 实现加载动画
- 添加菜单界面

### 7.4 音频系统

- 添加背景音乐
- 实现音效播放
- 添加音量控制

### 7.5 场景系统

- 添加更多场景元素
- 实现场景切换
- 添加交互对象

## 8. 总结

目前项目已经完成了基础架构搭建和核心功能实现，包括：

1. **渲染系统**：实现了3D场景的构建、相机控制和灯光设置
2. **角色系统**：实现了纸片人角色的渲染、纹理加载、点击交互和平滑旋转
3. **状态管理系统**：使用Zustand管理全局游戏状态，包含角色位置、速度和移动状态
4. **输入系统**：实现了键盘WASD控制，支持角色移动和空格键跳跃
5. **工具系统**：提供了游戏中使用的常量定义
6. **对话系统**：实现了AI对话功能，添加对话气泡，支持打字机效果
7. **UI系统**：添加状态面板、加载动画和准星，实现游戏风格UI
8. **场景系统**：实现地图渲染，使用大地图.png作为地面纹理，添加简单障碍物
9. **重力系统**：实现角色跳跃和重力模拟，支持地面碰撞检测
10. **相机系统**：实现第三人称相机控制，支持鼠标滚轮调整距离和自由旋转
11. **射击系统**：实现准星、黄色子弹、20秒消失、轻微下坠、连发功能
12. **物理系统**：实现子弹碰撞检测和生命周期管理

项目采用模块化架构，代码组织清晰，为后续的功能扩展和维护提供了良好的基础。通过添加音频系统，可以进一步丰富游戏的功能和体验。

开发团队将继续按照计划推进项目，确保按时完成所有功能模块的开发。