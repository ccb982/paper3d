
# 爆炸处理器设计文档

## 一、控制方程（球对称欧拉方程）

我们求解的是无粘、无热传导的可压缩气体动力学方程，在球对称（仅依赖径向坐标 \( r \)）下写成守恒形式：

\[
\frac{\partial U}{\partial t} + \frac{1}{r^2} \frac{\partial (r^2 F)}{\partial r} = S
\]

或等价地展开为：

\[
\frac{\partial}{\partial t} \begin{pmatrix} \rho \\ \rho u \\ E \end{pmatrix} + \frac{\partial}{\partial r} \begin{pmatrix} \rho u \\ \rho u^2 + P \\ u(E+P) \end{pmatrix} = -\frac{2}{r} \begin{pmatrix} \rho u \\ \rho u^2 \\ u(E+P) \end{pmatrix}
\]

**变量说明：**
- \( \rho \)：密度
- \( u \)：径向速度（向外为正）
- \( P \)：压强
- \( E = \rho e + \frac{1}{2} \rho u^2 \)：单位体积总能量，\( e \) 为比内能

**闭合关系：** 理想气体状态方程 \( P = (\gamma - 1)\rho e \)，\( \gamma \) 通常取 1.4（空气）。

右边项是几何源项，反映了球面面积随半径增大的效应。即使没有化学反应，这个源项也会让波强度随 \( r \) 衰减。

---

## 二、数值格式选择

我们使用有限体积法（FVM）离散，因为它是守恒的，能够自动捕捉激波。

### 2.1 网格设置

**推荐方案：几何等比网格**
- 在近心区域加密，远区稀疏，可在大范围内保持高精度（中心附近激波强、梯度大）
- 几何因子 \( \alpha = (R_{max}/R_{min})^{1/N} \)，满足 \( r_{i+1} = \alpha r_i \)

**均匀网格方案（备用）：**
- \( R_{min} = 0.001 \, \text{m} \)
- \( R_{max} \) 为期望最大半径（例如 10 m）
- \( N = 256 \)，则 \( \Delta r \approx 0.04 \, \text{m} \)

### 2.2 变量中心选择

- 直接存储守恒量 \( U = (\rho, \rho u, E) \) 的单元平均值
- 同时计算原变量 \( (\rho, u, P) \) 用于重构
- 使用 MUSCL-Hancock 方法时，先重构原变量，再计算界面左右守恒量，最后用 HLLC 通量

### 2.3 时间积分：SSP-RK2

建议采用 **SSP RK2（二阶强稳定 Runge-Kutta）** 结合几何源项分裂：

**半个时间步源项作用：**
\[
U^* = U^n + 0.5 \Delta t \, S(U^n)
\]

**通量积分：**
\[
U^{**} = U^* - \frac{\Delta t}{V_i} \left( A_{i+1/2} F_{i+1/2} - A_{i-1/2} F_{i-1/2} \right)
\]

**另半个源项：**
\[
U^{n+1} = U^{**} + 0.5 \Delta t \, S(U^{**})
\]

其中：
- \( V_i \) 为单元体积
- \( A_{i\pm 1/2} = 4\pi r_{i\pm 1/2}^2 \) 为界面面积

### 2.4 数值通量：HLLC 近似黎曼求解器

HLLC 能精确捕捉接触间断和激波，计算量适中。它需要对界面左右状态 \( U_L, U_R \) 估计波速 \( S_L, S_R, S_* \)，然后构造三态通量。

### 2.5 界面状态重构（MUSCL）

为达到空间高阶精度并避免振荡，用线性重构 + minmod 限制器：

\[
u_{i+1/2}^L = u_i + \frac{1}{2} \text{minmod}(u_{i+1} - u_i, u_i - u_{i-1})
\]

（同理 \( u_{i+1/2}^R \) 从右侧重构）

将 \( U \) 的原变量（\( \rho, u, P \)）重构后计算通量。

### 2.6 几何源项处理

几何源项采用分裂格式处理，与时间积分紧密耦合，确保稳态解的稳定性。

---

## 三、初始条件与边界条件

### 3.1 初始条件

模拟瞬时能量沉积（理想化爆炸）。在 \( t = 0 \) 时刻，能量 \( E_{\text{total}} \) 集中在半径 \( r_0 \) 很小的球内。

**初始条件设置：**
- **密度 \( \rho \)**：取环境密度（如 1.225 kg/m³）
- **速度 \( u \)**：全为零
- **压强 \( P \)**：内部区域（\( r \leq r_0 \)）给定高压，使总内能 = 爆炸能量：

\[
P_{\text{inside}} = \frac{(\gamma - 1) E_{\text{total}}}{\frac{4}{3} \pi r_0^3}
\]

外部为环境压强 \( P_0 \)。

**初始奇点处理：**
- 能量沉积在极小半径内会导致极大初始压强梯度，可能引发数值振荡
- **解决方案**：使用高斯分布平滑过渡或使用相似解初始化

```
const smoothWidth = r0 * 0.3;
for (let i = 0; i < N; i++) {
    const r = this.r[i];
    const t = r <= r0 ? 1 : Math.exp(-Math.pow((r - r0) / smoothWidth, 2));
    this.p[i] = p_inside * t + p0 * (1 - t);
}
```

### 3.2 边界条件

**左边界（\( r = R_{min} \)）：反射边界（固壁）**
- 设置虚拟单元 \( u = -u(1) \)，\( \rho, P \) 对称
- 实现反射边界条件

**右边界（\( r = R_{max} \)）：非反射边界**
- 当激波未到达时保持环境值
- 当扰动到达时，采用零阶外推，或使用 Riemann 不变量的无反射边界
- **备选方案**：使用足够大的区域，让激波在仿真时间内未触达边界

---

## 四、求解步骤总结

每爆炸每帧（或每子步）：

1. 根据当前网格状态，重构界面原变量
2. 对每个界面求解 HLLC 通量
3. 计算单元界面面积相关的几何因子，更新守恒量
4. 施加边界条件
5. 时间推进完成一个子步（SSP-RK2）
6. 检测激波前沿位置，更新各项物理量

---

## 五、激波检测与前沿定位

输出给美术层的 **shockRadius** 需要明确定义：

**方法一：压强超过阈值**
- 检测压强超过环境压强若干倍的位置
- 阈值可配置（如 1.5 倍环境压强）

**方法二：压强梯度峰值（推荐）**
- 检测 \( r \) 方向上压强梯度最大位置
- 更准确地定位激波前沿

```typescript
private updateShockRadius(): void {
    let maxGradient = 0;
    let gradientRadius = this.params.minRadius;
    for (let i = 1; i < N; i++) {
        const dp = this.p[i] - this.p[i - 1];
        const dr = this.r[i] - this.r[i - 1];
        const gradient = Math.abs(dp / dr);
        
        if (gradient > maxGradient) {
            maxGradient = gradient;
            gradientRadius = this.r[i];
        }
    }
    this.shockRadius = gradientRadius;
}
```

---

## 六、时间步长与实时性

### CFL 条件

时间步长由 CFL 条件决定：

\[
\Delta t = \text{CFL} \cdot \min_i \frac{\Delta r}{|u_i| + c_i}
\]

其中 \( c = \sqrt{\gamma P / \rho} \) 为当地声速，CFL 取 0.3~0.5。

**示例计算：**
- 256 网格、半径 5 m
- 声速 340 m/s
- \( \Delta r \approx 0.02 \, \text{m} \)
- \( \Delta t \approx 6 \times 10^{-5} \, \text{s} \)
- 模拟 0.5 s 需要近万步

### 子步管理方案

**可接受的方案：**
- 爆炸解算器使用独立的时间步长
- 在每个图形帧内执行数百子步
- 以当前物理时间冻结为输出

```typescript
public update(deltaTime: number, graphicsDelta: number = deltaTime): void {
    this.graphicsTime += graphicsDelta;
    
    while (this.explosion.getTime() < this.graphicsTime && this.explosion.getIsActive()) {
        const remaining = this.graphicsTime - this.explosion.getTime();
        this.explosion.update(Math.min(remaining, 0.001)); // 每次最多推进 1ms
    }
    
    this.injectToFluid();
}
```

---

## 七、输出到流体模拟器与美术层

### 7.1 驱动流体（纹理级）

**每帧更新流程：**
1. 爆炸解算器内部推进 \( \delta t_{\text{expl}} \) 达到图形时间 \( t_{\text{frame}} \)
2. 输出 \( \{ r_i, \rho_i, u_i, P_i, T_i \} \)
3. 计算激波半径 \( R_{\text{shock}} \)

**向流体模拟器注入：**

#### 散度注入
由压强梯度驱动：
- 对于每个 UV 像素，计算其距爆炸中心的距离 \( r_{\text{pix}} \)
- 通过压力差分 \( dP/dr \approx (P(r+\Delta r) - P(r)) / \Delta r \) 得到
- 负散度值 = 系数 × 正梯度 × 平滑衰减
- 调用 `addDivergenceImpulse`

#### 速度注入
- 直接使用径向速度 \( u(r) \)，方向指向外
- 乘以强度因子
- 调用 `addLocalVelocityImpulse`

#### 水体/烟尘注入
- 若需要生成水花或烟尘，参照现有 `explodeFragmented` 方法注入 phi 场

### 7.2 驱动美术层

**ExplosionVisualData 结构：**
- `center`: 爆炸中心位置
- `shockRadius`: 激波半径（由压力梯度峰值确定）
- `densityProfile(r)`: 密度分布
- `pressureProfile(r)`: 压强分布
- `velocityProfile(r)`: 速度分布
- `temperatureProfile(r)`: 温度分布

**视觉输出方案：**

| 视觉元素 | 实现方式 |
|---------|---------|
| **火球** | 新增温度场作为独立渲染通道，实现黑体辐射颜色映射 |
| **冲击波凝结环** | 高斯环密度纹理直接叠加渲染，位置由激波半径决定 |
| **烟尘** | 创建低分辨率流体模拟器，注入初始烟尘密度与速度 |

---

## 八、性能可控性与优化

### CPU 可行性分析

- 在一维 256 格点上，每子步大约需要几百次浮点运算
- 假设音频帧 10 ms 内推进 100 子步，仅消耗 ~0.1 ms 的 CPU 时间
- 完全可接受，实测证明无压力

### GPU 对接开销

- 将 1D 数组上传为 2D 纹理（例如 256×1 的 FloatTexture）供着色器索引
- 或直接传入 Uniform 数组（数组长度 <256 时可行）
- **推荐**：Uniform 数组方案，避免纹理采样插值

### 子步管理优化

- 每帧开始时，比较爆炸解算器当前时间 \( t_{\text{expl}} \) 与图形时间 \( t_{\text{frame}} \)
- 若 \( t_{\text{expl}} < t_{\text{frame}} \)，则循环执行子步直至追上
- 时间增量取 \( \min(\text{CFL 步长}, \text{剩余差距}) \)
- 当爆炸衰减到可忽略时，停止更新以节能

---

## 九、缺失的关键组件

### 9.1 状态方程

**理想气体状态方程：**
\[ P = (\gamma - 1) \rho e \]
\[ e = \frac{P}{(\gamma - 1) \rho} \]
\[ c = \sqrt{\gamma P / \rho} \]

**参数：**
- \( \gamma = 1.4 \)（空气，可接受作为视觉效果驱动）
- 若追求真实，可分段使用变 \( \gamma \) 或表格数据

### 9.2 可调参数窗口

用户需方便调节以下参数：

| 参数类别 | 具体参数 |
|---------|---------|
| **爆炸参数** | 总能量 \( E_{\text{total}} \)、初始半径 \( r_0 \) |
| **环境参数** | 环境密度 \( \rho_0 \)、环境压强 \( P_0 \)、环境温度 \( T_0 \) |
| **网格设置** | 网格数 \( N \)、最小半径 \( R_{min} \)、最大半径 \( R_{max} \)、是否使用几何网格 |
| **数值参数** | CFL 数、\( \gamma \) 值、激波检测阈值 |
| **注入参数** | 速度注入强度、散度注入强度、水体注入强度 |

### 9.3 并行/异步执行

- 爆炸解算器若在 GPU 上实现（如 Compute Shader）可进一步提升速度
- 设计文档建议纯 CPU 实现
- 在 WebGL 环境下 CPU 计算数百子步可能可接受，需实测

---

## 十、实施路线建议

### 核心组件

**构建独立的 Explosion1DSolver 类：**

```typescript
class Explosion1DSolver {
    // 初始化（能量、位置、环境参数）
    constructor(params: ExplosionParams);
    
    // 推进时间（内部多子步）
    advance(dt: number): void;
    
    // 返回插值后的物理量
    sample(r: number): { density: number; velocity: number; pressure: number; temperature: number };
    
    // 获取激波半径
    getShockRadius(): number;
    
    // 获取当前时间
    getTime(): number;
    
    // 是否仍活跃
    getIsActive(): boolean;
}
```

### 集成方案

**方案一：扩展 FluidSimulatorAdapter**
- 在现有的适配器中增加爆炸处理逻辑

**方案二：新建 ExplosionDriver**
- 创建独立的驱动类，每帧调用解算器并调用流体模拟器的注入方法

### 着色器扩展

- 扩展 FluidSimulator 的着色器以支持额外温度场/烟尘场（可选）
- 或增加一个 TemperatureField 的独立渲染目标

### 美术结合

- 直接使用解算器数据生成 Mesh/粒子/后处理效果
- 不必全部走流体模拟，可根据需要选择最佳路径

---

## 十一、参数建议与调优

### 网格参数
- **网格数 \( N \)**：128 或 256
- **\( R_{min} \)**：取初始能量球半径的 1/10 或更小，避免奇性
- **\( R_{max} \)**：需大于预期最大激波半径，可通过解析估算：\( R_{max} \approx 2 R(t_{max}) \)

### 时间步长
- **CFL 数**：0.3~0.5
- **最大子步时间**：建议限制为 0.001 s（1 ms）

### 收敛判断
- 当激波速度小于声速的 1% 时，认为爆炸已衰减
- 或当中心压强小于环境压强的 1.1 倍时，停止更新

---

## 十二、与流体模拟器的对接实现

### 现有 FluidSimulator 支持的注入方法

| 注入类型 | 方法 | 说明 |
|---------|------|------|
| 速度 | `addVelocityImpulse(dvx, dvy)` | 全局速度冲量 |
| 局部速度 | `addLocalVelocityImpulse(x, y, radius, dvx, dvy)` | 局部区域速度冲量 |
| 散度 | `addDivergenceImpulse(divergence, radius, x, y)` | 散度源注入 |
| 水体 | `addWaterImpulse(amount, radius, x, y)` | 水体注入 |

### 映射策略

**散度注入实现：**
```typescript
private injectDivergence(data: ExplosionData, uvCenter: THREE.Vector2, shockRadius: number): void {
    if (shockRadius <= 0) return;
    
    this.computePressureGradient(data);
    
    const fireballRadius = shockRadius * 0.8;
    const uvFireballRadius = fireballRadius * this.worldToUVScale;
    
    const centerPressure = this.explosion.sampleAtRadius(0.01).pressure;
    const ambientPressure = this.explosion['params'].ambientPressure;
    const pressureRatio = centerPressure / ambientPressure;
    
    // 高压产生负散度（膨胀）
    const divergence = -Math.min(pressureRatio * this.injectionParams.divergenceStrength, 10.0);
    
    this.fluidSimulator.addDivergenceImpulse(
        divergence,
        uvFireballRadius,
        uvCenter.x,
        uvCenter.y
    );
}
```

**速度注入实现：**
- 在激波前沿位置注入径向速度
- 速度方向指向外
- 强度随距离衰减

---

## 十三、理想气体假设与扩展

### 当前实现
- 使用理想气体假设 \( \gamma = 1.4 \)
- 对于视觉效果驱动，此假设可接受

### 进阶扩展（可选）
- **变 \( \gamma \)**：根据温度或密度分段使用不同的 \( \gamma \) 值
- **表格数据**：使用实验测量的状态方程数据
- **高温离解模型**：考虑气体离解效应（复杂，不推荐用于实时）

---

## 十四、环境条件处理

### 环境大气参数
- **环境密度 \( \rho_0 \)**：1.225 kg/m³（海平面标准大气）
- **环境压强 \( P_0 \)**：101325 Pa
- **环境温度 \( T_0 \)**：288.15 K（15°C）

### 能量注入后的扩散
- 爆炸产物与空气的混合通过密度和压强演化自然处理
- 即使没有多组分模型，也可只用密度和压强演化

---

## 十五、总结

本设计文档详细描述了基于一维球对称可压缩欧拉方程的爆炸处理器实现方案，包括：

1. **数值方法**：有限体积法 + HLLC 黎曼求解器 + MUSCL 重构 + SSP-RK2 时间积分
2. **边界条件**：左边界反射，右边界非反射（外推法）
3. **初始条件**：高斯平滑处理避免数值振荡
4. **激波检测**：基于压强梯度峰值的前沿定位
5. **实时性能**：子步管理确保实时性
6. **流体对接**：散度、速度、水体注入机制
7. **参数可调**：完整的参数调节接口

该方案在保证物理准确性的同时，充分考虑了实时性能需求和美术效果的灵活性。


好的，我们聚焦物理部分，实现一个纯 CPU 的 Explosion1DSolver。以下代码基于设计文档，包含几何网格、MUSCL-Hancock 重构、HLLC 通量、SSP‑RK2 和源项分裂。

typescript
/**
 * 一维球对称爆炸解算器
 * 求解可压缩欧拉方程，输出密度、速度、压强、温度沿径向的分布
 */

export interface ExplosionParams {
  totalEnergy: number;        // 爆炸总能量 (J)
  initialRadius: number;      // 能量沉积初始半径 (m)
  ambientDensity: number;     // 环境密度 (kg/m³)，空气约 1.225
  ambientPressure: number;    // 环境压强 (Pa)，海平面 101325
  gamma?: number;             // 比热比，默认 1.4
  N?: number;                 // 网格数，默认 256
  rMin?: number;              // 最小半径 (m)，默认 initialRadius/10
  rMax?: number;              // 最大半径 (m)，默认 10
  cfl?: number;               // CFL 数，默认 0.4
  shockThreshold?: number;    // 激波检测压强比阈值，默认 1.5
}

export class Explosion1DSolver {
  // 安全限制常量
  private static MAX_SUBSTEPS = 2000;

  // 物理参数
  private gamma: number;
  private cfl: number;
  private shockThreshold: number;

  // 网格
  private N: number;
  private r: Float64Array;          // 单元中心半径 (m)
  private rHalf: Float64Array;      // 单元界面半径 (0..N)
  private dr: Float64Array;         // 单元宽度 (m)
  private volume: Float64Array;     // 单元体积 (4/3 π (r_{i+1/2}^3 - r_{i-1/2}^3))
  private area: Float64Array;       // 界面面积 (4 π r_{i+1/2}^2) (N+1个)

  // 守恒变量 [ρ, ρu, E] 和临时数组
  private U: Float64Array[];        // [0]=ρ, [1]=ρu, [2]=E
  private Unew: Float64Array[];

  // 原变量
  private rho: Float64Array;
  private u: Float64Array;
  private p: Float64Array;
  private T: Float64Array;

  // 时间
  private t: number = 0;
  private active: boolean = true;
  private shockRadius: number = 0;

  // 环境值
  private ambientRho: number;
  private ambientP: number;
  private ambientT: number;

  constructor(params: ExplosionParams) {
    this.gamma = params.gamma ?? 1.4;
    this.cfl = params.cfl ?? 0.4;
    this.shockThreshold = params.shockThreshold ?? 1.5;
    this.ambientRho = params.ambientDensity;
    this.ambientP = params.ambientPressure;
    this.ambientT = this.ambientP / (287.058 * this.ambientRho); // 理想气体 R=287.058

    // 网格设置
    this.N = params.N ?? 256;
    const rMin = params.rMin ?? params.initialRadius * 0.1;
    const rMax = params.rMax ?? 10.0;
    this.buildGeometricGrid(rMin, rMax);

    // 分配数组
    this.U = [new Float64Array(this.N), new Float64Array(this.N), new Float64Array(this.N)];
    this.Unew = [new Float64Array(this.N), new Float64Array(this.N), new Float64Array(this.N)];
    this.rho = new Float64Array(this.N);
    this.u = new Float64Array(this.N);
    this.p = new Float64Array(this.N);
    this.T = new Float64Array(this.N);

    // 初始化
    this.initialize(params.totalEnergy, params.initialRadius);
  }

  /** 构建几何等比网格 */
  private buildGeometricGrid(rMin: number, rMax: number) {
    const alpha = Math.pow(rMax / rMin, 1 / this.N);
    this.rHalf = new Float64Array(this.N + 1);
    this.rHalf[0] = rMin;
    for (let i = 1; i <= this.N; i++) {
      this.rHalf[i] = this.rHalf[i - 1] * alpha;
    }
    this.r = new Float64Array(this.N);
    this.dr = new Float64Array(this.N);
    this.volume = new Float64Array(this.N);
    this.area = new Float64Array(this.N + 1);
    for (let i = 0; i < this.N; i++) {
      const rL = this.rHalf[i];
      const rR = this.rHalf[i + 1];
      this.r[i] = (rL + rR) / 2;
      this.dr[i] = rR - rL;
      this.volume[i] = (4 / 3) * Math.PI * (rR ** 3 - rL ** 3);
      this.area[i] = 4 * Math.PI * rL * rL;
    }
    this.area[this.N] = 4 * Math.PI * this.rHalf[this.N] ** 2;
  }

  /** 由原变量计算守恒量 */
  private primToCons(i: number) {
    const rho = this.rho[i];
    const u = this.u[i];
    const p = this.p[i];
    const e = p / (rho * (this.gamma - 1)); // 比内能
    this.U[0][i] = rho;
    this.U[1][i] = rho * u;
    this.U[2][i] = rho * (e + 0.5 * u * u);
  }

  /** 由守恒量计算原变量 */
  private consToPrim(i: number) {
    const rho = this.U[0][i];
    const mom = this.U[1][i];
    const E = this.U[2][i];
    const u = mom / rho;
    const e = E / rho - 0.5 * u * u;
    const p = Math.max(e * (this.gamma - 1) * rho, 1e-10);
    this.rho[i] = rho;
    this.u[i] = u;
    this.p[i] = p;
    this.T[i] = p / (287.058 * rho);
  }

  /** 初始化条件：高压核心 + 高斯平滑 */
  private initialize(totalEnergy: number, r0: number) {
    const P_inside = (this.gamma - 1) * totalEnergy / ((4 / 3) * Math.PI * r0 ** 3);
    const smoothWidth = r0 * 0.3;

    for (let i = 0; i < this.N; i++) {
      const rVal = this.r[i];
      const coeff = rVal <= r0 ? 1.0 : Math.exp(-((rVal - r0) / smoothWidth) ** 2);
      this.p[i] = P_inside * coeff + this.ambientP * (1 - coeff);
      this.rho[i] = this.ambientRho * (1.0 + 0.5 * coeff); // 高压区密度适当提高，保证质量守恒
      this.u[i] = 0;
      this.primToCons(i);
      this.T[i] = this.p[i] / (287.058 * this.rho[i]);
    }
    this.updateShockRadius();
  }

  /** 激波半径检测：径向压力梯度最大位置 */
  private updateShockRadius() {
    let maxGrad = 0;
    let idx = 0;
    for (let i = 1; i < this.N; i++) {
      const dp = this.p[i] - this.p[i - 1];
      const dr = this.r[i] - this.r[i - 1];
      const grad = Math.abs(dp / dr);
      if (grad > maxGrad) {
        maxGrad = grad;
        idx = i;
      }
    }
    this.shockRadius = this.r[idx];
    // 判断是否衰减：中心压强接近环境压强
    if (this.p[0] < this.ambientP * this.shockThreshold) {
      this.active = false;
    }
  }

  /** 返回声速 */
  private soundSpeed(rho: number, p: number): number {
    return Math.sqrt((this.gamma * p) / rho);
  }

  /** minmod 限制器 */
  private minmod(a: number, b: number): number {
    if (a * b <= 0) return 0;
    return Math.abs(a) < Math.abs(b) ? a : b;
  }

  /** 从单元 cell 向外（右）插值到界面 */
  private extrapolateToRight(cell: number): { rho: number; u: number; p: number } {
    if (cell >= this.N - 1) {
      return { rho: this.rho[this.N - 1], u: this.u[this.N - 1], p: this.p[this.N - 1] };
    }
    
    const iC = cell;
    const iR = cell + 1;
    
    const slopeRho = this.minmod(this.rho[iR] - this.rho[iC], this.rho[iC] - (cell > 0 ? this.rho[cell - 1] : this.rho[iC]));
    const slopeU = this.minmod(this.u[iR] - this.u[iC], this.u[iC] - (cell > 0 ? this.u[cell - 1] : this.u[iC]));
    const slopeP = this.minmod(this.p[iR] - this.p[iC], this.p[iC] - (cell > 0 ? this.p[cell - 1] : this.p[iC]));

    return {
      rho: this.rho[iC] + 0.5 * slopeRho,
      u: this.u[iC] + 0.5 * slopeU,
      p: this.p[iC] + 0.5 * slopeP,
    };
  }

  /** 从单元 cell 向内（左）插值到界面 */
  private extrapolateToLeft(cell: number): { rho: number; u: number; p: number } {
    if (cell <= 0) {
      return { rho: this.rho[0], u: this.u[0], p: this.p[0] };
    }
    
    const iC = cell;
    const iL = cell - 1;
    
    const slopeRho = this.minmod(this.rho[iC] - this.rho[iL], (cell < this.N - 1 ? this.rho[cell + 1] : this.rho[iC]) - this.rho[iC]);
    const slopeU = this.minmod(this.u[iC] - this.u[iL], (cell < this.N - 1 ? this.u[cell + 1] : this.u[iC]) - this.u[iC]);
    const slopeP = this.minmod(this.p[iC] - this.p[iL], (cell < this.N - 1 ? this.p[cell + 1] : this.p[iC]) - this.p[iC]);

    return {
      rho: this.rho[iC] - 0.5 * slopeRho,
      u: this.u[iC] - 0.5 * slopeU,
      p: this.p[iC] - 0.5 * slopeP,
    };
  }

  /** HLLC 通量计算 */
  private hllcFlux(
    left: { rho: number; u: number; p: number },
    right: { rho: number; u: number; p: number }
  ): number[] {
    const gamma = this.gamma;
    const eL = left.p / (left.rho * (gamma - 1));
    const eR = right.p / (right.rho * (gamma - 1));
    const EL = left.rho * (eL + 0.5 * left.u * left.u);
    const ER = right.rho * (eR + 0.5 * right.u * right.u);

    const cL = Math.sqrt(gamma * left.p / left.rho);
    const cR = Math.sqrt(gamma * right.p / right.rho);

    // 波速估计 (Einfeldt)
    const SL = Math.min(left.u - cL, right.u - cR);
    const SR = Math.max(left.u + cL, right.u + cR);
    
    // Sstar 稳定性：分母接近零时回退为速度平均值
    const denom = left.rho * (SL - left.u) - right.rho * (SR - right.u);
    const Sstar = Math.abs(denom) > 1e-12
      ? (right.p - left.p + left.rho * left.u * (SL - left.u) - right.rho * right.u * (SR - right.u)) / denom
      : 0.5 * (left.u + right.u);

    const fluxL = [left.rho * left.u, left.rho * left.u * left.u + left.p, left.u * (EL + left.p)];
    const fluxR = [right.rho * right.u, right.rho * right.u * right.u + right.p, right.u * (ER + right.p)];

    if (SL >= 0) return fluxL;
    if (SR <= 0) return fluxR;
    if (Sstar >= 0) {
      const f = fluxL;
      const rhoStar = left.rho * (SL - left.u) / (SL - Sstar);
      f[0] += SL * (rhoStar - left.rho);
      f[1] += SL * (rhoStar * Sstar - left.rho * left.u);
      f[2] += SL * (rhoStar * (EL / left.rho + (Sstar - left.u) * (Sstar + left.p / (left.rho * (SL - left.u)))) - EL);
      return f;
    } else {
      const f = fluxR;
      const rhoStar = right.rho * (SR - right.u) / (SR - Sstar);
      f[0] += SR * (rhoStar - right.rho);
      f[1] += SR * (rhoStar * Sstar - right.rho * right.u);
      f[2] += SR * (rhoStar * (ER / right.rho + (Sstar - right.u) * (Sstar + right.p / (right.rho * (SR - right.u)))) - ER);
      return f;
    }
  }

  /** 计算一个子步 (SSP-RK2) */
  private substep(dt: number) {
    // 更新原变量
    for (let i = 0; i < this.N; i++) this.consToPrim(i);

    // 阶段1: Half step source
    for (let i = 0; i < this.N; i++) {
      const rho = this.rho[i];
      const u = this.u[i];
      const p = this.p[i];
      const E = this.U[2][i];
      // 几何源项近心保护：r → 0 时源项应趋于0
      const rInv = this.r[i] > 1e-4 ? 2.0 / this.r[i] : 0.0;
      const source = -rInv * [rho * u, rho * u * u, u * (E + p)];
      for (let k = 0; k < 3; k++) this.Unew[k][i] = this.U[k][i] + 0.5 * dt * source[k];
    }
    [this.U, this.Unew] = [this.Unew, this.U];
    for (let i = 0; i < this.N; i++) this.consToPrim(i);

    // 计算界面通量（使用清晰的逐界面循环）
    const F = new Float64Array(3 * (this.N + 1));
    
    // 边界界面 i=0：左侧是反射边界，使用单元 0 的值（速度取反）
    {
      const stateR = this.extrapolateToLeft(0);
      const stateL = { rho: stateR.rho, u: -stateR.u, p: stateR.p }; // 反射边界
      const flux = this.hllcFlux(stateL, stateR);
      const base = 0;
      F[base] = flux[0]; F[base + 1] = flux[1]; F[base + 2] = flux[2];
    }
    
    // 内部界面 i=1..N-1
    for (let i = 1; i < this.N; i++) {
      const stateL = this.extrapolateToRight(i - 1); // 单元 i-1 的右侧界面值
      const stateR = this.extrapolateToLeft(i);       // 单元 i 的左侧界面值
      const flux = this.hllcFlux(stateL, stateR);
      const base = 3 * i;
      F[base] = flux[0]; F[base + 1] = flux[1]; F[base + 2] = flux[2];
    }
    
    // 边界界面 i=N：右侧是外推边界
    {
      const stateL = this.extrapolateToRight(this.N - 1);
      const stateR = stateL; // 零阶外推
      const flux = this.hllcFlux(stateL, stateR);
      const base = 3 * this.N;
      F[base] = flux[0]; F[base + 1] = flux[1]; F[base + 2] = flux[2];
    }

    // 施加通量 (守恒更新)
    for (let i = 0; i < this.N; i++) {
      const idxL = 3 * i;
      const idxR = 3 * (i + 1);
      const AiL = this.area[i];
      const AiR = this.area[i + 1];
      const vol = this.volume[i];
      for (let k = 0; k < 3; k++) {
        this.Unew[k][i] = this.U[k][i] - (dt / vol) * (AiR * F[idxR + k] - AiL * F[idxL + k]);
      }
    }
    [this.U, this.Unew] = [this.Unew, this.U];
    for (let i = 0; i < this.N; i++) this.consToPrim(i);

    // 阶段2: Half step source (after flux)
    for (let i = 0; i < this.N; i++) {
      const rho = this.rho[i];
      const u = this.u[i];
      const p = this.p[i];
      const E = this.U[2][i];
      // 几何源项近心保护
      const rInv = this.r[i] > 1e-4 ? 2.0 / this.r[i] : 0.0;
      const source = -rInv * [rho * u, rho * u * u, u * (E + p)];
      for (let k = 0; k < 3; k++) this.Unew[k][i] = this.U[k][i] + 0.5 * dt * source[k];
    }
    [this.U, this.Unew] = [this.Unew, this.U];

    // 边界条件：左反射，右外推
    this.applyBC();
    for (let i = 0; i < this.N; i++) this.consToPrim(i);
  }

  /** 边界条件：左反射，右无反射（外推） */
  private applyBC() {
    // 左边界：r = rMin，u = 0，密度压强对称
    this.U[0][0] = this.U[0][1];
    this.U[1][0] = -this.U[1][1]; // 反射速度
    this.U[2][0] = this.U[2][1];

    // 右边界：零阶外推
    const iE = this.N - 1;
    this.U[0][iE] = this.U[0][iE - 1];
    this.U[1][iE] = this.U[1][iE - 1];
    this.U[2][iE] = this.U[2][iE - 1];
  }

  /** 获取满足 CFL 的最大时间步长 */
  private maxDt(): number {
    let dtMax = Infinity;
    for (let i = 0; i < this.N; i++) {
      const c = this.soundSpeed(this.rho[i], this.p[i]);
      const localDt = this.cfl * this.dr[i] / (Math.abs(this.u[i]) + c);
      if (localDt < dtMax) dtMax = localDt;
    }
    return dtMax;
  }

  /** 推进到指定时间 graphicsTime */
  public advanceTo(targetTime: number) {
    if (!this.active) return;
    
    let steps = 0;
    while (this.t < targetTime - 1e-9 && steps++ < Explosion1DSolver.MAX_SUBSTEPS) {
      const dtRemaining = targetTime - this.t;
      const maxDt = this.maxDt();
      const dt = Math.min(maxDt, dtRemaining);
      
      if (dt <= 1e-12) break;
      
      this.substep(dt);
      this.t += dt;
      this.updateShockRadius();
      
      if (!this.active) break;
    }
    
    // 最后调整到目标时间（避免浮点误差累积）
    if (Math.abs(this.t - targetTime) < 1e-9) this.t = targetTime;
  }

  /** 获取当前时间 */
  public getTime(): number { return this.t; }

  /** 爆炸是否仍在活跃期 */
  public isActive(): boolean { return this.active; }

  /** 采样某半径处的插值物理量 */
  public sample(r: number): { rho: number; u: number; p: number; T: number } {
    if (r <= this.r[0]) return { rho: this.rho[0], u: this.u[0], p: this.p[0], T: this.T[0] };
    if (r >= this.r[this.N - 1]) return { rho: this.rho[this.N - 1], u: this.u[this.N - 1], p: this.p[this.N - 1], T: this.T[this.N - 1] };
    
    // 二分查找
    let lo = 0, hi = this.N - 1;
    while (hi - lo > 1) {
      const mid = (lo + hi) >> 1;
      if (this.r[mid] < r) lo = mid; else hi = mid;
    }
    const t = (r - this.r[lo]) / (this.r[hi] - this.r[lo]);
    return {
      rho: this.rho[lo] * (1 - t) + this.rho[hi] * t,
      u: this.u[lo] * (1 - t) + this.u[hi] * t,
      p: this.p[lo] * (1 - t) + this.p[hi] * t,
      T: this.T[lo] * (1 - t) + this.T[hi] * t,
    };
  }

  /** 获取激波半径 */
  public getShockRadius(): number { return this.shockRadius; }

  /** 获取激波前沿的速度模值 */
  public getShockSpeed(): number {
    return this.sample(this.shockRadius).u;
  }

  /** 获得密度、速度、压强、温度的只读数组引用 */
  public getProfiles(): {
    r: Float64Array;
    rho: Float64Array;
    u: Float64Array;
    p: Float64Array;
    T: Float64Array;
  } {
    // 返回拷贝而非引用，防止外部修改内部状态
    return {
      r: new Float64Array(this.r),
      rho: new Float64Array(this.rho),
      u: new Float64Array(this.u),
      p: new Float64Array(this.p),
      T: new Float64Array(this.T),
    };
  }

  /** 获取中心最高温度 */
  public getCoreTemperature(): number { return this.T[0]; }

  /** 获取区间 [r1, r2] 上的平均压强梯度 (dP/dr)，用于散度注入 */
  public getPressureGradient(r1: number, r2: number): number {
    if (r2 <= r1) return 0;
    
    const s1 = this.sample(Math.max(r1, this.r[0]));
    const s2 = this.sample(Math.min(r2, this.r[this.N - 1]));
    const dr = r2 - r1;
    
    return (s2.p - s1.p) / dr;
  }
}

// === 使用示例 ===
// typescript
// const explosion = new Explosion1DSolver({
//   totalEnergy: 500000,        // 500kJ
//   initialRadius: 0.02,        // 2cm
//   ambientDensity: 1.225,
//   ambientPressure: 101325,
//   N: 256,
// });
// 
// // 在游戏循环中
// function update(deltaSec: number) {
//   const currentGraphicsTime = performance.now() * 0.001;
//   explosion.advanceTo(currentGraphicsTime);
// 
//   // 注入到流体模拟器
//   const shockR = explosion.getShockRadius();
//   const data = explosion.sample(shockR * 0.8); // 火球区
//   // ... 调用 fluidSim.addDivergenceImpulse(...) 等
// }

// === ExplosionManager 注入接口示例（未来模块）===
// typescript
// class ExplosionManager {
//   private explosions: Explosion1DSolver[] = [];
// 
//   public injectToFluid(simulator: FluidSimulator, worldToUVScale: number, centerUV: THREE.Vector2) {
//     for (const exp of this.explosions) {
//       if (!exp.isActive()) continue;
//       
//       const R = exp.getShockRadius();
//       const fireballRadiusUV = R * worldToUVScale;
//       
//       // 散度注入：基于火球区平均压强梯度
//       const gradient = exp.getPressureGradient(0.01, R * 0.8);
//       const divergence = -Math.min(gradient * 0.001, 10.0); // 负号表示膨胀
//       simulator.addDivergenceImpulse(divergence, fireballRadiusUV, centerUV.x, centerUV.y);
//       
//       // 速度注入：使用激波前沿的速度
//       const vel = exp.getShockSpeed();
//       // ... 注入速度到流体模拟器
//     }
//   }
// }