# 流体编辑器 + 绘画主页面 —— 流体架构

## 四端统一约定

四端 = 流体编辑器（`src/fluid/editor/`）/ 绘画主页面（MainCanvas）/ 特效播放器（effects-player）/ 游戏端（全新的游戏）。
**修改任何一处必须同步其他三处。** 权威清单见 `src/fluid/fluidConfigIO.ts` 顶部注释。

### 1. 残差纹理 alpha 约定（背景透明 + 残差流动可见的关键）

```
alpha = "区域存在度"：
  shape 外（regionIdTex=0 / 多边形外 / 纹理透明像素）= 0
  shape 内 = 255
  RGB 中性 128（delta≈0），shape 外填 128
```

- 合成 `finalA = max(baseA, residual.a)` → shape 外 `max(0,0)=0` → 纯透明背景
- 残差平流时 alpha 随速度扩散 → 基础色=0 区域可见流动渐变（尾迹雾气）

四端位置：

| 端 | 位置 | 说明 |
|---|---|---|
| 编辑器 | `src/fluid/editor/FluidEditorUI.tsx` buildFluidTexturesFromRawFrame | bbox 局部，区域外默认 0 |
| 主绘画页面 | `src/stores/useAppStore.ts` residTex / residBase | 全帧 / bbox 局部 |
| 主绘画页面 | `src/fluid/fluidIntegration.ts` buildBaseHslFromFrame | alpha **不反推**（保留原始纹理 alpha） |
| 特效播放器 | `effects-player/src/core/ftx.ts` buildResidualData | 按 regionIdTex 区分 |
| 游戏端 | `全新的游戏/src/vendor/player/core/ftx.ts` buildResidualData | 与播放器同款 |

★ **实体 shader 流体模式禁止 discard**（alpha<0.5 丢弃会把残差流动渐变切掉）
→ 用 alpha 混合保留（MainCanvas 无 discard，天然正确；播放器/游戏端曾踩坑已修）。

### 2. 合成公式（MCSDA scalar / vector）

```
vector: finalA = max(baseA, residual.a)
scalar: final = base + delta + off（×通道开关 uChannels，静态 delta 始终参与）

sub（减去）模式：
  H/S/L 取 -|factor×mul|   （残差为 0 处依旧被密度拉出雾气）
  A 通道取 +|factor×mul|   （雾气越浓越实，不被减去模式压没）
  factor = density / baseline
```

位置：编辑器 `FluidEditorUI.tsx`（显示）/ 各端 `FluidSolver.buildCompositeMat`（计算）。

### 3. LevelSet 模块

- **reinit**：Godunov 上风格式 + φ₀ 快照（原中心差分不收敛/界面漂移）
- **双模式**：
  - 约束模式（`constrainLiquid`，编辑器默认 **开**）= 每帧「φ 重建 + reinit + 液体约束」
    把平流抹开的颜色晕圈裁剪到 φ<0 内 —— "烟雾感"克星；做雾化特效时关掉
  - 追踪模式 = φ 平流 + 每 `reinitInterval` 帧重建（编辑器默认 **1**=每帧重建，
    新注入液体立刻有表面；`reinitIterations` 默认 6，≥4 SDF 才充分收敛）
- **表面张力（CSF）**：`F = -σ·κ·δ(φ)·n̂`
  - ★ 负号不可省：约定 φ<0 为水、n̂ 指向空气侧，凸液面 κ≈+1/R>0，
    丢负号则力朝外 —— σ 越大越喷（历史 bug）
  - ★ σ 双向可调：正=向内收缩成水团，负=向外扩散（爆裂/雾化），门控 `!== 0`
  - ★ 必须在**压力投影之前**施加：张力的散度要靠投影转化为整体向心流动；
    投影之后再施力 = 张力速度不参与投影，只会把颜色抹开
  - ★ 曲率差分用 1.5px 宽距 + 归一化（κ=真实曲率 1/R）：单像素差分会被
    φ 噪声打穿符号 → 界面乱流发生器
  - 量级：像素单位制下 Δv/帧 ≈ σ/300R → 有效范围 **10⁵~10⁶**
    （UI ±5,000,000；编辑器默认 +1,000,000）
- **外向速度抑制**（`outwardDamping` 0~1，编辑器默认 **1**）：
  界面窄带内削减指向空气侧的法向速度 `v -= strength·max(v·n̂,0)·n̂`。
  **确定性收拢保证**——不依赖张力符号与 φ 噪声质量；1=界面完全不可外扩。
  在速度限幅之后执行，本帧用于平流的最终速度场无外扩分量。
- **φ 后处理**：空气钳制（`clampAirPhi`）+ 水体补偿（`compensateWaterPhi`）
- **参数导出**：`levelSetConfig{...}` 全字段经 `fluidConfigIO` 双向读写，
  各端 `config.ts` 解析键名必须一致

### 3.5 自由表面压力边界（真实感核心）

Level Set 启用时，压力求解（红黑 SOR）只在液体内部进行：

```
if (obstacle || φ > 0) p = 0        // 空气区恒零压
邻域采样：墙/空气邻居的压力视为 0     // 同硬边界处理
```

- 材质 key 区分：`sor_*_fs`（带 φ）/ `sor_*_obstacle`（全域，Level Set 关闭时回退）
- 物理意义：液体是**有表面的不可压缩整体**，注入动量推的是整块水而非染色剂；
  全域求解会让动量以压力波形式辐射进空气区 → 颜色像染料一样漂（不真实感的根源）
- 来源：废弃模拟器（git 历史 `src/lib/fluid-simulator/`）的 Jacobi 边界条件移植

### 3.6 粘度（速度场扩散）

- `∂v/∂t = ν∇²v`，ν 为运动粘度（cells²/s），离散 Laplacian = 四邻和−4·自身
- 效果：剪切层/射流按 e^{−νk²t} 最快衰减，大尺度运动保留 → 水团内聚
- 稳定性：显式扩散要求 ν·dt ≤ 0.25/步 → 子步分摊（每步 ≤0.2，上限 64 步，
  ν≈770@60fps 内精确，超出安全饱和不失稳）
- 位置：力相（表面张力之后、边界/压力投影之前）；墙内速度保持
- 默认 20（水感 10~60）；UI 滑条 0~1000 + 数字框任意值
- 实现：编辑器 `FluidEditor.applyViscosity` / 播放器副本 `FluidSolver.applyViscosity`

### 3.7 渲染（Level Set 视图 = 旧库分层水渲染）

- 编辑器"Level Set"视图已改用**废弃模拟器的 φ 驱动分层水着色**（FluidEditorUI levelsetMat）：
  深度混色（waterColor→deepColor，-φ/uDepthScale）+ ∇φ 法线漫反射 + Blinn-Phong 镜面 +
  边缘发光环 + 速度流动扰动；φ≥0 discard 背景透明
- 单位适配：旧库 φ 为 UV 归一化、编辑器为像素 —— 深度/边缘/透明度按 px 标定
  （uDepthScale=40px、边缘 3px），法线差分 1.5px 与张力 pass 同款去噪
- 原"🌊 分层水渲染"开关及 alpha 近似后处理已**移除**（按 alpha 深度近似，
  无法复现 φ 法线高光；真实感来源见 3.5 自由表面边界）

### 3.8 爆炸与限幅联动

- 散度符号语义（`ExplosionConfig.strength`）：**负 = 向外爆炸**（源，∇²p=∇·u+f 中 f<0
  → 投影后 ∇·v>0 → 推力），正 = 向内收缩（汇）。UI 强度滑条 ±50000 双向可调
- ★ `velCap` / `velCapDuration` / `velCapRecovery`：爆炸触发时临时抬升全局
  maxVelocity，保持 duration+0.5s 后按 **smoothstep 缓动回落**（默认 1.5s，
  瞬时回正会把飞散中的水团"急刹"钳停）—— 之后张力/外速抑制接管收拢成团
- ★ 径向冲击（`FluidInjector.injectRadialVelocity`）：爆炸主推力 = 逐像素沿
  "远离中心"方向注入速度（系数 0.12/秒，方向随 strength 符号翻转）；
  随机方向戳速度降为 3% 只做撕裂细节 —— 均匀矢量戳没有外推感
- 实现：`FluidEditor.explode()` 记录 `velCapOverride{value,until,recoverUntil}`，
  step 3.6 限幅时三段态（保持/回落/失效）；重置时同步清理

### 3.9 色块交界加墙（区域隔离防串色）

- **思路**：FTX 帧导入后，在"较大基础色块"的交界处自动生成障碍物墙 ——
  流体被物理隔离在各色块内部，数值扩散无法跨墙搬运颜色（比任何后处理滤波都干净）
- **精确性来源**：FTX 自带逐像素调色板 ID（`rawRegionIdTex`），
  同 ID = 同一平涂色块，分割零启发式、无颜色距离阈值
- **小色块策略**：面积低于阈值（默认 0.4% 全帧，UI 可调）的色块视为
  **可通行区**（语义上并入周围大色块）—— 描边/高光点不产生墙；
  墙的判定条件是两侧属于【不同的两个大色块】
- **泄漏防护**：墙做 1 次 8 邻域膨胀（单像素墙存在对角缝）
- 实现：`editor/regionWallMask.ts`（纯函数 buildRegionWallMask + packToBits），
  UI 按钮"🧱 色块交界加墙"（通用面板视图按钮下方），输出经
  `setObstacleBitmap` 进既有障碍物管线（自动启用障碍物 + 切墙体视图）

### 4. 参数流转链路

```
流体编辑器 buildRecipe
  → 只给绘画主页面（parseImportedFluidConfig → frameData.fluidConfig）
  → 绘画主页面导出最终版（assetBundleExport 写进 per_frame_data.physics，
     内部扁平格式）
  → 特效播放器 / 游戏端 parsePhysicsConfig 读取同一格式
```

## 求解器完整参考（★ 基准版 2026-08-26）

> 本节记录水团攻坚完成后的最终形态，作为**回归对照基准**：
> 后续任何改动如果让效果变差，先 diff 这里的每一条约定。

### 0. 模块分层（2026-08-31 架构整理后）

```
src/fluid/
├── core/                     基础设施（无物理语义）
│   ├── GPUOps.ts             全屏渲染 + 材质缓存（key 命中时刷新 uniforms）
│   ├── FluidGrid.ts          双缓冲 RenderTarget（read/write/swap/readTarget）
│   └── FluidInjector.ts      注入原语：颜色/速度/散度源/径向速度/印章
├── solvers/                  物理 pass（每个类 = 一个方程/一类效果）
│   ├── AdvectionSolver.ts    半拉格朗日平流
│   ├── PressureSolver.ts     ★红黑 SOR 泊松求解 + 自由表面边界 + 梯度修正
│   ├── VelocitySolver.ts     ★粘度扩散（子步护栏）+ 边界处理
│   └── LevelSetSolver.ts     φ 平流/Godunov reinit/CSF 张力/去斑/约束/外速抑制
├── editor/
│   ├── FluidEditor.ts        编排器：step() 七阶段调度 + 场管理 + IO/调试
│   │                         （不再内嵌物理 GLSL；decayDensity 与障碍物绘制除外）
│   └── FluidOperations.ts    注入队列 / 持续源 / 爆炸队列的业务层
└── FluidSolver.ts            ⚠️ 播放器副本——与 editor 平行的独立实现【遗留债务】
```

- **行为零变更承诺**：PressureSolver/VelocitySolver 迁出时 shader、材质 key、
  uniform 刷新语义逐字保留（2026-08-31，FluidEditor 2098→1856 行）
- **遗留债务**：`FluidSolver.ts`（播放器用）仍是编辑器逻辑的手工镜像，
  两处会漂移。统一方案：让 FluidSolver 组合同一批 solvers 类（其 SOR/粘度/
  边界 shader 与编辑器已几乎一致），迁移前先跑 __phiDebug 建立行为基线

### A. 单位制约定

| 量 | 单位 | 说明 |
|---|---|---|
| φ | **像素** | initPhiField 初值范围 ±2×窄带宽度；reinit 收敛到 \|∇φ\|=1/px |
| 速度 | px/s | 平流回溯 `uv - vel·dt/res` |
| dt | 秒（真实帧时长） | useFluidEditor 钳到 ≤0.05 |
| 散度源 f | 1/s | 一次性写入 divergenceGrid，压力投影消费后清零 |
| α（vector 模式） | 0~1 | colorGrid.A = 区域存在度；α>0.5 判定为液体内 |

### B. 每帧管线全序（FluidEditor.step）

顺序即依赖——**张力/粘度必须在投影前（力相），外速抑制必须在限幅后**：

```
 0  processQueue          一次性注入（颜色/速度/密度/残差印章）
 0  applyGravity          重力（px/s²·dt 累加）
 1  processContinuousSources  持续源（含路径点/波形/间歇门控）
 2  advectVelocity        半拉格朗日 + CFL 自适应子步
 2  advectColor/Density   vector=HSLA 场 / scalar=density 场 + 衰减
 2.45 applySurfaceTension ★CSF F=-σκδn̂（投影前施力，σ 双向）
 2.46 applyViscosity      ν∇²v 显式扩散（子步护栏）
 2.5 applyBoundary        速度边界（投影前，防拮抗）
 2.6 processExplosions    散度源 + 径向冲击 + 撕裂抖动 + 可选水量
 3  solvePressure         红黑 SOR ×iterations；LS 开启时自由表面边界（φ>0→p=0）
 3  applyPressureGradient ∇p 修正速度
 3.1 clearGrid(divergence) 散度源一次性消费
 3.5 scaleVelocity        全局 velocityScale
 3.6 clampVelocity        maxVelocity 限幅 ⊕ velCap 三段态（抬升→缓落→失效）
 3.7 applyOutwardVelDamping ★界面法向外速削减（确定性收拢）
 4  Level Set 形状管理
    ├ 约束模式：initPhiField(3×3均值) → Godunov reinit → φ后处理
    │            → despeckle 补洞拔刺 → liquidConstraint 裁剪色场
    └ 追踪模式：advectPhi → 每 reinitInterval 帧「重建+reinit+φ后处理+despeckle」
 +  time += dt
```

### C. 参数速查（默认值 = 当前基准）

| 参数 | 默认 | 有效范围 | 作用 / 备注 |
|---|---|---|---|
| surfaceTension σ | +1e6 | ±5e6 | 正收拢负扩散；Δv/帧≈σ/300R；<10⁵ 不可见 |
| narrowBandWidth | 5 px | 1~20 | 张力带+约束羽化+initPhiField 标定 |
| reinitInterval | 1 | 1~10 | 1=每帧重建（注入即同步） |
| reinitIterations | 6 | ≥4 | Godunov 轮数，SDF 收敛质量 |
| constrainLiquid | true | — | 视觉裁剪器；雾化特效时关 |
| outwardDamping | 1 | 0~1 | 确定性收拢保证；果冻感就调低 |
| viscosity ν | 20 | 0~770 精确区 | 内聚；>64 子步饱和 |
| enablePressure | true | — | 关掉则无不可压缩性，一切成空谈 |
| pressureIterations | 100 | ≥60 | 配合 ω=1.7 SOR |
| maxVelocity | 5000 | — | 日常限幅；观景可临时压低 |
| velCap | 2000 | 0=关 | 爆炸期临时抬升，smoothstep 回落 1.5s |
| explode strength | -25000 | ±50000 | 负=外炸源，正=内爆汇 |

### D. 稳定性红线（违反即翻车）

```
① 显式粘度：ν·dt ≤ 0.25/步 —— 已由子步机制守护，勿绕过
② 张力方向：F 必须带负号（-σκδn̂），且在压力投影【前】施加
③ κ 差分间距 ≥1.5px —— 1px 差分被 φ 噪声打穿成乱流发生器
④ φ 渲染 discard 前必须过 despeckle —— 否则 alpha 噪声像素变透明洞
⑤ 数据纹理 clear 强制黑色透明（事故 #001 红线）
⑥ 爆炸 kick 依赖 velCap 联动 —— 无联动时冲击被日常限幅当场钳死
```

### E. 调试工具

- **`__phiDebug()`**（控制台）/ Level Set 视图每 2s 自动打印：
  ①内部洞（应≈0）②孤立极值（应<1%）③红黑质心差（应≈0）④空气精确零值 ⑤NaN/Inf
- 指标①②突增 ⇒ alpha 噪声/去斑失效；④≈全画布 ⇒ clampAirPhi 在工作（正常现象）

### F. 推荐配方

| 目标 | 关键参数组合 |
|---|---|
| 真实水团（当前基准） | σ=1e6，ν=20，outwardDamping=1，constrain=true，压力开 |
| 内爆吸聚 | explode strength=+25000，velCap=2000 |
| 爆炸飞散 | strength=-25000，velCap≥2000，outwardDamping 临时降到 0.3~0.5 |
| 烟雾/雾化 | constrainLiquid=false，σ 取负或 0，ν 降低 |

## 已踩过的坑（历史记录）

1. **σ 量级**：表面张力像素单位制下 Δv/帧 ≈ σ/300R → 有效范围 **10⁵~10⁶**
   （早期估算 6000 偏小，旧 UI 0~200 怎么调都没效果；详见坑 #7/#8 的另外两个
   独立根因：符号反了 + 施力时机在投影之后）。
2. **残差 alpha 128 陷阱**：残差纹理全图 alpha=128（中性半透明）→ 合成
   `max(0, 0.5)=0.5` → 背景半透明灰/黑。统一为 shape 外 0 / 内 255。
3. **discard 陷阱**：实体 shader `fluid.a < 0.5 discard` → 残差流动的渐变 alpha
   （<0.5 的插值）全被丢弃 → 看不到流动。主绘画页面无此问题，播放器/游戏端已修。
4. **反推 alpha**：buildBaseHslFromFrame 曾 `bA = finalA - dA`（dA 来自残差 alpha）
   → 区域内 base alpha 被拉成 0.5 → 半透明。改为不反推 alpha。
5. **清屏色泄漏进场数据**（2026-08-23 事故 #001，★★★★★）：`clearGrid` 用裸
   `renderer.clear()` 清密度/颜色/速度场——它写入的是**全局 clearColor 而非零**。
   游戏端把清屏色从深蓝改成浅灰后，reset 后全场假密度 0.8 → MCSDA 整块近黑、
   周期性消散。本仓库 FluidSolver / FluidEditor（含障碍物掩码，R>0.5=墙）/ GPUOps /
   AdvectionSolver 已全部改为"强制黑透明清屏+保存恢复"。完整因果链见
   《全新的游戏/架构设计.md》事故记录 #001。
   **红线：数据纹理的 clear 必须强制黑；显示画布才可用主题色。**
6. **裸 GL 清绑导致 three 缓存失同步**：曾用 `gl.bindTexture(TEXTURE_2D, null)`
   循环断 feedback loop，但 three r185 `WebGLState.bindTexture` 有缓存跳过逻辑
   （缓存命中就不真正重绑）→ 后续材质采样空纹理（黑帧）。统一改用
   官方 `renderer.resetState()`。红线同上：禁止裸 GL 操作 three 托管状态。
7. **CSF 张力丢负号**（2026-08 水团攻坚）：`F = σκδn̂` 在 φ<0=水、n̂ 指向空气的
   约定下，凸液面 κ>0 → 力朝外，σ 越大越喷成烟雾。正确形式 `-σκδn̂`
   （Brackbill κ 取 −∇·n̂）。已改为 σ 双向：正=收缩、负=扩散。
8. **张力施力时机**：曾放在压力投影之后 → 张力速度不参与投影，直接被平流消费，
   σ 再大也只见颜色抹开。必须"先施力→后投影"。废弃模拟器正是此顺序
   （外力→散度→Jacobi×200→速度修正）+ σ=728000 饱和速度钳制，才有真实水感。
9. **κ 单像素差分噪声**：φ 从 alpha 阈值重建带锯齿，1px 差分的曲率正负乱跳
   → 张力退化为界面乱流源（表现为"无论 σ 正负都向外发散"）。
   修复：1.5px 宽距归一化差分 + 外向速度抑制兜底（确定性收拢）。
10. **粘度自由调控**：显式扩散稳定上限 ν·dt≤0.25/步；子步上限过小（8）会把
    等效粘度钳死。现 64 步（ν≈770@60fps 精确），UI 数字框可输任意值。

## 四端同步状态（2026-08-26 水团攻坚后）

本轮修改目前**只落在编辑器端**（`src/fluid/editor/` + 同项目播放器副本 `src/fluid/FluidSolver.ts`）。按四端约定，以下待同步：

| 项 | 内容 | 待同步端 |
|---|---|---|
| 1 | CSF 负号 + 1.5px 去噪差分 + σ 双向门控（`!==0`） | effects-player / 游戏端 LevelSetSolver |
| 2 | 表面张力移到压力投影**之前**（游戏端 FluidSolver 本来就在前，只需核对） | 游戏端 |
| 3 | 自由表面压力边界（SOR/Jacobi 的 φ 门控 + `sor_*_fs` 材质） | effects-player / 游戏端 |
| 4 | 粘度 pass（`viscosity` 配置 + applyViscosity + IO 字段） | 全部 |
| 5 | 外向速度抑制（`outwardDamping` 配置 + pass + UI） | 全部 |
| 6 | 默认值：编辑器默认 constrainLiquid=true / outwardDamping=1 / σ=1e6 / ν=20 / enablePressure=true(100 次)——各端按需对齐 water.phys.json（当前 σ=0.1 无效） | 游戏端资产 |

同步时以本文件第 3~3.6 节为权威语义，逐条对照 shader 实现。
