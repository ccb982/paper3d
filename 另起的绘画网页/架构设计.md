src/
├── components/
│   ├── Canvas/                   # 核心画布组件
│   │   ├── MainCanvas.tsx        # 主画布容器，处理鼠标事件，调用渲染模块
│   │   └── CanvasRenderer.ts     # 纯函数：根据状态绘制三个图层
│   ├── Sidebar/                  # 左侧控制区
│   │   ├── ImageImport.tsx       # 图片导入/清除
│   │   ├── LayerControl.tsx      # 图层可见性开关
│   │   ├── GroupManager.tsx      # 分组管理（增删改查，颜色）
│   │   ├── Toolbar.tsx           # 绘制工具切换（画笔/橡皮/形状等）
│   │   ├── AnnotationInput.tsx   # 标注文字输入
│   │   ├── AxisControl.tsx       # 坐标轴范围/重置/缩放
│   │   └── ExportButton.tsx      # 导出 JSON 按钮
│   └── common/
│       └── ColorPicker.tsx       # 分组颜色选择器
├── hooks/
│   ├── useCanvasEvents.ts        # 画布鼠标/触摸事件，坐标转换
│   └── useExportData.ts          # 导出数据序列化
├── stores/                       # Zustand 状态切片
│   ├── useAppStore.ts            # 主 store，聚合所有 slice
│   ├── imageSlice.ts             # 图片信息
│   ├── layerSlice.ts             # 图层可见性
│   ├── axisSlice.ts              # xMin, xMax, yMin, yMax
│   ├── groupsSlice.ts            # 分组列表 + 当前激活分组
│   ├── shapesSlice.ts            # 所有形状实体（点/线段/圆/正方形/二次曲线）
│   └── uiSlice.ts                # 当前工具, 临时绘制状态, 标注文本
├── types/
│   └── index.ts                  # Group, Shape, Point, 导出格式
├── utils/
│   ├── geometry.ts               # 世界坐标 <-> 画布坐标转换
│   ├── drawing.ts                # 形状生成辅助（圆形/正方形取样）
│   └── exportFormatter.ts        # 将 shapes + groups 转为所需 JSON
└── App.tsx   

5.23 19：00

不行，我受不了了
我必须开发一个用于纹理绘制的网页
支持导入图片的一部分
主体就是一个像素的画布，但是带坐标轴（支持动态调整坐标轴），支持看每个点的坐标，点在整个画布的所占的比例

支持给点分组
最后把所有数据导出为json文件
而且后期支持我的流体静态纹理查看和图像编码算法（上色的纹理我还没开发）

三个图层，一个导入图片，一个绘制点，一个坐标轴和坐标

支持绘制正方形，三角形，圆形，画笔，线段，二次函数的线，橡皮
我也可以给某些点加标注
导出的时候json文件标清楚属于哪一组，什么形状，各个点，能够很方便的创建纹理


我先实现网页的基本架构和选取图片部分导入功能

