我们重新设计一套统一外力接口，让同一个外力描述符能自动适配“详细纹理”（FluidSimulatorAdapter，256×256 静态流体）和“简单纹理”（LightFluidEntity，32×32 移动液滴），并且对于详细纹理不施加世界运动（它不可移动）。

一、核心接口
1. 外力数据结构 FluidExternalForce
ts
// FluidExternalForce.ts
import * as THREE from 'three';

/**
 * 流体内部注入（纹理空间，UV 0~1）
 */
export interface FluidInternalInjection {
  centerUV?: THREE.Vector2; // 默认 (0.5, 0.5)
  radius?: number;          // 影响半径（UV空间），默认 0.2
  falloff?: 'linear' | 'gaussian'; // 衰减方式，默认 'linear'
}

/**
 * 速度场注入
 */
export interface FluidVelocityInjection extends FluidInternalInjection {
  velocity: THREE.Vector2;  // 速度向量（纹理空间，uv单位/秒）
}

/**
 * 散度注入（正=收缩，负=膨胀）
 */
export interface FluidDivergenceInjection extends FluidInternalInjection {
  divergence: number;       // 散度值，推荐 ±5000~±20000
}

/**
 * 水体/phi 注入（正=添加水，负=移除水）
 */
export interface FluidWaterInjection extends FluidInternalInjection {
  amount: number;           // 水量变化，1.0 表示在该半径内填满水
}

/**
 * 外力描述符 —— 同时包含世界运动影响 + 内部流场影响
 */
export interface FluidExternalForce {
  /** 世界空间瞬时冲量（仅对可移动实体生效） */
  worldImpulse?: THREE.Vector3;
  /** 世界空间持续加速度（m/s²），每帧累加到速度上（仅对可移动实体） */
  worldAcceleration?: THREE.Vector3;
  /** 流体内部速度场注入 */
  velocityInjection?: FluidVelocityInjection;
  /** 流体内部散度注入 */
  divergenceInjection?: FluidDivergenceInjection;
  /** 水体/phi 注入 */
  waterInjection?: FluidWaterInjection;
}
2. 目标接口 IFluidForceTarget
ts
// IFluidForceTarget.ts
export interface IFluidForceTarget {
  /** 是否可以在世界空间移动 */
  isMovable(): boolean;
  /** 应用一个统一外力，内部自动分派 */
  applyFluidForce(force: FluidExternalForce): void;
}
二、实现适配
1. LightFluidEntity 实现 IFluidForceTarget
在 LightFluidEntity 中添加方法。要点：

世界力直接修改 worldVelocity。

内部注入需要将世界方向转换到纹理空间（因为液滴有随机旋转）。

根据当前 LOD 决定是否实际注入纹理（LOW/OFF 时跳过，保持性能）。

内部注入默认作用中心为纹理中心 (0.5, 0.5)，也可由外力指定。

ts
// LightFluidEntity.ts 新增部分
import { IFluidForceTarget, FluidExternalForce } from './FluidExternalForce';

export class LightFluidEntity extends Entity implements IFluidForceTarget {
  // ... 原有代码 ...

  /** 外部力累积（每帧加速度） */
  private externalAccel = new THREE.Vector3();

  isMovable(): boolean {
    return true; // 小液滴可以在世界空间移动
  }

  applyFluidForce(force: FluidExternalForce): void {
    // ========== 1. 世界运动 ==========
    if (force.worldImpulse) {
      this.applyImpulse(force.worldImpulse);
    }
    if (force.worldAcceleration) {
      // 加速度累积，在 update() 中每帧施加
      this.externalAccel.add(force.worldAcceleration);
    }

    // ========== 2. 内部流场注入（仅在高/中 LOD 时生效） ==========
    if (this.lod >= FluidLOD.LOW) return; // 低LOD或OFF时纹理冻结，跳过注入

    const sim = this.simulator;
    if (!sim) return;

    // 将世界方向转为纹理空间方向（考虑液滴随机旋转）
    const worldToTex = (v: THREE.Vector3): THREE.Vector2 => {
      // 使用 mesh 的 rotation.z 逆旋转
      const angle = -this.mesh.rotation.z; // 注意方向
      const cos = Math.cos(angle);
      const sin = Math.sin(angle);
      const tx = v.x * cos - v.y * sin;
      const ty = v.x * sin + v.y * cos;
      // 归一化到纹理空间长度
      const len = Math.sqrt(tx * tx + ty * ty);
      if (len < 0.0001) return new THREE.Vector2(0, 0);
      // 世界单位 -> UV 单位转换：假设世界空间 1 单位 = 纹理中 scale 对应，但这里我们直接使用速度值
      // 因为内部 sim.addVelocityImpulse 接受的是像素/秒（？实际上 FluidSimulator 的 addVelocityImpulse 直接叠加向量）
      // 为了简单，我们将世界速度直接映射为 UV 空间的速度，不做缩放，由调用者负责合适的数值
      return new THREE.Vector2(tx, ty);
    };

    // 速度注入
    if (force.velocityInjection) {
      const inj = force.velocityInjection;
      const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
      // 这里 FluidSimulator 没有直接按区域注入速度的方法，我们可以用多个 addVelocityImpulse 近似
      // 但更地道的做法：调用 sim.addVelocityImpulse() 仅对全图施加均匀速度？不行。
      // 目前 FluidSimulator 提供了 addDivergenceImpulse、addWaterImpulse，但没有局域速度注入。
      // 作为兼容，我们可以将速度注入转化为一个短时间内持续的力，或直接调用 sim.addVelocityImpulse(vel.x, vel.y) 全局施加；
      // 若要支持局域，需扩展 FluidSimulator。这里先用全局速度脉冲作为降级方案，
      // 并标注 TODO：实现局域速度注入。
      const vel = worldToTex(inj.velocity as any as THREE.Vector3); // velocity 是 Vector2，直接使用
      // 降级：全局速度增加（所有像素）
      sim.addVelocityImpulse(vel.x, vel.y);
    }

    // 散度注入
    if (force.divergenceInjection) {
      const inj = force.divergenceInjection;
      const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
      sim.addDivergenceImpulse(inj.divergence, inj.radius ?? 0.2, center.x, center.y);
    }

    // 水体注入
    if (force.waterInjection) {
      const inj = force.waterInjection;
      const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
      sim.addWaterImpulse(inj.amount, inj.radius ?? 0.2, center.x, center.y);
    }
  }

  // 修改 update() 方法，应用外部加速度
  update(delta: number): void {
    if (!this.isActive) return;

    // 应用累积的外部加速度
    if (this.externalAccel.lengthSq() > 0.0001) {
      this.worldVelocity.add(this.externalAccel.clone().multiplyScalar(delta));
      // 不自动清零，因为加速度可能是持续的（由外力每帧重新设置）
    }

    // ... 原有更新逻辑 ...
  }
}
2. FluidSimulatorAdapter 实现 IFluidForceTarget
FluidSimulatorAdapter 代表静态的详细纹理，不可移动，因此仅应用内部注入。

ts
// FluidSimulatorAdapter.ts 新增实现
import { IFluidForceTarget, FluidExternalForce } from './FluidExternalForce';

export class FluidSimulatorAdapter implements ITextureGenerator, IFluidForceTarget {
  // ... 原有代码 ...

  isMovable(): boolean {
    return false; // 详细纹理不移动
  }

  applyFluidForce(force: FluidExternalForce): void {
    // 忽略世界运动力
    const sim = this.simulator;
    if (!sim) return;

    // 速度注入
    if (force.velocityInjection) {
      const inj = force.velocityInjection;
      const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
      // 同样，FluidSimulator 没有局域速度注入，暂时用全局脉冲降级
      sim.addVelocityImpulse(inj.velocity.x, inj.velocity.y);
    }

    // 散度注入
    if (force.divergenceInjection) {
      const inj = force.divergenceInjection;
      const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
      sim.addDivergenceImpulse(inj.divergence, inj.radius ?? 0.2, center.x, center.y);
    }

    // 水体注入
    if (force.waterInjection) {
      const inj = force.waterInjection;
      const center = inj.centerUV ?? new THREE.Vector2(0.5, 0.5);
      sim.addWaterImpulse(inj.amount, inj.radius ?? 0.2, center.x, center.y);
    }
  }
}
三、全局统一调用（示例）
在游戏逻辑中，你可以将所有流体目标收集起来，统一施力：

ts
// GameForceSystem.ts
import { EntityManager } from '@core/EntityManager';
import { LightFluidEntity } from '@entities/fluid/LightFluidEntity';
import { FluidSimulatorAdapter } from '@your-path/FluidSimulatorAdapter';
import { FluidExternalForce } from '@entities/fluid/FluidExternalForce';

export function applyGlobalExplosion(center: THREE.Vector3, radius: number, strength: number) {
  const em = EntityManager.getInstance();
  
  // 收集所有流体目标
  const targets: IFluidForceTarget[] = [];
  
  // 1. 详细纹理（假设你在场景中有个 FluidSimulatorAdapter 实例）
  const detailSim = getDetailFluidSimulator(); // 你的获取方式
  if (detailSim) targets.push(detailSim);
  
  // 2. 所有小液滴
  const droplets = em.getDroplets() as LightFluidEntity[];
  targets.push(...droplets);

  for (const target of targets) {
    const isMovable = target.isMovable();
    const pos = isMovable ? (target as LightFluidEntity).position : center; // 静态纹理用爆炸中心估计

    const dir = new THREE.Vector3().subVectors(pos, center);
    const dist = dir.length();
    if (dist > radius) continue;

    const falloff = 1 - dist / radius;
    const worldImpulse = dir.normalize().multiplyScalar(strength * falloff);

    // 构建外力（自动忽略不可移动实体的世界力）
    const force: FluidExternalForce = {
      worldImpulse: isMovable ? worldImpulse : undefined,
      divergenceInjection: {
        divergence: -strength * 0.8 * falloff, // 负散度 = 膨胀
        radius: 0.3 * falloff,
        centerUV: isMovable ? undefined : calculateUVFromWorld(pos, detailSim), // 静态纹理需要映射
      },
      waterInjection: isMovable ? undefined : { // 详细纹理额外生成水花
        amount: 0.5 * falloff,
        radius: 0.25 * falloff,
      }
    };

    target.applyFluidForce(force);
  }
}
说明：

对于 LightFluidEntity，worldImpulse 会推开液滴，divergenceInjection 会让内部流体膨胀。

对于 FluidSimulatorAdapter，worldImpulse 被自动忽略，仅内部散度生效。

静态纹理需要手动将世界坐标映射到 UV，这里假定你有 calculateUVFromWorld 函数。

四、后续优化建议
局域速度注入
目前 FluidSimulator 缺少“在特定区域叠加速度”的着色器，建议添加 addLocalVelocityImpulse(cx, cy, radius, vx, vy)，然后在 LightFluidEntity.applyFluidForce 中直接使用，而非全局脉冲。

LOD 缓存
当小液滴处于 LOW/OFF 时，可以先将外力暂存，待 LOD 恢复时一次性应用，避免效果丢失。

性能保护
对内部注入施加频率限制（例如每秒最多 10 次），避免高频外力导致 GPU 渲染压力过大。

这套接口让你可以用完全相同的外力描述符同时影响详细纹理和移动液滴，系统自动区分目标是否可移动，符合“外力可以不让详细纹理实体移动”的需求。

