# 背包箱子管理机制文档

## 1. 系统概述

本系统实现了一个完整的背包和箱子管理机制，支持：

- **三种状态管理**：只有背包、只有箱子、同时有背包和箱子
- **跨背包物品转移**：支持背包与箱子之间的物品拖拽
- **智能UI层级**：鼠标悬停时自动提升当前UI层级
- **统一状态控制**：打开箱子自动打开背包，关闭箱子自动关闭背包
- **背景变暗效果**：只显示一次，先变暗再打开UI

## 2. 核心组件

### 2.1 状态管理 (gameStore.ts)

```typescript
// 状态定义
export interface GameState {
  // ... 其他状态
  isBackpackVisible: boolean;
  isBoxOpened: boolean;
  currentBox: any;

  // UI状态操作方法
  toggleBackpack: () => void;
  openBox: (box: any) => void;
  closeBox: () => void;
  setCurrentBox: (box: any) => void;
}

// 状态实现
export const useGameStore = create<GameState>((set, get) => ({
  // 初始状态
  isBackpackVisible: false,
  isBoxOpened: false,
  currentBox: null,

  // UI状态操作方法
  toggleBackpack: () => set((state) => {
    const newBackpackState = !state.isBackpackVisible;
    let newBoxState = state.isBoxOpened;
    let newCurrentBox = state.currentBox;
    
    // 关闭背包时如果箱子是打开的，也关闭箱子
    if (state.isBackpackVisible && state.isBoxOpened) {
      newBoxState = false;
      newCurrentBox = null;
      (window as any).currentBox = null;
    }
    
    return {
      isBackpackVisible: newBackpackState,
      isBoxOpened: newBoxState,
      currentBox: newCurrentBox
    };
  }),
  
  openBox: (box: any) => set(() => {
    (window as any).currentBox = box;
    return {
      isBoxOpened: true,
      isBackpackVisible: true,
      currentBox: box
    };
  }),
  
  closeBox: () => set(() => {
    (window as any).currentBox = null;
    return {
      isBoxOpened: false,
      isBackpackVisible: false,
      currentBox: null
    };
  }),
  
  setCurrentBox: (box: any) => set({ currentBox: box })
}));
```

### 2.2 背包UI组件 (BackpackUI.tsx)

```typescript
export const BackpackUI: React.FC<BackpackUIProps> = ({ isVisible, onClose }) => {
  const [slots, setSlots] = useState(backpackManager.getInventory().getSlots());
  const [hoveredItemId, setHoveredItemId] = useState<string | null>(null);
  const dragManager = DragManager.getInstance();
  const [isHovered, setIsHovered] = useState(false);

  useEffect(() => {
    if (!isVisible) return;

    // 监听背包变化
    const updateSlots = () => {
      setSlots([...backpackManager.getInventory().getSlots()]);
    };

    // 监听拖拽状态变化
    const updateDragState = () => {
      setSlots([...backpackManager.getInventory().getSlots()]);
    };

    backpackManager.getInventory().addListener(updateSlots);
    dragManager.addListener(updateDragState);

    return () => {
      backpackManager.getInventory().removeListener(updateSlots);
      dragManager.removeListener(updateDragState);
    };
  }, [isVisible]);

  // 处理拖拽开始
  const handleDragStart = (slot: any) => {
    if (slot.item) {
      dragManager.startDrag(
        slot.item,
        backpackManager.getInventory(),
        { x: slot.x, y: slot.y }
      );
    }
  };

  return (
    <div className="backpack-ui-overlay" style={{ zIndex: isHovered ? 1000 : 999 }}>
      <div 
        className="backpack-ui" 
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
        style={{ zIndex: isHovered ? 2002 : 2000 }}
      >
        {/* UI内容 */}
      </div>
    </div>
  );
};
```

### 2.3 箱子UI组件 (BoxUI.tsx)

```typescript
export const BoxUI: React.FC<BoxUIProps> = ({ 
  isVisible, 
  inventory, 
  boxName, 
  onClose 
}) => {
  const [slots, setSlots] = useState<any[]>([]);
  const [hoveredItemId, setHoveredItemId] = useState<string | null>(null);
  const dragManager = DragManager.getInstance();
  const [isHovered, setIsHovered] = useState(false);

  useEffect(() => {
    if (!isVisible || !inventory) return;

    const updateSlots = () => {
      setSlots([...inventory.getSlots()]);
    };

    const updateDragState = () => {
      setSlots([...inventory.getSlots()]);
    };

    inventory.addListener(updateSlots);
    dragManager.addListener(updateDragState);

    updateSlots();

    return () => {
      inventory.removeListener(updateSlots);
      dragManager.removeListener(updateDragState);
    };
  }, [isVisible, inventory]);

  const handleDragStart = (slot: any) => {
    if (!inventory || !slot.item) return;
    
    dragManager.startDrag(
      slot.item,
      inventory,
      { x: slot.x, y: slot.y }
    );
  };

  return (
    <div
      className="backpack-ui-overlay"
      onClick={onClose}
      style={{
        zIndex: isHovered ? 1000 : 999,
        justifyContent: 'flex-end',
        paddingRight: '20px',
        paddingLeft: 0
      }}
    >
      <div 
        className="backpack-ui" 
        onClick={(e) => e.stopPropagation()}
        onMouseEnter={() => setIsHovered(true)}
        onMouseLeave={() => setIsHovered(false)}
        style={{
          right: '20px',
          left: 'auto',
          transform: 'none',
          zIndex: isHovered ? 2002 : 2000
        }}
      >
        {/* UI内容 */}
      </div>
    </div>
  );
};
```

### 2.4 拖拽管理器 (DragManager.ts)

```typescript
export class DragManager {
  private static instance: DragManager;
  private draggedItem: DraggedItem | null = null;
  private listeners: Array<() => void> = [];

  private constructor() {}

  public static getInstance(): DragManager {
    if (!DragManager.instance) {
      DragManager.instance = new DragManager();
    }
    return DragManager.instance;
  }

  public startDrag(item: Item, inventory: InventorySystem, position: { x: number; y: number }): void {
    // 确定物品来源
    const isBackpack = inventory === backpackManager.getInventory();
    
    this.draggedItem = {
      item,
      sourceInventory: inventory,
      sourcePosition: position,
      currentPosition: position,
      currentInventory: isBackpack ? 'backpack' : 'box'
    };
    // 从源背包移除物品
    inventory.removeItemAt(position.x, position.y);
    this.notifyListeners();

    // 添加全局鼠标事件监听
    this.addGlobalListeners();
  }

  private handleGlobalMouseMove(e: MouseEvent): void {
    if (!this.draggedItem) return;

    // 检查鼠标是否在背包网格内
    const backpackElement = document.querySelector('.backpack-ui .backpack-grid');
    if (backpackElement) {
      const rect = backpackElement.getBoundingClientRect();
      if (e.clientX >= rect.left && e.clientX <= rect.right && e.clientY >= rect.top && e.clientY <= rect.bottom) {
        const x = Math.floor((e.clientX - rect.left - 10) / 62);
        const y = Math.floor((e.clientY - rect.top - 10) / 62);
        if (x >= 0 && x < 5 && y >= 0 && y < 8) {
          this.draggedItem.currentPosition = { x, y };
          this.draggedItem.currentInventory = 'backpack';
          this.notifyListeners();
          return;
        }
      }
    }

    // 检查鼠标是否在箱子网格内
    const boxElements = document.querySelectorAll('.backpack-ui');
    let boxElement: HTMLElement | null = null;
    
    boxElements.forEach(el => {
      if (el.style.right === '20px') {
        boxElement = el as HTMLElement;
      }
    });
    
    if (boxElement) {
      const gridElement = boxElement.querySelector('.backpack-grid');
      if (gridElement) {
        const rect = gridElement.getBoundingClientRect();
        if (e.clientX >= rect.left && e.clientX <= rect.right && e.clientY >= rect.top && e.clientY <= rect.bottom) {
          const x = Math.floor((e.clientX - rect.left - 10) / 62);
          const y = Math.floor((e.clientY - rect.top - 10) / 62);
          if (x >= 0 && x < 4 && y >= 0 && y < 3) {
            this.draggedItem.currentPosition = { x, y };
            this.draggedItem.currentInventory = 'box';
            this.notifyListeners();
            return;
          }
        }
      }
    }

    // 鼠标不在任何网格内
    this.draggedItem.currentInventory = null;
    this.notifyListeners();
  }

  private handleGlobalMouseUp(e: MouseEvent): void {
    if (!this.draggedItem) {
      this.removeGlobalListeners();
      return;
    }

    // 检查鼠标是否在背包网格内
    const backpackElement = document.querySelector('.backpack-ui .backpack-grid');
    if (backpackElement) {
      const rect = backpackElement.getBoundingClientRect();
      if (e.clientX >= rect.left && e.clientX <= rect.right && e.clientY >= rect.top && e.clientY <= rect.bottom) {
        const x = Math.floor((e.clientX - rect.left - 10) / 62);
        const y = Math.floor((e.clientY - rect.top - 10) / 62);
        if (x >= 0 && x < 5 && y >= 0 && y < 8) {
          // 从背包管理器获取背包库存
          this.endDrag(backpackManager.getInventory(), { x, y });
          this.removeGlobalListeners();
          return;
        }
      }
    }

    // 检查鼠标是否在箱子网格内
    const boxElements = document.querySelectorAll('.backpack-ui');
    let boxElement: HTMLElement | null = null;
    
    boxElements.forEach(el => {
      if (el.style.right === '20px') {
        boxElement = el as HTMLElement;
      }
    });
    
    if (boxElement) {
      const gridElement = boxElement.querySelector('.backpack-grid');
      if (gridElement) {
        const rect = gridElement.getBoundingClientRect();
        if (e.clientX >= rect.left && e.clientX <= rect.right && e.clientY >= rect.top && e.clientY <= rect.bottom) {
          const x = Math.floor((e.clientX - rect.left - 10) / 62);
          const y = Math.floor((e.clientY - rect.top - 10) / 62);
          if (x >= 0 && x < 4 && y >= 0 && y < 3) {
            // 从App组件获取当前箱子的库存
            const currentBox = (window as any).currentBox;
            if (currentBox && currentBox.getInventory) {
              this.endDrag(currentBox.getInventory(), { x, y });
              this.removeGlobalListeners();
              return;
            }
          }
        }
      }
    }

    // 鼠标不在任何网格内，放回原位置
    this.endDrag(null, null);
    this.removeGlobalListeners();
  }

  public endDrag(targetInventory: InventorySystem | null, targetPosition: { x: number; y: number } | null): boolean {
    if (!this.draggedItem) {
      return false;
    }

    let success = false;

    if (targetInventory && targetPosition) {
      // 尝试放置到目标背包
      success = targetInventory.addItem(this.draggedItem.item, targetPosition.x, targetPosition.y);
    }

    if (!success) {
      // 放置失败，放回源背包
      this.draggedItem.sourceInventory.addItem(
        this.draggedItem.item,
        this.draggedItem.sourcePosition.x,
        this.draggedItem.sourcePosition.y
      );
    }

    this.draggedItem = null;
    this.notifyListeners();
    return success;
  }

  // 其他方法...
}
```

### 2.5 应用整合 (App.tsx)

```typescript
function App() {
  // 状态管理
  const isBackpackVisible = useGameStore(s => s.isBackpackVisible);
  const isBoxOpened = useGameStore(s => s.isBoxOpened);
  const currentBox = useGameStore(s => s.currentBox);
  const toggleBackpack = useGameStore(s => s.toggleBackpack);
  const openBox = useGameStore(s => s.openBox);
  const closeBox = useGameStore(s => s.closeBox);

  // 处理箱子关闭
  const handleCloseBox = () => {
    closeBox();
  };

  // 处理交互
  const handleInteract = (object: any) => {
    if (object.isBox) {
      openBox(object);
    }
  };

  return (
    <div className="game-container">
      {/* 其他组件 */}
      
      {/* 背景变暗效果 */}
      {(isBackpackVisible || isBoxOpened) && (
        <div
          style={{
            position: 'fixed',
            top: 0,
            left: 0,
            width: '100%',
            height: '100%',
            background: 'rgba(0, 0, 0, 0.5)',
            zIndex: 997
          }}
        />
      )}
      
      {/* 背包UI */}
      <BackpackUI
        isVisible={isBackpackVisible}
        onClose={toggleBackpack}
      />
      
      {/* 交互提示 */}
      <InteractionPrompt
        interactiveObjects={interactiveObjects}
        onInteract={handleInteract}
      />
      
      {/* 箱子UI */}
      <BoxUI
        isVisible={isBoxOpened}
        inventory={currentBox?.getInventory()}
        boxName="箱子"
        onClose={handleCloseBox}
      />
    </div>
  );
}
```

## 3. 实现原理

### 3.1 状态管理机制

系统使用Zustand进行状态管理，实现了三种状态的控制：

- **只有背包**：`isBackpackVisible = true`, `isBoxOpened = false`
- **只有箱子**：`isBackpackVisible = false`, `isBoxOpened = true`（实际会自动打开背包）
- **同时有背包和箱子**：`isBackpackVisible = true`, `isBoxOpened = true`

### 3.2 跨背包物品转移

通过DragManager实现跨背包物品转移：

1. **拖拽开始**：从源背包移除物品，创建拖拽状态
2. **鼠标移动**：实时检测鼠标位置，更新拖拽物品的位置
3. **拖拽结束**：根据鼠标位置判断目标背包，尝试放置物品
4. **放置失败**：如果目标位置不可放置，物品会自动返回原位置

### 3.3 UI层级管理

实现了智能UI层级管理：

- 默认状态下，背包和箱子UI的z-index都是999
- 当鼠标悬停在某个UI上时，该UI的z-index会提升到1000
- 这样确保了当前操作的UI始终在最上层

### 3.4 背景变暗效果

通过条件渲染实现背景变暗效果：

- 当背包或箱子打开时，显示一个半透明的黑色覆盖层
- 覆盖层的z-index为997，确保在UI下方
- 这样实现了先变暗再打开UI的效果

## 4. 使用方法

### 4.1 打开背包

按B键打开/关闭背包：

```typescript
// 在输入系统中
if (keys['b']) {
  toggleBackpack();
}
```

### 4.2 打开箱子

按E键打开箱子（会自动打开背包）：

```typescript
// 在交互系统中
if (keys['e'] && interactiveObject) {
  if (interactiveObject.isBox) {
    openBox(interactiveObject);
  }
}
```

### 4.3 关闭箱子

点击箱子UI外的区域或关闭按钮：

- 关闭箱子会同时关闭背包
- 确保状态的一致性

## 5. 技术特点

1. **响应式状态管理**：使用Zustand实现高效的状态管理
2. **全局拖拽系统**：支持跨容器物品转移
3. **智能UI层级**：鼠标悬停时自动提升层级
4. **事件监听机制**：通过观察者模式实现组件间通信
5. **性能优化**：使用浅拷贝避免不必要的重渲染
6. **用户体验**：背景变暗效果和流畅的拖拽动画

## 6. 代码优化

### 6.1 无限更新循环修复

**问题**：在useEffect中直接使用`setSlots(inventory.getSlots())`会导致无限更新循环。

**解决方案**：使用浅拷贝创建新的数组引用：

```typescript
// 修复前
setSlots(inventory.getSlots());

// 修复后
setSlots([...inventory.getSlots()]);
```

### 6.2 性能优化

- **使用useCallback**：对于事件处理函数，使用useCallback避免重复创建
- **依赖数组优化**：确保useEffect的依赖数组正确设置
- **批量更新**：通过DragManager的notifyListeners()实现批量更新

## 7. 扩展建议

1. **物品分类系统**：增加物品分类和筛选功能
2. **物品详情**：添加物品详细信息展示
3. **快捷键支持**：增加更多快捷键操作
4. **物品数量管理**：支持堆叠物品
5. **视觉效果**：添加更丰富的动画效果

## 8. 总结

本系统实现了一个功能完整、用户体验良好的背包箱子管理机制。通过Zustand状态管理、全局拖拽系统和智能UI层级管理，实现了：

- 三种状态的灵活切换
- 跨背包物品的无缝转移
- 智能的UI层级管理
- 统一的背景变暗效果
- 流畅的用户交互体验

系统代码结构清晰，易于维护和扩展，为游戏中的物品管理提供了可靠的解决方案。