考虑到你目前的工程架构（GPU 上的纹理迭代），预条件共轭梯度法（PCG） 是比多重网格更现实、更容易上手的升级方案。多重网格需要构建金字塔纹理、进行复杂的限制/插值操作，在着色器中手动管理非常麻烦，而 PCG 只需要几个标准的线性代数运算，完全可以复用你现有的全屏四边形渲染管线。

下面，我直接给你一套可实施的 PCG 改造方案，包括需要增加的纹理、着色器，以及如何修改你的 update 流程来替换掉 Jacobi。

1. 核心思想：用 PCG 替代 Jacobi
你现在的做法：

text
for i = 0..N:
    压力 = Jacobi(压力, 散度)
本质上是求解 Poisson 方程 Ap = b，其中 A 是离散 Laplace 算子，b 是散度。Jacobi 是最慢的迭代法，收敛需要大量迭代。PCG 则可以在 10~20 次迭代内就得到高精度解。

PCG 的每一次迭代包含：

一次稀疏矩阵-向量乘（SpMV）A * p

向量内积（dot product）

向量更新（AXPY）

这些都可以用全屏四边形在 GPU 上实现。

2. 新增的辅助纹理
你需要增加几张临时纹理（尺寸和你的模拟域一致）来存放 CG 算法的中间向量：

typescript
private rTex!: THREE.WebGLRenderTarget;    // 残差 r = b - Ap
private dTex!: THREE.WebGLRenderTarget;    // 搜索方向 d
private qTex!: THREE.WebGLRenderTarget;    // q = A * d
private zTex!: THREE.WebGLRenderTarget;    // 预条件后的残差 (M⁻¹r)
在 createTextures() 中创建它们（和现有 RT 一样的创建方式）。

3. 新增的着色器
3.1 SpMV 着色器（计算 q = A * x）
这是 Poisson 矩阵 A 与向量 x 的乘积。对于标准五点有限差分，A * x 在像素 (i,j) 的值就是 4*x(i,j) - x(i-1,j) - x(i+1,j) - x(i,j-1) - x(i,j+1)，除以网格间距平方。由于你的 Poisson 方程系数已经包含 h²，我们直接实现拉普拉斯算子（负的拉普拉斯）：

glsl
// spmvShader
uniform sampler2D x;          // 输入向量纹理
uniform sampler2D levelset;   // 用于自由表面边界
uniform vec2 resolution;
varying vec2 vUv;

void main() {
    vec2 uv = vUv;
    float dx = 1.0 / resolution.x;
    vec2 dxVec = vec2(dx, 0.0);
    vec2 dyVec = vec2(0.0, dx);

    float phi = texture2D(levelset, uv).r;
    // 空气或固体区域，直接输出 0（不参与系统）
    if (phi > 0.0) { gl_FragColor = vec4(0.0); return; }

    float xC = texture2D(x, uv).r;
    float xL = texture2D(x, uv - dxVec).r;
    float xR = texture2D(x, uv + dxVec).r;
    float xD = texture2D(x, uv - dyVec).r;
    float xU = texture2D(x, uv + dyVec).r;

    // Neumann 边界：如果邻居是空气，就用当前值代替
    float phiL = texture2D(levelset, uv - dxVec).r;
    float phiR = texture2D(levelset, uv + dxVec).r;
    float phiD = texture2D(levelset, uv - dyVec).r;
    float phiU = texture2D(levelset, uv + dyVec).r;
    if (phiL > 0.0) xL = xC;
    if (phiR > 0.0) xR = xC;
    if (phiD > 0.0) xD = xC;
    if (phiU > 0.0) xU = xC;

    // 标准的负拉普拉斯: -∇²x ≈ (4*xC - xL - xR - xD - xU) / (dx*dx)
    // 注意这里 h² 已在右侧处理，我们返回的就是 Ax
    float Ax = (4.0 * xC - xL - xR - xD - xU) / (dx * dx);
    gl_FragColor = vec4(Ax, 0.0, 0.0, 1.0);
}
3.2 内积着色器（计算 dot(a, b)）
两个纹理对应像素相乘，然后累加。由于 GPU 上没有直接的全局求和，你需要一个归约（reduction） 过程，但 CG 的内积可以在 CPU 端用 readPixels 读回一个小尺寸纹理或者用多次逐级降采样来实现。为了简单，初期你可以先维持现有的 Jacobi 迭代次数，或者用一个低效但容易实现的方法：使用 readPixels 读回整个域然后 CPU 求和（仅调试用），但不推荐。更好的办法是实现一个两 Pass 归约：先将每个像素的乘积写入一个临时纹理，然后不断缩小到 1×1，最后读取那个像素的值。

不过为了让你快速看到效果，这里提供一个替代思路：直接使用 Jacobi 的预条件，也就是 Jacobi 预条件共轭梯度。Jacobi 预条件非常简单：M⁻¹r 就是 r / diag(A)，而 A 的对角元在均匀网格上恒为 4/dx²。这样你就完全不需要归约，因为 PCG 中所有涉及预条件的操作都可以就地完成。

但你需要内积，没有内积就无法计算 α 和 β。所以归约是绕不开的。我们可以实现一个单归约着色器，将 256x256 的纹理逐级缩小到 1×1，把过程写在 update 里的一个循环中。

3.3 向量更新着色器（y = ax + y 或 y = x + ay）
glsl
// axpyShader: y = a * x + y
uniform sampler2D x;
uniform sampler2D y;
uniform float a;
varying vec2 vUv;

void main() {
    vec4 xVal = texture2D(x, vUv);
    vec4 yVal = texture2D(y, vUv);
    gl_FragColor = vec4(a * xVal.r + yVal.r, 0.0, 0.0, 1.0);
}
类似地，可以组合出其他 AXPY 变体。

4. 修改 update() 流程
你的压力求解步骤（第 6 步）将被替换为一个 PCG 迭代循环：

typescript
// 在 update 中，第 6 步改为：
const cgIter = 15; // CG 迭代次数，通常远小于 Jacobi
// 初始化：设初始压力 p=0，残差 r = b (即散度*某个系数)，d = r
// 这里 b 需要根据 Poisson 方程的右侧构造。
// 注意你的散度 divergenceTex 存储的是 ∇·u*，Poisson 方程的右边是 (ρ/dt) * div
// 我们统一将右侧乘以系数，作为 b。

// 因此首先构造 right-hand side bTex:
// b = (density / dt) * div
// 可以在 CG 初始化时用一个着色器完成。

// 然后 p = 0 (已有的 pressureTex 已经是 0)，r = b, d = z = M⁻¹r (Jacobi预条件)
// 开始 CG 循环:
for (let iter = 0; iter < cgIter; iter++) {
    // 1. q = A * d
    // 2. alpha = dot(r, z) / dot(d, q)   (需要内积)
    // 3. p = p + alpha * d
    // 4. r = r - alpha * q
    // 5. z_new = M⁻¹ * r
    // 6. beta = dot(r_new, z_new) / dot(r_old, z_old)
    // 7. d = z_new + beta * d
    // 8. 更新 z = z_new, r = r_new
}
你需要把每个步骤翻译成着色器调用，内积通过归约得到标量值，然后作为 uniform 传入后续着色器。归约的实现可以在每帧 CG 循环中调用，但这样会很慢（因为每步要读回数据）。更好的办法是实现全 GPU 的 PCG，但内积还是需要 CPU 介入读取一个 1×1 的纹理值。这在 Three.js 中是可以的：renderer.readRenderTargetPixels 读取一个 1×1 像素，耗时极小。

因此归约策略：多次全屏 pass，每次将分辨率减半，直到 1×1，然后将结果读回 CPU。

5. 如何更新你的文档
当你实现完成后，可以在 2D 单相自由表面流模拟库.md 和 2D流体模拟器完整流程图.md 中：

在“关键优化点”或“每帧计算流程”部分，增加一节 “高级压力求解器（PCG）”。

描述 PCG 相比 Jacobi 的优势（收敛快、迭代次数少）。

列出新增的纹理和着色器。

更新流程图中“第 6 步”下面的子步骤，画出 PCG 迭代 的内部流程。

保留原有的 Jacobi 作为备选方案，可通过参数切换。

6. 给你的起步代码片段
这里给出一个简化的 PCG 初始化步骤（TypeScript 部分），帮你快速理解调用顺序：

typescript
// 在 update 中替换原来的 Jacobi 循环
// 假设已经创建了相应的着色器材质: spmvMat, dotMat, axpyMat, computeBMat, jacobiPreconditionMat 等

// 1. 计算 b = (density/dt) * divergence
this.computeBMat.uniforms.divergence.value = this.divergenceTex.texture;
this.renderFullscreen(this.computeBMat, this.bTex);

// 2. 初始化 p = 0 (pressure 纹理已为0)
//    r = b, z = M⁻¹ r (Jacobi预条件)
this.copyTextureToTarget(this.bTex, this.rTex);
this.jacobiPreconditionMat.uniforms.r.value = this.rTex.texture;
this.renderFullscreen(this.jacobiPreconditionMat, this.zTex);
// 初始化 d = z
this.copyTextureToTarget(this.zTex, this.dTex);
// 计算初始 rz = dot(r, z)
let rz = this.computeDot(this.rTex, this.zTex); // 自定义归约函数

for (let i = 0; i < cgIter; i++) {
    // q = A * d
    this.spmvMat.uniforms.x.value = this.dTex.texture;
    this.spmvMat.uniforms.levelset.value = this.curPhiTex.texture;
    this.renderFullscreen(this.spmvMat, this.qTex);

    // dq = dot(d, q)
    let dq = this.computeDot(this.dTex, this.qTex);
    let alpha = rz / dq;

    // p = p + alpha * d
    this.axpyMat.uniforms.x.value = this.dTex.texture;
    this.axpyMat.uniforms.y.value = this.curPressureTex.texture;
    this.axpyMat.uniforms.a.value = alpha;
    this.renderFullscreen(this.axpyMat, this.tempPressureTex);
    // 交换 curPressureTex 和 tempPressureTex...

    // r = r - alpha * q
    // ...
    // z_new = M⁻¹ r
    // ...
    // 计算新的 rz_new
    // beta = rz_new / rz
    // d = z_new + beta * d
    // 更新 rz = rz_new, d, z
}
你需要补充具体的归约函数 computeDot，它反复渲染直到 1×1，然后读取该像素的值。

7. 最终建议
先从 Jacobi 预条件共轭梯度 开始，因为它实现简单，而且 Jacobi 预条件几乎免费。

归约部分可以先做一个单独的模块，因为很多地方会用到。

整个改造大约需要 200~300 行新代码（着色器 + JS），但能使你的压力求解器性能提升一个数量级。

当你完成后，你会对数值线性代数在 GPU 上的实现拥有深刻的理解，这对你未来的图形学/仿真项目极有帮助。

你可以把这个改造过程作为一个新的章节写入你的文档，记录下从 Jacobi 到 PCG 的进化，又是一份极具价值的项目展示内容。