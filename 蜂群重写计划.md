# 蜂群重写计划 —— 引擎 / 小队 / 代理 / 基类 / 寻路（2026-09-24 定稿）

> 状态：🔨 **施工中（P4）**。范围 = rts 游戏逻辑层全重写；**地基（地形管线 / 渲染 / 物理 / 战斗 / 验证网）保留**。
> **本次收敛四件事：命令种类收敛、管理层显式化（防区/小队）、能力规范化、信息单源。**
> 现状真源仍是《RTS架构.md》；验收以本文 §6 实测基线为准。
> **施工记录 2026-09-25**：旧战斗指挥链**已删除**（`tacticalTick`/`dispatchMission`/`fallbackTick`/抵舰驻留/强制归位/`SquadLeaderAI`；`?swarm=old` 移除）；唯一发令器执行板续期 + 玩家令 TTL 到位；切向散开精确解（余弦定理，r 不变）。
> **同日第二刀（工兵收编）**：`EngineerManager` 全权工兵（位置查询=`FortifyPlanner.targetOf` 危险点优先/扇区弧链随机可达点 → 发令 → 到件 3m 计时 6s/10s → 落地）；工兵不进攻击队列；**旧文件已删**：`EngineerCorps/EngineerDispatch/CommanderAnchorSelect/Decide/BattleLine/MemberTaskBoard/MemberTaskNav/SquadDoctrine`（部署表）。`SwarmCommander` 1743→~750 行（退出大文件名单），全库 366→358 文件。
> **引擎时钟修正**：新引擎 `now` 改**实秒**（原 `simT/1000` 导致施工计时慢 1000×、旧板 TTL 秒掉）；执行板 TTL 与 `applyOrders` 同钟。
> 自检 148/148，check 全绿；浏览器探针基线 10/10（含"工事建成>0"）。

## 0. 为什么重写（本轮实测证据）

| 症状 | 数据 | 根因 |
|---|---|---|
| 命令风暴 | 台账 **559 条/75s**（同队 0.7s 一条），mission 在 build/regroup 间翻 | 命令层**多源同拍互写**（tacticalTick / fallback / engineering / 掩体重发 / 受阻重试），优先级靠调用顺序隐含 |
| 补丁→回归循环 | 掩体轮转 / 游弋进命令 / 施工豁免 / 8 向候选 / 保护锚错位…… 改完即回滚 | 状态散在 ~15 个 `Map`，**语义边界不清**（保护锚 / 扇区 / 件归属） |
| 文件膨胀 | Commander **1665 行**、System **1200 贴顶** | 无模块边界，靠拆文件控行数 |
| 混合时钟 | 命令计时实秒/游戏分钟并存 | 时间尺度没有单源 |
| 聚堆 / 转圈 | 同兵种 20~48m 聚、左右翻面 | 移动 / 转向 / 跟随规则补丁叠补丁 |

## 1. 收敛后的架构总览

```
蜂群引擎（指挥层，只指挥队长）
  ├─ **复合命令**（发给队长）：保护 / 行动 / 防御
  │     · 复合 = **原子能力组合**：保护 = 巡逻 + 驻守 + 行动
  │     · 以后新增复合命令 = 组合既有原子，不动原子层
  │     · **保护口径**（用户定）：引擎**同时提供** 被保护队长位置 G + 玩家位置 P；
  │       队长自主用 行动/巡逻/驻守 做**阻挡校验**（基类 `blockCheck`：P-B-G 三点一线，
  │       不齐 → 给调整点）直到真挡住玩家
  │     · **驻守也用阻挡校验**：保证自己也被掩体挡住（guard 模式，不成立给"躲到掩体后"调整点）
  │     · 校验输出的**调整点 = 寻路目标**：给位置 → 移动（到点再校验，收敛）
  ├─ SectorManager   扇形防区管理器
  │     · 按时查安全度（很安全 → 小队调去别的防区）
  │     · 按时查在编（小队被回收/被收走 → 补派）
  │     · 按情况派遣工兵小队（分区）
  ├─ SquadManager    小队管理器
  │     · 每队一条记录（order/progress/状态；禁止散 Map）
  │     · 接收小队自动状态汇报（进度/伤亡/位置）
  ├─ **四兵种管理器（显式）**：EngineerManager / MeleeManager / RangedManager / FlyerManager
  │     · 工兵：分区/派件数据；近战：前排/压上/缠斗；远程：射程环/保距/边撤边打；飞天：空中层/航线
  │     · 各自维护本兵种小队的编成与目标分配（互不越权）
  ├─ AttackQueues   **攻击队列制**（比逐单位索敌简单）
  │     · 队列：以**玩家**为半径、以**舰船**为半径（后续可加祖宗/友军队列）
  │     · 入队：**敌人离哪个实体近就进哪个队列**（队列去重：一个敌人只在一个队列）
  │     · **更新节拍：1Hz**（每秒一次；入队/去重/开火检验都在这一拍）
  │     · 队列内的单位做**能否开火检验**（射程/视线/ROE）
  │     · **开火许可 = 闩锁态**：一旦允许 → **持续开火**，直到许可被去除
  │       （1Hz 检验只负责置/撤许可，不逐拍重判）
  ├─ OrderValidator  **发令统一校验链**（所有复合命令共用，一处实现）
  │     · ① 事态范围：目标夹进 [frontMinD, frontMaxD] 环内
  │     · ② 密度：同兵种目标间距 ≥40m（太密集 → 横向错开/换点）
  │     · ③ 可达：初级寻路核验（不可达 → 缩近/换点/不发）
  └─ 信息单源：各小队位置 / 玩家位置 / 舰船位置（只此一家提供）
        │ 命令（唯一方向：引擎 → 队长）
        ▼
小队（队长层，只导航 + 汇报，不给代理下命令）
  ├─ **原子能力四件套**：巡逻 / 驻守 / 行军 / 行动
  │     · 行军 = 距离长 → 长寻路（坡度加权 A*，可含爬坡标注）
  │     · 行动 = 距离短 → 贪心短跳（LOS 10m）
  │     · 巡逻 / 驻守 = 防区内值守（位置由引擎给）
  │     · **原子层稳定**：只增不改；复合命令在上层组合
  ├─ 命令接口：显式接收（引擎三命令）+ 自动状态汇报
  └─ 信息按需：拿到任务后按需申请必要信息（引擎单源）
        │  （队长只走锚点/走廊；代理与成员"跟队长"）
        ▼
代理（L2）与实体（L3）
  ├─ 只做"跟队长"：近=直线；远+被挡=沿队走廊（长寻路）
  ├─ **升降格在实体基类**：变化后**向引擎汇报**（引擎只记录，不逐拍指挥）
  ├─ 能力规范化（统一接口）：爬坡 / 爬掩体 / 脱困 / 计时销毁
  ├─ **被打反应**：玩家打小队 → 引擎把**玩家位置**告知该小队 → 代理/成员**直接压上**
  └─ 战斗 / 施工 = 原子能力，不逐拍指挥
```

## 2. 铁律（重写必须内建，不再靠补丁）

1. **指挥单源**：引擎**只**指挥队长；队长**只**导航（锚点/走廊）+ 汇报；**队长不给代理下命令**——代理与成员一律"跟队长走"。
   - **命令两处都显式**：①引擎→小队（复合命令：保护/行动/防御，含原子组合）；②队长→自己（自身命令同样显式，可复用原子能力）；两者都过 `OrderValidator` 校验链。
2. **两层收敛**：
   - **原子层（小队）= 巡逻 / 驻守 / 行军 / 行动**（稳定；行军/行动由**距离**自动选长/短寻路）；
   - **复合层（引擎→队长）= 保护 / 行动 / 防御**（= 原子能力组合，如"保护 = 巡逻+驻守+行动"）；
     新增复合命令 = 组合既有原子，**不改原子层**。
3. **单源决策**：每队每拍只有一个决策源产出命令（干预链显式排序，不靠调用顺序）。
   - **玩家命令走解耦新架构**（沿用 `order/OrderBus.ts` 思路）：玩家 → 命令单源 → 同一校验链，不与引擎互踩。
4. **命令生命周期显式化**：issued → executing(带进度) → 换令条件（进度 ≥50% / 长时间静止 / 事态变 / 玩家令 / 重伤）。
5. **单一时间尺度**：命令/规划 = 游戏分钟（`GAME_MIN=12` 实秒）；物理/动画/回收 = 实秒。换算只在一处。
6. **信息单源**：小队位置 / 玩家位置 / 舰船位置由引擎提供；小队**按任务按需**取用，不各自扫世界。
   - 例：**玩家打小队 → 引擎把玩家位置告知该小队 → 代理直接压上**（不用小队自己扫）。
7. **升降格归属实体**：L2↔L3 升降由**实体基类自己处理**，变化后**向引擎汇报**（引擎记录状态，不逐拍指挥）。
   · **单一发令器**（用户定）：所有蜂群引擎命令只经 `OrderWriter` 下发（引擎各管理器只产决策，不直接写令）。
   · **单一汇报接收器**（用户定）：所有队长汇报只经 `SquadManager.report` 接收（其余模块不私存小队状态）。
8. **地形 = 唯一通行真源**：表=地形裁决（weld/cliff）、格=4m 块对齐；坡=普通可行边；硬边 ≤0.6 可走、>0.6 **上墙下可行**；深坑恒禁。
9. **爬坡显式**：寻路输出 `climb` 标注 = "此处必须爬坡"；执行按标注程序化爬坡；**坡面不许驻留**（要么上要么下）。
10. **能力规范化**：爬坡 / 爬掩体 / 脱困 / 计时销毁——统一接口、统一参数，L2/L3 同内核。
11. **工程师**：引擎分区（`SectorManager`）→ 队长派件（危险点优先 → 弧链随机可达点）→ 工兵寻路到件 → **到件 3m 内**开工计时（6s/10s）。无扫描半径。
12. **两个命令体系（用户定）**：**径向**（飞船方向）管 **前近 / 拉开距离**；**切向**管 **同兵种间距**（互不干扰）。
    · 引擎**有义务**让同兵种各小队别挨得太近：发令时校验目标间距（<40m → **极坐标切向滑动**，半径严格不变，`Spread.ts`）；
    · **直接给各队下前进令**——不碰保护锚（保护锚=保护令专用）。
13. **发令统一校验链**：任何复合命令在下发前统一过 **①事态范围（环内；到上限 → 建议防御）→ ②密度（同兵种间距）→ ③可达（初级寻路）**——一处实现、不许旁路（`OrderValidator.ts`）。
    **到事态函数上限 → 执行防御**（用户定）：`selectComposite` 判定，命令随之下发为防御复合。
    **命令格式灵活**（用户定）：位移命令 = 径向+切向同时发力得 `target` + 长寻路检测（`Displacement.composeMove`）；防御命令可只给 `object`（防御对象）或只给 `kind`（守原地）；**作用对象只有队长**。
14. **兵种管理器显式**：**工兵 / 近战 / 远程 / 飞天**各有管理器（编成/目标分配/策略），互不越权。
15. **攻击队列制**：维护"以玩家为半径""以舰船为半径"两个队列（后续可加祖宗/友军）；**敌人离哪个实体近就进哪个队列**（去重：一个敌人只在一个队列）；**队列 1Hz 更新**；队列内单位做**能否开火检验**（射程/视线/ROE），**许可为闩锁态——允许后持续开火直到许可被去除**。
16. **文件预算**：单文件 ≤800 行；无 KNOWN_BIG；每次提交过 `check/guard/smoke/probe`。
17. **保护 = 引擎给双点、队长自主挡**：引擎**同时提供**（G 被保护队长位置 + P 玩家位置）；**阻挡校验是基类功能**（`blockCheck`：P-B-G 三点一线，不齐给调整点）；队长按结果选 行军/行动/驻守——引擎不逐拍指挥。
    **驻守同用此校验**（guard 模式）：保证自己也在掩体后面——P-B-G 共线 + 掩体在中间 + 与掩体留间隙；不成立 → 给出"躲到掩体后"的调整点。
    **调整点即寻路目标**：校验给出位置 → 直接寻路过去移动即可（不额外写行为逻辑）；到点再校验，收敛到真挡住。

## 3. 保留（地基，别动）

- 地形管线（`ChunkManager / RasterMap / Refinements / FaceTable / TerrainPresets / TerrainSampler…`）；
- 渲染 / 物理 / 战斗（`services/**`、`CombatSystem`、`BulletManager`、实体表现与战斗部分）；
- **寻路（沿用现有，刚重写完）**：`PassTable`（裁决/格对齐/爬坡位/深坑）+ `FeasibilityPath`（坡度加权 A*）
  + 短跳贪心 + 走廊（含 climb 标注）——重写只做**组织归位**（`nav/`），算法不改；
- **地形语义表（沿用现有）**：`TerrainSemantics`（L1：HighGround/Choke/Hollow/FrontSlope/ReverseSlope）；
- **事态函数（沿用现有）**：`PostureFn`/环形活动区（上下限/波次/态势）；
- `entity/TerrainAssist` 共享内核；
- **验证网**：`scripts/arch-guard.mjs / smoke.mjs / probe-rts.mjs / probe-mountain.mjs / probe-cmd.mjs / probe-dir.mjs / probe-squad.mjs`；
- 名册 / 数值 / 素材（`ENEMY_ROSTER`、`EnemyScaling`、`mobDefs`…）。

## 4. 模块（重写后）

```
systems/swarm/engine/
  contracts.ts        ★ P0 已落：命令模型真源（复合/原子/生命周期/汇报/能力/队列归属）
  Positions.ts        ★ 已落：位置信息单源（玩家/舰船/各队队长；nearestSquad；只此一处）
  SquadManager.ts     ★ 已落：每队一条记录 + 自动汇报 + 编成/存活统计 + 过期（四兵种真源）
  RoleManager.ts      ★ 已落：兵种管理器基类（编成缓存/目标分配/环内夹取/刷怪执行/dbg）
  EngineerManager.ts  ★ 已落：分区登记 + 派件数据 + 本兵种刷怪（引擎只下达"补几队"）
  MeleeManager.ts     ★ 已落：压到接敌距离（ENGAGE=12）
  RangedManager.ts    ★ 已落：保距到射程环（STANDOFF=18，带内不动）
  FlyerManager.ts     ★ 已落：轰炸航线（ORBIT=10，四相位）
  EngineCore.ts       ★ 已落：相位 tick（perceive→situation→decide→write→debug；节拍可调，骨架+端口注入）
  EngineBridge.ts     ★ 已落：实机接线桥（**影子模式**只算不发 / 实机模式 emit 下发；LiveView 注入）
  SectorManager.ts    ★ 已落：扇形防区（建区/归区/空区补派/安全度调区/扇区中心点）
  AttackQueues.ts     ★ 已落：攻击队列（**1Hz**）：玩家/舰船（可扩展祖宗/友军）；最近实体入队 + 去重 + 开火检验；**许可闩锁**（允许→持续，直到撤除；离场自动撤）
  Protect.ts          保护命令：关系登记 + **双点下发（G/P，信息单源）** + 阻挡校验记账；锚只在此产生（G5）
  TimerManager.ts     **统一实体计时管理器**：所有实体（含代理）的计时销毁/回收计时一处管理
                      （卡死窗口 / 远距超时 / 单位寿命）——实体只上报"我在动/我到位" 
  OrderValidator.ts   发令统一校验链：①环内 ②同兵种密度 ③可达
  SquadManager.ts     小队管理器：每队一条记录 + 状态汇报接收 + 信息单源提供
  Composites.ts       复合命令定义（保护/行动/防御 = 原子能力组合；数据化、可扩展）
  DecisionChain.ts    显式优先链：玩家令 > 重伤 > 事态介入 > 干预(扎堆/越位/磨蹭) > 常规部署
  Composites.ts       ★ 已落：复合命令选择（**到事态函数上限 → 防御**；环内+保护→保护；否则行动）
  OrderValidator.ts   ★ 已落：发令统一校验链（①环内夹取+到上限建议防御 ②同兵种间距 ③可达）
  Spread.ts           ★ 已落：同兵种目标间距（≥40m；**极坐标切向滑动，半径不变**；异兵种不约束）
  Displacement.ts     ★ 已落：**位移命令**（径向+切向同时发力 → 目标位置 → **长距离寻路检测**；
                      不可达逐档缩近；作用对象只有队长）
  OrderWriter.ts      ★ 已落：唯一发令器（G1/G2）：稳定门（同签名豁免/进度≥50%/静止≥25s）+ 干预/玩家/重伤旁路 + 唯一写口 SquadOrderStore + dbg
  （事态函数**沿用现有 `PostureFn`**；仅组织归位，不重写）
  Spread.ts           同兵种目标间距（OrderValidator ②的实现；保留）

systems/swarm/squad/
  SquadCore.ts        ★ 已落：队长核心（接令=唯一写口 / 距离分流 行军·行动 / 汇报=唯一接收器；只给队长）
  Abilities.ts        原子能力四件套：巡逻 / 驻守 / 行军 / 行动（稳定层）
  MarchAction.ts      行军(长寻路) / 行动(短跳) 的任务分流（按距离）
  Follow.ts           成员/代理跟队长：近=直线；远+被挡=沿走廊；停步半径
  Formation.ts        槽位（围绕队长）

systems/swarm/nav/   ← **算法全部沿用现有，仅目录归位**
  PassTable.ts        表（地形裁决/格对齐/爬坡位/深坑）
  LongPath.ts         坡度加权 A*（上坡 +0.6/m；斜向仅平/下坡；输出 climb）
  ShortHop.ts         LOS 10m 贪心（爬升加价 2/m；惯性）
  Corridor.ts         走廊类型（路点 + climb + 代价）+ 末程回落

entity/base/
  contracts.ts        ★ P1 已落：能力/升降格类型（实体层不依赖 systems）
  CharacterCore.ts    ★ P1 已落：两载体同内核（推进/程序化爬坡/立面阻挡/贴地回退；TerrainProbe 注入）
  Locomotion.ts       ★ P1 已落：移动内核统一出口（16 向 pickSteer/承诺/危险/惯性 + CharacterCore 一并 re-export）
  TerrainAssist.ts    坡/危险/爬坡/上岸（保留）
  Abilities.ts        ★ P1 已落：能力规范化状态机（爬坡/爬掩体/脱困/计时销毁，L2/L3 同内核）
  Blocking.ts         ★ 已落：阻挡校验基类功能（P-B-G 三点一线；block=挡玩家 / guard=驻守躲掩体；两个调整点 + 建议原子）

entity/enemy/
  EnemyBase.ts        载体（收敛为三件套接口）
  EnemyBrain.ts       战斗原子能力
  EnemyPresentation.ts 表现
```

## 4.1 UI 与探针（接入新架构）

- **UI = 管理器的只读视图**：面板（敌人列表/小地图/时间轴）改读 `SquadManager`/四兵种管理器/`SectorManager` 的只读 API；实体只保留自身表现（血条/动画/贴图）。UI **不反向指挥**。
- **探针 = 黑盒脚本 + 新调试口**：5 个探针（rts/mountain/cmd/dir/squad）改为读新调试口（`__rts.<管理器>` + `dbg` 计数）；**调试口是契约**，改动须同步探针。
- 两者都进 **P4 接入期**，验收 = §6 基线全套复跑。

## 5. 迁移（四期，每期可独立验证）

| 期 | 内容 | 验收 |
|---|---|---|
| **P0 冻结** ✅ | 新目录并行开发；旧路径 feature flag 可一键回退 | smoke/probe 6/6 不变 |
| | **已完成**：`engine/contracts.ts`（命令模型真源）+ `SwarmConfig.REWRITE_ON`（`?swarm=new`）+ arch-guard G1~G9（含负向测试）| check/guard/smoke 全绿 |
| **P1 基类** ✅ | CharacterCore/Locomotion/Abilities 换入（寻路不动）——**CharacterBase 已换入 CharacterCore.step**（爬坡/立面/贴地同内核）；A/B：山地 40m→20m、卡死 1→0 | 湖岸 6/6 上岸；爬坡按标注；坡面不驻留；**山地=A/B 对照（新内核 ≤ 旧内核的耗时/回收，非 12/12 硬线——山地本身是已知烂点）** |
| | **已完成**：`entity/base/contracts.ts`（能力/升降格类型，实体层不依赖 systems）+ `entity/base/Abilities.ts`（四能力状态机：爬坡定速直推/免立面/跳分离/到顶或超时；L2/L3 同内核）+ `engine/TimerManager.ts`（统一实体计时：卡死窗口同 STUCK 口径 + 计时销毁 deadline + 开火闩锁）| check/guard/smoke 全绿 |
| | **已完成**：`entity/base/CharacterCore.ts`（两载体同内核：推进/程序化爬坡/立面阻挡/贴地回退，TerrainProbe 注入）+ `entity/base/Locomotion.ts`（移动内核统一出口：SteerPick/TerrainAssist/CharacterCore 一并 re-export）+ `CLIMB_TIMEOUT_S` 单一来源 | check/guard/smoke 全绿 |
| | **已完成**：两载体同内核——L3 `CharacterBase` 与 L2 代理（`AgentPool.stepAgent`）**均换入 `CharacterCore`**；地形探针抽成共用一份 `entity/base/RasterProbe.ts`（一处改、两载体同变）| 湖岸 6/6；山地 A/B：换核后池存活 3（换核前 0）无新回归；**山地本身仍是已知烂点（L3 最后 20m 慢、个别消失），用户后置自修** |
| **P2 小队** | SquadCore/Follow/MarchAction（命令接口+汇报）换入 | 成员门拦截 ≥90%、空转 <5% |
| **P3 引擎** 🔨 | EngineCore/SectorManager/SquadManager/四兵种管理器/AttackQueues/TimerManager/OrderValidator 换入 | cmdChanges ≤5、回收 ≤10/min、同兵种间距 ≥40m |
| | **已完成**：`Positions`（位置单源）+ `SquadManager`（记录/汇报/统计）+ `RoleManager` 基类 + **四兵种管理器**（编成/分配/环内夹取/刷怪）+ `TimerManager`/`Protect`（P1 已落）；**`npm run test:engine` 自检 49/49**（纯模块，esbuild 打包直跑）| check/guard/smoke/test 全绿 |
| | **已完成**：`Spread` + `OrderValidator` + `Composites` + `Displacement` + `OrderWriter` + `AttackQueues` + `SectorManager` + `EngineCore` + **`EngineBridge`（影子模式接线）**；**自检 112/112** | check/test/smoke 全绿 |
| | **已完成**：`EngineBridge`（影子接线）+ `squad/SquadCore`（队长核心：接令/距离分流/汇报）+ 玩家命令入口（player 旁路直达队长）；**自检 122/122** | check/test/smoke 全绿 |
| | **山地根因已修**（2026-09-24）：`SquadTactics.currentTargetOf` 的**锚点滞回把 `climb` 标记丢了** → `climbOrdered` 永不置位 → 爬坡态不触发 → 坡上卡死。修法：`anchorClimb` 随锚点一起存取。实测 `climbing 0→12`（12/12 进入爬坡态），完整探针 45m→40m（仍差最后 40m，待续）。 |
| | **山地口径修正**：山地寻路**本来就是稀烂**（用户定）——`probe:mountain` 只作**测量/A-B 对照**，不作通过门槛；换入验收改为"同场景下新内核 ≤ 旧内核"。 |
| | **① 接线已挂**：`main.ts` 影子 tick（`?swarm=new`）+ LiveView 适配 + `__rts.shadowBridge` 调试口；**实机实测**：squads=5（3工兵/1近战/1远程）、四管理器在算、唯一发令器 issued=5/kept=43（稳定门生效）| probe 6/6 不变 |
| | **已完成 2026-09-25（单写口收口）**：旧战斗指挥链删除——`tacticalTick`/`dispatchMission`/`fallbackTick`（磨蹭/层级/扎堆）/第一波抵舰驻留/事态强制归位/`SquadLeaderAI` 全删，无回退开关；`EngineBridge.write` 每拍续期执行板（防旧板 TTL 丢令）；玩家令 TTL 30 游戏分钟（到期释放）；`Spread` 切向精确解（余弦定理；r 严格不变，几何不可满足 θ→π，**无径向推力**）。`SwarmCommander` 1743→1276 行。自检 148/148 | check + test 全绿 |
| | **待续（P4 余件）**：① 队长侧执行替换（SquadCore 驱动成员，替换 SquadDispatch 写指令）② 事态/环/波次/刷怪迁入新引擎（Commander 只剩数据）③ 消费 AttackQueues/TimerManager ④ UI 接新只读 API ⑤ 能力缺口迁移（磨蹭/越位/扎堆 → DecisionChain.intervention；抵舰驻留 → 波次决策源）⑥ §6 指标调优（成员空转 <5%、收拢态间距口径、cmdChanges） | 见 §6 |
| **P4 接入** | **UI 与探针接入新架构**：面板/小地图改读新管理器只读 API；探针改读新调试口 | §6 基线全套复跑通过 |

## 5.1 新规则清单（写进 arch-guard）

| # | 规则 | 检查方式 |
|---|---|---|
| G1 | **指挥单源**：引擎→队长唯一写口 `OrderWriter`；队长→成员只导航（锚点/走廊），**不得写队令** | 静态：`tactics.issue`/`issueOrder` 只许从 `engine/` 出 |
| G2 | **每队每拍单写口**：`SquadOrderStore` 只许 `OrderWriter` 写 | 静态：Store 的写方法只被 Writer 引用 |
| G3 | **代理不收指令**：成员指令（directive）只写 L3 载体；代理只"跟队长" | 静态：directive 写列只许 `squad/` 出；代理列只许 Follow 写 |
| G4 | **信息单源**：小队/引擎不得直读 `spawn`/世界位置（玩家/舰船位置由引擎提供） | 静态：`spawn.x`/`hooks.playerX` 只许 `engine/` 与 `main.ts` 引用 |
| G5 | **保护锚专用**：`anchor`（保护对象）只许在保护命令路径出现，禁止位置改写 | 静态：`order.anchor` 的写点白名单 |
| G6 | **时间尺度**：命令层计时必须用 `GAME_MIN`；实秒只许物理/回收/动画 | 静态：`engine/`/`squad/` 不得用裸 `performance.now()` 做命令计时 |
| G7 | **寻路不改地形**：`nav/` 只读 `PassTable`，不得写裁决/高度（表构建器 `PassTable.ts` 自身除外） | 静态：表列写只许 `nav/PassTable.ts` |
| G8 | **文件预算**：单文件 ≤800 行；无 KNOWN_BIG | 行数扫描 |
| G9 | **调试口契约**：`__rts.<管理器>` + `dbg` 计数存在且可读（探针依赖） | 探针冒烟（P4 验收） |

## 6. 验收基线（重写后必须达到，均为本轮实测值）

- `npm run smoke` **6/6**、`npm run probe` **6/6**；
- 山地（`probe:mountain`）：L2 **12/12 到达**、用时 130~145 模拟秒；
- 命令稳定（`probe:cmd`）：`cmdChanges ≤ 5`（62s 窗口）；
- 成员指令（`probe:dir`）：门拦截 ≥90%、空转 <5%、反向 <5%；
- 卡死回收：**≤10/分钟**（当前 9.5）；
- 同兵种小队间距：发令后 60s 观测 **≥40m**；
- 文件：无 >800 行文件、无 KNOWN_BIG。

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| 换入回归 | 每期带探针基线；P0 flag 一键回退 |
| 语义边界再错（保护锚/扇区/件） | §2 铁律 + 模块头注释写"**谁拥有**"（owner 声明） |
| 行尾/编码事故 | 改文件前 `git show HEAD:file` 验行尾；保持原行尾（CRLF 勿转 LF） |
| SoA 列纪律 | 新增列必须 `push/copy/snapshot` 三处同步 |
| 混合时钟复发 | 换算只在 `SwarmConfig`；`performance.now()` 仅物理/回收 |
