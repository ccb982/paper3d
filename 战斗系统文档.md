# 战斗系统文档

## 1. 系统概述

战斗系统是游戏的核心玩法之一，包含伤害计算、血条显示、碰撞检测等核心功能。本文档详细说明战斗系统的设计和实现。

## 2. 伤害系统

### 2.1 伤害信息接口

```typescript
// src/core/Entity.ts
interface DamageInfo {
  amount: number;            // 伤害值
  type?: 'physical' | 'fire' | 'ice' | 'electric';  // 伤害类型
  source?: Entity;           // 伤害来源
}
```

### 2.2 伤害类型

| 伤害类型 | 描述 | 特殊效果 |
|---------|------|----------|
| physical | 物理伤害 | 基础伤害类型 |
| fire | 火焰伤害 | 可造成持续伤害 |
| ice | 冰冻伤害 | 可减缓移动速度 |
| electric | 电击伤害 | 可造成范围伤害 |

### 2.3 实体伤害处理

**Entity 基类方法**：

```typescript
// src/core/Entity.ts
public takeDamage(damage: DamageInfo | number): void {
  const damageInfo = typeof damage === 'number' ? { amount: damage } : damage;
  this.health = Math.max(0, this.health - damageInfo.amount);
  if (this.health <= 0) {
    this.onDeath();
  }
}

public onDeath(): void {
  this.isActive = false;
}
```

**CharacterEntity 重写**：

```typescript
// src/entities/CharacterEntity.ts
public takeDamage(amount: number): void {
  this.health = Math.max(0, this.health - amount);
  if (this.health <= 0) {
    this.isActive = false;
    this.onDeath();
  }
}

protected onDeath(): void {
  eventBus.emit('entity:death', { id: this.id, faction: this.faction });
}
```

**StaticEntity 实现**：

```typescript
// src/entities/StaticEntity.ts
public takeDamage(amount: number): void {
  this.health -= amount;
  if (this.health <= 0) {
    this.onDestroy();
    this.isActive = false;
  } else {
    this.onHit();
  }
}

protected onHit(): void {
  // 子类可重写，实现命中效果
}

protected onDestroy(): void {
  // 子类可重写，实现销毁效果
}
```

## 3. 血条系统

### 3.1 血条UI组件

**EntityHealthBars 组件**：

```typescript
// src/ui/pages/EntityHealthBars.tsx
export const EntityHealthBars = () => {
  const mode = useGameStore(s => s.mode);

  // 只在战斗模式显示血条
  if (mode !== GameMode.BATTLE) {
    return null;
  }

  // 获取实际的相机和渲染器
  const camera = cameraStore.getCamera() || new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
  const renderer = cameraStore.getRenderer();
  const rendererDomElement = renderer?.domElement || { clientWidth: window.innerWidth, clientHeight: window.innerHeight };

  // 获取所有实体并过滤出需要显示血条的实体
  const entities = EntityManager.getInstance().getAllEntities().filter(entity => 
    (entity instanceof CharacterEntity) || 
    (entity instanceof StaticEntity && (entity as any).isShootable)
  );

  // 生成血条
  const entityHealthBars = entities.map(entity => {
    // 计算屏幕坐标
    const screenPos = new THREE.Vector3(
      entity.position.x,
      entity.position.y + 2, // 血条显示在实体上方
      entity.position.z
    );
    
    screenPos.project(camera);
    
    // 转换为屏幕坐标
    const x = (screenPos.x * 0.5 + 0.5) * rendererDomElement.clientWidth;
    const y = (1 - screenPos.y * 0.5 - 0.5) * rendererDomElement.clientHeight;
    
    // 计算血条宽度
    const healthPercent = entity.health / entity.maxHealth;
    const barWidth = 80;
    const barHeight = 10;
    
    // 只有当实体在屏幕内且生命值小于最大值时显示血条
    if (screenPos.z > 1) return null;
    if (entity.health >= entity.maxHealth) return null;
    
    return (
      <div
        key={entity.id}
        style={{
          position: 'absolute',
          left: x - barWidth / 2,
          top: y - 30,
          width: barWidth,
          height: barHeight,
          backgroundColor: 'rgba(0, 0, 0, 0.6)',
          borderRadius: '4px',
          overflow: 'hidden',
          zIndex: 999,
          pointerEvents: 'none'
        }}
      >
        <div
          style={{
            width: `${Math.max(0, healthPercent) * 100}%`,
            height: '100%',
            backgroundColor: entity instanceof CharacterEntity && (entity as CharacterEntity).faction === 'enemy' 
              ? '#ff4444' 
              : '#44ff44',
            transition: 'width 0.2s ease-out'
          }}
        />
      </div>
    );
  });

  return <>{entityHealthBars}</>;
};
```

### 3.2 血条显示规则

| 条件 | 显示状态 |
|------|----------|
| 非战斗模式 | ❌ 不显示 |
| 战斗模式 | ✅ 显示 |
| 实体在屏幕外 | ❌ 不显示 |
| 实体生命值满 | ❌ 不显示 |
| 敌人实体 | ✅ 红色血条 |
| 友方实体 | ✅ 绿色血条 |
| 静态实体 | ✅ 绿色血条 |

### 3.3 相机存储系统

**CameraStore 单例**：

```typescript
// src/core/CameraStore.ts
export class CameraStore {
  private static instance: CameraStore;
  private camera: THREE.PerspectiveCamera | null = null;
  private renderer: THREE.WebGLRenderer | null = null;

  private constructor() {}

  public static getInstance(): CameraStore {
    if (!CameraStore.instance) {
      CameraStore.instance = new CameraStore();
    }
    return CameraStore.instance;
  }

  public setCamera(camera: THREE.PerspectiveCamera): void {
    this.camera = camera;
  }

  public setRenderer(renderer: THREE.WebGLRenderer): void {
    this.renderer = renderer;
  }

  public getCamera(): THREE.PerspectiveCamera | null {
    return this.camera;
  }

  public getRenderer(): THREE.WebGLRenderer | null {
    return this.renderer;
  }
}

export const cameraStore = CameraStore.getInstance();
```

**在 GameWorld 中设置相机**：

```typescript
// src/components/GameWorld.tsx
useEffect(() => {
  cameraStore.setCamera(camera);
  cameraStore.setRenderer(gl);
}, [camera, gl]);
```

## 4. 攻击系统

### 4.1 子弹实体

**BulletEntity 类**：

```typescript
// src/entities/BulletEntity.ts
export class BulletEntity extends Entity {
  private velocity: THREE.Vector3;
  private damage: number;
  private lifetime: number = 5; // 5秒生命周期
  private age: number = 0;

  constructor(
    position: THREE.Vector3,
    direction: THREE.Vector3,
    velocity: number,
    color: number = 0xffaa00,
    damage: number = 10
  ) {
    const geometry = new THREE.SphereGeometry(0.1, 8, 8);
    const material = new THREE.MeshStandardMaterial({ color });
    const mesh = new THREE.Mesh(geometry, material);
    
    super(`bullet-${Date.now()}-${Math.random()}`, 'bullet', mesh);
    
    this.position.copy(position);
    this.mesh.position.copy(position);
    
    this.velocity = direction.clone().normalize().multiplyScalar(velocity);
    this.damage = damage;
  }

  public update(delta: number): void {
    if (!this.isActive) return;
    
    // 更新位置
    this.position.add(this.velocity.clone().multiplyScalar(delta));
    this.mesh.position.copy(this.position);
    
    // 生命周期
    this.age += delta;
    if (this.age >= this.lifetime) {
      this.isActive = false;
    }
  }

  public getDamage(): number {
    return this.damage;
  }

  protected onHit(entity: Entity): void {
    if (typeof (entity as any).takeDamage === 'function') {
      (entity as any).takeDamage({
        amount: this.damage,
        type: 'physical',
        source: this
      });
    }
    this.isActive = false;
  }
}
```

### 4.2 碰撞检测与伤害处理

**碰撞管理器注册**：

```typescript
// src/components/GameWorld.tsx
const collisionManager = CollisionManager.getInstance();

// 注册子弹 vs 角色（敌人）碰撞
collisionManager.registerCollision('bullet', 'character', (bullet, character) => {
  const bulletEntity = bullet as BulletEntity;
  const characterEntity = character as CharacterEntity;
  if (characterEntity.isEnemy(bulletEntity as any)) { // 简化判断：敌人阵营
    characterEntity.takeDamage({
      amount: bulletEntity.getDamage() ?? 1,
      type: 'physical',
      source: bulletEntity
    });
    bulletEntity.isActive = false;
  }
});

// 注册子弹 vs 静态物品（靶子）碰撞
collisionManager.registerCollision('bullet', 'static', (bullet, staticObj) => {
  const staticEntity = staticObj as StaticEntity;
  if (staticEntity.isShootable) {
    staticEntity.takeDamage({
      amount: 1,
      type: 'physical',
      source: bullet
    });
    bullet.isActive = false;
  }
});

// 角色 vs 敌人碰撞（玩家与敌人相撞扣血）
collisionManager.registerCollision('character', 'character', (a, b) => {
  const charA = a as CharacterEntity;
  const charB = b as CharacterEntity;
  if (charA.isEnemy(charB)) {
    // 敌人碰撞玩家：玩家扣血，敌人反弹或停止移动
    if (charA.isPlayerControlled) charA.takeDamage({
      amount: 10,
      type: 'physical',
      source: charB
    });
    if (charB.isPlayerControlled) charB.takeDamage({
      amount: 10,
      type: 'physical',
      source: charA
    });
  }
});
```

### 4.3 射击系统集成

**自由式射击**：

```typescript
// src/systems/shooting/implementations/FreeStyleShootingSystem.ts
private shootBullet(): void {
  if (!this.camera || !this.characterPosition) return;
  
  const bulletPosition = this.characterPosition.clone();
  const direction = this.getShootDirection();
  
  if (direction) {
    const bulletEntity = new BulletEntity(
      bulletPosition,
      direction,
      this.bulletVelocity,
      0xffaa00
    );
    
    EntityManager.getInstance().addEntity(bulletEntity);
    
    this.callbacks.onBulletCreated?.({
      id: Date.now(),
      position: bulletPosition.toObject(),
      direction: direction.toObject(),
      velocity: this.bulletVelocity
    });
  }
}
```

**锁定式射击**：

```typescript
// src/systems/shooting/implementations/LockonShootingSystem.ts
private shootBullet(target: THREE.Object3D): void {
  if (!this.characterPosition) return;
  
  const bulletPosition = this.characterPosition.clone();
  const direction = new THREE.Vector3()
    .subVectors(target.position, bulletPosition)
    .normalize();
  
  const bulletEntity = new BulletEntity(
    bulletPosition,
    direction,
    this.bulletVelocity,
    0xff0000
  );
  
  EntityManager.getInstance().addEntity(bulletEntity);
  
  this.callbacks.onBulletCreated?.({
    id: Date.now(),
    position: bulletPosition.toObject(),
    direction: direction.toObject(),
    velocity: this.bulletVelocity
  });
}
```

## 5. 敌人AI系统

### 5.1 敌人追踪行为

**EnemyEntity AI**：

```typescript
// src/entities/EnemyEntity.ts
protected updateAI(delta: number): void {
  const mode = useGameStore.getState().mode;

  if (mode !== GameMode.BATTLE) {
    this.updateRandomMovement(delta);
  } else {
    this.updateChaseMovement(delta);
  }
}

private updateRandomMovement(delta: number): void {
  this.directionChangeTimer += delta;
  if (this.directionChangeTimer >= this.directionChangeInterval) {
    this.directionChangeTimer = 0;
    this.directionChangeInterval = 2 + Math.random() * 3;
    this.randomDirection.set(Math.random() - 0.5, 0, Math.random() - 0.5).normalize();
  }
  this.moveDirection.copy(this.randomDirection);
}

private updateChaseMovement(delta: number): void {
  const target = EntityManager.getInstance().getEntitiesByType('character')
    .find(e => (e as CharacterEntity).isPlayerControlled === true) as CharacterEntity;
  if (!target) {
    this.moveDirection.set(0, 0, 0);
    return;
  }

  const direction = new THREE.Vector3().subVectors(target.position, this.position);
  direction.y = 0;
  if (direction.length() > 0.01) {
    this.moveDirection.copy(direction);
  } else {
    this.moveDirection.set(0, 0, 0);
  }
}
```

### 5.2 敌人属性

**EnemyEntity 构造函数**：

```typescript
// src/entities/EnemyEntity.ts
constructor(id: string, texturePath: string, position: THREE.Vector3) {
  super(id, 'enemy', texturePath, position);
  this.health = 30;
  this.maxHealth = 30; // 同时设置maxHealth，确保血条显示正确
  this.speed = 2;
  this.isPlayerControlled = false;
  this.randomDirection.set(Math.random() - 0.5, 0, Math.random() - 0.5).normalize();
}
```

### 5.3 敌人AI模式切换

**行为模式**：

| 模式 | 敌人行为 | 移动方式 |
|------|---------|----------|
| 非战斗 | 自由漫步 | 随机方向，2-5秒改变方向 |
| 战斗 | 追踪玩家 | 朝向玩家位置移动 |

## 6. 战斗流程

### 6.1 战斗模式进入

1. **用户点击**：用户从日常模式点击进入战斗
2. **自动切换**：子弹击中敌人时自动从日常模式切换到战斗模式
3. **模式切换**：`gameStore.setMode(GameMode.BATTLE)`
4. **敌人生成**：在战斗场景中生成敌人实体
5. **血条显示**：`EntityHealthBars` 组件开始显示

### 6.2 战斗循环

1. **用户输入**：玩家使用鼠标/键盘控制角色
2. **射击处理**：射击系统检测目标并发射子弹
3. **实体更新**：`EntityManager.update()` 更新所有实体位置
4. **碰撞检测**：`CollisionManager.update()` 检测碰撞
5. **伤害处理**：处理子弹命中和实体碰撞造成的伤害
6. **血条更新**：血条UI实时更新显示
7. **死亡处理**：处理生命值为0的实体

### 6.3 战斗结束

1. **所有敌人死亡**：检测到所有敌人实体都已死亡
2. **战斗胜利**：显示胜利界面
3. **模式切换**：返回日常模式或主菜单

## 7. 性能优化

### 7.1 血条渲染优化

- **条件渲染**：只在战斗模式显示
- **视锥体剔除**：只显示屏幕内的实体血条
- **状态优化**：只显示生命值不满的实体血条

### 7.2 碰撞检测优化

- **空间分区**：使用网格空间分区减少碰撞检测次数
- **碰撞冷却**：防止同一对实体在短时间内重复触发碰撞
- **距离检测**：使用距离平方比较代替开方运算

### 7.3 实体管理优化

- **对象池**：子弹实体使用对象池管理
- **生命周期**：自动清理过期的子弹实体
- **状态管理**：通过 `isActive` 状态控制实体是否需要更新

## 8. 扩展与未来规划

### 8.1 计划中的功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 技能系统 | 角色技能，造成特殊伤害效果 | 高 |
| 装备系统 | 武器和防具，影响伤害和防御 | 高 |
| 状态效果 | 中毒、燃烧、冰冻等持续效果 | 中 |
|  boss 战 | 大型敌人，特殊攻击模式 | 中 |
| 多人合作 | 多玩家协同战斗 | 低 |

### 8.2 扩展接口

**伤害系统扩展**：

```typescript
// 未来可能的扩展
interface AdvancedDamageInfo extends DamageInfo {
  critical?: boolean;         // 是否暴击
  piercing?: number;          // 穿透值
  elementalReaction?: string; // 元素反应
}
```

**血条系统扩展**：

```typescript
// 未来可能的扩展
interface HealthBarOptions {
  showShield?: boolean;       // 显示护盾
  showBuffIcons?: boolean;    // 显示 buff 图标
  showLevel?: boolean;        // 显示等级
}
```

## 9. 代码结构

```
src/
├── core/
│   ├── Entity.ts            # 实体基类（包含伤害系统）
│   ├── EntityManager.ts     # 实体管理器
│   ├── CollisionManager.ts  # 碰撞管理器
│   └── CameraStore.ts       # 相机存储
│
├── entities/
│   ├── CharacterEntity.ts   # 角色实体基类
│   ├── FriendlyEntity.ts    # 友方实体
│   ├── EnemyEntity.ts       # 敌方实体
│   ├── StaticEntity.ts      # 静态物品基类
│   ├── TargetEntity.ts      # 靶子实体
│   └── BulletEntity.ts      # 子弹实体
│
├── ui/pages/
│   └── EntityHealthBars.tsx # 血条UI组件
│
└── systems/shooting/        # 射击系统
    ├── base/                # 基础射击系统
    ├── implementations/     # 具体实现
    └── managers/            # 系统管理器
```

## 10. 测试指南

### 10.1 功能测试

1. **血条显示**：进入战斗模式，观察敌人和靶子是否显示血条
2. **伤害测试**：射击敌人和靶子，观察血条是否减少
3. **死亡测试**：持续攻击敌人，观察敌人是否死亡并消失
4. **碰撞测试**：让玩家角色与敌人相撞，观察双方是否扣血
5. **AI测试**：观察敌人是否会追踪玩家角色
6. **模式切换**：在日常模式下射击敌人，观察是否自动切换到战斗模式
7. **敌人行为**：在非战斗模式下观察敌人是否随机移动

### 10.2 性能测试

1. **大量敌人**：生成多个敌人，测试碰撞检测性能
2. **子弹密度**：快速射击，测试子弹实体管理性能
3. **血条渲染**：多个实体同时显示血条，测试UI渲染性能

### 10.3 边界情况

1. **屏幕外实体**：观察屏幕外的实体血条是否正确隐藏
2. **满血实体**：观察生命值满的实体血条是否正确隐藏
3. **非战斗模式**：在非战斗模式下确认血条不显示
4. **敌人死亡**：观察敌人死亡后是否正确从场景中移除

## 11. 结论

战斗系统已经实现了完整的核心功能，包括：

- ✅ 多类型伤害系统
- ✅ 实时血条UI
- ✅ 碰撞检测与伤害处理
- ✅ 敌人AI追踪（战斗模式）
- ✅ 敌人随机移动（非战斗模式）
- ✅ 自动战斗模式切换
- ✅ 性能优化

系统设计具有良好的扩展性，可以轻松添加新的功能和特性。通过模块化的架构，战斗系统可以与其他系统（如装备、技能）无缝集成，为游戏提供丰富的战斗体验。