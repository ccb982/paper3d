建议的开发顺序（实际操作）
搭建基础地图结构

定义 MapData 类型，创建第一个手动地图（如一个平面 + 两三棵树）。

实现 MapRenderer 渲染地面和简单立方体。

集成碰撞检测

修改 CharacterController，让角色不能穿过立方体。

实现可交互物体

点击树时调用现有的 useDialogue().triggerDialogue，显示 AI 生成的文本。

添加程序化生成

实现 MapGenerator，生成一片随机森林。

在游戏启动时加载程序化地图，测试生成效果。

实现区域触发器

添加一个简单传送门：走到某个矩形区域后角色跳到地图另一端。

开发手动编辑面板

浮动 UI，实时添加/删除物体，导出 JSON。

性能优化与扩展

实例化网格、动态加载、多地图切换。