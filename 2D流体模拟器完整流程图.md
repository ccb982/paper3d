# 2D 单相自由表面流模拟器 - 完整流程图

## 一、整体架构总览（新架构）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FluidSimulator                                   │
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    核心渲染基础设施                                  │   │
│  │  Scene + OrthographicCamera + PlaneGeometry (全屏四边形)            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      纹理缓冲系统                                    │   │
│  │                                                                    │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  速度场（双缓冲）                                              │   │   │
│  │  │  velTexA ◄─── curVelTex ───► velTexB                         │   │   │
│  │  │  RGBA: [vx, vy, 0, 1]                                        │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                    │                               │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  Level Set 场（双缓冲）                                        │   │   │
│  │  │  phiTexA ◄─── curPhiTex ───► phiTexB                         │   │   │
│  │  │  RGBA: [phi, 0, 0, 1]  phi > 0 空气 | phi < 0 水              │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                    │                               │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  压力场（双缓冲）                                              │   │   │
│  │  │  pressureTexA ◄─── curPressureTex ───► pressureTexB          │   │   │
│  │  │  RGBA: [p, 0, 0, 1]                                          │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                    │                               │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  辅助纹理（单缓冲）                                            │   │   │
│  │  │  divergenceTex     - 散度场                                   │   │   │
│  │  │  forcedVelTex      - 外力后速度场                             │   │   │
│  │  │  velAfterCollisionTex - 墙碰撞后速度场                        │   │   │
│  │  │  velCorrectTex     - 速度修正后                               │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                    │                               │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  寿命系统纹理（双缓冲）                                        │   │   │
│  │  │  ageTexA ◄─── curAgeTex ───► ageTexB                        │   │   │
│  │  │  RGBA: [age, 0, 0, 1]  age >= 0 水粒子年龄（秒）            │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                    │                               │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  固体相关纹理（预计算）                                         │   │   │
│  │  │  solidMaskTex      - 固体掩码（外部传入）                      │   │   │
│  │  │  solidNormalTex    - 固体法线（自动计算）                      │   │   │
│  │  │  RGBA: [nx, ny, 0, 1]  法线方向（指向固体外部）               │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────────────┐
│  │                      渲染层（分层渲染架构）                              │
│  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  水体渲染着色器 - 分层合成                                  │   │
│  │  输入: curPhiTex.texture (phi), curVelTex.texture (vel)     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ═══════════════════════════════════════════════════════════════   │
│                                                                    │
│  【分层渲染架构 - 各层独立计算，最终叠加】                          │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 1: 基础颜色层 (Base Color)                          │   │
│  │  - 根据 depth 混合 waterColor 和 deepColor                  │   │
│  │  - 不受光照影响                                            │   │
│  │  - 公式: baseColor = mix(waterColor, deepColor, depth)     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 2: 漫反射光照层 (Diffuse Lighting)                   │   │
│  │  - 法线与光源夹角决定明暗                                    │   │
│  │  - 公式: diffuse = baseColor × diff                        │   │
│  │  - diff = max(0.1, dot(normal, lightDir))                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 3: 高光层 (Specular Lighting)                       │   │
│  │  - 镜面反射，单独控制强度                                    │   │
│  │  - 公式: specular = white × spec × specularIntensity      │   │
│  │  - spec = pow(max(dot(normal, halfDir), 0.0), 64.0)       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 4: 边缘发光层 (Edge Glow / Emissive)                 │   │
│  │  - 仅在水面表面附近（phi 接近 0）生效                        │   │
│  │  - 不参与漫反射计算，单独叠加                                │   │
│  │  - 公式: emissive = glowColor × edge × edgeIntensity       │   │
│  │  - glowColor = vec3(0.2, 0.6, 1.0) 淡蓝色                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 5: 流动扰动层 (Flow Detail)                          │   │
│  │  - 速度场产生的视觉效果，作为细节噪点                        │   │
│  │  - 不大面积改变色调                                          │   │
│  │  - 公式: flowColor = vec3(0.15, 0.25, 0.35) × flow          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ═══════════════════════════════════════════════════════════════   │
│                                                                    │
│  【最终合成公式】                                                  │
│  finalColor = diffuse + specular + emissive + flowColor          │
│                                                                    │
│  【渲染决策】                                                      │
│  - phi >= 0.0 → discard（空气和界面完全透明）                      │
│  - phi < 0.0 → 渲染流体各层                                         │
│                                                                    │
│  【透明度】                                                        │
│  alpha = clamp(0.6 - phi * 2.0, 0.2, 0.9)                         │
│                                                                    │
│  接口: getLevelSetTexture(), getVelocityTexture()                │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、每帧完整计算流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              每帧 update(dt)                                │
│                                                                             │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 0 步】初始化                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  dt = min(传入的deltaTime, 0.033)  // 限制最大时间步长                │   │
│  │  resolution = [width, height]                                        │   │
│  │  dtReinit = 0.5 / min(width, height)                               │   │
│  │                                                                        │   │
│  │  // 自定义流体形状初始化（可选）                                       │   │
│  │  if (params.initialLevelSet) {                                       │   │
│  │      copyTextureToTarget(params.initialLevelSet, phiTexA/B)         │   │
│  │  } else {                                                            │   │
│  │      renderFullscreen(initLevelSetShader(), phiTexA/B)              │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 0.5 步】预计算固体法线（仅当 solidMask 改变时执行一次）               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  solidMaskTex ──[计算梯度]──→ solidNormalTex                        │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                               │  │   │
│  │  │      solid_r = solidMask[x+1, y]                      │  │   │
│  │  │      solid_l = solidMask[x-1, y]                      │  │   │
│  │  │      solid_t = solidMask[x, y+1]                      │  │   │
│  │  │      solid_b = solidMask[x, y-1]                      │  │   │
│  │  │                                                               │  │   │
│  │  │      // 法线 = 梯度，归一化                                     │  │   │
│  │  │      nx = solid_r - solid_l                                    │  │   │
│  │  │      ny = solid_t - solid_b                                    │  │   │
│  │  │      normal = normalize([nx, ny])                              │  │   │
│  │  │                                                               │  │   │
│  │  │      solidNormal[x,y] = normal                                 │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ✅ 预计算后，每帧直接读取 solidNormalTex，无需重复计算！        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 1 步】固体边界清理 #1（可选）                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: solidBoundaryClearShader()                               │   │
│  │  输入: curVelTex, curPhiTex, solidMaskTex                         │   │
│  │  输出: velTexA, phiTexA                                           │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      isSolid = texture2D(solidMask, uv).r                    │  │   │
│  │  │      vel = texture2D(velInput, uv).rg                         │  │   │
│  │  │      phi = texture2D(phiInput, uv).r                          │  │   │
│  │  │      if (isSolid > 0.5) {                                     │  │   │
│  │  │          vel = vec2(0.0)                                       │  │   │
│  │  │          phi = -1.0                                           │  │   │
│  │  │      }                                                         │  │   │
│  │  │      gl_FragColor = vec4(vel, phi, 1.0)                       │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  更新: curVelTex = velTexA, curPhiTex = phiTexA                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 2 步】速度平流                                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: velocityAdvectionShader()                                  │   │
│  │  输入: curVelTex                                                    │   │
│  │  输出: velTexB                                                     │   │
│  │                                                                        │   │
│  │  公式: vel_new = vel_old (反推法)                                    │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      vel = texture2D(velocity, uv).rg                         │  │   │
│  │  │      step = vel * dt / resolution                            │  │   │
│  │  │      back_uv = uv - step                                     │  │   │
│  │  │      vel_new = texture2D(velocity, back_uv).rg               │  │   │
│  │  │      gl_FragColor = vec4(vel_new, 0.0, 1.0)                  │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  更新: curVelTex = velTexB                                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 3 步】外力计算（重力 + 粘性 + 表面张力）                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: externalForcesShader()                                     │   │
│  │  输入: curVelTex, curPhiTex                                         │   │
│  │  输出: forcedVelTex                                                 │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      vel = texture2D(velocity, uv).rg                         │  │   │
│  │  │      phi = texture2D(levelset, uv).r                          │  │   │
│  │  │                                                               │  │   │
│  │  │      // 1. 重力（UV坐标系：y=0底部，y=1顶部，向下应减小vel.y）    │  │   │
│  │  │      vel.y -= gravity * dt                                   │  │   │
│  │  │                                                               │  │   │
│  │  │      // 2. 粘性扩散（Laplacian）                              │  │   │
│  │  │      vel += (viscosity/density) * dt * ∇²vel                  │  │   │
│  │  │                                                               │  │   │
│  │  │      // 3. 表面张力（仅在界面附近）                            │  │   │
│  │  │      if (|phi| < eps) {                                       │  │   │
│  │  │          f_st = sigma * kappa * delta * n                     │  │   │
│  │  │          vel += (f_st / density) * dt                        │  │   │
│  │  │      }                                                         │  │   │
│  │  │                                                               │  │   │
│  │  │      // 4. 流体注入（只在空气中 phi > 0）                      │  │   │
│  │  │      if (injectionEnabled && dist < size && phi > 0.0) {      │  │   │
│  │  │          mask = 1.0 - smoothstep(0.0, size, dist)            │  │   │
│  │  │          phi = phi - injectionFlowRate * dt * mask          │  │   │
│  │  │          vel += injectionVel * mask                           │  │   │
│  │  │      }                                                         │  │   │
│  │  │                                                               │  │   │
│  │  │      gl_FragColor = vec4(vel, phi, 1.0)                      │  │   │  │  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  更新: curVelTex = forcedVelTex ⭐                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 3.5 步】外力注入（带物理时间保护）⭐ 新增                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  方法: processPendingForces(dt) → addLocalVelocityImpulse(...)     │   │
│  │  输入: pendingForces[], curVelTex, dt                              │   │
│  │  输出: velTexA/B（双缓冲）                                          │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      d = distance(uv, center)                               │  │   │
│  │  │      mask = 1.0 - smoothstep(0.0, radius, d)                │  │   │
│  │  │      vel = texture2D(curVel, uv).rg                         │  │   │
│  │  │      newVel = vel + impulse * mask                           │  │   │
│  │  │      speed = length(newVel)                                  │  │   │
│  │  │      if (speed > maxSpeed) {                                │  │   │
│  │  │          newVel = normalize(newVel) * maxSpeed                │  │   │
│  │  │      }                                                         │  │   │
│  │  │      gl_FragColor = vec4(newVel, 0.0, 1.0)                   │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ✅ 时间保护机制:                                                       │   │
│  │  - 力缓存: applyFluidForce() 只缓存力，不在立即注入                 │   │
│  │  - 时间步缩放: 速度注入 = velocity(加速度) × dt                      │   │
│  │  - 与重力一致: 遵循 vel += acceleration × dt 模型                  │   │
│  │  - 防止累积: 无论外部调用频率，力的施加都与时间步绑定               │   │
│  │                                                                        │   │
│  │  特点:                                                                 │   │
│  │  - 局部影响：只在指定中心和半径范围内叠加速度                          │   │
│  │  - 边缘平滑：使用 smoothstep 实现渐变过渡                             │   │
│  │  - 速度上限：防止速度无限累积，maxSpeed 默认 10.0                     │   │
│  │  - 外部接口：通过 IFluidForceTarget.applyFluidForce() 调用            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 4 步】墙碰撞处理（使用预计算法线）⭐ 新增                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: wallCollisionShader()                                      │   │
│  │  输入: forcedVelTex, solidMaskTex, solidNormalTex（预计算）        │   │
│  │  输出: velAfterCollisionTex                                        │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      isSolid = texture2D(solidMask, uv).r                    │  │   │
│  │  │                                                               │  │   │
│  │  │      if (isSolid < 0.5) {                                     │  │   │
│  │  │          output = texture2D(forcedVel, uv).rg  // 直接通过    │  │   │
│  │  │      } else {                                                 │  │   │
│  │  │          // ========== 墙区域：反弹计算 ==========              │  │   │
│  │  │          normal = texture2D(solidNormal, uv).rg  // 预计算！ │  │   │
│  │  │          vel = texture2D(forcedVel, uv).rg                   │  │   │
│  │  │                                                               │  │   │
│  │  │          // 分解速度                                           │  │   │
│  │  │          vn = dot(vel, normal)  // 法向分量                 │  │   │
│  │  │          vt = vel - vn * normal  // 切向分量                 │  │   │
│  │  │                                                               │  │   │
│  │  │          // 法向反向 × 恢复系数 e                             │  │   │
│  │  │          vn_new = -vn * restitution  // e = 0.8               │  │   │
│  │  │                                                               │  │   │
│  │  │          // 切向 × 摩擦系数                                   │  │   │
│  │  │          vt_new = vt * friction  // μ = 0.95                │  │   │
│  │  │                                                               │  │   │
│  │  │          vel_new = vt_new + vn_new * normal                 │  │   │
│  │  │          gl_FragColor = vec4(vel_new, 0.0, 1.0)             │  │   │
│  │  │      }                                                         │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ✅ 关键：直接读取预计算的 solidNormalTex，无需每帧计算梯度！   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 4.5 步】构建爆炸散度源 ⭐ 新增                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  输入: activeExplosions, curPhiTex, curAgeTex                     │   │
│  │  输出: explosionDivTex, curPhiTex (可选), curAgeTex (可选)        │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  // 阶段1: 遍历活跃爆炸列表（使用帧计数计时）                       │  │   │
│  │  │  清空 explosionDivTex（必须！避免上帧残留累积）                                          │  │   │
│  │  │  for each explosion in activeExplosions:                     │  │   │
│  │  │      frameElapsed = currentFrame - startFrame                 │  │   │
│  │  │      t = frameElapsed / durationFrames                        │  │   │
│  │  │      // 二次抛物线包络：先升后降，峰值在0.5                                 │  │   │
│  │  │      u = clamp(t, 0.0, 1.0)\nenvelope = 4.0 * u * (1.0 - u)                  │  │   │
│  │  │      if (u >= 1.0 || durationFrames <= 0): remove explosion   │  │   │
│  │  │                                                               │  │   │
│  │  │      // ⚠️ 负号关键！注入负散度才能产生向外膨胀效果                 │  │   │
│  │  │      S = -strength * envelope * mask(x)                        │  │   │
│  │  │      renderToTarget(S, explosionDivTex, false)  // 不清屏叠加  │  │   │
│  │  └───────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  // 阶段2: 水花生成（createWater=true时）                   │  │   │
│  │  │  if (createWater):                                          │  │   │
│  │  │      for each pixel in explosionRadius:                       │  │   │
│  │  │          // 仅在空气区域生成水花（phi >= 0）                   │  │   │
│  │  │          // 不修改已有水体，避免"削平"效果                     │  │   │
│  │  │          if (mask > 0.0 && phi >= 0.0):                      │  │   │
│  │  │              phi = -radius * mask * envelope * 2.0           │  │   │
│  │  │                                                               │  │   │
│  │  │      // 使用双缓冲更新 phi，避免原地读写                       │  │   │
│  │  │      phiDst = (curPhiTex == phiTexA) ? phiTexB : phiTexA     │  │   │
│  │  │      curPhiTex = phiDst                                      │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  // 阶段3: 重置新生水花的年龄为0                           │  │   │
│  │  │  if (createWater):                                          │  │   │
│  │  │      ageDst = (curAgeTex == ageTexA) ? ageTexB : ageTexA     │  │   │
│  │  │      renderToTarget(ageResetMat, ageDst)                     │  │   │
│  │  │      curAgeTex = ageDst                                      │  │   │
│  │  └───────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ✅ 关键设计: 爆炸不直接修改速度，而是注入散度源                   │   │
│  │  → 散度源通过压力求解器生成压力梯度，再由速度修正驱动流体         │   │
│  │  → 水花只在空气中生成（phi >= 0），避免破坏已有水体结构           │   │
│  │  → 使用帧计数代替wall-clock时间，确保每次模拟结果一致            │   │
│  │  ⭐ 速度场同步: velForDiv = curVelTex.texture（包含水花速度）    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 5 步】计算散度                                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: divergenceShader()                                         │   │
│  │  输入: curVelTex（含水花速度）, explosionDivTex                    │   │
│  │  输出: divergenceTex                                                │   │
│  │                                                                        │   │
│  │  公式: div = ∂vx/∂x + ∂vy/∂y + explosionDiv                       │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      vx_R = texture2D(velocity, uv+dx).x                     │  │   │
│  │  │      vx_L = texture2D(velocity, uv-dx).x                     │  │   │
│  │  │      vy_T = texture2D(velocity, uv+dy).y                     │  │   │
│  │  │      vy_B = texture2D(velocity, uv-dy).y                     │  │   │
│  │  │      div = (vx_R - vx_L) / 2dx + (vy_T - vy_B) / 2dy        │  │   │
│  │  │      div += texture2D(explosionDivTex, uv).r  // 注入爆炸散度  │  │   │
│  │  │      gl_FragColor = vec4(div, 0.0, 0.0, 1.0)                 │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 6 步】压力求解（Jacobi 迭代）                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  ⚠️ 注意: PCG求解器已创建但未启用，当前使用Jacobi迭代                │   │
│  │                                                                        │   │
│  │  新增纹理: rTex(残差), dTex(搜索方向), qTex(A*d), zTex(M⁻¹r), bTex(右侧)│   │
│  │  （PCG专用纹理，当前未启用）                                          │   │
│  │                                                                        │   │
│  │  公式: p_new = (p_L + p_R + p_D + p_U - (ρ/dt)*div*h²) / 4           │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  // Jacobi迭代（双缓冲，Dirichlet边界条件）                             │  │   │
│  │  │  pressureSrc = pressureTexA                                    │  │   │
│  │  │  pressureDst = pressureTexB                                    │  │   │
│  │  │  for i = 0 to pressureIterations:                            │  │   │
│  │  │      for each pixel (x, y):                                   │  │   │
│  │  │          p_L = texture2D(pressure, uv-dx).r                   │  │   │
│  │  │          p_R = texture2D(pressure, uv+dx).r                   │  │   │
│  │  │          p_D = texture2D(pressure, uv-dy).r                   │  │   │
│  │  │          p_U = texture2D(pressure, uv+dy).r                   │  │   │
│  │  │          div = texture2D(divergence, uv).r                    │  │   │
│  │  │          h = 1.0 / resolution.x                               │  │   │
          // Neumann边界：空气/固体邻居用中心压力代替           │  │   │
          center_p = texture2D(pressure, uv).r                 │  │   │
          // 采样邻居phi，若为空气(phi>0)或固体则替换         │  │   │
          if (texture2D(phiTex, uv-dx).r > 0.) p_L = center_p  │  │   │
          if (texture2D(phiTex, uv+dx).r > 0.) p_R = center_p  │  │   │
          if (texture2D(phiTex, uv-dy).r > 0.) p_D = center_p  │  │   │
          if (texture2D(phiTex, uv+dy).r > 0.) p_U = center_p  │  │   │
          p_new = (p_L+p_R+p_D+p_U-(ρ/dt)*div*h*h) / 4.0      │  │   │
│  │  │      render → pressureDst                                      │  │   │
│  │  │      [pressureSrc, pressureDst] = [pressureDst, pressureSrc]  │  │   │
│  │  │  curPressureTex = pressureSrc  // 最终压力场                   │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ✅ 优点: 实现简单，GPU友好，无需全局归约                            │   │
│  │  ⚠️ 缺点: 收敛较慢，需要100~200次迭代                               │   │
│  │                                                                        │   │
│  │  📋 PCG求解器状态（已创建但未启用）:                                  │   │
│  │  - 已实现着色器: spmvShader, axpyShader, reduceShader等              │   │
│  │  - 未启用原因: 需要GPU归约实现内积计算，当前版本暂未集成              │   │
│  │  - 性能对比: PCG(15~30次迭代) ≈ Jacobi(100~200次迭代) 的收敛精度     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 7 步】速度修正（压力梯度减法）                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: velocityCorrectShader()                                    │   │
│  │  输入: velAfterCollisionTex, curPressureTex                        │   │
│  │  输出: velCorrectTex                                               │   │
│  │                                                                        │   │
│  │  公式: vel_corrected = vel - ∇p / ρ                                  │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      vel = texture2D(velocity, uv).rg                         │  │   │
│  │  │      p_L = texture2D(pressure, uv-dx).r                       │  │   │
│  │  │      p_R = texture2D(pressure, uv+dx).r                       │  │   │
│  │  │      p_D = texture2D(pressure, uv-dy).r                       │  │   │
│  │  │      p_U = texture2D(pressure, uv+dy).r                       │  │   │
│  │  │      vel.x -= (dt/density) * (p_R-p_L) / (2dx)               │  │   │
│  │  │      vel.y -= (dt/density) * (p_U-p_D) / (2dy)               │  │   │
│  │  │      gl_FragColor = vec4(vel, 0.0, 1.0)                       │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  更新: curVelTex = velCorrectTex                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 8 步】Level Set 平流                                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: levelSetAdvectionShader()                                 │   │
│  │  输入: curVelTex, forcedVelTex, curPhiTex                          │   │
│  │  输出: phiTexB                                                     │   │
│  │                                                                        │   │
│  │  公式: phi_new = phi_old (反推法)                                    │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  for each pixel (x, y):                                       │  │   │
│  │  │      vel = texture2D(velocity, uv).rg                         │  │   │
│  │  │      step = vel * dt / resolution                            │  │   │
│  │  │      back_uv = uv - step                                     │  │   │
│  │  │      if (injectionEnabled) {                                  │  │   │
│  │  │          phi = texture2D(forcedVel, back_uv).b                │  │   │
│  │  │      } else {                                                 │  │   │
│  │  │          phi = texture2D(levelset, back_uv).r                 │  │   │
│  │  │      }                                                         │  │   │
│  │  │      gl_FragColor = vec4(phi, 0.0, 0.0, 1.0)                  │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  更新: curPhiTex = phiTexB                                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 8.5 步】流体寿命系统（当 maxLifetime > 0 时启用）⭐ 新增              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  三阶段流程：年龄平流 → 年龄更新 → 流体消散                           │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  阶段1: 年龄平流（追踪水滴而非像素）                           │  │   │
│  │  │  Shader: ageAdvectionShader()                                │  │   │
│  │  │  输入: curAgeTex, curVelTex, curPhiTex                       │  │   │
│  │  │  输出: ageTexA/B                                             │  │   │
│  │  │                                                               │  │   │
│  │  │  核心逻辑:                                                    │  │   │
│  │  │      prevPos = uv - vel * dt / resolution                    │  │   │
│  │  │      age_new = texture2D(age, clamp(prevPos, 0, 1)).r        │  │   │
│  │  │                                                               │  │   │
│  │  │  ✅ 关键：反向追踪速度场，让年龄跟随水流移动                   │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  阶段2: 年龄更新（使用真实时间）                              │  │   │
│  │  │  Shader: ageUpdateShader()                                   │  │   │
│  │  │  输入: curAgeTex, curPhiTex                                  │  │   │
│  │  │  输出: ageTexA/B                                             │  │   │
│  │  │                                                               │  │   │
│  │  │  核心逻辑:                                                    │  │   │
│  │  │      if (phi < 0) age += realDelta  // 水区域年龄增加        │  │   │
│  │  │      else age = 0                    // 空气区域年龄为0      │  │   │
│  │  │      if (注入区域) age = 0           // 新注入水重置年龄     │  │   │
│  │  │                                                               │  │   │
│  │  │  ✅ 关键：使用真实时间增量 realDelta，确保按秒计时             │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  阶段3: 流体消散（年龄达到上限）                              │  │   │
│  │  │  Shader: dissipationShader()                                 │  │   │
│  │  │  输入: curPhiTex, curAgeTex                                  │  │   │
│  │  │  输出: phiTexA/B (双缓冲)                                    │  │   │
│  │  │                                                               │  │   │
│  │  │  核心逻辑:                                                    │  │   │
│  │  │      if (age >= maxLifetime) phi = 0.1  // 水变空气         │  │   │
│  │  │                                                               │  │   │
│  │  │  ✅ 注意1: maxLifetime = 0 时禁用整个寿命系统                 │  │   │
│  │  │  ✅ 注意2: 使用双缓冲避免读写冲突（Feedback loop）            │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  │                                                                        │   │
│  │  更新: curAgeTex, curPhiTex                                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 9 步】Level Set 重新初始化（窄带限制 + 双缓冲交替，保持界面锐利）    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Shader: levelSetReinitShader()                                     │   │
│  │  输入: curPhiTex                                                    │   │
│  │  输出: phiTexA/B (ping-pong，双缓冲避免读写冲突)                     │   │
│  │                                                                        │   │
│  │  公式: phi_new = phi - dt_reinit * sign(phi) * (|∇phi| - 1)        │   │
│  │  dt_reinit = 0.1 / min(width, height)  // 伪时间步长               │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  // 双缓冲交替：每次迭代从 src 读取，写入 dst，然后交换         │  │   │
│  │  │  phiSrc = curPhiTex                                          │  │   │
│  │  │  phiDst = (phiSrc === phiTexA) ? phiTexB : phiTexA           │  │   │
│  │  │  for i = 0 to reinitIterations:                              │  │   │
│  │  │      for each pixel (x, y):                                   │  │   │
│  │  │          phi_0 = texture2D(levelset, uv).r                    │  │   │
│  │  │          dx = 1.0 / resolution.x                             │  │   │
│  │  │                                                               │  │   │
│  │  │          // 窄带限制：只重初始化界面附近（abs(phi) < 2*dx）    │  │   │
│  │  │          if (abs(phi_0) > 2.0 * dx) {                        │  │   │
│  │  │              gl_FragColor = vec4(phi_0, 0.0, 0.0, 1.0);    │  │   │
│  │  │              continue;                                       │  │   │
│  │  │          }                                                   │  │   │
│  │  │                                                               │  │   │
│  │  │          phi_grad = length(∇phi)                              │  │   │
│  │  │          // sign(0) = 0 会导致 phi 不演化，用 eps 区间处理     │  │   │
│  │  │          eps = 0.001                                         │  │   │
│  │  │          sign = phi_0 > eps ? 1.0 : (phi_0 < -eps ? -1.0 : 1.0)│  │   │
│  │  │          phi_new = phi_0 - dt_reinit * sign * (phi_grad - 1)  │  │   │
│  │  │                                                               │  │   │
│  │  │          // 避免符号反转                                       │  │   │
│  │  │          if (sign > 0 && phi_new < 0.0) phi_new = 0.0;       │  │   │
│  │  │          if (sign < 0 && phi_new > 0.0) phi_new = 0.0;       │  │   │
│  │  │                                                               │  │   │
│  │  │          gl_FragColor = vec4(phi_new, 0.0, 0.0, 1.0)          │  │   │
│  │  │      render → phiDst                                          │  │   │
│  │  │      [phiSrc, phiDst] = [phiDst, phiSrc]  // 交换           │  │   │
│  │  │  curPhiTex = phiSrc  // 最终结果                              │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【第 10 步】固体边界清理 #2（可选）                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  同第 1 步：清理固体区域的速度和 Level Set                          │
│  │  更新: curVelTex, curPhiTex                                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【底部墙体边界】防止流体穿透底部                                            │
  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Shader: bottomWallMat                                             │   │
│  │  底部一行 (vUv.y < 1/resolution.y + 0.0001) 的 vel.y = 0           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【扩展功能】爆炸系统 ⚡（散度源模型，压力驱动）                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Method: explode(cx, cy, radius, strength, createWater,    │   │
│  │                  duration = 0.1)                            │   │
│  │  参数: cx/cy=中心坐标(UV), radius=半径                      │   │
│  │        strength=散度强度(推荐25000~5000递减), duration=持续时间  │   │
│  │        createWater=true: 生成水花 | false: 仅注入散度        │   │
│  │                                                                        │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  阶段1: 爆炸注册                                   │  │   │
│  │  │      将爆炸加入 activeExplosions 列表            │  │   │
│  │  │      explosion = {cx, cy, radius, strength,     │  │   │
│  │  │                     createWater, startTime,     │  │   │
│  │  │                     duration}                   │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                               │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  阶段2: 每帧构建爆炸散度 (buildExplosionDiv)   │  │   │
│  │  │      elapsed = currentTime - startTime        │  │   │
│  │  │      envelope = exp(-t²/(2σ²))  // 高斯脉冲    │  │   │
│  │  │      S = strength * envelope * mask / (ρ*dt)  │  │   │
│  │  │      累加渲染到 explosionDivTex（不清屏叠加）   │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                               │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  阶段3: 水花生成（createWater=true时）        │  │   │
│  │  │      仅修改 phi，使用周围速度加权平均          │  │   │
│  │  │      新生水花速度由后续压力求解驱动            │  │   │
│  │  │      ✅ TODO: 需额外Pass重置新水年龄为0        │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                               │   │
│  │  ✅ 核心原理:                                                    │   │
│  │  - 爆炸不直接修改速度，而是注入散度源 S                   │   │
│  │  - 散度源通过压力求解器生成压力梯度 ∇p                    │   │
│  │  - 速度修正步骤: vel -= ∇p/ρ 驱动流体向外膨胀             │   │
│  │                                                               │   │
│  │  ⚡ 参数说明:                                                    │   │
│  │  - strength: 控制散度源强度，非直接速度增量              │   │
│  │    → 推荐值: [25000, 20000, 15000, 10000, 5000] 递减   │   │
│  │  - duration: 爆炸持续时间(秒)，默认0.1                    │   │
│  │  - createWater=true: 生成水花但不直接加速                │   │
│  │  └─────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、轻量级流体实体架构 ⭐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        LightFluidEntity 架构                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        实体状态管理                                  │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  age: number         // 当前年龄（秒）                        │   │   │
│  │  │  maxAge: number = 5  // 最大寿命（秒），默认5秒              │   │   │
│  │  │  isActive: boolean   // 活跃状态                            │   │   │
│  │  │  lod: FluidLOD       // LOD等级                            │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                              │                                     │   │
│  │                              ▼                                     │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │                    物理属性                                 │   │   │
│  │  │  worldVelocity: THREE.Vector3  // 世界空间速度              │   │   │
│  │  │  prevWorldVelocity: THREE.Vector3  // 上帧速度（用于加速度）│   │   │
│  │  │  position: THREE.Vector3        // 当前位置                 │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                              │                                     │   │
│  │                              ▼                                     │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │                   内部流体模拟器                            │   │   │
│  │  │  simulator: FluidSimulator  // 32x32 小型模拟器             │   │   │
│  │  │  simUpdateAccumulated: number  // 累积模拟时间              │   │   │
│  │  │  frameCount: number            // 帧计数器                  │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         update(delta) 流程                        │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  1. 状态检查: if (!isActive) return                        │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  2. 生命周期: age += delta                                  │   │   │
│  │  │         if (age > maxAge) { isActive = false; return; }    │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  3. LOD检查: if (lod === OFF) { visible=false; return; }   │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  4. 位置更新: mesh.position += worldVelocity × delta       │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  5. 累积时间: simUpdateAccumulated += delta                │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  6. 模拟更新: if (accumulated >= interval)                │   │   │
│  │  │            → 计算加速度（使用累积时间）                      │   │   │
│  │  │            → 应用内部力 impulse                            │   │   │
│  │  │            → simulator.update(accumulated)                 │   │   │
│  │  │            → 重置 accumulated = 0                          │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  7. 纹理控制: LOD=LOW → freeze texture                    │   │   │
│  │  │              else → update material                        │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【LOD 等级定义】                                                           │
│  ┌─────────────────┬───────────────┬─────────────────┬──────────────────┐   │
│  │     LOD         │   距离条件     │   模拟频率      │   纹理更新       │   │
│  ├─────────────────┼───────────────┼─────────────────┼──────────────────┤   │
│  │      HIGH       │    ≤3 单位     │    每4帧       │     启用         │   │
│  │     MEDIUM      │    ≤6 单位     │    每4帧       │     启用         │   │
│  │       LOW       │   ≤10 单位     │    每4帧       │     冻结         │   │
│  │      OFF        │    >10 单位    │     -          │    销毁区域      │   │
│  └─────────────────┴───────────────┴─────────────────┴──────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、流体区域管理器架构 ⭐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        FluidRegionManager 架构                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         区域属性                                   │   │
│  │  center: THREE.Vector3        // 区域中心坐标                     │   │
│  │  radius: number               // 区域半径                         │   │
│  │  entities: LightFluidEntity[] // 管理的液滴列表                   │   │
│  │  currentLOD: FluidLOD         // 当前LOD等级                      │   │
│  │  markedForRemoval: boolean    // 是否标记待删除                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         update(delta, lod)                        │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  1. 更新当前LOD: currentLOD = lod                           │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  2. 清理失效实体: entities = entities.filter(e => e.isActive)│   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  3. LOD=OFF检查: 标记区域待销毁                             │   │   │
│  │  │         → markedForRemoval = true                           │   │   │
│  │  │         → 所有液滴 isActive = false                         │   │   │
│  │  │         → return                                          │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  4. 重置标记: markedForRemoval = false                     │   │   │
│  │  │                              │                             │   │   │
│  │  │                              ▼                             │   │   │
│  │  │  5. 派发LOD: 为每个液滴设置 lod = currentLOD               │   │   │
│  │  │         → 实体自行处理更新逻辑                              │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【EntityManager 集成】                                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  updateWithPlayer(delta, playerPosition)                          │   │
│  │  │                                                                │   │
│  │  │  1. 更新普通实体: updateEntities(delta)                        │   │
│  │  │                                                                │   │
│  │  │  2. 更新流体区域: updateFluidRegions(delta, playerPosition)   │   │
│  │  │     │                                                          │   │
│  │  │     ├─ 计算距离: dist = center.distanceTo(player) - radius    │   │
│  │  │     ├─ 确定LOD: HIGH/MEDIUM/LOW/OFF                          │   │
│  │  │     ├─ region.update(delta, lod)                              │   │
│  │  │     └─ 收集待删除区域 → 循环外安全删除                         │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                                                │   │
│  │  【安全删除机制】                                                  │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  1. 标记阶段: 遍历实体时标记 isActive = false                │   │   │
│  │  │  2. 收集阶段: 收集 toRemove[] / regionsToRemove[]           │   │   │
│  │  │  3. 删除阶段: 循环外部统一删除                                │   │   │
│  │  │  → 避免迭代期间修改集合导致的运行时错误                       │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  【循环依赖解决】动态导入                                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  // EntityManager.ts 中使用动态导入                                 │   │
│  │  let FluidRegionManager: typeof import('@entities/fluid').FluidRegionManager;  │   │
│  │  let FluidLOD: typeof import('@entities/fluid').FluidLOD;           │   │
│  │                                                                    │   │
│  │  // 在需要时动态加载                                               │   │
│  │  if (!FluidRegionManager) {                                        │   │
│  │      const module = await import('@entities/fluid');               │   │
│  │      FluidRegionManager = module.FluidRegionManager;               │   │
│  │      FluidLOD = module.FluidLOD;                                   │   │
│  │  }                                                                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】解耦边界处理 ⭐（增强爆炸飞溅效果）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        解耦边界处理（Decoupled Boundary）                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  核心原理：在压力求解的上下游对边界环区域进行局部阻尼，软化刚性边界          │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  参数配置:                                                  │   │
│  │    decoupledBoundary: true   // 启用解耦边界处理            │   │
│  │    boundaryRingWidth: 0.03   // 边界环宽度（UV空间）        │   │
│  │    boundaryDivDamping: 0.4   // 散度阻尼系数（0~1）        │   │
│  │    boundaryVelDamping: 0.2   // 速度修正阻尼系数（0~1）    │   │
│  │                                                              │   │
│  │  参数效果:                                                   │   │
│  │    boundaryRingWidth → 越大软化区域越厚                      │   │
│  │    boundaryDivDamping → 越小边界越不"不可压"                │   │
│  │    boundaryVelDamping → 越小流体越易滑过边界                │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  实现位置:                                                  │   │
│  │    1. divergenceMat（散度计算）                             │   │
│  │       → dampingFactor = mix(divDamping, 1.0, smoothstep(...))│   │
│  │       → div *= dampingFactor                                │   │
│  │                                                              │   │
│  │    2. velocityCorrectMat（速度修正）                        │   │
│  │       → dampingFactor = mix(velDamping, 1.0, smoothstep(...))│   │
│  │       → pressureGrad *= dampingFactor                       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 技术优势:                                                               │
│  - 成本极低，无需修改核心求解器                                           │
│  - 兼容Jacobi和PCG两种求解器                                               │
│  - 配合 restitution=1.0 + friction=0.0 效果最佳                         │
│  - 模拟"流体-固体相互作用分离处理"效果                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】爆炸随机扰动系统 ⭐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        爆炸随机扰动系统（可选启用）                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【核心原理】模拟真实爆炸的三个层次不均匀性：                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  1. 爆源各向异性 → 噪声调制散度强度 → 指状突出                        │   │
│  │  2. 切向速度扰动 → 噪声驱动切线方向 → 涡旋效果                        │   │
│  │  3. 多团块爆炸 → 分裂成N个子团块 → 碎片效果                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【阶段A】噪声纹理生成（双通道解耦，构造时一次性生成）                                    │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  generateNoiseTexture(64)                                   │   │
│  │  → hash1 = x×374761393 + y×668265263 (R通道)   │   │
│  │  → R: 4八度叠加基础扰动，G: 独立方向扰动                           │   │
│  │  → RepeatWrapping + LinearFilter                            │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                           │
│                              ▼                                           │
│  【阶段B】散度源扰动（explosionDivMat，含随机相位偏移）                                  │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  uniform sampler2D noiseTex;                                │   │
│  │  uniform float perturbationStrength;  // 推荐 0.3~0.5       │   │
│  │                                                              │   │
│  │  perturb = texture2D(noiseTex, uv×8.0 + noiseOffset).r        │   │
│  │  perturb = (perturb - 0.5) * 2.0;  // -1..1                │   │
│  │  noiseMask = 1.0 + perturb * perturbationStrength;          │   │
│  │  mask = smoothstep_mask * noiseMask;                        │   │
│  │  mask = clamp(mask, 0.0, 1.0);  // 防止负值                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                           │
│                              ▼                                           │
│  【阶段C】切向速度扰动（waterVelInitMat，使用未扰动速度）                                │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  vec2 tangent = vec2(dir.y, -dir.x);  // 垂直于径向         │   │
│  │  tangentStrength = (noise.g - 0.5) * 2.0;                   │   │
│  │  vel += tangent × tangentStrength × baseSpeed × 0.3            │   │
│  │  // 切向强度约为径向的30%，避免过度扰动                     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                           │
│                              ▼                                           │
│  【阶段D】多团块爆炸（explodeFragmented，支持确定性种子）                               │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  public explodeFragmented(cx, cy, radius, strength,
                         createWater, duration,
                         fragmentCount=4, seed?)   │   │
│  │      for i in fragmentCount (默认4):                       │   │
│  │          angle = random(0, 2π)                             │   │
│  │          offset = radius × 0.3 × random()                  │   │
│  │          fx = cx + cos(angle) × offset                     │   │
│  │          fy = cy + sin(angle) × offset                     │   │
│  │          fr = radius × (0.6 + 0.4×random())                │   │
│  │          fs = strength/N × (0.8 + 0.4×random())            │   │
│  │          explode(fx, fy, fr, fs, ...)                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 改进要点:                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  - 双通道解耦: R/G使用独立哈希函数，降低相关性               │   │
│  │  - 相位偏移: 每次爆炸独立noiseOffset，避免重复感           │   │
│  │  - 切向解耦: 使用未扰动baseSpeed，避免正反馈                 │   │
│  │  - 确定性重放: 支持seed参数，便于调试和可重复播放           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 预期效果: 爆炸不再是均匀圆环，而是充满指状突出、涡旋、碎片的不规则冲击波 │
  ✅ 性能影响: 仅增加一张64×64噪声纹理 + 2~3次纹理采样，几乎无开销           │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】各向异性爆炸系统 ⭐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        各向异性爆炸系统（方向性爆炸效果）                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【核心原理】通过方向调制函数实现不同方向的膨胀/收缩效果                          │
│                                                                             │
│  【三种模式】                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  模式0: 各向同性 (默认)                                                │   │
│  │        → dirMod = 1.0 → 正常均匀爆炸                                  │   │
│  │                                                                     │   │
│  │  模式1: 四极子 (Quadrupole)                                          │   │
│  │        → dirMod = cos(2*angle)                                       │   │
│  │        → 两个方向膨胀，两个方向收缩（如: 左右膨胀，上下收缩）            │   │
│  │                                                                     │   │
│  │  模式2: 偶极子 (Dipole)                                              │   │
│  │        → dirMod = cos(angle)                                         │   │
│  │        → 一侧膨胀，对侧收缩（如: 右侧膨胀，左侧收缩）                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【关键设计】收缩阻尼（避免收缩强于膨胀）                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  问题: 收缩区域视觉效果强于膨胀区域                                    │   │
│  │  解决方案: shrinkDamping = 0.3 (收缩减弱70%)                         │   │
│  │                                                                     │   │
│  │  dirMod = dirMod > 0.0 ? dirMod : dirMod * shrinkDamping            │   │
│  │         → 膨胀区域保持原值，收缩区域减弱强度                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【接口方法】                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  public explodeAnisotropic(cx, cy, radius, strength,               │   │
│  │                             createWater=true, duration=0.1,        │   │
│  │                             mode=0, phase=0, anisoStrength=0.0)    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【参数说明】                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  mode:        各向异性模式 (0/1/2)                                   │   │
│  │  phase:       相位偏移（弧度），旋转各向异性方向                       │   │
│  │  anisoStrength: 各向异性强度 (0~1)，0=关闭，1=完全各向异性           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【使用示例】                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  // 四极子爆炸：左右向外，上下向内                                   │   │
│  │  explodeAnisotropic(0.5, 0.5, 0.15, 8000, true, 0.1, 1, 0.0, 1.0) │   │
│  │                                                                     │   │
│  │  // 偶极子爆炸：右侧膨胀，左侧收缩（相位旋转90°）                     │   │
│  │  explodeAnisotropic(0.5, 0.5, 0.15, 8000, true, 0.1, 2, π/2, 1.0) │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 适用场景: 击打效果、定向冲击波、水流喷射等需要方向性的爆炸效果              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】多次爆炸调度改进 💥

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        多次爆炸调度优化方案                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【调度策略】                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  第1次爆炸: createWater=true  → 生成水花 + 注入散度                 │   │
│  │  第2-5次爆炸: createWater=false → 仅注入散度，不生成新水            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【位置偏移】避免完美同心圆                                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  cx = centerX + random(-0.05, 0.05)  // 随机偏移 ±0.05            │   │
│  │  cy = centerY + random(-0.05, 0.05)                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【强度序列】斐波那契式递减                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  strengths = [25000, 20000, 15000, 10000, 5000]                  │   │
│  │  后一次强度 = 前一次 × 0.8（递减）                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 效果: 避免水体体积失控，产生自然衰减的爆炸序列                         │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】稳定性修复 ⚠️

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        稳定性修复（防止爆炸"把水炸没"）                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  1. 速度上限限制（velocityCorrectMat + waterVelInitMat）   │   │
│  │     → float maxVel = 200.0;                                │   │
│  │     → if (length(vel) > maxVel) vel *= maxVel/length(vel); │   │
│  │     → 防止超高速度导致 CFL 条件失效                         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  2. phi 场 clamp（levelSetAdvectionMat）                    │   │
│  │     → phi = clamp(phi, -1.0, 1.0);                         │   │
│  │     → 防止平流后 phi 大面积变负                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  3. 水体销毁修复（dissipationMat）                          │   │
│  │     → 销毁时 phi = 1.0（原为 0.05）                         │   │
│  │     → 确保被销毁水体正确标记为空气                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】轻量级流体实体 ⭐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        轻量级流体实体系统（LightFluidEntity）                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【核心概念】将流体模拟封装为独立实体，可被 EntityManager 统一管理               │
│                                                                             │
│  【架构设计】                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  LightFluidEntity (实体层)                                          │   │
│  │  ├─ 继承 Entity 基类                                               │   │
│  │  ├─ 持有 THREE.Mesh (可视化)                                       │   │
│  │  ├─ 包含内部 FluidSimulator (32x32)                               │   │
│  │  ├─ lod: FluidLOD (LOD等级)                                        │   │
│  │  └─ maxAge: number (寿命，默认5秒)                                 │   │
│  │                                                                     │   │
│  │  EntityManager (管理层)                                            │   │
│  │  ├─ setRenderer() - 设置渲染器                                     │   │
│  │  ├─ createDroplets() - 批量创建液滴 (异步)                         │   │
│  │  ├─ createDroplet() - 创建单个液滴 (异步)                          │   │
│  │  ├─ getDroplets() - 获取所有液滴                                   │   │
│  │  └─ updateWithPlayer() - 带玩家位置的更新（LOD计算）               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【生命周期】                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  1. 创建 → 初始化内部模拟器 + 设置初始水量                           │   │
│  │  2. 更新 → 应用物理力 + LOD控制 + 内部流体模拟 + 位置同步          │   │
│  │  3. 销毁 → 自动释放模拟器、几何体、材质资源                         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【关键参数】                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  waterVolume:   水量占比 (0~1)，默认0.45                           │   │
│  │  maxAge:        最大生命周期（秒），默认5秒                         │   │
│  │  worldVelocity: 世界空间速度，供外部物理使用                        │   │
│  │  lod:           LOD等级 (HIGH/MEDIUM/LOW/OFF)                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【LOD控制的更新流程】                                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  update(delta):                                                    │   │
│  │    ├─ age += delta (始终维护生命周期)                               │   │
│  │    ├─ if (age > maxAge) → isActive = false                         │   │
│  │    ├─ if (lod == OFF) → mesh.visible = false; return               │   │
│  │    ├─ position += velocity × delta (始终更新位置)                   │   │
│  │    ├─ simUpdateAccumulated += delta (累积时间步)                    │   │
│  │    └─ if (accumulated >= interval):                               │   │
│  │         ├─ accel = (vel - prevVel) / accumulated                   │   │
│  │         ├─ simulator.update(accumulated)                           │   │
│  │         └─ accumulated = 0                                         │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 适用场景: 水滴下落、流体碎片、独立流体粒子等小型流体效果                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】流体区域管理器 ⭐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        流体区域管理器（FluidRegionManager）                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【核心概念】管理一片区域内的多个小液滴，支持基于距离的LOD性能优化              │
│                                                                             │
│  【架构关系】                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │     EntityManager                                                   │   │
│  │           │                                                         │   │
│  │           ▼                                                         │   │
│  │     FluidRegionManager ─────────────────────┐                       │   │
│  │           │                                 │                       │   │
│  │           │ 管理                            │                       │   │
│  │           ▼                                 ▼                       │   │
│  │     ┌──────────┐                    ┌──────────┐                    │   │
│  │     │LightFluid│                    │LightFluid│  ...               │   │
│  │     │ Entity 1 │                    │ Entity N │                    │   │
│  │     └──────────┘                    └──────────┘                    │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【LOD等级定义】                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  ┌──────┬────────────┬──────────────┬─────────────┬─────────────┐  │   │
│  │  │ LOD  │ 距离条件    │ 位移更新频率  │ 内部模拟频率 │ 纹理刷新    │  │   │
│  │  ├──────┼────────────┼──────────────┼─────────────┼─────────────┤  │   │
│  │  │ HIGH │ ≤3 单位     │ 每帧         │ 每4帧       │ 启用       │  │   │
│  │  │ MED  │ ≤6 单位     │ 每帧         │ 每4帧       │ 启用       │  │   │
│  │  │ LOW  │ ≤10 单位    │ 每帧         │ 每4帧       │ 冻结       │  │   │
│  │  │ OFF  │ >10 单位    │ -           │ -           │ 销毁区域   │  │   │
│  │  └──────┴────────────┴──────────────┴─────────────┴─────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【安全更新策略】避免迭代时修改数组导致的运行时错误                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  FluidRegionManager.update(delta, lod):                            │   │
│  │    ├─ 过滤已失效的液滴                                              │   │
│  │    ├─ if (lod == OFF):                                             │   │
│  │    │     ├─ markedForRemoval = true                                │   │
│  │    │     └─ for all droplets: isActive = false                     │   │
│  │    └─ else:                                                        │   │
│  │          ├─ markedForRemoval = false                               │   │
│  │          └─ for all droplets: droplet.lod = lod                    │   │
│  │                                                                     │   │
│  │  EntityManager.updateFluidRegions():                               │   │
│  │    ├─ 收集 regionsToRemove (标记为待移除的区域)                    │   │
│  │    └─ 在循环外部安全删除                                          │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 核心优势:                                                               │
│  - 安全删除: 标记-延迟删除机制，避免迭代时修改集合                          │
│  - 性能优化: 距离过远时自动销毁区域，释放资源                              │
│  - 职责分离: FluidRegionManager 设置LOD，实体自己处理更新                  │
│  - 累积时间: 使用累积时间步计算加速度，跳帧时仍保持准确                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】循环依赖解决 ⭐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        循环依赖解决（动态导入）                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【问题背景】                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  EntityManager 需要导入 FluidRegionManager                          │   │
│  │  FluidRegionManager 需要导入 EntityManager                          │   │
│  │  → 编译时循环依赖错误                                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【解决方案：动态导入】                                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  // EntityManager.ts                                                │   │
│  │  let FluidRegionManager: typeof import('@entities/fluid').          │   │
│  │                          FluidRegionManager;                        │   │
│  │  let FluidLOD: typeof import('@entities/fluid').FluidLOD;          │   │
│  │                                                                     │   │
│  │  // 在需要时动态加载                                                │   │
│  │  async someMethod() {                                              │   │
│  │      if (!FluidRegionManager) {                                     │   │
│  │          const module = await import('@entities/fluid');            │   │
│  │          FluidRegionManager = module.FluidRegionManager;            │   │
│  │          FluidLOD = module.FluidLOD;                                │   │
│  │      }                                                             │   │
│  │      // 使用 FluidRegionManager                                     │   │
│  │  }                                                                 │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 效果: 编译时不依赖，运行时按需加载，打破循环依赖                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 【扩展功能】WebGL 反馈循环修复 ⚠️

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        WebGL 反馈循环错误修复                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【问题原因】                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  多个 FluidSimulator 实例创建冲突的 WebGL 资源                        │   │
│  │  → GL_INVALID_OPERATION: Feedback loop formed between Framebuffer   │   │
│  │    and active Texture                                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【解决方案：单例初始化】                                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  // EntityManager.ts                                                │   │
│  │  private hasInitializedFluidRegion = false;                         │   │
│  │                                                                     │   │
│  │  public setRenderer(renderer: THREE.WebGLRenderer): void {          │   │
│  │      this.renderer = renderer;                                      │   │
│  │                                                                     │   │
│  │      // 只初始化一次流体区域                                         │   │
│  │      if (!this.hasInitializedFluidRegion) {                         │   │
│  │          this.hasInitializedFluidRegion = true;                     │   │
│  │          this.initDefaultFluidRegion();                             │   │
│  │      }                                                             │   │
│  │  }                                                                 │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ✅ 效果: 确保只创建一个 FluidRegionManager，避免 WebGL 资源冲突              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、分层渲染架构详解

### 3.1 分层渲染设计原则

传统渲染方式将所有效果直接累加到颜色中，导致颜色过曝、整体发白。分层渲染将颜色计算拆解为多个独立层，各层使用合理的混合方式组合，最终叠加形成最终颜色。

### 3.2 分层架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        分层渲染架构图                                        │
└─────────────────────────────────────────────────────────────────────────────┘

        ┌─────────────────────────────────────┐
        │     Layer 1: 基础颜色层              │
        │     Base Color Layer                │
        │                                     │
        │  depth = clamp(-phi * 2.0, 0, 1)   │
        │  baseColor = mix(waterColor,       │
        │               deepColor, depth)     │
        └─────────────────┬───────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │     Layer 2: 漫反射光照层             │
        │     Diffuse Lighting Layer          │
        │                                     │
        │  normal = normalize(∇phi)          │
        │  diff = max(0.1, dot(normal, L))   │
        │  diffuse = baseColor × diff        │
        └─────────────────┬───────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │     Layer 3: 高光层                 │
        │     Specular Lighting Layer         │
        │                                     │
        │  viewDir = (0, 0, 1)               │
        │  halfDir = normalize(L + viewDir) │
        │  spec = pow(max(dot(n,h),0), 64)  │
        │  specular = white × spec ×         │
        │             specularIntensity      │
        └─────────────────┬───────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │     Layer 4: 边缘发光层              │
        │     Edge Glow / Emissive Layer      │
        │                                     │
        │  edge = 1 - smoothstep(0, edgeW,   │
        │                    abs(phi))        │
        │  glowColor = vec3(0.2, 0.6, 1.0)   │
        │  emissive = glowColor × edge ×     │
        │             edgeIntensity          │
        └─────────────────┬───────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │     Layer 5: 流动扰动层              │
        │     Flow Detail Layer               │
        │                                     │
        │  flow = length(velocity) × 0.5     │
        │  flowColor = vec3(0.15, 0.25,      │
        │               0.35) × flow ×       │
        │               flowIntensity        │
        └─────────────────┬───────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │     最终合成                        │
        │     Final Composition               │
        │                                     │
        │  finalColor = diffuse + specular +  │
        │               emissive + flowColor │
        │                                     │
        │  alpha = clamp(0.6 - phi*2, 0.2,    │
        │              0.9)                   │
        └─────────────────────────────────────┘
```

### 3.3 各层说明

| 层名称 | 内容 | 混合方式 | 说明 |
|--------|------|----------|------|
| Base Color | 水体的基础颜色 | 直接输出 | 根据深度混合浅蓝和深蓝 |
| Diffuse Lighting | 漫反射光照 | 乘以基础色 | 决定水体明暗，不添加额外颜色 |
| Specular | 高光反射 | 最后添加 | 白色高光，可独立控制强度 |
| Emissive | 边缘发光 | 最后叠加 | 仅在水面附近生效，不参与漫反射 |
| Flow Detail | 流动扰动 | 作为细节 | 不大面积改变色调 |

### 3.4 最终合成公式

```
finalColor = diffuse + specular + emissive + flowColor
           = (baseColor × diff) + (white × spec × specInt) + 
             (glowColor × edge × edgeInt) + (flowBaseColor × flow × flowInt)

alpha = clamp(0.6 - phi × 2.0, 0.2, 0.9)
```

### 3.5 渲染决策

```glsl
// 空气区域和界面：完全丢弃，不参与渲染
// phi >= 0 表示空气或水-空气界面（phi = 0），都不绘制
if (phi >= 0.0) {
    discard;
}

// 流体区域：渲染各层
float depth = clamp(-phi * 2.0, 0.0, 1.0);
vec3 baseColor = mix(waterColor, deepColor, depth);

// ... 计算各层并叠加
```

### 3.6 分层渲染优势

1. **互不干扰**：漫反射、高光、发光互相独立，修改边缘光强度不会影响光照强度
2. **易于调试**：可以暂时关闭某一层来观察其它层效果
3. **可扩展**：未来增加屏幕空间反射、折射等效果，只需额外添加层，不影响已有层
4. **性能不变**：依然是一次绘制，只是计算逻辑更清晰

---

## 四、纹理格式规范

### 4.1 速度场纹理 (velTexA/B)
```
RGBA: [vx, vy, 0, 1]
- R: 水平速度分量
- G: 垂直速度分量
- B: 未使用
- A: 未使用（固定为1）
```

### 4.2 Level Set 纹理 (phiTexA/B)
```
RGBA: [phi, 0, 0, 1]
- R: Level Set 值
  - phi > 0: 空气区域
  - phi < 0: 水体区域
  - phi = 0: 自由表面
- G, B: 未使用
- A: 未使用（固定为1）
```

### 4.3 压力场纹理 (pressureTexA/B)
```
RGBA: [p, 0, 0, 1]
- R: 压力值
- G, B: 未使用
- A: 未使用（固定为1）
```

### 4.4 固体掩码纹理 (solidMaskTex)
```
RGBA: [mask, 0, 0, 1]
- R: 固体标记
  - mask > 0.5: 固体
  - mask <= 0.5: 流体/空气
- G, B: 未使用
- A: 未使用
```

### 4.5 固体法线纹理 (solidNormalTex)
```
RGBA: [nx, ny, 0, 1]
- R: 法线 X 分量
- G: 法线 Y 分量
- B: 未使用
- A: 未使用（固定为1）
- 预计算后每帧直接读取，无需重复计算
```

---

## 五、关键优化点

### 5.1 预计算固体法线
- 固体法线在 solidMask 改变时计算一次
- 存储到 solidNormalTex
- 每帧墙碰撞直接读取，无需重复计算梯度

### 5.2 双缓冲 Ping-Pong
- 速度场、Level Set、压力场都使用双缓冲
- 避免读写冲突，提高 GPU 并行度

### 5.3 固体边界清理
- 第 1 步和第 10 步清理固体内部的速度和 Level Set
- 确保固体内部不会残留错误的速度或 Level Set 值

### 5.4 压力迭代的自由表面边界条件
- 当 phi > 0（空气）或 isSolid > 0.5（固体）时，压力强制设为 0
- 避免空气区域建立压力场，提高体积守恒

### 5.5 流体区域判断容差
- 使用 phi >= 0.0 丢弃空气和界面（phi = 0 是水-空气界面）
- 避免线性纹理过滤导致的边缘锯齿

---

## 六、渲染输出接口

```typescript
// 获取 Level Set 纹理（用于渲染）
getLevelSetTexture(): THREE.Texture {
    return this.curPhiTex.texture;
}

// 获取速度场纹理（用于渲染）
getVelocityTexture(): THREE.Texture {
    return this.curVelTex.texture;
}

// 获取固体掩码纹理
getSolidMaskTexture(): THREE.Texture | null {
    return this.solidMaskTex;
}
```

---

## 七、调试录制功能

### 7.1 功能概述

FluidSimulator 内置了调试录制功能，可自动录制前 N 帧的关键场数据（phi、velX、velY）并导出为 JSON 文件，用于分析模拟过程中的数值变化和调试问题。

### 7.2 调试录制工作流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         调试录制流程                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 启用录制                                                                │
│     enableDebugRecording(true, 20)  // 录制前20帧                           │
│                    │                                                        │
│                    ▼                                                        │
│  2. 每帧 update() 末尾自动捕获                                               │
│     ┌───────────────────────────────────────────────────────────────────┐   │
│     │  if (debugRecordingEnabled && debugCurrentFrame < framesToRecord)│   │
│     │      captureCurrentState()                                       │   │
│     │      debugCurrentFrame++                                         │   │
│     │      if (debugCurrentFrame >= framesToRecord) {                  │   │
│     │          debugRecordingEnabled = false                           │   │
│     │          downloadDebugData()  // 自动下载                         │   │
│     │      }                                                           │   │
│     └───────────────────────────────────────────────────────────────────┘   │
│                    │                                                        │
│                    ▼                                                        │
│  3. 数据捕获                                                               │
│     ┌───────────────────────────────────────────────────────────────────┐   │
│     │  captureCurrentState():                                           │   │
│     │      phiPixels = readTextureData(curPhiTex)                       │   │
│     │      phiData = textureTo2DArray(phiPixels, 'r')                   │   │
│     │                                                                   │   │
│     │      velPixels = readTextureData(curVelTex)                       │   │
│     │      velXData = textureTo2DArray(velPixels, 'r')                  │   │
│     │      velYData = textureTo2DArray(velPixels, 'g')                  │   │
│     │                                                                   │   │
│     │      debugRecordedData.push({ frame, phi, velX, velY })           │   │
│     └───────────────────────────────────────────────────────────────────┘   │
│                    │                                                        │
│                    ▼                                                        │
│  4. 自动导出 JSON 文件                                                      │
│     downloadDebugData() → 浏览器下载 fluid_debug_xxx.json                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.3 调试接口

| 方法 | 功能 | 参数 |
|------|------|------|
| `enableDebugRecording(enable, frames)` | 启用/禁用调试录制 | `enable`: boolean, `frames`: number (默认20) |
| `exportDebugData()` | 手动触发数据导出 | 无 |

### 7.4 使用示例

```typescript
const simulator = new FluidSimulator(renderer, params);

// 启用录制，记录前20帧
simulator.enableDebugRecording(true, 20);

// 正常驱动模拟
function animate() {
    simulator.update(0.016);
    // ... 渲染逻辑
    requestAnimationFrame(animate);
}
animate();

// 运行后经过20帧，浏览器会自动下载 fluid_debug_xxx.json
```

### 7.5 导出数据格式

```json
{
    "resolution": { "width": 256, "height": 256 },
    "frames": [
        {
            "frame": 0,
            "phi": [[...], [...]],      // 二维数组 [y][x], Level Set 值
            "velX": [[...], [...]],     // 二维数组 [y][x], X 速度分量
            "velY": [[...], [...]]      // 二维数组 [y][x], Y 速度分量
        }
        // ... 更多帧
    ],
    "params": {
        "density": 1000,
        "viscosity": 0.001,
        "surfaceTension": 0.072,
        "gravity": 9.8,
        "pressureIterations": 30,
         "reinitIterations": 5,
         "timeStep": 0.002,
         "usePCG": true
    }
}
```

### 7.6 调试字段说明

| 字段 | 说明 | 数据范围 |
|------|------|----------|
| `phi` | Level Set 场 | phi < 0: 水体, phi > 0: 空气, phi ≈ 0: 自由表面 |
| `velX` | 水平速度分量 | -∞ ~ +∞ |
| `velY` | 垂直速度分量 | -∞ ~ +∞ |

### 7.7 性能特点

- **仅录制时读取纹理**：不录制时无性能开销
- **同步阻塞读取**：录制帧数少（20帧），可接受短暂阻塞
- **自动停止**：达到帧数后自动禁用录制，避免持续性能影响

---

## 八、近期优化更新

### 8.1 流动扰动层优化 ⭐

**问题**：高速度时流动扰动层会导致纹理变白

**解决方案**：降低速度敏感度系数

```glsl
// 修改前
float flow = length(vel) * 0.5;

// 修改后
float flow = length(vel) * 0.02;  // 降低速度敏感度，避免高速度时变白
```

**效果对比**：
- 速度为 100 时：
  - 修改前：`flow = 100 * 0.5 = 50`，颜色过亮甚至变白
  - 修改后：`flow = 100 * 0.02 = 2`，颜色变化柔和自然

### 8.2 纹理更新控制 ⭐

支持动态控制渲染纹理的更新，用于性能优化（如 LOD 系统）。

#### 核心接口

| 方法 | 说明 |
|------|------|
| `setTextureUpdateEnabled(enabled: boolean)` | 启用/禁用纹理更新 |
| `getRenderMaterial()` | 获取渲染材质（根据更新标志决定是否更新纹理引用） |

#### 实现原理

```typescript
private textureUpdateEnabled = true;

public setTextureUpdateEnabled(enabled: boolean): void {
    this.textureUpdateEnabled = enabled;
}

public getRenderMaterial(): THREE.ShaderMaterial {
    // 根据纹理更新标志决定是否更新纹理引用
    if (this.textureUpdateEnabled) {
        this.renderMaterial.uniforms.phiTex.value = this.curPhiTex.texture;
        this.renderMaterial.uniforms.velTex.value = this.curVelTex.texture;
    }
    // 否则维持上一次纹理，画面静止
    return this.renderMaterial;
}
```

### 8.3 小流体区域管理器 ⭐

`FluidRegionManager` 用于管理一片区域内的多个小液滴实体（`LightFluidEntity`），支持基于距离的 LOD 性能优化。

#### LOD 等级定义

| LOD | 距离条件 | 位移更新频率 | 内部模拟频率 | 纹理刷新 |
|-----|----------|--------------|--------------|----------|
| HIGH | ≤3 单位 | 每帧 | 每4帧 | 启用 |
| MEDIUM | ≤6 单位 | 每2帧 | 每4帧 | 启用 |
| LOW | ≤10 单位 | 每4帧 | 每4帧 | 冻结 |
| OFF | >10 单位 | - | - | 销毁区域 |

#### 更新策略分离

```typescript
// 位移更新：根据 LOD 动态调整
const positionUpdateInterval = lod === FluidLOD.HIGH ? 1 : 
                                lod === FluidLOD.MEDIUM ? 2 : 4;

// 内部纹理模拟：固定每4帧更新一次
if (this.frameSkipCounter % 4 === 0) {
    // 更新内部流体模拟
}
```

#### 性能优化

1. **距离分级**：远离玩家的液滴区域完全冻结甚至销毁，节省 CPU/GPU
2. **分离更新**：位移和内部模拟采用不同频率，平衡视觉效果和性能
3. **纹理冻结**：LOW 模式下停止纹理刷新，大幅减少 GPU 纹理传输
4. **自动销毁**：距离 >10 单位时自动销毁区域管理器，释放资源
