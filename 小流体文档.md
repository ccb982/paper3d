# LightFluidEntity 小流体类文档

## 概述

`LightFluidEntity` 是一个轻量级流体实体类，继承自 `Entity` 并实现 `IFluidForceTarget` 接口。内部包含一个 32x32 分辨率的 `FluidSimulator`，专门用于模拟水滴、碎片等小型流体效果。

**文件位置**: `src/entities/fluid/LightFluidEntity.ts`

---

## 核心特性

### 1. 独立模拟器
- 内部分辨率: 32x32 像素
- 独立的速度场、压力场、phi 场
- 支持纹理居中追踪（防止水飘出纹理边界）

### 2. 水量与大小换算
```
waterVolume: 0~1，表示水量占比（1 = 完整的32x32纹理被水填满）
纹理中的水半径 = sqrt(waterVolume) * texSize * 0.45
显示大小 = baseScale * sqrt(waterVolume) * sizeMultiplier * randomFactor
```

### 3. 水滴形状
初始形状为泪滴状（顶部尖细，底部圆润）：
- 垂直拉伸系数: 1.3
- 顶部收缩系数: 0.6
- 底部膨胀系数: 1.2

---

## 构造函数参数

```typescript
constructor(
    id: string,                           // 实体唯一标识
    renderer: THREE.WebGLRenderer,         // Three.js 渲染器
    initialPosition?: THREE.Vector3,       // 初始世界位置
    initialVelocity?: THREE.Vector3,       // 初始世界速度
    waterVolume: number = 0.45,           // 水量占比 (0.01 ~ 1.0)
    maxAge: number = 5                    // 最大存活时间（秒）
)
```

---

## 公开属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `waterVolume` | `number` | 水量占比 (0.01 ~ 1.0) |
| `worldVelocity` | `THREE.Vector3` | 当前世界速度 |
| `maxAge` | `number` | 最大存活时间（秒） |
| `lod` | `FluidLOD` | LOD 级别 (HIGH/MEDIUM/LOW/OFF) |
| `radius` | `number` | 碰撞半径 |

---

## 核心方法

### 生命周期

#### `update(delta: number): void`
每帧调用，更新实体状态。
- 维护生命周期 (`age`)
- 更新世界位置
- 执行流体模拟
- 处理外部力

#### `onDestroy(): void`
销毁实体，释放所有资源。
- 释放材质和几何体
- 释放模拟器

### 物理控制

#### `setVelocity(velocity: THREE.Vector3): void`
设置世界速度。

#### `applyForce(force: THREE.Vector3, delta: number): void`
施加持续力。

#### `applyImpulse(impulse: THREE.Vector3): void`
施加瞬时冲量。

#### `setInitialVelocity(vx: number, vy: number): void`
设置模拟器初始速度。

### 流体控制

#### `getSimulator(): FluidSimulator`
获取内部流体模拟器，可直接调用：
- `addVelocityImpulse(vx, vy)` - 速度注入
- `addDivergenceImpulse(div, radius, cx, cy)` - 散度注入
- `addWaterImpulse(amount, radius, cx, cy)` - 水量注入
- `explode(cx, cy, radius, strength, ...)` - 爆炸效果

### 可见性

#### `setCamera(camera: THREE.Camera): void`
设置用于可见性检测的相机。

#### `getIsVisible(): boolean`
获取当前是否在相机视野内。

### 状态查询

#### `isEmpty(): boolean`
判断水量是否低于 0.05。

---

## LOD 系统

| 级别 | 模拟更新频率 | 说明 |
|------|-------------|------|
| `HIGH` | 30 FPS | 高精度模拟 |
| `MEDIUM` | 20 FPS | 中等精度 |
| `LOW` | 12 FPS | 低精度，减少计算 |
| `OFF` | - | 完全禁用模拟 |

---

## IFluidForceTarget 接口

实现流体力的施加接口：

### `applyFluidForce(force: FluidExternalForce): void`
接收外部力并缓存，支持：
- `worldImpulse`: 世界空间冲量
- `worldAcceleration`: 世界空间加速度
- `velocityInjection`: 速度场注入
- `divergenceInjection`: 散度场注入
- `waterInjection`: 水量注入

**注意**: 注入会自动将世界空间转换为局部纹理空间（考虑水滴随机旋转）。

---

## 内部机制

### 子步模拟
为了保持物理精度，使用多子步模拟：
```typescript
simTimeStep = 0.008 // 8毫秒
// 根据累积时间执行多个子步，最多10步
```

### 惯性挤压
当水滴加速运动时，会在加速反方向注入散度，模拟流体惯性。

### 随机微风
HIGH LOD 下，每帧注入随机速度微扰，增加视觉生动性。

### 纹理居中
每 0.5 秒计算水体边界框中心，自动平移 phi/vel 场使水体居中。

---

## 使用示例

### 创建水滴
```typescript
const droplet = new LightFluidEntity(
    'droplet_1',
    renderer,
    new THREE.Vector3(0, 0, 0),
    new THREE.Vector3(0, 5, 0),
    0.5,  // 50% 水量
    10    // 10秒寿命
);

// 设置相机（用于可见性检测）
droplet.setCamera(camera);
```

### 在实体系统中使用
```typescript
entityManager.addEntity(droplet);

// 施加爆炸力
const force = new FluidExternalForce();
force.divergenceInjection = {
    divergence: -5000,
    radius: 0.2,
    centerUV: new THREE.Vector2(0.5, 0.5)
};
droplet.applyFluidForce(force);
```

### 直接控制模拟器
```typescript
const sim = droplet.getSimulator();

// 添加爆炸效果
sim.explode(0.5, 0.5, 0.15, 10000, true, 0.3);

// 添加速度扰动
sim.addVelocityImpulse(2.0, 1.0);
```

---

## 性能优化

1. **低分辨率**: 32x32 纹理，减少 GPU 开销
2. **LOD 系统**: 根据重要性动态调整更新频率
3. **可见性裁剪**: 不可见时暂停模拟
4. **禁用 PCG**: 避免 GPU 回读卡顿
5. **禁用重初始化**: 仅在必要时平滑界面

---

## 注意事项

1. **水消失问题**: 重力过大或表面张力不足可能导致水快速扩散并消失
2. **寿命管理**: 默认 5 秒后自动销毁
3. **资源释放**: 销毁时需调用 `onDestroy()` 释放资源
4. **相机设置**: 需要设置相机才能启用可见性裁剪


