# 自定义子弹形状使用指南

## 概述

本功能允许您将绘画网页导出的JSON形状应用到流体子弹实体（LightFluidEntity），实现自定义子弹外观。

## 文件说明

| 文件 | 说明 |
|------|------|
| `polygonToLevelSet.ts` | 核心转换函数：将多边形转换为Level Set纹理 |
| `applyCustomShape.ts` | 便捷函数：加载并应用JSON形状 |
| `customShapeDemo.ts` | 使用演示：4种不同的使用方式 |
| `public/bullet-shape.json` | 示例形状：子弹形状JSON数据 |

## 快速开始

### 方法1：创建子弹时直接使用自定义形状

```typescript
import { createBulletWithCustomShape } from '@utils/applyCustomShape';

const bullet = await createBulletWithCustomShape(
    renderer,
    new THREE.Vector3(0, 10, 0),  // 初始位置
    new THREE.Vector3(0, -20, 0), // 初始速度
    '/bullet-shape.json'          // JSON文件路径
);
```

### 方法2：先创建子弹，再应用形状

```typescript
import { LightFluidEntity } from '@entities/fluid/LightFluidEntity';
import { applyShapeFromJsonFile } from '@utils/applyCustomShape';

// 创建子弹
const bullet = new LightFluidEntity('bullet1', renderer, position, velocity);

// 应用自定义形状
await applyShapeFromJsonFile(bullet, '/bullet-shape.json');
```

### 方法3：使用内联JSON对象

```typescript
import { applyShapeFromJsonObject } from '@utils/applyCustomShape';

const shapeJson = {
    "version": "1.2",
    "regionAnnotations": [{
        "polygon": [[
            { "x": 0.3, "y": 0.7 },
            { "x": 0.7, "y": 0.7 },
            { "x": 0.5, "y": 0.2 }
        ]]
    }]
};

applyShapeFromJsonObject(bullet, shapeJson);
```

## 完整流程

### 步骤1：在绘画网页中绘制形状

1. 打开绘画网页（`另起的绘画网页`）
2. 使用绘图工具绘制想要的形状
3. 使用"区域注释"工具，点击形状内部
4. 输入注释文本（可选）
5. 点击"导出JSON"按钮

### 步骤2：准备JSON文件

将导出的JSON文件放到项目的 `public/` 目录下：

```bash
cp ~/Downloads/drawing-export-xxx.json public/bullet-shape.json
```

### 步骤3：在代码中使用

```typescript
import { createBulletWithCustomShape } from '@utils/applyCustomShape';

// 在射击系统中
async function shoot(renderer: THREE.WebGLRenderer) {
    const bullet = await createBulletWithCustomShape(
        renderer,
        playerPosition.clone(),
        shootDirection.clone().multiplyScalar(20),
        '/bullet-shape.json'
    );
    
    entityManager.add(bullet);
}
```

## API参考

### polygonToLevelSet.ts

#### `polygonToLevelSetTexture(rings, width, height, normalize?)`

将多边形转换为Level Set纹理。

**参数：**
- `rings`: 多环数组（第一个是外环，后续是内环/孔洞）
- `width`: 纹理宽度
- `height`: 纹理高度
- `normalize`: 是否归一化到纹理中心（默认true）

**返回：**
- `THREE.DataTexture`: Level Set纹理

#### `extractPolygonFromJson(json)`

从绘画导出JSON中提取第一个区域注释的多边形。

#### `loadPolygonFromJson(jsonPath, width, height)`

从JSON文件路径加载并转换为Level Set纹理（异步）。

### applyCustomShape.ts

#### `applyShapeFromJsonFile(bullet, jsonPath)`

从JSON文件路径加载形状并应用到子弹。

**参数：**
- `bullet`: LightFluidEntity实例
- `jsonPath`: JSON文件路径（相对于public目录）

**返回：**
- `Promise<boolean>`: 是否成功

#### `applyShapeFromJsonObject(bullet, jsonObj)`

从JSON对象应用形状到子弹。

#### `createBulletWithCustomShape(renderer, position, velocity, jsonPath)`

创建子弹并直接使用自定义形状。

**返回：**
- `Promise<LightFluidEntity | null>`: 创建的子弹实例

#### `applyShapeToMultipleBullets(bullets, jsonPath)`

批量应用形状到多个子弹。

## 形状设计建议

### 1. 方向

- 子弹的"尖端"应该朝上（纹理坐标系中Y值较小的一端）
- 半圆/钝端应该朝下（运动方向）
- 形状会根据子弹速度自动旋转

### 2. 大小

- 形状会自动缩放到纹理大小（32x32或64x64）
- 建议在绘画网页中使用接近正方形的画布
- 形状的包围盒会被缩放到纹理的80%大小

### 3. 复杂度

- 多边形顶点数建议在50-200之间
- 过多的顶点会增加距离场计算开销
- 形状边缘应该平滑，避免过于尖锐的角

### 4. 孔洞

- 支持多环多边形（带孔洞的形状）
- 绘画网页会自动识别孔洞
- JSON中的polygon数组：第一个是外环，后续是内环

## 性能优化

### 1. 预加载

```typescript
// 游戏初始化时预加载JSON
const bulletShapeJson = await fetch('/bullet-shape.json').then(r => r.json());

// 运行时直接使用
applyShapeFromJsonObject(bullet, bulletShapeJson);
```

### 2. 缓存

```typescript
// 缓存JSON对象
class BulletShapeCache {
    private static cache: Map<string, any> = new Map();
    
    static async get(path: string) {
        if (!this.cache.has(path)) {
            const json = await fetch(path).then(r => r.json());
            this.cache.set(path, json);
        }
        return this.cache.get(path);
    }
}
```

### 3. 批量创建

```typescript
// 使用批量函数减少重复加载
import { applyShapeToMultipleBullets } from '@utils/applyCustomShape';

const bullets = bullets.map(() => new LightFluidEntity(...));
await applyShapeToMultipleBullets(bullets, '/bullet-shape.json');
```

## 调试

### 控制台日志

打开浏览器控制台，可以看到详细的加载日志：

```
[polygonToLevelSet] 成功加载多边形，外环156点，1个环
[LightFluidEntity] 设置自定义多边形形状，外环156点，1个环
[LightFluidEntity] 成功从JSON设置形状: "这是一个子弹的形状，很多边的那一部分是尾部"
```

### 错误处理

如果形状加载失败，会自动回退到默认形状：

```
[applyShapeFromJsonFile] 加载失败: /not-exist.json
[LightFluidEntity] JSON中没有区域注释
⚠️ 使用默认形状
```

## 示例项目

完整示例请参考：

- `src/utils/customShapeDemo.ts` - 4种使用方式的完整演示
- `public/bullet-shape.json` - 子弹形状JSON示例

## 常见问题

### Q: 形状方向不对怎么办？

A: 在绘画网页中旋转形状，或者修改LightFluidEntity的旋转逻辑。

### Q: 形状太小/太大怎么办？

A: 调整LightFluidEntity的 `baseScale` 和 `sizeMultiplier` 参数。

### Q: 如何支持多种形状？

A: 准备多个JSON文件，根据需要选择加载：

```typescript
const shapePaths = {
    bullet: '/bullet-shape.json',
    arrow: '/arrow-shape.json',
    star: '/star-shape.json',
};

await applyShapeFromJsonFile(bullet, shapePaths[type]);
```

### Q: 形状有锯齿怎么办？

A: 增加纹理分辨率（修改LightFluidEntity的texSize），或在绘画时使用更多顶点。

## 技术原理

### Level Set（水平集）

Level Set是一种隐式曲面表示方法，使用距离场函数 φ(x,y)：

- φ < 0：内部（水）
- φ = 0：边界（水面）
- φ > 0：外部（空气）

流体模拟器使用Level Set追踪水面运动，实现真实的流体效果。

### 多边形转距离场

使用有向距离函数（SDF）计算每个像素到多边形边界的距离：

1. 对每条边计算点到线段的有向距离
2. 取最小距离作为该点的距离值
3. 通过卷绕数判断点在内部还是外部

### 孔洞处理

多环多边形的外环和内环使用不同的距离计算：

- 外环：内部为负，外部为正
- 内环：内部为正（空气），外部为负（水）

通过取最大值合并内外环的距离场。

## 更新日志

- **2026-06-09**: 初始版本，支持从绘画JSON加载自定义形状
