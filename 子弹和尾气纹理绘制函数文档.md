# 子弹和尾气纹理绘制函数文档

## 1. 整体架构

### 1.1 核心文件
- **DawnExplosionBulletEntity.ts**：子弹实体类，负责子弹和尾气的创建与绑定
- **BulletTrailTexture.ts**：尾气纹理和几何体的创建函数

### 1.2 工作流程
1. 子弹创建时生成尾气特效
2. 尾气纹理通过Canvas动态生成
3. 尾气几何体使用自定义顶点数据
4. 子弹和尾气通过位置偏移保持绑定
5. 每帧更新尾气位置和朝向

## 2. 纹理绘制函数

### 2.1 `createBulletTrailTexture` 函数

**功能**：创建子弹尾气的纹理，使用Canvas动态生成渐变效果

**参数**：
- `textureManager: TextureManager`：纹理管理器实例
- `width: number = 256`：纹理宽度，默认256
- `height: number = 256`：纹理高度，默认256

**实现细节**：
```typescript
export function createBulletTrailTexture(textureManager: TextureManager, width: number = 256, height: number = 256): void {
  const generator = new CanvasTextureGenerator(width, height, (ctx, w, h) => {
    ctx.clearRect(0, 0, w, h);
    
    // 绘制填充区域（使用归一化的顶点数据）
    ctx.beginPath();
    const firstPoint = normalizedPoints[0];
    ctx.moveTo((firstPoint[0] + 0.5) * w, (-firstPoint[1]) * h);
    
    for (let i = 1; i < normalizedPoints.length; i++) {
      const [x, y] = normalizedPoints[i];
      ctx.lineTo((x + 0.5) * w, (-y) * h);
    }
    
    ctx.closePath();
    
    // 创建渐变效果
    const gradient = ctx.createLinearGradient(w * 0.5, 0, w * 0.5, h);
    gradient.addColorStop(0, 'rgba(1, 1, 3, 1)'); // 头部（末尾）：接近黑色
    gradient.addColorStop(0.5, 'rgba(20, 25, 40, 1)'); // 上半部分结束
    gradient.addColorStop(0.55, 'rgba(60, 25, 40, 1)'); // 过渡开始
    gradient.addColorStop(0.6, 'rgba(127, 24, 39, 1)'); // 过渡中
    gradient.addColorStop(0.65, 'rgba(163, 27, 43, 1)'); // 过渡继续
    gradient.addColorStop(0.7, 'rgba(200, 30, 48, 1)'); // 下半部分开始
    gradient.addColorStop(0.95, 'rgba(200, 30, 48, 1)'); // 下半部分
    gradient.addColorStop(1, 'rgba(254, 122, 145, 1)'); // 最底部：#fe7a91
    
    ctx.fillStyle = gradient;
    ctx.fill();
    
    // 绘制边框
    ctx.strokeStyle = 'rgba(40, 20, 60, 1)'; // 紫色边框
    ctx.lineWidth = 2;
    ctx.stroke();
  });
  
  textureManager.register('bullet-trail', generator);
}
```

**关键点**：
- 使用预定义的顶点数据 `normalizedPoints` 绘制尾气形状
- 应用垂直渐变，从头部的黑色过渡到尾部的红色
- 注册纹理到纹理管理器，键名为 'bullet-trail'

### 2.2 顶点数据处理

**归一化函数**：
```typescript
function normalizePoints(points: [number, number][]): [number, number][] {
  let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
  points.forEach(([x, y]) => {
    minX = Math.min(minX, x);
    minY = Math.min(minY, y);
    maxX = Math.max(maxX, x);
    maxY = Math.max(maxY, y);
  });
  
  const xMid = (minX + maxX) / 2;
  const xRange = maxX - minX || 1; // 防止除以零
  const yRange = maxY - minY || 1; // 防止除以零
  
  // 归一化：以X中点为原点，Y最高点为原点
  // X: (x - xMid) / xRange → 范围从 -0.5 到 0.5
  // Y: (y - maxY) / yRange → 范围从 -1 到 0
  return points.map(([x, y]) => [
    (x - xMid) / xRange,
    (y - maxY) / yRange
  ]);
}
```

**找最高点函数**：
```typescript
function findMaxYPoint(points: [number, number][]): [number, number] {
  let maxYPoint = points[0];
  let maxY = points[0][1];
  
  for (const point of points) {
    if (point[1] > maxY) {
      maxY = point[1];
      maxYPoint = point;
    }
  }
  
  return maxYPoint;
}
```

## 3. 几何体创建函数

### 3.1 `createBulletTrailGeometry` 函数

**功能**：创建尾气的3D几何体，使用三角形拼接技术

**实现细节**：
```typescript
export function createBulletTrailGeometry(): THREE.BufferGeometry {
  const geometry = new THREE.BufferGeometry();
  
  const vertices = [];
  const uvs = [];
  const layerIndices = []; // 用于区分不同层
  
  const layerOffset = 0; // 两层之间无距离，完全对齐
  const scaleFactor = 0.7; // 缩放因子，控制尾气大小
  
  // 创建两层尾气
  for (let layer = 0; layer < 2; layer++) {
    const zOffset = layer * layerOffset;
    
    // 从Y值最大的点到每个顶点创建三角形
    for (let i = 1; i < normalizedPoints.length - 1; i++) {
      const center = normalizedMaxYPoint; // 使用Y值最大的点作为中心点
      const current = normalizedPoints[i];
      const next = normalizedPoints[i + 1];
      
      // 三角形 - 将Y轴映射到Z轴
      const centerZ = 3 + center[1] + zOffset;
      const currentZ = 3 + current[1] + zOffset;
      const nextZ = 3 + next[1] + zOffset;
      
      // 确保没有NaN值
      if (!isNaN(center[0]) && !isNaN(centerZ) &&
          !isNaN(current[0]) && !isNaN(currentZ) &&
          !isNaN(next[0]) && !isNaN(nextZ)) {
        vertices.push(
          center[0] * scaleFactor, 0, centerZ,
          current[0] * scaleFactor, 0, currentZ,
          next[0] * scaleFactor, 0, nextZ
        );
        
        // UVs - 转换回0-1范围
        uvs.push(
          center[0] + 0.5, -center[1],
          current[0] + 0.5, -current[1],
          next[0] + 0.5, -next[1]
        );
        
        // 层索引
        for (let j = 0; j < 3; j++) {
          layerIndices.push(layer);
        }
      }
    }
  }
  
  // 设置几何体属性
  const vertexBuffer = new Float32Array(vertices);
  geometry.setAttribute('position', new THREE.BufferAttribute(vertexBuffer, 3));
  
  const uvBuffer = new Float32Array(uvs);
  geometry.setAttribute('uv', new THREE.BufferAttribute(uvBuffer, 2));
  
  // 添加层索引属性
  const layerBuffer = new Float32Array(layerIndices);
  geometry.setAttribute('layer', new THREE.BufferAttribute(layerBuffer, 1));
  
  // 计算法线
  geometry.computeVertexNormals();
  
  return geometry;
}
```

**关键点**：
- 使用三角形拼接技术创建复杂形状
- 支持两层尾气，通过 `layer` 属性区分
- Z轴范围从2到3，使尾气有一定长度
- 确保顶点数据无NaN值，避免渲染错误

### 3.2 `createBulletTrailMaterial` 函数

**功能**：创建尾气的着色器材质，实现摆动效果和发光效果

**参数**：
- `texture: THREE.Texture`：尾气纹理

**实现细节**：
```typescript
export function createBulletTrailMaterial(texture: THREE.Texture): THREE.ShaderMaterial {
  texture.wrapS = THREE.ClampToEdgeWrapping;
  texture.wrapT = THREE.ClampToEdgeWrapping;
  
  const vertexShader = `
    uniform float uTime;
    uniform float uWobbleIntensity;
    uniform float uWobbleSpeed;
    varying vec2 vUv;
    varying float vPositionZ;
    attribute float layer;
    
    void main() {
      vUv = uv;
      vPositionZ = position.z;
      vec3 pos = position;
      
      // 头部固定，尾部摆动
      float normalizedZ = position.z - 2.0; // 归一化到0-1范围
      float tailFactor = 1.0 - normalizedZ;
      float wobbleAmplitude = pow(tailFactor, 3.0) * uWobbleIntensity * 1.5;
      
      // 根据层索引创建不同的摆动效果
      if (layer == 0.0) {
        // 第一层：快速小幅摆动
        float wobbleX = sin(uTime * uWobbleSpeed * 1.5 + (1.0 - position.z) * 12.0) * wobbleAmplitude * 0.8;
        wobbleX += sin(uTime * uWobbleSpeed * 2.0 + (1.0 - position.z) * 18.0) * wobbleAmplitude * 0.4;
        
        float wobbleY = cos(uTime * uWobbleSpeed * 1.2 + (1.0 - position.z) * 10.0) * wobbleAmplitude * 0.6;
        wobbleY += cos(uTime * uWobbleSpeed * 1.6 + (1.0 - position.z) * 14.0) * wobbleAmplitude * 0.3;
        
        pos.x += wobbleX;
        pos.y += wobbleY;
      } else {
        // 第二层：慢速大幅摆动
        float wobbleX = sin(uTime * uWobbleSpeed * 0.8 + (1.0 - position.z) * 8.0) * wobbleAmplitude * 1.2;
        wobbleX += sin(uTime * uWobbleSpeed * 1.0 + (1.0 - position.z) * 12.0) * wobbleAmplitude * 0.6;
        
        float wobbleY = cos(uTime * uWobbleSpeed * 0.6 + (1.0 - position.z) * 6.0) * wobbleAmplitude * 1.0;
        wobbleY += cos(uTime * uWobbleSpeed * 0.8 + (1.0 - position.z) * 10.0) * wobbleAmplitude * 0.5;
        
        pos.x += wobbleX;
        pos.y += wobbleY;
      }
      
      gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);
    }
  `;
  
  const fragmentShader = `
    uniform sampler2D uTexture;
    uniform float uTime;
    varying vec2 vUv;
    varying float vPositionZ;
    
    void main() {
      vec4 texColor = texture2D(uTexture, vUv);
      
      // 确保完全透明的区域被正确处理
      if (texColor.a < 0.1) {
        discard;
      }
      
      // 为尾气前5%的部分添加发光效果，只在中心区域
      float normalizedZ = vPositionZ - 2.0; // 归一化到0-1范围
      if (normalizedZ > 0.95 && abs(vUv.x - 0.5) < 0.1) {
        float glowIntensity = (normalizedZ - 0.95) * 20.0; // 从0到1的强度
        float widthIntensity = 1.0 - abs(vUv.x - 0.5) / 0.1;
        vec3 glowColor = vec3(0.8, 0.6, 0.8); // 粉色发光
        texColor.rgb = mix(texColor.rgb, glowColor, glowIntensity * widthIntensity * 0.5);
        texColor.a = mix(texColor.a, 1.0, glowIntensity * widthIntensity * 0.3);
      }
      
      gl_FragColor = texColor;
    }
  `;
  
  return new THREE.ShaderMaterial({
    uniforms: {
      uTexture: { value: texture },
      uTime: { value: 0 },
      uWobbleIntensity: { value: 0.1 },
      uWobbleSpeed: { value: 4.0 }
    },
    vertexShader,
    fragmentShader,
    transparent: true,
    side: THREE.DoubleSide
  });
}
```

**关键点**：
- 实现两层不同的摆动效果，增强视觉层次感
- 尾部摆动幅度大于头部，模拟火焰自然效果
- 为尾气前端添加粉色发光效果
- 支持纹理透明度，实现半透明效果

## 4. 子弹和尾气的绑定

### 4.1 初始化绑定

**在 `DawnExplosionBulletEntity` 构造函数中**：
```typescript
constructor(
  position: THREE.Vector3,
  direction: THREE.Vector3,
  speed: number,
  color: number = 0xdd5b42
) {
  super(position, direction, speed, color);
  // 设置更高的伤害值
  this.setDamage(2);
  
  // 创建尾气特效（前端附着在子弹上，后端独立）
  this.createTrailEffect();
  // 修改子弹材质，添加前端尾气效果
  this.modifyBulletMaterial();
}
```

**创建尾气特效**：
```typescript
private createTrailEffect(): void {
  const textureManager = new TextureManager();
  
  // 创建尾气纹理
  createBulletTrailTexture(textureManager);
  const texture = textureManager.getTexture('bullet-trail');
  
  if (!texture) return;
  
  // 创建尾气几何体和材质
  const geometry = createBulletTrailGeometry();
  this.trailMaterial = createBulletTrailMaterial(texture);
  
  // 创建尾气网格
  this.trailMesh = new THREE.Mesh(geometry, this.trailMaterial);
  
  // 增大尾气大小（缩放为原来的3倍）
  this.trailMesh.scale.set(3, 3, 3);
  
  // 添加到场景
  const scene = EntityManager.getInstance().getScene();
  if (scene) {
    scene.add(this.trailMesh);
  }
  
  // 初始化尾气位置和朝向
  const flightDir = this.velocity.clone().normalize();
  const distance = 8.5;
  this.trailMesh.position.copy(this.position.clone().add(flightDir.clone().multiplyScalar(-distance)));
  this.trailMesh.quaternion.copy(this.mesh.quaternion);
}
```

### 4.2 每帧更新绑定

**在 `update` 方法中**：
```typescript
public update(delta: number): void {
  super.update(delta); // 更新子弹位置、朝向等
  
  // 更新尾气时间
  this.trailTime += delta;
  
  // 更新子弹前端尾气纹理动画
  if (this.mesh.material instanceof THREE.ShaderMaterial) {
    this.mesh.material.uniforms.time.value = this.trailTime;
  }
  
  if (this.trailMesh && this.trailMaterial) {
    const flightDir = this.velocity.clone().normalize();
    
    // 强制尾气位置：放在子弹后方固定距离
    const distance = 8.5; // 尾气与子弹中心的距离
    const trailPos = this.position.clone().add(flightDir.clone().multiplyScalar(-distance));
    this.trailMesh.position.copy(trailPos);
    
    // 强制尾气朝向：与子弹朝向相同
    this.trailMesh.quaternion.copy(this.mesh.quaternion);
    
    // 更新着色器时间
    this.trailMaterial.uniforms.uTime.value = this.trailTime;
  }
}
```

**关键点**：
- 尾气位置通过 `flightDir.clone().multiplyScalar(-distance)` 计算，始终在子弹后方
- 尾气朝向通过 `this.trailMesh.quaternion.copy(this.mesh.quaternion)` 保持与子弹一致
- 每帧更新着色器时间，实现动画效果

### 4.3 子弹材质修改

**添加前端尾气效果**：
```typescript
private modifyBulletMaterial(): void {
  const textureManager = new TextureManager();
  
  // 创建尾气纹理用于子弹前端
  createBulletTrailTexture(textureManager);
  const trailTexture = textureManager.getTexture('bullet-trail');
  
  if (!trailTexture) return;
  
  // 创建新的子弹材质，添加尾气纹理作为前端效果
  const bulletMaterial = new THREE.ShaderMaterial({
    uniforms: {
      baseColor: { value: new THREE.Color(this.mesh.material instanceof THREE.MeshStandardMaterial ? this.mesh.material.color : 0xdd5b42) },
      trailTexture: { value: trailTexture },
      time: { value: 0 },
      speed: { value: this.velocity.length() }
    },
    vertexShader: `
      varying vec2 vUv;
      varying vec3 vPosition;
      
      void main() {
        vUv = uv;
        vPosition = position;
        gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
      }
    `,
    fragmentShader: `
      uniform vec3 baseColor;
      uniform sampler2D trailTexture;
      uniform float time;
      uniform float speed;
      
      varying vec2 vUv;
      varying vec3 vPosition;
      
      void main() {
        // 基础颜色
        vec3 color = baseColor;
        
        // 计算纹理坐标，使用尾气纹理的前30%部分
        float trailLength = 0.3; // 只使用尾气纹理的前30%
        float zNormalized = (vPosition.z + 0.3) / 0.6; // 归一化Z位置到0-1范围
        float textureY = zNormalized * trailLength; // 映射到纹理的前30%
        
        // 添加时间偏移，使纹理流动
        float timeOffset = mod(time * speed * 0.1, trailLength);
        float trailUvY = textureY + timeOffset;
        
        // 确保纹理坐标在0-1范围内
        trailUvY = mod(trailUvY, 1.0);
        
        vec2 trailUv = vec2(vUv.x, trailUvY);
        vec4 trailColor = texture2D(trailTexture, trailUv);
        
        // 混合基础颜色和尾气纹理
        float alpha = trailColor.a * 0.8;
        color = mix(color, trailColor.rgb, alpha);
        
        // 为子弹前端5%的部分添加发光效果
        if (zNormalized > 0.95) {
          float glowIntensity = (zNormalized - 0.95) * 20.0;
          vec3 glowColor = vec3(0.8, 0.6, 0.8); // 粉色发光
          color = mix(color, glowColor, glowIntensity * 0.5);
        }
        
        // 为子弹末端添加红色过渡效果
        if (zNormalized < 0.3) {
          float endIntensity = 1.0 - (zNormalized / 0.3);
          vec3 endColor = vec3(1.0, 0.0, 0.21); // #fe0036 红色
          color = mix(color, endColor, endIntensity * 0.8);
        }
        
        gl_FragColor = vec4(color, 1.0);
      }
    `,
    side: THREE.DoubleSide,
    transparent: true
  });
  
  // 替换子弹材质
  this.mesh.material = bulletMaterial;
}
```

**关键点**：
- 子弹表面使用尾气纹理的前30%，实现前端附着效果
- 添加时间偏移，使纹理流动，增强动态效果
- 为子弹前端添加粉色发光，末端添加红色过渡
- 混合基础颜色和尾气纹理，实现融合效果

## 5. 性能优化

1. **纹理尺寸优化**：
   - 纹理尺寸从512x512减小到256x256，减少GPU负载

2. **噪点效果优化**：
   - 移除噪点计算，提升纹理生成速度

3. **几何体优化**：
   - 确保顶点数据无NaN值，避免渲染错误
   - 跳过 `computeBoundingSphere` 计算，避免NaN错误

4. **动画优化**：
   - 使用着色器实现摆动效果，利用GPU并行计算
   - 每帧更新最小化，只更新必要的uniform变量

## 6. 代码调用示例

### 6.1 创建子弹实体

```typescript
// 在射击系统中创建子弹
const bulletEntity = new DawnExplosionBulletEntity(
  new THREE.Vector3(position.x, position.y, position.z),
  new THREE.Vector3(direction.x, direction.y, direction.z),
  bulletVelocity, // 子弹速度
  0xfd4d74 // 子弹颜色：#fd4d74
);
EntityManager.getInstance().addEntity(bulletEntity);
```

### 6.2 纹理和几何体使用

```typescript
// 单独使用纹理
const textureManager = new TextureManager();
createBulletTrailTexture(textureManager);
const texture = textureManager.getTexture('bullet-trail');

// 单独使用几何体
const geometry = createBulletTrailGeometry();

// 单独使用材质
const material = createBulletTrailMaterial(texture);

// 创建网格
const trailMesh = new THREE.Mesh(geometry, material);
```

## 7. 总结

本实现通过以下技术实现了子弹和尾气的绑定与效果：

1. **动态纹理生成**：使用Canvas创建渐变尾气纹理
2. **自定义几何体**：使用三角形拼接技术创建复杂尾气形状
3. **高级着色器**：实现两层不同的摆动效果和发光效果
4. **位置绑定**：通过每帧更新保持尾气与子弹的相对位置
5. **材质融合**：子弹前端使用尾气纹理，实现视觉上的融合效果

这种实现方式不仅视觉效果丰富，而且性能优化得当，适合在游戏中使用。