# 爆炸库架构设计文档

## 一、概述

爆炸库（Explosion Processor）是一个基于 **1D 球对称可压缩欧拉方程** 的物理模拟库，用于模拟爆炸波的传播和与流体的交互。该库采用模块化设计，支持与游戏引擎的无缝集成。

---

## 二、架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      应用层（游戏逻辑）                          │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                  EntityManager（实体管理器）                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              ExplosionManager（爆炸管理器）               │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │         Explosion1DSolver（物理求解器）          │   │   │
│  │  │  ┌─────────────────────────────────────────┐   │   │   │
│  │  │  │  守恒量 U[ρ, ρu, E]、原变量[ρ, u, P, T]  │   │   │   │
│  │  │  │  有限体积法 + HLLC 通量 + MUSCL 重构      │   │   │   │
│  │  │  └─────────────────────────────────────────┘   │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │       FluidIntegrator（流体集成器）              │   │   │
│  │  │  ┌─────────────────────────────────────────┐   │   │   │
│  │  │  │    FluidExternalForce（外力接口）        │   │   │   │
│  │  │  │    散度注入 + 速度注入 + 水体注入        │   │   │   │
│  │  │  └─────────────────────────────────────────┘   │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │              IFluidForceTarget（目标接口）        │   │   │
│  │  │    静态纹理 / 动态实体 自动区分                   │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责划分

| 模块 | 职责 | 核心功能 |
|------|------|----------|
| **Explosion1DSolver** | 物理求解核心 | 球对称欧拉方程求解、激波捕捉、时间积分 |
| **FluidIntegrator** | 流体交互桥梁 | 构建外力描述符、注入流体模拟器 |
| **ExplosionManager** | 爆炸生命周期管理 | 创建/更新/销毁爆炸、空间查询、目标绑定 |
| **EntityManager** | 实体级集成 | 统一管理游戏实体与爆炸的交互 |

---

## 三、核心接口设计

### 3.1 IFluidForceTarget（目标接口）

所有可被爆炸影响的对象必须实现此接口：

```typescript
interface IFluidForceTarget {
  isMovable(): boolean;              // 是否可移动
  getPosition(): THREE.Vector3;      // 获取世界位置
  getBoundingRadius(): number;       // 获取碰撞半径
  applyFluidForce(force: FluidExternalForce): void;  // 应用外力
}
```

### 3.2 FluidExternalForce（外力描述符）

统一的外力数据结构，同时支持世界空间力和内部流场注入：

```typescript
interface FluidExternalForce {
  worldImpulse?: THREE.Vector3;           // 瞬时冲量
  worldAcceleration?: THREE.Vector3;      // 持续加速度
  velocityInjection?: FluidVelocityInjection;   // 速度场注入
  divergenceInjection?: FluidDivergenceInjection; // 散度注入
  waterInjection?: FluidWaterInjection;   // 水体注入
}
```

### 3.3 ExplosionParams（爆炸参数）

```typescript
interface ExplosionParams {
  energy: number;           // 爆炸能量（焦耳）
  radius: number;          // 初始火球半径（米）
  gamma: number;           // 比热比（空气=1.4）
  ambientP: number;        // 环境压强（帕斯卡）
  ambientRho: number;      // 环境密度（kg/m³）
  ambientT: number;        // 环境温度（开尔文）
}
```

---

## 四、数据流与调用链

### 4.1 创建爆炸流程

```
用户输入 / 游戏事件
        │
        ▼
EntityManager.createExplosion(id, position, radius)
        │
        ├─→ findTargetsInRange()  ──→ 空间查询范围内实体
        │
        ├─→ registerTarget()      ──→ 注册可影响目标
        │
        └─→ ExplosionManager.create()
                    │
                    ├─→ Explosion1DSolver 初始化
                    │
                    └─→ 存储爆炸位置和参数
```

### 4.2 每帧更新流程

```
Game Loop
    │
    ▼
EntityManager.update(deltaTime)
    │
    ├─→ updateEntities()      ──→ 更新普通实体
    │
    └─→ updateExplosions()
            │
            ├─→ ExplosionManager.updateAll()
            │       │
            │       ├─→ 推进爆炸时间 (advanceTo)
            │       │
            │       ├─→ 计算物理量 (getShockRadius, getPressureGradient)
            │       │
            │       ├─→ 构建外力 (buildFluidForce)
            │       │
            │       └─→ 应用到目标 (applyFluidForce)
            │
            └─→ cleanupInactive() ──→ 清理失效爆炸
```

---

## 五、类关系图

```
┌──────────────────┐        ┌──────────────────┐
│  EntityManager   │        │  ExplosionParams │
│  (单例)          │        │  (配置参数)      │
└────────┬─────────┘        └──────────────────┘
         │
         │ 创建/管理
         ▼
┌──────────────────┐        ┌──────────────────┐
│ ExplosionManager │        │ IFluidForceTarget│
│                  │        │  (接口)          │
└────────┬─────────┘        └────────┬─────────┘
         │                           │ 实现
         │ 包含                      ▼
         │              ┌──────────────────┐
         ▼              │  LightFluidEntity│
┌──────────────────┐    │  (流体实体)       │
│Explosion1DSolver │    └──────────────────┘
│  (物理求解器)     │
└────────┬─────────┘
         │ 注入力
         ▼
┌──────────────────┐
│ FluidIntegrator  │
│  (集成器)        │
└────────┬─────────┘
         │ 使用
         ▼
┌──────────────────┐
│FluidExternalForce│
│  (外力描述符)    │
└──────────────────┘
```

---

## 六、核心类详解

### 6.1 Explosion1DSolver

**职责**：求解 1D 球对称欧拉方程，提供物理状态查询接口。

**核心方法**：

| 方法 | 功能 | 返回值 |
|------|------|--------|
| `advanceTo(targetTime)` | 推进到目标时间 | void |
| `getShockRadius()` | 获取激波前沿半径 | number |
| `getShockSpeed()` | 获取激波速度 | number |
| `getPressureGradient(r1, r2)` | 获取压力梯度 | number |
| `getCoreTemperature()` | 获取核心温度 | number |
| `sample(r)` | 采样径向位置的状态 | {rho, u, p, T} |
| `getProfiles()` | 获取全场快照 | {r, rho, u, p, T} |
| `isActive()` | 检查是否活跃 | boolean |
| `destroy()` | 释放资源 | void |

### 6.2 FluidIntegrator

**职责**：将爆炸物理量转换为流体外力，注入到目标对象。

**核心方法**：

| 方法 | 功能 |
|------|------|
| `inject(explosion, x, y)` | 注入爆炸效果 |
| `buildFluidForce(explosion, x, y)` | 构建外力描述符 |
| `setWorldToUVScale(scale)` | 设置世界到UV缩放 |
| `destroy()` | 释放资源 |

### 6.3 ExplosionManager

**职责**：管理多个爆炸实例，处理空间查询和目标绑定。

**核心方法**：

| 方法 | 功能 |
|------|------|
| `create(id, params, x, y, radius)` | 创建爆炸 |
| `registerTarget(target)` | 注册可影响目标 |
| `unregisterTarget(target)` | 注销目标 |
| `updateAll(deltaTime)` | 更新所有爆炸 |
| `cleanupInactive()` | 清理失效爆炸 |
| `clear()` | 清空所有爆炸 |

---

## 七、使用示例

### 7.1 基础用法

```typescript
// 1. 获取实体管理器
const entityManager = EntityManager.getInstance();

// 2. 创建爆炸（自动查询范围内目标）
entityManager.createExplosion(
  'explosion_001',
  new THREE.Vector3(10.0, 5.0, 0),  // 位置
  15.0,                              // 影响范围
  { energy: 2000 }                   // 可选参数
);

// 3. 游戏主循环
function update(deltaTime: number) {
  entityManager.update(deltaTime);
}
```

### 7.2 手动控制爆炸

```typescript
// 创建爆炸管理器
const explosionManager = new ExplosionManager();

// 注册目标
explosionManager.registerTarget(fluidEntity);

// 创建爆炸
const explosion = explosionManager.create(
  'exp1',
  DEFAULT_EXPLOSION_PARAMS,
  0, 0,  // 位置
  10     // 影响范围
);

// 手动推进时间
explosion.advanceTo(0.5);  // 推进到 0.5 秒

// 获取爆炸状态
console.log(explosion.getShockRadius());
console.log(explosion.getCoreTemperature());
```

---

## 八、内存管理

### 8.1 资源释放流程

```
爆炸结束 (isActive() == false)
        │
        ▼
cleanupInactive()
        │
        ├─→ Explosion1DSolver.destroy()
        │       ├─→ active = false
        │       └─→ 释放数组引用 (undefined as any)
        │
        ├─→ FluidIntegrator.destroy()
        │       ├─→ destroyed = true
        │       └─→ 释放目标引用
        │
        └─→ 从 Map 中删除爆炸记录
```

### 8.2 防护机制

| 防护点 | 实现方式 |
|--------|----------|
| 空值检查 | 所有方法入口检查 `destroyed` 和 `target` |
| 幂等销毁 | `destroy()` 可安全调用多次 |
| 资源隔离 | 使用 `WeakMap` 管理集成器，避免内存泄漏 |

---

## 九、性能优化策略

### 9.1 时间步长控制

- **CFL 条件**：\( \Delta t \leq \frac{\Delta r}{\vert u \vert + c} \)
- **最大子步限制**：防止无限循环
- **子循环模式**：物理时间与渲染时间解耦

### 9.2 空间查询优化

- 当前实现：线性搜索（适合少量目标）
- 未来扩展：空间哈希 / 四叉树（适合大量目标）

### 9.3 目标缓存

使用 `WeakMap` 缓存 `FluidIntegrator`，避免重复创建：

```typescript
private integrators: WeakMap<IFluidForceTarget, FluidIntegrator> = new WeakMap();
```

---

## 十、与流体系统的集成

### 10.1 外力注入流程

```
Explosion1DSolver
        │
        ├─→ getPressureGradient() ──→ divergenceInjection
        │
        ├─→ getShockSpeed()       ──→ velocityInjection
        │
        └─→ getCoreTemperature()  ──→ waterInjection
                    │
                    ▼
            FluidIntegrator.buildFluidForce()
                    │
                    ▼
            FluidExternalForce
                    │
                    ▼
            IFluidForceTarget.applyFluidForce()
                    │
                    ├─→ 静态纹理：直接注入流场
                    │
                    └─→ 动态实体：同时应用世界力 + 流场注入
```

### 10.2 自动区分机制

```typescript
// 在 applyFluidForce 中
if (force.worldImpulse && this.isMovable()) {
  // 动态实体：应用世界空间冲量
  this.applyImpulse(force.worldImpulse);
}

if (force.divergenceInjection) {
  // 所有目标：应用流场注入
  this.simulator.addDivergenceImpulse(...);
}
```

---

## 十一、未来扩展方向

| 扩展项 | 说明 |
|--------|------|
| **GPU 加速** | 将物理求解器移植到 WebGL 着色器 |
| **多爆炸叠加** | 支持多个爆炸的叠加影响 |
| **障碍物交互** | 爆炸与场景几何体的碰撞检测 |
| **声音同步** | 与音频系统的同步触发 |
| **粒子系统集成** | 爆炸粒子效果的生成 |

---

## 十二、版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 1.0 | 2026-05 | 初始版本，完成物理求解核心 |
| 1.1 | 2026-05 | 添加流体集成器和外力接口 |
| 1.2 | 2026-05 | 集成到 EntityManager，添加空间查询 |
| 1.3 | 2026-05 | 完善内存管理和防护机制 |

---

## 十三、参考资料

1. Toro, E. F. (2009). *Riemann Solvers and Numerical Methods for Fluid Dynamics*.
2. LeVeque, R. J. (2002). *Finite Volume Methods for Hyperbolic Problems*.
3. 理想气体状态方程与可压缩流动基础
