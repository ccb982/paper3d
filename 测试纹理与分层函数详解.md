# 测试纹理与分层函数详解

## 1. 概述

本文档详细介绍 `TestRedBlueTexture` 测试纹理生成器的实现原理、样式效果，以及 `GradientTexture` 分层函数的技术细节。

## 2. 测试纹理 (TestRedBlueTexture)

### 2.1 基本信息

| 属性 | 值 |
|------|-----|
| **纹理名称** | test-red-blue |
| **纹理类型** | Canvas（画布生成） |
| **分辨率** | 8 × 8 像素 |
| **颜色方案** | 红蓝双色，带有渐变分层 |
| **更新方式** | 动态（每帧更新，实现流动效果） |

### 2.2 核心实现

#### 2.2.1 类结构

```typescript
export class TestRedBlueTexture implements ITextureGenerator {
  type: 'canvas' | 'shader' = 'canvas';
  private texture: THREE.Texture | null = null;
  private time: number = 0;

  generate(): THREE.Texture | THREE.Material {
    // 生成纹理逻辑
  }

  update(delta?: number): void {
    // 更新逻辑
  }

  dispose(): void {
    // 清理逻辑
  }
}
```

#### 2.2.2 关键技术点

1. **Canvas 动态生成**
   - 使用 `document.createElement('canvas')` 创建 8x8 画布
   - 通过 2D 上下文绘制纹理内容

2. **流动效果实现**
   - 基于时间的流动偏移：`flowOffset = this.time * flowSpeed`
   - 动态波浪参数：使用正弦函数实现波浪的振幅和频率变化
   - 随机杂波：每帧生成随机噪声，使波形更加自然

3. **分层纹理应用**
   - 使用 `createGradientCanvas` 函数生成红色和蓝色的渐变纹理
   - 应用到上下两部分，创建层次感

4. **无缝循环**
   - 当纹理流动到边缘时，会从另一侧重新出现
   - 通过正弦函数的周期性实现平滑过渡

### 2.3 流动效果参数

| 参数 | 描述 | 值 |
|------|------|-----|
| **flowSpeed** | 流动速度 | 2.0 单位/秒 |
| **baseAmplitude** | 基础波浪振幅 | 1.0 + Math.sin(this.time * 1.5) * 0.5 |
| **baseFrequency** | 基础波浪频率 | 0.6 + Math.sin(this.time * 0.8) * 0.4 |
| **noiseAmplitude** | 随机杂波振幅 | Math.random() * 0.5 |
| **noiseX** | 杂波X偏移 | Math.random() * 8 |
| **noiseY** | 杂波Y偏移 | Math.random() * 2 - 1 |

### 2.4 视觉效果

- **上半部分**：红色渐变纹理，带有波浪边界
- **下半部分**：蓝色渐变纹理，带有波浪边界
- **交界处**：动态正弦波形 + 随机杂波，随时间流动
- **整体效果**：红蓝两色流动，带有层次感和动态变化

## 3. 纹理分层函数 (GradientTexture)

### 3.1 核心功能

`createGradientCanvas` 函数是一个强大的纹理生成工具，用于创建带有分层效果的渐变纹理。

### 3.2 函数签名

```typescript
export function createGradientCanvas(
  baseColor: string | THREE.Color | number[],
  width: number = 512,
  height: number = 512,
  options: GradientOptions = {}
): HTMLCanvasElement
```

### 3.3 关键参数

| 参数 | 类型 | 默认值 | 描述 |
|------|------|--------|------|
| **baseColor** | string  THREE.Color  number[] | - | 基础颜色 |
| **width** | number | 512 | 画布宽度 |
| **height** | number | 512 | 画布高度 |
| **options.regionCount** | number | 8 | 分层数量 |
| **options.blockCount** | number | 3 | 色块数量 |
| **options.internalGradStrength** | number | 0.15 | 内部渐变强度 |
| **options.seed** | number | 42 | 随机种子 |
| **options.hueVariation** | number | 0.1 | 色相变化幅度 |
| **options.satVariation** | number | 0.2 | 饱和度变化幅度 |
| **options.lightVariation** | number | 0.15 | 明度变化幅度 |

### 3.4 分层算法实现

#### 3.4.1 HSL 颜色生成

```typescript
const regionColors: Array<{ h: number; s: number; l: number; rgb: number[] }> = [];
for (let i = 0; i < regionCount; i++) {
  // 使用HSL算法生成分层基色：色相固定，明度递增
  const lightness = 0.35 + (i / (regionCount - 1)) * 0.4; // 0.35 ~ 0.75
  const saturation = 0.8; // 固定饱和度
  const hue = baseH; // 使用基础颜色的色相
  regionColors.push({ h: hue, s: saturation, l: lightness, rgb: hslToRgb(hue, saturation, lightness) });
}
```

#### 3.4.2 Y轴分层与Sin分界线

```typescript
// 计算分层边界（y轴上的横线，加入sin调整）
const layerBoundaries = [];
for (let i = 0; i < regionCount - 1; i++) {
  const baseY = (i + 1) / regionCount * height;
  layerBoundaries.push(baseY);
}

for (let y = 0; y < height; y++) {
  for (let x = 0; x < width; x++) {
    // 计算当前位置的sin调整值
    const sinOffset = Math.sin(x * 0.05) * 3; // 波浪振幅为3像素
    const adjustedY = y + sinOffset;
    
    // 确定当前位置属于哪个层次
    let layerIndex = 0;
    for (let i = 0; i < layerBoundaries.length; i++) {
      if (adjustedY >= layerBoundaries[i]) {
        layerIndex = i + 1;
      }
    }
    
    // 获取当前层次的颜色
    const layerColor = regionColors[layerIndex];
    
    // 加入轻微的内部渐变
    const t = y / height;
    const hueShift = 0.01 * Math.sin(x * 0.1 + y * 0.1) * internalGradStrength;
    const satShift = 0.05 * Math.cos(x * 0.08) * internalGradStrength;
    const litShift = 0.08 * Math.sin(y * 0.1) * internalGradStrength;
    
    const h = ((layerColor.h + hueShift % 1) + 1) % 1;
    const s = Math.min(1, Math.max(0, layerColor.s + satShift));
    const l = Math.min(1, Math.max(0, layerColor.l + litShift));
    const [r, g, b] = hslToRgb(h, s, l);
    
    const idx = (y * width + x) * 4;
    data[idx] = r * 255;
    data[idx + 1] = g * 255;
    data[idx + 2] = b * 255;
    data[idx + 3] = 255;
  }
}
```

### 3.5 技术亮点

1. **HSL 颜色空间**：使用 HSL 颜色空间生成颜色，确保颜色过渡自然
2. **Y轴分层**：按照高度平均分层，创建垂直方向的层次感
3. **Sin 分界线**：使用正弦函数调整分层边界，创建波浪效果
4. **内部渐变**：为每层添加轻微的内部渐变，增强视觉效果
5. **可配置参数**：通过选项参数控制分层数量、渐变强度等

## 4. 纹理管理器集成

### 4.1 注册与使用

```typescript
// 创建纹理管理器实例（单例模式）
const textureManager = TextureManager.getInstance();

// 创建测试纹理
const testTexture = new TestRedBlueTexture();

// 注册到纹理管理器
textureManager.register('test-red-blue', testTexture);

// 获取纹理
const texture = textureManager.getTexture('test-red-blue');
```

### 4.2 更新机制

```typescript
// 每帧更新所有纹理
textureManager.updateAll(delta);

// 内部实现
public updateAll(delta: number): void {
  this.textures.forEach(entry => {
    // 更新所有类型的纹理生成器
    entry.generator.update(delta);
  });
}
```

## 5. 性能优化

### 5.1 内存管理
- **Canvas 复用**：重复使用同一个 Canvas 元素
- **Texture 更新**：只更新纹理的 image 属性，不创建新的 Texture 对象
- **资源释放**：提供 dispose() 方法释放纹理资源

### 5.2 渲染性能
- **小分辨率**：8x8 像素的纹理，内存占用极低（约256字节）
- **高效绘制**：每帧重新生成 8x8 纹理，性能开销很小
- **硬件加速**：Canvas 绘制和 Texture 更新都由 GPU 加速

## 6. 应用场景

### 6.1 测试用途
- **纹理系统测试**：验证纹理管理器的注册、更新和获取功能
- **流动效果测试**：测试动态纹理的实现方式
- **分层效果测试**：验证渐变纹理生成器的效果

### 6.2 实际应用
- **水面纹理**：可以修改为模拟水面的流动效果
- **背景纹理**：用于创建动态变化的背景
- **特效纹理**：用于粒子系统或其他特效

## 7. 扩展建议

### 7.1 增加更多颜色方案

```typescript
// 支持多种颜色方案
const colorSchemes = {
  redBlue: { top: '#ff0000', bottom: '#0066ff' },
  greenBlue: { top: '#00ff00', bottom: '#0000ff' },
  orangePurple: { top: '#ff6600', bottom: '#9900ff' }
};
```

### 7.2 添加交互效果

```typescript
// 响应鼠标或触摸事件
const handleMouseMove = (event: MouseEvent) => {
  const mouseX = event.clientX / window.innerWidth;
  const mouseY = event.clientY / window.innerHeight;
  // 根据鼠标位置调整流动效果
  flowSpeed = 1.0 + mouseX * 3.0;
  waveAmplitude = 1.0 + mouseY * 2.0;
};
```

### 7.3 优化移动设备性能

```typescript
// 检测设备性能，调整更新频率
const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
const updateInterval = isMobile ? 0.1 : 0.016; // 移动设备每100ms更新一次

let lastUpdateTime = 0;
public update(delta?: number): void {
  if (delta && this.texture) {
    lastUpdateTime += delta;
    if (lastUpdateTime >= updateInterval) {
      this.time += lastUpdateTime;
      this.generate();
      lastUpdateTime = 0;
    }
  }
}
```

## 8. 技术规格

| 参数 | 规格 |
|------|------|
| **测试纹理分辨率** | 8 × 8 像素 |
| **渐变纹理默认分辨率** | 512 × 512 像素 |
| **颜色格式** | RGB |
| **颜色空间** | sRGB |
| **纹理格式** | CanvasTexture |
| **更新频率** | 每帧更新（约60FPS） |
| **内存占用** | 极低（测试纹理约256字节） |
| **渲染性能** | 高（每帧更新开销很小） |

## 9. 代码优化建议

### 9.1 缓存优化

```typescript
// 缓存渐变纹理，避免重复生成
private static gradientCache: Map<string, HTMLCanvasElement> = new Map();

// 生成缓存键
const cacheKey = `${baseColor}-${width}-${height}-${JSON.stringify(options)}`;

// 检查缓存
if (TestRedBlueTexture.gradientCache.has(cacheKey)) {
  return TestRedBlueTexture.gradientCache.get(cacheKey)!;
}

// 生成后缓存
const canvas = createGradientCanvas(...);
TestRedBlueTexture.gradientCache.set(cacheKey, canvas);
```

### 9.2 并行处理

```typescript
// 使用 Web Worker 生成纹理，避免阻塞主线程
const generateTextureInWorker = (params) => {
  return new Promise((resolve) => {
    const worker = new Worker('texture-worker.js');
    worker.onmessage = (event) => {
      resolve(event.data);
      worker.terminate();
    };
    worker.postMessage(params);
  });
};
```

### 9.3 按需更新

```typescript
// 只在纹理可见时更新
public update(delta?: number): void {
  if (delta && this.texture && this.isVisible) {
    this.time += delta * 0.5;
    this.generate();
  }
}

// 设置可见性
public setVisible(visible: boolean): void {
  this.isVisible = visible;
}
```

## 10. 总结

`TestRedBlueTexture` 和 `GradientTexture` 是一套功能强大的纹理生成系统，具有以下特点：

1. **动态流动效果**：通过时间驱动的正弦函数实现平滑的流动效果
2. **分层渐变**：使用 HSL 颜色空间和 Y 轴分层创建丰富的层次感
3. **波浪分界线**：通过 sin 函数调整分层边界，增加视觉趣味性
4. **高性能**：小分辨率纹理和高效的 Canvas 绘制确保性能开销很小
5. **可扩展性**：通过配置参数和扩展接口，可以适应各种纹理需求

这套系统不仅可以用于测试目的，还可以应用于实际游戏开发中的多种场景，如水面模拟、背景效果、特效纹理等。

---

**创建日期**：2026-04-26
**最后更新**：2026-04-26
**作者**：AI Assistant