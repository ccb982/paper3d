# 实体架构 —— 类树重构 + 蜂群适配（2026-09-18 设计稿，待实施）

> 本文是实体层（entity/）类树与生命周期的唯一设计权威。
> 关联：《蜂群架构.md》（敌人系统 / LOD / v2 编队与寻路唯一权威）、
> 《开发说明书.md》（总体分层 / 实体管线 / 战斗与掉落管线）、
> 《地形与渲染管线架构.md》（地形查询 / 空间索引 / 渲染管线）。
>
> **决策记录（2026-09-18 用户拍板）**
> - **类树形态**：`EntityBase → CharacterBase → { EnemyBase / AllyBase / Player }`
>   —— 保留 CharacterBase 中间层（角色运动/地形/受击表现是三方共用的真公共代码）。
>   ★ 实施修正（E2）：见 §6.2/§6.6。
> - **文档载体**：新建本文档，不把玩家/友军塞进《蜂群架构.md》；
>   蜂群 v2 只负责"逻辑归属与寻路"，实体侧负责"载体与骨架"，两边正交。
> - **重构纪律**：E0~E2 阶段是**纯搬运**（行为零变化，只拆文件/拆职责）；
>   **E3 起才允许动行为**（引入蜂群接口/编队移动来源）。
> - ★ **管线设计（2026-09-18 追加拍板）**：实体管线改**显式六相**（输入→大脑→模拟→战斗→
>   升降格桥接→表现），生命周期改**状态机 + 退役原因**，销毁与业务事件解耦；
>   所有"会改实体集合"的操作集中到**唯一桥接相位**，为蜂群 v2 的
>   升格/降格/编队下发留好标准接口（见 §9）。
> - ★ **字段预留（2026-09-18 追加拍板）**：敌实体先落**非可选**字段——编队层次
>   （battalionId/squadId/formSlot/corridorIdx）、载体（carrier）、飞天（isAir/altitude）、
>   移动位置（moveTarget）、攻击类型（attackType）与 role；默认值、行为零变化，
>   v2 由 `SwarmTierPort` 填值（见 §5.3）。
> - ★ **实施顺序（2026-09-18 四次拍板）**：**先重做玩家与友军基类**——
>   友军分**飞天 / 陆地跟随 / 陆地站桩**三类；玩家要求**架构独立**（不与敌友耦合）。
>   **敌人基类（EnemyBase）本轮不动**：它后续要接蜂群 v2 的大量内容，
>   等 v2 启动时与 E3/P4 一起做。**装备/弹药类实体不在重做范围**。
> - ★ **敌人基类 v2 定稿（2026-09-19 拍板）**：**单一单位模型**——代理与实体是同一
>   `SwarmUnit` 的 `dormant/active` 两态（代理具备全部能力、只是被门控）；
>   `EnemyBase` = 表现载体（血条/影子/受击染料/死亡动画/帧）+ 基础行动/攻击 +
>   **常开感知**，行为归蜂群引擎；小队**同质**（一队一兵种 4~12）、Boss/高威胁**单例**；
>   指挥权 `leaderUid` 只挂 active、Phase 4 原子转移、代理被击可升格接管；
>   情报**个体→小队→大队**（战略级全知仅用于寻路目标）。详见 §5。
>
> **一句话**：把"上帝基类"拆成三段——EntityBase 只做载体骨架、
> CharacterBase 只做角色运动与表现、三个阵营基类各自管自己的驱动方式；
> 管线改"显式相位 + 幂等退役"，让 L3 敌人成为蜂群引擎可随时接管/交还的
> **标准载体**（steer / 快照 / 编队字段 / 退役原因），
> v2 编队引擎落地时**不需要再动实体类树和管线顺序**。

---

## 目录

1. 一句话 + 现状瓶颈
2. 目标类树与职责边界
3. EntityBase 瘦身（正交职责收口）
4. CharacterBase 收口
5. EnemyBase（敌人）
6. AllyBase（友军，新增）
7. Player（玩家）
8. 非角色实体与未来实体
9. ★ 管线设计（六相更新 / 生命周期 / 蜂群桥接接口）
10. ★ 蜂群适配（与《蜂群架构.md》v2 的接口契约）
11. 冻结契约（不得破坏）
12. 迁移路线 E0~E6
13. 风险与取舍

---

## 1. 现状瓶颈

### 1.1 代码证据（2026-09-18）

| 类 | 行数 | 混了哪些职责 |
|---|---|---|
| `EntityBase` | 542 | 骨架 + 贴地剪影影子（≈128 行）+ 附属特效槽 + **完整战斗属性**（hp/攻防/暴击/闪避/格挡/效果列表——子弹/物品也背）+ 流体钩子 + 生命周期 |
| `CharacterBase` | 568 | 移动控制器 + 地形立面/空中层 + 角色间/装饰物推挤 + **受击染料流体（≈157 行）** + 死亡动画 + 影子声明 |
| `EnemyBase` | 426 | AI 状态机驱动 + **危害地形扫描（≈90 行，与代理层重复）** + 相机帧/朝向/扭曲 + 眩晕 + 击杀统计 |
| `Player` | 134 | 输入驱动 + 载具 + 死亡复活 + 操作锁 |
| `DroneEntity` | 479 | **友军没有基类**：无人机（跟随近战）与祖宗（站桩激光）挤在一个类；`WorldMode` 注入 6 个回调当事实上的"友军基类" |
| `NpcEntity` / `VisitorNpcBase` / `ItemBase` / `ShipEntity` / `BulletEntity` | 62~578 | 直连 EntityBase 或 CharacterBase |

### 1.2 真正的病灶（不是"EntityBase 一个类太大"）

| 症状 | 证据 | 后果 |
|---|---|---|
| **载体骨架 / 角色运动 / 阵营驱动 三段混在一起** | `EnemyBase.moveBy` 既做 AI 移动、又做地形危害探测、又做贴片朝向；`WorldMode` 还要帮友军注入回调 | 加一个兵种/友军要动 3 个类 |
| **双轨行为分叉** | 代理层（`SwarmSystem`/`AgentPool`）与 L3（`EnemyBase` + `AISystem`）各有一套危害探测/游走/追击 | 改一处忘一处；升降格后行为漂移 |
| **友军无基类** | `DroneEntity` 里 6 个回调 + `stationary` 布尔分叉 | 第二种友军只能复制整个类 |
| **蜂群 v2 字段无家可归** | `squadId/uid/formSlot/corridorIdx/role` 在 `AgentPool` 侧已设计（《蜂群架构.md》§16.2），实体侧没有对应物 | 升格丢编队 → 队伍散架（v2 铁律 1） |
| **公共表现代码不可复用** | 受击染料/死亡动画/影子都写死在两个基类里 | 友军/NPC 想用只能复制 |
| **`AISystem` 只收 `EnemyBase`** | `AISystem.ts:11` | 与"敌人/友军共用行为库"的文档口径不符，友军只能另写 |

### 1.3 重构目标（验收口径）

- 三个阵营基类各自只回答一个问题：**"谁在驱动我"**（AI / 世界端口 / 输入）。
- L3 敌人 = 蜂群引擎的**表现载体**，不是独立 AI 权威；v2 编队字段跨 LOD 无损。
- 行为零变化地完成 E0~E2；E3~E4 的改动全部有降级链兜底、可逐阶段回滚。
- `npx tsc --noEmit` 零报错、`npm run guard` 通过；每阶段"能玩、能看、能测"。

---

## 2. 目标类树与职责边界

### 2.1 类树

```
EntityBase（载体骨架：你是谁、在哪、怎么显示、怎么销毁）
 ├─ ItemBase / BulletEntity / ShipEntity / NpcEntity / DecorEntity   ← 装备/弹药不动
 ├─ StructureEntity（★ 新增，v2 §22 工事：固定刚体 + 可 applyDamage + 不进 AI/编队）
 ├─ AllyBase（友 · ★ 本轮重做：三种骨架；直连 EntityBase，见 §6.6）
 │    ├─ AirAlly（飞天）────────► DroneAlly（原无人机）
 │    ├─ GroundFollowAlly（陆地跟随）
 │    └─ GroundStationaryAlly（陆地站桩）──► SentinelAlly（原祖宗）
 └─ CharacterBase（角色运动层：怎么走、怎么被地形挡住、挨打怎么演）
     ├─ EnemyBase（敌：个体战斗 AI + 蜂群代理投影 + 编队字段）★ 本轮不动，随 v2 做
     │    └─ 飞行兵不单开子类：`airborne + role:'flyer'` 标记（v2 §25）
     ├─ Player（★ 本轮架构独立：entity/player/ + systems/player/PlayerPipeline）
     └─ VisitorNpcBase（非战斗角色：访客/事件 NPC，沿用 CharacterBase 管线）
```

### 2.2 职责边界（写代码时对照这张表）

| 层 | 只管 | 绝不管 |
|---|---|---|
| **EntityBase** | Entity 容器/位置/camp、物理同步模式（none/kinematic/read）、动画管线（anim/state）、渲染器生命周期、特效槽、LOD/视锥/影子**调度**、`dispose/onDeath` 骨架、`hitAnchorY/minimapInfo` | 角色移动、输入、AI、地形规则、战斗结算、具体渲染实现 |
| **CharacterBase** | `CharacterController`、碰撞体积/kinematic、贴地与立面规则（`blockCliffClimb`/`airborne`）、角色间/静态物推挤、受击表现（染料/死亡动画）、贴片缩放与影子声明 | 阵营、索敌、攻击意图、输入语义、蜂群编队 |
| **EnemyBase** | 个体战斗 AI（状态机/攻击节奏/眩晕）、敌人显示帧与朝向、击杀统计、蜂群代理投影（快照出入） | 全局寻路（HPA/流场）、编队决策、地形危害全局探测 |
| **AllyBase**（EntityBase 直连） | 世界端口（索敌实体+代理双通道/攻击结算/挖矿）、攻击节奏、跟随锚点/基座、槽位与存活、光束/影子表现、三种运动骨架（§6） | 玩家输入、蜂群编队（远期可选才接）、地面角色规则（陆地骨架接入时再定，§6.6） |
| **Player** | 输入驱动、载具、死亡/复活、操作锁 | 任何 AI/编队逻辑 |
| **systems/swarm** | 编队/寻路/攻击槽/导演/代理池 | 实体类内部状态（只经标准接口：snapshot / steer / combat 字段） |

### 2.3 依赖方向（保持既有分层不破坏）

```
modes（WorldMode）
  → spawn（WorldSpawner：创建/升格/降格实体）
  → entity（类树；不 import systems/swarm 的运行时代码）
systems/swarm → hooks（promote/demote 回调，由 modes 装配）→ spawn → entity
services ← entity（渲染/物理/战斗/特效；entity 不反向引 modes/ui/swarm）
```

★ **铁律**：`entity/` 永远不 import `systems/swarm/`。
蜂群要的东西通过 `hooks.promote/demote` 与数据接口进入；实体的状态通过快照/公共字段出去。

---

## 3. EntityBase 瘦身（正交职责收口）

> 原则：**继承管"是什么"，组合管"会什么"**。不改类树，把内联的大段职责搬成独立模块。

### 3.1 瘦身后保留（≈200 行）

- `entity/position/camp`、`physicsMode`、`collisionVolume`、`physicsBodyOffsetY`。
- `anim/state` + `createRenderer/attachToScene/render`、`getFluidTexture` 钩子。
- `update()` 骨架：① 子类行为 → ② 物理同步 → ③ 动画 → ④ 渲染同步 → ⑤ 特效 → ⑥ 空间索引 → ⑦ 影子。
- `applyViewDistance` / `viewLod` / `inFrustum` / `lodExempt`。
- `onTakeDamage/onDeath/dispose`、`hitAnchorY/minimapInfo`、`visible/billboard`。

### 3.2 搬出为组合模块（文件 + 类，不改继承）

| 现状位置 | 去向 | 状态 |
|---|---|---|
| `EntityBase` 影子块（110~237） | `services/render/GroundShadowController.ts` | ✅ 已完成（host 回调惰性读宿主状态；裁剪顺序/降频/白昼浓度语义不变） |
| `EntityBase.effectSlots`（261~297） | `services/fx/EffectSlots.ts` | ✅ 已完成（attach/detach/get/update/render/disposeAll 原样迁移） |
| `CharacterBase` 受击染料（301~457） | `services/fx/CharacterHitDye.ts` | ✅ 已完成（`spawn/update/getCompositeTexture/dispose`；配置默认值随迁） |
| `CharacterBase` 死亡动画（296~299, 511~557） | `services/fx/CharacterDeathFx.ts` | ✅ 已完成（`spawn`；`deathAnimEnabled` 转发访问器保留） |
| 战斗属性（299~355） | **暂留 EntityBase**，E5（可选）再提 `CombatStats` | 不动 |

**行数结果（2026-09-18）**：`EntityBase` 542→468、`CharacterBase` 568→395；
新组合件 137+61+179+39 = 416 行（注释随迁）。

★ E1 验收标准：**行为零变化**（同一存档/同一波次，肉眼与日志对比无差异），只是行数与文件数变了。

---

## 4. CharacterBase 收口

保留角色公共能力（三方真正共用的部分）：

1. **运动**：`CharacterController`（移动/跳跃/朝向/攻击动作）+ `moveDir` 速度驱动。
2. **地形**：`climbAnyTerrain` / `blockCliffClimb`（只走插值坡）/ `airborne`（空中层，v2 §25 已就绪）。
3. **推挤**：`separateFromOthers`（角色间）+ `separateFromStatics`（JS 静态障碍索引）。
4. **表现**：贴片缩放、`shadowShape`、受击表现（转发到 `CharacterHitDye`）、死亡动画（转发到 `CharacterDeathFx`）。
5. **扩展点**：`onUpdate`（子类行为）、`heightOffset`、`physicsBodyOffsetY`。

搬出染料/死亡动画后目标 ≈ 260 行。**不拆 `CharacterController`**（已经是独立类），
不再往 CharacterBase 加任何阵营/玩法语义。

---

## 5. EnemyBase（敌人）

> ★ **2026-09-19 定稿：单一单位模型 + 载体定位**（E3b/E4/E5 分步实施；实施前保持现状）
> 敌人不是"代理 or 实体"两种兵，而是同一个 `SwarmUnit` 的两种**激活态**；
> `EnemyBase` = 该单位在 L3 的**载体**（表现 + 基础行动/攻击 + 常开感知），
> **行为归蜂群引擎**（去哪、编队、集火、姿态、情报汇聚）。

### 5.1 定位与能力门控（activation）

| 能力 | dormant（代理 L1/L2） | active（L3 实体） |
|---|---|---|
| 跟随指挥 / 消费 steer | ✅ | ✅ |
| 被击感知 / 听警报 / 短距直觉（`PROXY_INSTINCT_R` ≤5m） | ✅ | ✅ |
| 长距索敌 / 主动 LOS | ❌ 关 | ✅ |
| 完整 AI 状态机 | ❌ 关（只跑极简 follow + 到期攻击） | ✅ |
| 指挥资格（`leaderUid` 可指向） | ❌ 关 | ✅ |
| 高保真战斗（攻击槽/令牌/真弹道） | 简化（到期结算） | ✅ |
| 表现（血条/影子/受击染料/死亡动画/帧/扭曲） | ❌ | ✅ |

- **状态属于逻辑单位**（血量/属性/感知/AI/编队/意图），跨 LOD 全程保留：
  降格不失忆、升格不重置。
- **EnemyBase 的三块职责**：
  1. **表现载体**：`HealthBar` / `SilhouetteShadow` / `CharacterHitDye` / `CharacterDeathFx` /
     帧与扭曲 / 小地图 / LOD 档位（§5.6）；
  2. **基础能力**（swarm 失联也不停摆）：游走/巡逻、直线追击、近战挥击/远程射击
     （节奏+冷却）、受击/眩晕/死亡、贴地、**2m 局部避障**；
  3. **常开感知**：自身索敌半径 + 事件感知（被击/听警报），写入小队情报（§5.4）。
- **内部结构（E5 组合件拆分，纯搬运）**：`EnemyBrain`（战斗决策，只产意图）/
  `EnemyLocomotion`（消费 steer 或本地兜底）/ `EnemyPresentation`（按 viewLod 的表现档）。

### 5.2 移动来源三态（★ v2 适配核心）

现状：`EnemyBase.moveBy` 自己探测坑/水/立面（`isDangerAhead`，≈90 行），与代理层重复。
目标：**移动决策上移，实体只消费 steer**。

```ts
// CharacterBase 统一移动入口（E3）
export interface SteerIntent {
  dirX: number; dirZ: number;   // 单位方向（0,0 = 停）
  speed: number;                // m/s（0 = 停）
  /** formation=编队槽位 / flow=流场 / local=本地 AI / none=停 */
  source: 'formation' | 'flow' | 'local' | 'none';
}
```

| 来源 | 谁下发 | 实体做什么 |
|---|---|---|
| `formation`（v2 F1+） | `SwarmSystem`：编队槽位目标 / corridor 车道 | 转向 + 速度积分 + **最后 2m 局部避障** |
| `flow`（现状降级链） | `FlowField` 梯度 | 同上 |
| `local`（AI 兜底） | `EnemyBase` 自身（游走/直扑 + 极近避障） | 同上 |
| `none` | 眩晕/死亡/ROE 停火补位 | 不位移（但仍允许转向/索敌） |

- 危害地形探测（坑/深水/立面）**统一收进 swarm 层**（流场代价 + HPA 代价已编码），
  实体侧只保留 **2m 级局部避障**（防贴墙抖动，属于运动层）。
- `EnemyBase.moveBy` 保留为兼容入口（E3 内部转发到 `applySteer`），E4 后删除。
- 降级链（v2 §15.6）在 swarm 层：HPA corridor → 流场 → 直线；**任何一层都不能让敌人停摆**。

### 5.3 蜂群字段（★ 在 EnemyBase 上先预留，v2 填值）

> ★ **E3a 已落地（2026-09-18）**：`entity/SwarmUnit.ts`（`UnitRole/UnitAttackType/
> SteerIntent/SwarmUnit/SwarmSnapshot/SwarmCarrier`）+ `EnemyBase implements SwarmCarrier`
> （字段默认值 + `applySteer/hasFreshSteer/hydrate/drain`；`isAir/altitude` 转发
> `airborne/airAltitude` 单一事实源）；`AgentSnapshot extends SwarmSnapshot`；
> `WorldSpawner` 升格 `hydrate(snap)`、降格 `...e.drain()`；`MobDef.role/attackType` 透传。
> **E3b（AgentPool 新增列 + 快照感知/AI 连续性 + 指挥权 + 被击升格 + `SwarmTierPort`）随 v2 一起做**（详见 §5.7）。

**预留策略**：E3 先落字段并给默认值（散兵 / 未编队 / 地面），**行为零变化**；
F0/F1 起由 `SwarmTierPort` 在升格 `hydrate` / 降格 `drain` 时统一写入。
实体永远**不主动查询 swarm**（依赖方向铁律），所有值随快照进出。

```ts
// ★ 兵种角色 / 攻击类型：实体侧用可读联合类型；AgentPool SoA 用 Uint8 常量
export type UnitRole = 'shield' | 'assault' | 'grunt' | 'ranged' | 'flyer';
export type UnitAttackType = 'none' | 'melee' | 'ranged' | 'bombard';
export const ATTACK_NONE = 0, ATTACK_MELEE = 1, ATTACK_RANGED = 2, ATTACK_BOMBARD = 3;
```

```ts
// entity/SwarmUnit.ts（★ 数据面；完整载体契约 SwarmCarrier 见 §9.4）
export interface SwarmUnit {
  /** ★ 稳定 uid（升格/降格往返不变；替代裸 index） */
  swarmUid: number;
  /** ★ 当前载体：L3 实体 = 'entity'；L1/L2 代理 = 'agent'（carrier==='agent' 即"是代理"） */
  carrier: 'entity' | 'agent';
  /** ★ 激活态（单一单位模型，2026-09-19）：dormant = 能力门控（代理）；active = 全能力（L3） */
  activation: 'dormant' | 'active';
  /** ★ 是否本队队长（指挥资格只挂 active；dormant 恒 false） */
  isLeader: boolean;
  /** ★ 大编队（-1 = 未编队；权威在 Squad.battalion，实体只存副本） */
  battalionId: number;
  /** ★ 小编队（-1 = 散兵/未编队） */
  squadId: number;
  /** 阵型槽位（-1 = 未分配） */
  formSlot: number;
  /** 沿走廊推进进度（waypoint 索引） */
  corridorIdx: number;
  /** 兵种角色（软约束；同质小队里 = 小队类型角色） */
  role: UnitRole;      // 'shield' | 'assault' | 'grunt' | 'ranged' | 'flyer'

  // ---- ★ 移动属性（飞天 + 移动位置） ----
  /** 是否空中单位（飞天：不进地面导航/车道/工事代价/陷阱；直线 + 避障） */
  isAir: boolean;
  /** 空中悬停高度（米，相对地表；isAir=false 恒 0） */
  altitude: number;
  /** ★ 移动目标点（世界坐标；编队槽位/寻路下发，hold 语义；null = 无目标）
   *  实现可用 3 个 number + NaN 哨兵（零分配），此处为语义定义 */
  moveTarget: { x: number; y: number; z: number } | null;

  // ---- ★ 攻击属性 ----
  /** ★ 攻击类型：none 无 / melee 近战 / ranged 远程弹道 / bombard 轰炸（飞行兵） */
  attackType: UnitAttackType;
}
```

| 字段 | 预留默认值 | 权威来源 | 写入时机 | 用途 |
|---|---|---|---|---|
| `swarmUid` | 0 | 池/实体共用分配器 | 生成时 | 跨 LOD 身份；★ 替代 index |
| `carrier` | `'entity'` | 载体自身 | 常量 | 统一处理"实体 or 代理" |
| `activation` | `'active'` | 载体派生（池=dormant / 实体=active） | 载体切换时 | 能力门控（§5.1） |
| `isLeader` | false | `Squad.leaderUid` 命中本 uid | Phase 4 指挥权转移 | 指挥链可视化 / 情报锚点 |
| `battalionId` | -1 | `Squad.battalion`（副本） | hydrate/drain | 指挥链着色 / 调试 / 大编队统计 |
| `squadId` | -1 | Squad 表 | hydrate/drain | 编队归属（v2 铁律 1） |
| `formSlot` | -1 | Squad 阵型分配 | hydrate/drain | 阵型槽位硬分配 |
| `corridorIdx` | -1 | Squad / corridor | 0.3~0.5Hz 推进 | "谁在前面" / 车道收拢 |
| `role` | `'grunt'` | `MobDef.role` | 生成 / 升格 | 小队类型 / 大队位置分配 |
| `isAir` | false | `MobDef.isAir`（现 `airborne`） | 生成 / 升格 | **飞天**：独立空中层 |
| `altitude` | 0 | `MobDef.airAltitude` | 生成 / 升格 | 悬停高度（渲染/寻路/投弹） |
| `moveTarget` | null | Brain（编队槽位 / 走廊 / 流场） | 5~10Hz（hold） | **移动位置**：实体按它走，降格随快照带走 |
| `attackType` | `'melee'` | `MobDef.attackType`（缺省从 `aiConfig.ranged` 推导） | 生成 / 升格 | **攻击类型**：近战/远程/轰炸分支 + 攻击槽分圈 |

- 这些字段**非可选（不用 `?`）**：v2 上线时不允许到处 `??` 兜底，一律以默认值参与逻辑。
- `moveTarget` 与 §9.4 `SteerIntent` 的分工：**`moveTarget` 是"要去哪"（低频、保持）**，
  `SteerIntent` 是"这一帧怎么走"（方向/速度，流场或槽位求导后下发）；两者都由 swarm 写，实体只读。
- 只在 `EnemyBase` 上落字段；`CharacterBase` 只保留运动层所需的 `isAir` 语义（现 `airborne`）。
- ★ **禁止**在实体上存 `agentIndex`（`AgentPool` 是 swap-remove，裸 index 会静默指错人）；
  需要引用时用 `swarmUid`。
- **友军/玩家不实现 `SwarmUnit`**（玩家永不进蜂群；友军远期可选，`AllyBase` 只留兼容位）。
- 完整载体契约（`controlSource / applySteer / hydrate / drain / retire`）见 §9.4 `SwarmCarrier`。

### 5.4 感知与情报（个体 → 小队 → 大队）

```
个体（逻辑单位；感知跨 LOD 保留）
   · 主动（仅 active）：半径索敌、小队级预算的 LOS
   · 被动（含 dormant）：被击事件、听警报（小队广播/枪声）、短距直觉 ≤5m
        │ 写 contact {x,z,kind,time}（零分配）
        ▼ 5~10Hz
小队：汇总/去重 → 广播全队（个体延迟 0.2~1.5s）→ 上报大队
        ▼ 0.5~1Hz（变化触发优先）
大队：contactMap + 姿态机 + 目标分配 + 是否重算 HPA*
```

- **状态字段**（随快照跨 LOD）：`lastSeenX/Z/At`、`alertUntil`、`aggroFrom`、`isLeader`。
- **战略 vs 战术**：大队战略级全知（玩家/舰船真位置 → HPA*）；战术级决策吃情报。
- **成本红线**：不做逐个体 LOS；小队级 LOS 预算 + 事件驱动；合并 O(n) 零分配。

### 5.5 指挥权（`Squad.leaderUid`）

- 指挥权**只挂 active 单位**（dormant 无资格）；单例小队 `leaderUid` = 自身。
- 更替只在 **Phase 4** 原子执行：
  | 事件 | 处理 |
  |---|---|
  | 队长降格/死亡 | 本队 active 成员接任（有最新情报 > 血量 > 靠队心）；全队 dormant → 大队直管 |
  | **代理被击** | 置顶升格（限 `L3_RADIUS` 内）；成功 → **接管指挥权**（谁接敌谁指挥），原队长降为普通成员 |
  | 单例 Boss | `noDemote`：永保 active，不因距离降格 |
- 队长 = **名义指挥节点（方案 A）**：小队决策仍由 `SwarmSystem` 计算，实体不跑第二套微 AI。

### 5.6 与 LOD 的契约（强化）

| 事项 | 权威 | EnemyBase 侧 |
|---|---|---|
| 分档（L3/L2/L1）与升降格（30/40m 滞回、≤2/帧、L3≤30） | `SwarmSystem` | 绝不自己升降格；实现 `hydrate/drain` 钩子 |
| 切换执行点 | Phase 4（`SwarmTierPort` 唯一桥接） | 被动被搬运；`activation` 由载体派生 |
| 跨 LOD 状态连续 | 逻辑单位 | 快照携带 AI/感知/编队/意图（降格不失忆、升格不重置） |
| 表现档位（viewLod） | `renderAll` 下发 | `EnemyPresentation` 按档降级：动画 fps / 扭曲 / 影子裁剪 / 血条 / 受击染料 |
| 行为档位 | 表驱动 | LOD0 全量；LOD1/2 危险探测降频、扭曲关；**L3 不降决策频率**（上限 30 已够便宜） |

★ **物理位置与 LOD 无关**：无论 viewLod 多少，L3 实体都跑完整 `simulate`（玩法一致），
只有 `present` 按档降级——这与"Present 可跳过"的六相契约一致。

### 5.7 迁移（E3b / E4 / E5）

| 阶段 | 内容 | 验收 |
|---|---|---|
| **E3b** | AgentPool 新增列（`isLeader/lastSeen*/aggroFrom/aiStateIdx/aiTimer`）+ 快照往返；`activation` 派生；指挥权转移 + 被击升格（限 L3 范围）；`MobDef.squadMode/noDemote` | 升降格往返后：AI 状态/感知/编队/队长全部保留；无编队时行为与现状一致 |
| **E4a** | 接入 `applySteer` 消费（默认 `local`，编队开关打开才生效） | 编队模式成队形推进；关掉即恢复现状 |
| **E4b** | `swarm` 模式删全量危害扫描，只留 2m 局部避障；`local` 兜底保留 | 跨区能绕障碍找到玩家；帧率不掉 |
| **E5** | `EnemyBrain / EnemyLocomotion / EnemyPresentation` 组合件拆分（纯搬运） | 行为零变化；EnemyBase 收口 |

### 5.8 ★ E3b 开工清单（2026-09-19 准备完成 · 待拍板实施）

> 目的：把 E3b 从"设计行"落成"可逐步骤执行、每步可回滚"的施工单。
> 以下前置事实为 2026-09-19 对代码的逐项核对（file:line 均真实）。

**前置事实（代码现状 vs 设计差距）**

1. `SwarmUnit/SwarmSnapshot` **未落** `activation/isLeader` 与感知/AI 连续性字段
   （`entity/SwarmUnit.ts:39-81`）；`AgentSnapshot`（`systems/swarm/AgentPool.ts:65-88`）同理。
2. `EnemyBase` E3a 字段在位（`EnemyBase.ts:65-91`），但 `hydrate/drain`（:115-145）
   **不覆盖感知/AI 状态**；`isLeader` 无字段。
3. `AgentPool` 无新列；`copy()` 是**逐列手动搬移**（`AgentPool.ts:228-263`）——
   ★ 新列必须同步 `push/copy/snapshot` 三处，漏一列 = swap-remove 后丢值（踩过的坑）。
4. `snapshot()` 只导出 15 列（`AgentPool.ts:266-279`）；`SwarmSystem.demote()`
   （`SwarmSystem.ts:148-165`）丢弃 `e.drain()` 的
   `uid/battalionId/squadId/formSlot/corridorIdx/role/moveTarget/attackType`
   → **当前降格即丢编队**（v2 铁律 1 违例，E3b 必修）。
5. `MobDef.role/attackType` 在位（`WorldSpawner.ts:85-87`）但 `EnemySpec` 无对应字段、
   `WorldMode` 装配未映射（`WorldMode.ts:834-848`）→ 实际恒 `grunt/melee`；
   `squadMode/noDemote` 代码中不存在。
6. 无 `Squad` 表 / `leaderUid` / 指挥权代码（全仓零命中）；无 `SwarmTierPort`。
   现有链路：promote = `SwarmSystem.update` → `hooks.promote`（`WorldMode.ts:861`）
   → `WorldSpawner.promoteAgent`（`WorldSpawner.ts:872-886`）；
   demote = `tickDemote` → `demoteFarEnemies` → `swarm.demote`（`WorldSpawner.ts:492-529`）。
7. 代理**无被击钩子**：`damageAgent`（`SwarmSystem.ts:584-592`）只扣血+白闪；
   调用点仅 `CombatSystem.updateAgentHits`（`CombatSystem.ts:71-98`）与
   `WorldMode.fireSentinelShotAtAgent`（`WorldMode.ts:3638-3642`）。
8. AI 状态：`aiStateMachine.currentState/stayTimer` 私有（`AIStateMachine.ts:13-15`）；
   实体计时器 `aiTurnTimer/aiAttackTimer/aiSwingDone`（`EnemyBase.ts:147-152`）；
   代理层 AI 在 `SwarmSystem.think` 内联（无独立状态对象）。
9. 感知：实体走 `conditions.seePlayer` + 共享 `ctx.target`；代理走共享警戒场
   （`FlowField.paintAlert`）+ `aggro` 半径；`lastSeen*/aggroFrom` 不存在。
10. 通信层零代码：无 `Squad`/`SquadBlackboard`/`SquadTactics`/`EnemyCommsPort`/
    `SquadOrder/UnitDirective/SwarmReport/SquadRating`（全仓零命中）；警觉上报尚未落地
    （设计口径：队员 → 队长 → 大队，见 §5.10/§5.11）。

**实施步骤（建议顺序，每步独立可回滚）**

| # | 步骤 | 落点 | 验收 |
|---|---|---|---|
| **1 ✅** | 类型面：`SwarmUnit/SwarmSnapshot` 补 `activation/isLeader` + `lastSeenX/Z/At/aggroFrom/aiStateIdx/aiTimer`（`activation` 不存快照 = 载体派生）；附 `ROLE_*/ROLE_ORDER/ATTACK_ORDER + roleCode/roleFromCode/attackCode/attackFromCode` SoA 换算口 | `entity/SwarmUnit.ts` | tsc 通过；无消费方（纯字段） |
| **2 ✅** | 实体面：`EnemyBase` 落 `activation='active'/isLeader=false`；`AIStateMachine` 增 `exportState()/importState()`（状态名↔idx + `stayTimer`）；`drain/hydrate` 覆盖 `isLeader/感知/AI 状态` | `EnemyBase.ts`、`AIStateMachine.ts` | 状态机导出→导入后 `currentState/stayTimer` 一致 |
| **3 ✅** | 池面：`AgentPool` 新列（`lastSeenX/Z/At`、`aggroFrom`、`aiStateIdx/aiTimer`；`isLeader/编队/role/attackType/moveTarget*/swarmUid` 随步骤 4 已落）；`push/copy/snapshot` **三处同步** + `demote` 透传 | `AgentPool.ts`、`SwarmSystem.ts` | swap-remove 后逐列断言（含新列） |
| **4 ✅** | 搬运面：`AgentPool` 新列（`swarmUid/battalionId/squadId/formSlot/corridorIdx/role/attackType/isLeader/moveTarget*+hasMoveTarget`，`push/copy/snapshot` 三处同步）+ `SwarmSystem.demote` 全字段透传（**修“降格即丢编队”**）；`EnemyBase.drain/hydrate` 覆盖 `isLeader`；`promoteAgent` 全量回灌 | `AgentPool.ts`、`SwarmSystem.ts`、`EnemyBase.ts` | 升格→降格→再升格：编队/意图/移动目标逐字段保留 |
| **5 ✅** | 指挥权：`SquadTable`（同质就近编队 ≤12、首员即队长、选举=最新情报>血量>靠队心、uid 分配器、adopt 兜底）+ `SwarmSystem` 接入（spawn/demote 归属与标记、`removeAgent` 注销并接任、`syncLeaderFlags`、`onLeaderChanged` 帧末广播、**升格不注销归属**）+ `WorldSpawner.byUid/setLeaderFlag` 镜像实体；`UnitRole` 补 `logistics` | `SquadTable.ts`（新）、`SwarmSystem.ts`、`WorldSpawner.ts`、`WorldMode.ts` | 队长阵亡/回收后 1 帧内新队长产生且 `isLeader` 唯一；升格保留归属 |
| 6 | 被击升格：`damageAgent` 增可选回调 `onAgentHit?(i, dmg)`；L3 半径内被击 → 走同一 promote 预算立即升格；`noDemote`（Boss）不降格 | `SwarmSystem.ts`、`WorldSpawner.ts` | 代理被玩家子弹打中 → 当帧升格；Boss 永不降格 |
| 7 | 名册透传：`EnemySpec` 增 `role/attackType/squadMode/noDemote` + `WorldMode` 装配映射；缺省推导（`ranged → attackType='ranged'`） | `config/enemyRoster.ts`、`WorldMode.ts` | 配了 role 的兵种代理/实体同口径 |
| 8 | `SwarmTierPort` 收口：promote/demote/recall 三链路统一接口（管线 P4），WorldMode hooks 只做转发 | 新 `systems/swarm/SwarmTierPort.ts` | 三链路仅此一处改实体集合 |
| 9 | 通信层（载体）：`SquadOrder/UnitDirective/SwarmReport/SquadRating/SquadType` 类型 + `EnemyBase` 命令字段与 `applyOrder/report` + `EnemyCommsPort` 注入 + `AgentPool` 同名列 + `SquadBlackboard/SquadTactics`（分解矩阵）+ 小队间 `SquadMessage` 中转 | `SwarmUnit.ts`、`EnemyBase.ts`、`AgentPool.ts`、`SwarmSystem.ts`、新 `systems/swarm/SquadTactics.ts`、`WorldMode.ts` | 命令变更 1 帧内到达载体字段；队员上报只进队长黑板；队长评级 0.5~1Hz 上报；升降格往返后命令/指令/评级全部保留 |
| **9e ✅** | **上报 / 小队间消息（最小切片）**（2026-09-19）：`SquadMessage`（requestSupport/shareContact 已实现；warn/regroupWith/flankCall 预留）+ 黑板消息队列（同 from+to 去重、收件即消、全灭清理）；队长：危急→向最近友邻求援、健康→赴援、有新目击→共享给友邻、无情报→按共享目击去查；评级 `threats` = 近期目击成员数（≤3s） | `SquadTactics.ts`、`SquadTable.ts` | 面板可见友邻小队互相赴援；评级威胁数不再恒 0 |
| **8 ✅** | **`SwarmTierPort` 三链路收口**（2026-09-19）：新 `systems/swarm/SwarmTierPort.ts`（promote/demote/recall 唯一接口）；`WorldSpawner implements SwarmTierPort`（`promote` 委托 / `demote` 单实体提取 / `recall` 扣配额）；`SwarmHooks` 删除 `promote`/`onAgentRecalled` → 单一 `tierPort`；WorldMode 只转发 | `SwarmTierPort.ts`（新）、`WorldSpawner.ts`、`SwarmSystem.ts`、`WorldMode.ts` | 升降格/回收只经端口；行为零变化 |
| **6 ✅** | **被击升格（限 L3 范围）**（2026-09-19）：`AgentPool.forcePromote` 标记；代理挨打且距玩家 ≤ `L3_RADIUS` → `update` 在回收判定前**当帧立即升格**（仍受 L3 上限/每帧预算约束） | `SwarmSystem.ts`、`AgentPool.ts` | 近处代理被子弹打中 → 当帧变实体（可被后续命中/有 AI） |
| **7 ✅** | **名册 `squadMode/noDemote`**（2026-09-19）：`EnemySpec/MobDef` 增字段 + 装配透传；原石虫巨人 = `singleton + noDemote`；`SquadTable` 单例编制（不并入/不被并入）+ `AgentPool.singleton` 列；降格门控读 `noDemote` 永保 active | `enemyRoster.ts`、`WorldSpawner.ts`、`WorldMode.ts`、`SquadTable.ts`、`SwarmSystem.ts`、`AgentPool.ts` | 小 boss 独立成队且永不降格；普通兵照旧同质编队 |
| **10 ✅** | **自主 LOD**（2026-09-19）：单位 `noDemoteUntil`（实体 `onTakeDamage` 置位+`enemy_hit` 广播；代理 `damageAgent` 直置；随快照跨 LOD）+ 小队警觉（`SquadTable.alertUntil`；任一成员被击→全队 8s）+ 大队警戒（12s 窗口内被击不同小队 ≥3 且玩家 ≤80m → **倾盆而出** 20s：全图警戒 220m、全体免降格/免回收、动态算力 `L3_CAP 30→45`/`PROMOTE 2→6`）+ 降格/回收门控（`WorldSpawner` / `SwarmSystem`） | `SwarmSystem.ts`、`SquadTable.ts`、`AgentPool.ts`、`EnemyBase.ts`、`SwarmUnit.ts`、`WorldSpawner.ts`、`WorldMode.ts`、`EventBus.ts` | 交火中不降频；3 支不同小队被击 → 倾盆而出且实体上限 +15；结束后自然回落 |
| **9d ✅** | **队长自主发令**（2026-09-19）：`SquadLeaderAI`（1Hz；20m 内 → `advance` 进攻玩家；`hpRatio ≤ 0.3` → `retreat` 背离集结点；**引擎命令优先不抢**；TTL 4s）接入 `SwarmSystem.update`；命令来源 `source: 'engine' \| 'leader'` | `SquadTactics.ts`、`SwarmSystem.ts` | 面板命令/指令不再是 none；接敌自主进攻、残血自主撤退；引擎命令覆盖队长命令 |
| **9c ✅** | **执行层（原子 + 两级掷）**（2026-09-19）：`entity/AtomExecutor.ts`（15 指令基准表 + 7 命令调制 + 情境/角色覆写 + 承诺窗口 + `rollMove/rollFire`）；代理侧 `think` 掷原子、开火掷门控攻击、`move` 原子方向覆盖 + `speedMul`；实体侧 `applyDirectiveAtoms`（AI 后覆盖移动 + `fireHold`），`meleeSwing/rangedShot` 消费禁火；`SquadType/桶映射` 上移契约层 | `AtomExecutor.ts`（新·实体层）、`SwarmUnit.ts`、`SquadTable.ts`、`SquadTactics.ts`、`AgentPool.ts`、`SwarmSystem.ts`、`EnemyBase.ts`、`behaviors.ts` | 发 `advance` → 代理/实体按表掷移动+开火；`retreat` → 高概率后撤；禁火段真的不开火；无指令回落旧行为 |
| **9b ✅** | **命令/指令载体**（2026-09-19）：`SquadOrderKind/DirectiveKind/TacticalOrder/UnitDirective` + SoA 编码（`ORDER_CODES/DIRECTIVE_CODES/FIRE_*`）+ `EnemyBase` 命令字段与 `applyOrder`（seq 防回放）+ `AgentPool` 13 列三处同步 + `SquadTactics`（黑板/缺参降级/默认分解矩阵/2Hz 分解）+ `SwarmSystem.issueOrder` + `onDirective` 推送（池写列 / 实体走 uid 映射）；全灭同步清黑板 | `SwarmUnit.ts`、`EnemyBase.ts`、`AgentPool.ts`、`SquadTactics.ts`（新）、`SwarmSystem.ts`、`WorldSpawner.ts`、`WorldMode.ts` | 发令后 ≤0.5s 到达全队载体字段；升降格往返后命令/指令保留；缺参命令自动降级 |
| **9a ✅** | **评级与阵亡上报**（2026-09-19）：`SquadTable.ratingOf/ratings`（血量为主；残血→retreating/broken）+ 池/实体成员同步（4Hz / 0.25s 节拍）+ 单人阵亡只下调评分 + **全灭 `onSquadWiped` 一次上报**（随后注销，不再需要支援）+ `SwarmSystem.ratings()` 引擎信息面 | `SquadTable.ts`、`SwarmSystem.ts`、`WorldSpawner.ts`、`WorldMode.ts` | 残血小队评级下降；全灭恰好上报一次且从评级表消失；单人死亡无事件 |
| 10 | 自主 LOD：单位 `noDemoteUntil` + 小队警觉 + 大队警觉度/倾盆而出 + 动态算力（`L3_CAP/PROMOTE` 加成）+ 降格/回收门控 | `SwarmSystem.ts`、`WorldSpawner.ts`、`EnemyBase.ts` | 交火中不降频；3 支不同小队被击 → 20s 倾盆而出且实体上限 +15；行为回滚开关 |

**进度（2026-09-19）**：**E3b 步骤 1~10 全部落地（含 9a~9e）**；**E5/E6 + CombatStats 已完成**；**五轴命令接入 + `SwarmCommander` 已落地**：`CombatStats` 组合件（`entity/CombatStats.ts`）——12 个战斗字段（hp/maxHp/attackPower/defense/attackSpeed/damageReduction/hpRegen/crit/dodge/block）从 `EntityBase` 收进 `stats`，同名访问器转发（读写 API 与行为零变化），子弹/物品不再背散落字段：`entity/enemy/` 三组合件——`EnemyLocomotion`（危险地形绕行 106 行）/ `EnemyPresentation`（显示帧/转身/扭曲 120 行）/ `EnemyBrain`（眩晕/保底攻击/指令→原子 245 行），**EnemyBase 884→513 行**（纯搬运行为零变化；`fireHold/atomMove/isStunned` 以 getter 保留、`applyStun/moveBy` 转发，外部零改动）（`tsc`/`guard` 通过；AI 状态/感知跨 LOD 通道就绪；小队注册表+队长选举/接任/镜像就绪；评级/全灭上报就绪）；
**设计定稿（本轮）**：兵种画像 §5.9 / 自主 LOD §5.10 / 通信与命令 §5.11（含五轴命令模型与 `BattalionView`）/ 执行契约 §5.12 / 原子与两级掷 §5.13 / 复杂度分级 §5.14；
后续（可选）：E5（EnemyBase 组合件拆分）/ E6（清理收口）；执行层近似值（实体移速 2.5、justHit）待配置化；9e 其余消息类型按需补。
★ **战术不在本轮**：蜂群战术决策（F3+）后续以"小队状态评级"为基本单位消费本节接口（见 §5.11）。

**验收与回滚**：升降格往返逐字段断言（`swarmUid/battalionId/squadId/formSlot/corridorIdx/
role/attackType/moveTarget/hp/意图/AI 状态+计时/感知/队长`）；无编队时行为与现状一致；
`npx tsc --noEmit` + `npm run guard` 通过。步骤 1~4 为纯字段（行为零变化，可单独 revert）；
5~10 每步独立（编队/指挥/被击升格/通信/自主 LOD 默认关，可回滚为现状）。

### 5.9 兵种画像字段（2026-09-19 用户定调 · E3b 落位）

| 维度 | 字段 | 取值 | 说明 |
|---|---|---|---|
| 运动域 | `isAir` + `altitude` | 地面 / 飞天 | 单一事实源 = `airborne/airAltitude`；地面走导航/车道/陷阱，飞天直线 + 避障 |
| 攻击方式 | `attackType` | none/melee/ranged/bombard | 近战 / 远程弹道 / 轰炸；实体与代理同口径（E3a 已落） |
| 兵种角色 | `role` | shield/assault/grunt/ranged/flyer/**logistics** | ★ `logistics` = 后勤（不冲锋，随队补给/维修/治疗；行为后续接） |
| 载体 | `carrier` / `activation` / `isAgent` | entity·agent / active·dormant / 派生 | `isAgent` = `carrier==='agent'` 派生只读位（实体恒 false） |
| 自爆 | `suicide` | bool | 攻击 = 范围爆炸（`aoe`）+ **自身死亡**（爆炸飞行怪；跨 LOD 随快照，2026-09-19） |
| 免降格 | `noDemoteUntil` | 秒 | 被击后短时免降格（**单位级**；小队/大队级见 §5.10） |
| Boss | `noDemote`（MobDef，步骤 7） | bool | 单例永保 active，不因距离降格 |

★ 画像用**数据字段**表达（不按类型炸子类）：同一条执行管线按 `isAir/attackType/role`
分支行为；将来需要专属行为再接子类（E5 组合件拆分时再议）。

### 5.10 自主 LOD（单位/小队/大队三级警觉 → 倾盆而出）

> 定调（2026-09-19 用户）：**LOD 状态由敌人自主决定**——不再只由"离玩家多远"决定；
> 交火中的单位/小队/大队拥有降格豁免；持续挨打触发大队反击（倾盆而出）。

| 级别 | 触发 | 效果 | 参数（`SwarmSystem.AUTONOMY`） |
|---|---|---|---|
| 单位 | 被击（实体 `onTakeDamage` / 代理 `damageAgent`） | `noDemoteUntil` = now+6s：本人免降格/免回收 | `UNIT_HOLD_S` |
| 小队 | 任一成员上报 `underAttack`（队长聚合） | 全小队免降格/免回收 8s | `SQUAD_ALERT_S` |
| 大队 | **玩家近（≤80m）且** 12s 窗口内**被攻击的不同小队数 ≥ 3**（队长上报累积） | **倾盆而出** 20s：全体免降格/免回收 + `paintAlert(玩家, 220m, 20s)` 全队收拢 | `BATTALION_WINDOW_S/COUNTER_SQUADS/COUNTER_S/COUNTER_ALERT_R` |
| 算力 | 倾盆而出期间 | `L3_CAP 30→45`、`PROMOTE_PER_FRAME 2→6`（适当增加算力、增多非代理敌人） | `L3_CAP_BOOST/PROMOTE_BOOST` |

- **聚合路径走通信（§5.11）**：队员 `underAttack` → 队长 → 小队警觉 → 大队累计；实体不直连引擎。
- 门控点：`WorldSpawner.demoteFarEnemies`（实体降格）与 `SwarmSystem.update`（代理回收/升格）
  统一问 `holdDemote(squadId, battalionId, now)` + `noDemoteUntil`。
- 大队警觉度 `swarm.alertLevel`（0..1）可读（调试/HUD 预留）。

**大队警戒度分级（大队 LOD 的驱动，2026-09-19 用户定调）**

> **整个大队也按警戒状态做 LOD**：玩家近、很多小队被打 → **最高警戒度**；
> 警戒度驱动算力/复杂度/倾盆而出（与 §5.14 复杂度、§5.10 豁免联动）。

| 警戒度 | 触发 | 大队 LOD 效果 |
|---|---|---|
| **A0 平静** | 无接敌、玩家远 | 各小队按距离基线（C0~C1）；基础算力 |
| **A1 警觉** | 目击/枪声，或 1 支小队被击 | 搜索加强；涉事小队 C3；算力微增 |
| **A2 警戒** | 玩家近（≤80m）或 2 支小队被击 | 队长分解全开；`L3_CAP +8`、升格预算 +2；被击小队免降格 |
| **A3 最高警戒（倾盆而出）** | **玩家近（≤80m）且 ≥3 支小队被击**（12s 窗口） | 全体免降格/免回收；`L3_CAP 30→45`、`PROMOTE 2→6`；`paintAlert(玩家,220m,20s)`；全部小队 C3 |

- **只升即时、回落渐降**：升级立即生效；无新被击后按衰减回落（回距离基线，无状态残留）。
- "玩家近"与"多小队被击"是**与**条件：单独玩家近 → 至多 A2；多小队被击但玩家远 → 也至多 A2
  （不倾巢，防"隔屏狙击全图暴走"）。

### 5.11 通信与命令（2026-09-19 定稿 · 载体侧）

> 本轮只做**载体**：能正确执行命令、小队内/小队间通信、队长上报**小队状态评级**。
> 蜂群引擎的**战术决策**（F3+）后续以"小队状态评级"为基本单位消费这些接口。

**决策流水线（端到端，2026-09-19 定稿）**

```
蜂群引擎 ──SquadOrder（明确的小队命令）──▶ 队长 ──UnitDirective（队长分配的个体指令）──▶ 每个士兵
                                                                        │
                一层掷（决策合成：SquadOrder + UnitDirective + 本地态势 → 复杂决策）
                                                                        │
                二层掷（原子偏置：fire/forward/back/strafeL·strafeR/hold/face → 本拍行为）
```

★ 引擎给"明确的小队命令"，队长再分配"个体指令"，**每个人再二层掷得到最终行为**
（一层选"做什么"、二层掷"这一拍怎么做"）；执行决策要掷、每个原子操作再掷（§5.11 决策合成 / §5.13）。

**拓扑（三级；收/发都只对队长）**

```
大队/引擎 ──▲ 上报（仅队长：小队评级/黑板摘要）──┐
           └─▼ 命令（仅队长）──────────────────┤
                       队长（active 优先；dormant 代理队长亦可）
      ┌─▲ 上报（队员→队长）        ┌─ 小队间：队长↔队长（经引擎中转）
      └─▼ 两层命令（整体+个体）    └─ 队员↔队员：仅 ≤5m 直觉
                       队员（实体/代理同构）
```

- 队长缺失（阵亡/降格/全队休眠）→ 大队直管该队。
- **小队间通信**：队长 ↔ 队长，经大队/引擎中转
  （`SquadMessage`：`shareContact / requestSupport / warn / regroupWith / flankCall`），
  保持"收发只对队长"的规则。

**两层命令（队内）**

**命令层级与职责边界（2026-09-19 用户定调）**

| 层级 | 谁下 | 内容 | 覆盖关系 |
|---|---|---|---|
| **大队任务 `BattalionMission`** | 蜂群架构 | **路径 + 目标区域/点**（去哪、打哪片） | 全部小队的基线 |
| **小队命令 `SquadOrder`** | 蜂群架构（额外覆盖）/ 队长 | 七条 mode + 参数 | **覆盖大队任务**（仅该小队） |
| **个体指令 `UnitDirective`** | 队长 | 十五条 do + 参数 | 覆盖小队命令的执行方式 |
| **攻击目标** | **小队自主**（队长 / 本地 AI） | 打谁（最近威胁 / 威胁度 / 射程 / 嘲讽 / 集火指定） | 引擎只管"打哪片"，**不管"打谁"** |

- **路径与目标 = 蜂群架构负责**：走廊 / 寻路 / 目标区域由引擎生成；队长的 `SquadTactics`
  只做**执行分解**（选谁去、怎么打），**不改任务路径**。
- **引擎侧信息面**：引擎只读各队 `SquadRating`（**属性 + 状态 + 玩家目击 + 在打谁**）
  + 全局玩家/舰船位置；小队只需汇报这两样，引擎即可**随意派发命令**
  （《蜂群架构.md》§16.6 `BattalionView`）。
- **引擎可给特定小队"额外覆盖命令"**（包抄 / 偷袭 / 保护 / 撤退…）带其离开大队任务区；
  队长**不能自主**带队脱离任务区（弱权限，见下）。

① 小队整体命令 `SquadOrder`（引擎→队长→全队，mode）——存系统侧 `SquadBlackboard`
（可带**路径数组**）；镜像进载体快照（`orderKind/orderTargetX/Z/orderUntil/orderSeq`）供跨 LOD 读上下文：

| 命令 | 语义 | 必要参数 | 可选 | 缺参降级 |
|---|---|---|---|---|
| `advance` 进攻 | 沿路径向目标推进并交战 | `target`（点）或 `path` | `urgency` | → `regroup` |
| `retreat` 撤退 | 脱离交战，向集结点后撤 | `rally`（点）或 `path` | `holdLine`（殿后线） | → `regroup` |
| `protect` 保护 | 护卫单位 / 点位 / 区域 | `wardUid` 或 `wardPoint` | `radius`（默认 8m） | → `regroup` |
| `flank` 偷袭 / 绕后 | 沿路径绕至侧后突击 | `path`（≥2 点）或 `via + strike` | `delay`（发动延迟） | 无路径 → `advance(strike)` |
| `bound` 边打边移动 | 向目标交替跃进 | `target` | `boundLen`（段长） | → `advance` |
| `focus` 集火 | 集中火力打指定目标 | `targetUid` 或 `targetPoint` | — | → `advance` |
| `regroup` 归队 | 回队长 / 阵位 | 无（队长位置即目标） | — | — |

- **路径不进载体字段**：路径 / 目标存系统侧，系统侧按进度滚动更新载体 `targetX/Z`
  （走廊 `corridorIdx` 同步）——载体只看到"当前目标点"（SoA 友好，无数组进快照）。
- **参数校验**：引擎 / 队长下发前校验必要参数；缺参按上表降级（**绝不发"无法执行"的命令**）——
  保护缺护卫对象、偷袭缺路径都不会发生。

**通用复杂命令模型（战术可扩展；执行层只认这几根正交轴，2026-09-19 用户定调）**

> 未来战术（包围 / 穿插 / 偷袭 / 防御…）**不新增执行原语**——全部表达为下列正交轴的组合；
> 小队执行层只认轴，**战术配方（轴 → 分解）在系统侧**，加战术不改载体。

```ts
interface TacticalOrder {          // SquadOrder / BattalionMission 共用外壳
  kind: SquadOrderKind;            // 现有七条（可扩展）
  path?: { x: number; z: number }[];             // 运动轴：直推 / 绕行 / 分路
  target?: { x: number; z: number; r?: number }; // 目标点 / 区域
  roe: 'engage' | 'holdFire' | 'fireOnArrival' | 'focusOnly'; // 交战轴
  formation?: 'column' | 'line' | 'loose' | 'wings';          // 队形轴
  startAfter?: number; signal?: number;          // 时序轴：延迟发动 / 等信号
  subTargets?: { x: number; z: number; squadId?: number }[];  // 分工轴（引擎分派子目标）
  urgency?: number; deadline?: number; seq: number;
}
```

| 未来战术 | 轴组合（示例） |
|---|---|
| **包围** | 多路 `path`（各小队一条）+ `subTargets` 各分一点 + `formation: loose` + `roe: engage` |
| **穿插** | 直线 `path`（穿隙）+ `roe: holdFire`（到位前）+ 高 `urgency` + `formation: column` |
| **偷袭** | 绕行 `path` + `roe: holdFire` + `startAfter / signal` + 到位后转 `engage` |
| **防御** | `target`（点 / 线 / 区域）+ 驻守 + `formation: line` + `roe: engage` |

- **执行保证**：任意命令 → `SquadTactics` 分解为「沿 `path` 滚动 `target` + ROE + 队形槽位 +
  个体指令」→ 原子偏置表；**只要命令由这五轴组合，任何小队都能听懂并执行**（载体零改动）。
- **扩展边界**：若未来出现无法用五轴表达的战术（新执行原语，如"施工事 / 埋雷 / 载具"），
  才需要扩执行层——在文档显式标记为**执行原语扩展**（不在本轮）。
- **本轮范围**：只做**敌人基类 + 小队内部执行层**（命令解析 / 分解 / 两级掷 / 原子执行）；
  **战术配方（包围/穿插/偷袭/防御 的具体算法）后续再开发**。

② 个体指令 `UnitDirective`（队长→每个士兵，do）：

```ts
interface UnitDirective {
  kind: DirectiveKind;                    // 见分解矩阵
  targetX?: number; targetZ?: number;     // 目标点
  wardUid?: number;                       // 护卫对象（protect）
  until: number;                          // 截止（弱权限：短 TTL）
  fire: 'free' | 'hold' | 'moving';       // 开火策略
  speedMul: number;                       // 移速倍率（脱离 1.2 / 边打边撤 0.7）
  seq: number;                            // 防旧命令回放
}
```

**个体指令规格（15 条；必要参数缺失 = 该指令不合法，队长不分配）**

| 指令 | 语义 | 必要参数 |
|---|---|---|
| `push` 前压 | 向目标压上 | `target` |
| `suppress` 火力压制 | 保持距离输出 | `target` |
| `screen` 殿后 | 面向敌人后撤掩护 | — |
| `fallback` 脱离 | 快速撤出战斗 | `rally` |
| `boundBack` 边打边撤 | 交替后撤 / 还击 | `rally` |
| `guardWard` 贴身 | 贴着护卫对象 | `wardUid` |
| `block` 卡位 | 挡在护卫对象与威胁之间 | `wardUid` |
| `intercept` 截击 | 迎击接近护卫对象的敌人 | `wardUid` |
| `sneak` 低姿态 | 隐蔽行进（禁火 / 慢速） | `path`（系统侧滚动 `target`） |
| `pin` 牵制 | 正面吸引火力 | `target` |
| `strike` 背击 | 突击侧后 | `target` |
| `bound` 跃进 | 向前交替跃进 | `target` |
| `cover` 掩护 | 停火掩护队友跃进 | — |
| `focusFire` 集火 | 打指定目标 | `targetUid` |
| `regroup` 归队 | 回队长 / 阵位 | — |

**分解矩阵（小队命令 × 角色 → 个体指令）**

| 小队命令 | 个体指令池 | 典型分配 |
|---|---|---|
| **advance 进攻** | `push` / `suppress` / `screen` | shield→push，ranged→suppress，assault→push |
| **retreat 撤退** | `fallback` / `screen` / `boundBack` | logistics·低血→fallback，shield→screen，assault/ranged→boundBack |
| **protect 保护** | `guardWard` / `block` / `intercept` | shield→block，assault→intercept，远程→guardWard 圈内 |
| **flank 偷袭** | `sneak` / `pin` / `strike` | 路径段全员 sneak；接敌后 ranged→pin，assault→strike |
| **bound 边打边移动** | `bound` / `cover` | 定时轮换：一半 bound、一半 cover（按槽位奇偶） |
| **focus 集火** | `focusFire` | 所有有射程者；近战转 `push` 到目标 |
| **regroup 归队** | `regroup` | 全员 |

★ **默认个体指令**：队长未分配时按上表（分解矩阵）的"典型分配"兜底；命令缺参降级后同样落到默认。

**队长策略表（按小队属性；2026-09-19 用户定调：不同属性不同策略）**

| 属性 | 接敌半径 | 行为 | 残血撤退 |
|---|---|---|---|
| assault 突击 | 22m | 直扑贴身 | ≤30% 撤 18m |
| defense 防御 | 18m | 稳推进（略近接敌） | ≤22% 撤 14m |
| ranged 远程 | 28m | **保持射程环 20m（不追脸）** | ≤35% 撤 22m |
| logistics 后勤 | 18m | 缩后（站位 10m） | ≤55% 撤 24m |
| flyer 飞行 | 24m | 直扑 | ≤30% 撤 18m |
| suicide 自爆 | 34m | **冲锋（不撤）** | — |
| mixed 混编 | 20m | 直扑（折中） | ≤30% 撤 18m |

★ 后续战术重写只改 `LEADER_STRATEGY` 表 / `SquadLeaderAI`（策略表可扩展）。

**★ 五轴命令接入小队执行链（2026-09-19）**：`path`（队质心前方 >4m 路点滚动为指令目标）/
`subTargets`（按 squadId 分派子目标，优先于总目标）/ `startAfter`+`signal`（时序门控：
`notBefore` + 黑板信号集，`emitSignal` 触发）/ `roe`（`holdFire` → 开火偏置 ×0.05 软禁火、
`moving` ×0.95）/ `urgency`（限速乘子 1+u×0.3 上限 1.5）。

**★ 引擎侧指挥器 `SwarmCommander`（2026-09-19）**：`setMission`（大队任务，全队基线，
每 10s 重发保 TTL）/ `orderSquad`（小队覆盖，引擎优先）/ `emitSignal` / `view()` 返回
`BattalionView`（各队评级 + 全局玩家/舰船位置）——战术决策（F3+）的输入面。

★ **队长管队内（2026-09-19 用户定调）**：分解**按成员血量逐个下发**——个体 `hpRatio ≤ 0.3`
→ 该员 `fallback`（禁火 + 1.2× 撤出战斗，撤退档除外）；队长在"**过半成员残血**"时下全队 `retreat`
（即使队均血量还行；自爆档不撤）。引擎不管队内残血，队长管。

**队长分解规则（系统侧 `SquadTactics`）**：0.5s 节拍或命令/成员变化时重算；
输入 = 命令 + 成员（role/hp/位置/槽位）+ 黑板（目击/威胁）；**输出尽量稳定**
（同命令下只改必要的人，防抖）；硬约束：shield 不排 `fallback`（除非低血）、
logistics 优先 `fallback`、撤退时至少留 1 人 `screen`。

**上报与评级**

- `SwarmReport`（队员→队长）：`contact / underAttack / casualty / lowHp / blocked / arrived / needSupport`；
  节流 ≥0.5s/条；队长聚合进 `SquadBlackboard`（最后目击点/威胁/伤亡/求援）。
- **小队属性 `SquadType`（系统侧 `SquadBlackboard.type`，随评级上报）**：
  同质编队 → 由成员角色派生（Boss 单例取自身）：
  `defense 防御 / assault 突击 / ranged 远程 / logistics 后勤 / flyer 飞行 / mixed 混编（过渡态）`。
  ★ **战术分派的两大依据 = 小队属性（是什么队）+ 状态评级（现在怎么样）**。
- **`SquadRating`（队长→引擎，0.5~1Hz 或显著变化）** —— **战术决策的基本单位**：

```ts
type SquadType = 'defense' | 'assault' | 'ranged' | 'logistics' | 'flyer' | 'mixed';

interface SquadRating {
  squadId: number; battalionId: number; at: number;   // at = 上报时刻（判新鲜度）
  type: SquadType;      // ★ 小队属性（防御/突击/远程/后勤/飞行；战术分派第一依据）
  grade: number;        // 综合评级 0~1（= hpRatio·0.7 + morale·0.3）
  hpRatio: number;      // ★ 评级主因：全队血量比（Σhp / ΣmaxHp）
  power: number;        // 战力（存活人数 × 血量比）
  morale: number;       // 士气（血量比 − 阵亡累计扣减）
  status: 'idle' | 'contact' | 'pursuing' | 'retreating' | 'broken';
  cx: number; cz: number; heading: number;            // 质心/朝向（引擎分派用）
  // ---- ★ 玩家情报（小队自己知道的，带新鲜度；引擎的全局玩家位置另供寻路，不混入评级） ----
  lastSeenX?: number; lastSeenZ?: number; lastSeenAt?: number;
  // ---- ★ 当前交战：在打谁（步骤 9 通信接线后填；缺省 none） ----
  engaging: 'none' | 'player' | 'ship' | 'sentinel' | 'ally';
  targetX?: number; targetZ?: number;
  threats: number;      // 黑板威胁数
  alive: number;        // 存活人数（全灭的小队已注销，不会出现在评级表）
}
```

**评级规则（2026-09-19 用户定调）**

- **评级主要与队内血量有关**：`hpRatio = Σhp/ΣmaxHp`；`grade = hpRatio·0.7 + morale·0.3`。
- **残血必撤**：`hpRatio ≤ 0.3` → `status='retreating'`（无力再战，必须回撤）；`≤ 0.12` → `broken`。
- **总攻期例外（A3 倾盆而出）**：即使濒死也参战——由战术层改为"**位置靠后**"
  （`suppress/cover` 类指令），让其他小队顶前；评级本身不做特殊处理。
- **阵亡上报口径**：**单人阵亡只下调评分**（`casualties → morale`），**不发事件**；
  **只有全灭才上报**（`onSquadWiped` 每队一次），全灭后小队**注销**、不再出现在评级表
  → "死了就直接不用管了"。
- **支援**：高价值/低评分（残血）且**存活**的小队由引擎派支援（战术层消费评级）；
  已全灭的小队无从支援、也不占名单。

**权限边界（"弱权限"）**：`个体指令 > 小队命令 > 大队任务 > 本地自主交战 > 巡逻/游荡`；
**无命令 → 自主决策；有明确命令 → 命令优先**（命令合计权重 ≥0.85，见"决策合成"）；
- 队长只改"**怎么打**"与"**打谁**"，不改"**去哪**"（路径/目标 = 蜂群架构）；
- 队长**不能自主**带队脱离大队任务区；蜂群架构可用**覆盖命令**带特定小队离开；
- `retreat` 有距离/时限帽（≤25m/≤6s），超限自动 `regroup` 并上报；
- `orderSeq/directiveSeq` 防旧命令回放。

**决策合成（共同决策 + 权重掷概率，2026-09-19 用户定调）**

每个代理/实体的每个决策拍，把**小队整体命令**与**队长个体指令**放进同一张候选动作表
**共同决策**（不是单一优先级硬覆盖）：

- 候选动作：`advance/engage · suppress · screen · fallback · boundBack · guard · sneak · hold · patrol …`
- 得分：`S(a) = wSquad·U(a|SquadOrder) + wLeader·U(a|UnitDirective) + wLocal·U(a|本地态势)`
- 权重（有命令）：`wSquad 0.35 / wLeader 0.50 / wLocal 0.15`（可调；**命令合计 ≥0.85** → 仍满足"命令优先"）
- 无命令 / 过期：`wLocal = 1`（完全自主）
- **掷概率（两级）**：一级（复杂决策）按 `softmax(S/τ)` 轮盘赌选取（τ 默认偏冷 = 命令主导）；
  二级（原子操作，§5.13）拆**两次独立掷**——**移动掷**（五选一）+ **开火掷**（二元，与移动正交）；
  所有原子恒可执行，按命令偏置掷——明确进攻下小概率游荡/后退也正常
- **承诺窗口**：复杂决策保持 1~2s（命令变更 / 目标死亡 / 被击立即重掷）；原子级 0.3~0.5s 节拍 + 最小段长
- 命令**不移除动作，只改概率**（软约束）

**载体接口（实体永不查询 swarm）**

- 字段（随快照往返）：`orderKind/orderTargetX/Z/orderUntil/orderSeq` +
  `directiveKind/directiveTargetX/Z/directiveWard/directiveUntil/directiveFire/directiveSpeedMul/directiveSeq`
- 接收：`applyOrder(order, directive)`（低频推送，与 `applySteer` 同范式；实体只读自己字段）
- 上报：`report(kind, payload)` 内部走注入端口 `EnemyCommsPort`
  （模式层实现路由：队员→队长、队长→大队/引擎）
- 代理侧：`AgentPool` 同名列 + `snapshot()` 携带；`SquadBlackboard/SquadTactics` 全在系统侧，天然跨 LOD

**与行为层衔接**：`directive` = "要做什么"，`SteerIntent` = "这一帧怎么走"；系统侧翻译：
`fallback` = 背对敌人、禁火、全速；`screen` = 面向敌人、允许开火、后退限速；
`boundBack` = 向后跃进（移动段/停火段交替）；`bound` = 向前跃进；`sneak` = 隐蔽路线 + 禁火。

### 5.12 命令执行契约（载体侧保证）

- **收到即执行**：指令编译成**原子偏置表**（§5.13）后逐原子执行；偏置是**软约束**
  （所有原子恒可执行），但 `speedMul/target` 等数值参数仍按命令（限速期不超速、目标按指定）。
- **过期回落**：`until` 到期 → 按 `orderKind` 回落（撤退 → `regroup`；其余 → 本地 AI），
  **绝不停摆**（v2 铁律 3）。
- **★ 无命令自主 / 有命令命令优先（2026-09-19 用户定调）**：
  - **无命令**（无 `SquadOrder/UnitDirective` 或已过期）→ 载体自主决策：检测到敌人
    （视野 / 警戒场 / 被击）即自主索敌交战（锁定最近威胁 → 追击 + 攻击；近战贴身、
    远程保持射程），无敌人则巡逻/游荡；同时上报 `contact/underAttack` 给队长。
  - **有命令**（未过期）→ **命令优先**：命令明确的维度（目标点 / 开火策略 / 限速 /
    护卫对象）一律按命令执行；本地自主只在命令**未覆盖**的维度补位；命令过期即回落自主。
  - 命令是"增强/约束"，**不是开战开关**；两路命令与本地态势按"决策合成"（§5.11）
    加权掷概率共同决策；代理（dormant）同样适用（警戒场 / `aggro` / 被击 → 追击攻击）。
  - **实体保底攻击（2026-09-19）**：实体无指令且 AI 状态机未处于 `attack` 时，目标在射程内
    直接开火（构造期从 AI 配置解析 inRange 半径 + meleeSwing/rangedShot 参数；冷却节流）——
    防"状态机卡在 patrol（minStay）/贴脸不打"；状态机在打时让位（防双开火）。
- **顺序**：`seq` 小于当前值的旧命令直接丢弃（防升格/降格后旧包回放）。
- **跨 LOD**：命令/指令全字段随快照往返；休眠代理不 tick，升格后按字段继续执行。
- **可观测**：`commsRole`（leader/member）+ 当前 `order/directive` 供调试可视化读取。

### 5.13 原子操作与行为组合（2026-09-19 用户定调）

> 每个敌人的**执行层只有"原子操作"**；一切复杂决策（指令/自主决策）都是这些原子的
> **组合执行**——例如"保护" = 在护卫对象附近**边开火边左右游荡**。

**原子操作（执行层最小单位）**

| 原子 | 语义 | 参数 |
|---|---|---|
| `fire` 开火 | 对当前目标执行攻击动作（近战挥击 / 远程射击；受冷却与射程约束） | 目标（实体/代理/点） |
| `forward` 前进 | 向目标点 / 朝向推进 | `speedMul` |
| `back` 后退 | 背向目标点 / 朝向退 | `speedMul` |
| `strafeL / strafeR` 左右游荡 | 以朝向为轴横向绕行（保持距离 / 找角度） | `speedMul` |
| `hold` 驻停 | 原地不动（仍可转向 / 开火） | — |
| `face` 朝向 | 面向目标（实体 / 点） | 目标 |

**原子级掷概率（2026-09-19 用户定调）**：原子决策也是**掷**出来的——**所有原子恒在候选集、
恒可执行**，命令只改**概率偏置**；即使是明确进攻，小概率左右游荡 / 后退也并无不妥。

**★ 二级投掷拆成两次独立掷（2026-09-19 用户定调）**——开火与移动**正交**，分开掷：

- **移动掷**：`forward / back / strafeL / strafeR / hold`（互斥五选一）
- **开火掷**：`fire / no-fire`（独立二元；与移动**可同时成立** → "边打边撤 / 边打边推"自然涌现）
- 朝向不掷（接敌恒面向目标/威胁）；目标不掷（最近威胁或队长指定）

**基准概率表（按个体指令，15 条；示例可调，数据化 = config 常量）**

| 指令 | forward | back | strafeL | strafeR | hold | P(fire) |
|---|---|---|---|---|---|---|
| `push` 前压 | 0.55 | 0.05 | 0.15 | 0.15 | 0.10 | 0.85 |
| `suppress` 压制 | 0.10 | 0.05 | 0.20 | 0.20 | 0.45 | 0.95 |
| `screen` 殿后 | 0.05 | 0.40 | 0.20 | 0.20 | 0.15 | 0.85 |
| `fallback` 脱离 | 0.05 | 0.50 | 0.20 | 0.20 | 0.05 | 0.25 |
| `boundBack` 边打边撤 | 0.10 | 0.45 | 0.15 | 0.15 | 0.15 | 0.45（时段） |
| `guardWard` 贴身 | 0.20 | 0.10 | 0.30 | 0.30 | 0.10 | 0.90 |
| `block` 卡位 | 0.15 | 0.10 | 0.25 | 0.25 | 0.25 | 0.70 |
| `intercept` 截击 | 0.55 | 0.05 | 0.15 | 0.15 | 0.10 | 0.90 |
| `sneak` 低姿态 | 0.70 | 0.02 | 0.10 | 0.10 | 0.08 | 0.05 |
| `pin` 牵制 | 0.15 | 0.10 | 0.30 | 0.30 | 0.15 | 0.90 |
| `strike` 背击 | 0.60 | 0.03 | 0.15 | 0.15 | 0.07 | 0.85 |
| `bound` 跃进 | 0.45 | 0.05 | 0.15 | 0.15 | 0.20 | 0.55（时段） |
| `cover` 掩护 | 0.05 | 0.05 | 0.20 | 0.20 | 0.50 | 0.90 |
| `focusFire` 集火 | 0.05 | 0.05 | 0.15 | 0.15 | 0.60 | 0.95 |
| `regroup` 归队 | 0.60 | 0.02 | 0.12 | 0.12 | 0.14 | 0.30（无敌时 0） |

**小队命令调制（叠加在个体指令基准上）**——**任意（小队命令 × 个体指令）组合都有确定掷表**：

`P = normalize( 基准[指令] ⊕ 调制[命令] ⊕ 调制[情境] )`
（逐项相加、`clamp(≥0.01)` 后归一化；`P(fire)` 加后 clamp 到 `[0.02, 0.98]`——软约束，原子恒可执行）

| 小队命令 | forward | back | strafe | hold | ΔP(fire) |
|---|---|---|---|---|---|
| `advance` | +0.10 | −0.05 | −0.05 | 0.00 | +0.05 |
| `retreat` | −0.10 | +0.15 | 0.00 | −0.05 | −0.10 |
| `protect` | −0.05 | −0.05 | +0.05 | +0.05 | +0.05 |
| `flank` | +0.05 | −0.05 | 0.00 | 0.00 | −0.15（接敌前） |
| `bound` | +0.05 | 0.00 | −0.05 | 0.00 | 0.00 |
| `focus` | −0.05 | −0.05 | 0.00 | +0.10 | +0.05 |
| `regroup` | +0.05 | −0.05 | 0.00 | 0.00 | −0.10 |

- `strafe` 调制同时加到 L/R；叠加后归一化 → **矩阵全覆盖**（不逐对枚举 7×15 张表）。

**情境调制（在基准上乘/加；每拍应用）**

- 超射程（远程）：`P(fire) ×0.1`，移动偏 `forward`（进入射程）
- 无敌人可见：`P(fire) → 0`（不打空气）；移动回 `regroup/patrol`
- 低血（≤30%）：移动 `back` +0.15、`P(fire) −0.2`（保命）
- 被击：本拍 `strafe` +0.2（躲闪），下一拍回落
- `boundBack` 时段：back 段（0.6s）`P(fire) 0.2`；cover 段（0.4s）`P(fire) 0.85`
- τ（纪律）：低 τ → 更贴表；高 τ → 更均匀（杂兵感）
- **角色覆写**：`logistics` 的 `protect` 表 `P(fire) 更低 / strafe 更高`；`ranged` 的 `advance` 表
  `forward 更低`（保持射程）；覆写同走 config

- **偏置 = 软程序**：`boundBack` 的"移动段 / 停火段"只是**偏置切换**，不是硬开关；
  概率分布由 `softmax(偏置/τ)` 给出，τ 控纪律性（低 = 命令主导、动作更纯；高 = 更随机）。
- **编译位置**：系统侧（`SquadTactics` / 行为层）把指令编译成**原子偏置表**；载体只负责
  执行原子（移动三态 + 开火触发 + 朝向），不关心战术语义（依赖方向不反转）。
- **两级掷概率**：§5.11 权重掷概率决定**选哪个复杂决策**；本节的偏置分布决定**每个决策拍掷出哪个原子**。
- **节奏**：原子级按 0.3~0.5s 节拍掷 + 最小段长（防每拍翻转）；复杂决策承诺窗口 1~2s；
  被击 / 命令变更 / 目标死亡立即重掷。

### 5.14 决策复杂度分级（与 LOD 配合；2026-09-19 用户定调）

> 命令与决策的**复杂度随距离逐级增加**；**小队被打了直接提到最高复杂度**
> （与 §5.10 免降格同一触发）。

| 复杂度 | 基线触发（距玩家/焦点） | 命令面 | 决策面 | 节奏 |
|---|---|---|---|---|
| **C0 行军** | > 80m（L1 远群） | 仅大队 `SquadOrder`（走廊/目标推进） | 无个体指令；无两级掷（遇敌仅"追击 + 攻击"） | think 2Hz（现 tier1） |
| **C1 编队** | 35~80m（L2 代理） | `SquadOrder` + 阵型槽位/角色分组 | 无逐人指令；原子级低频掷 | think 2~5Hz（现 tier2） |
| **C2 战术** | < 35m（L3 实体） | `SquadOrder` + `UnitDirective` 全量 | 两级掷全开 | think 5Hz |
| **C3 交火（最高）** | **被击**（小队任一成员上报 `underAttack`） | 同 C2 + 全量上报/黑板 | 两级掷 + 事件驱动立即重掷 | think 5Hz + 立即重掷 |

- **提升**：距离分级给**基线**；小队被击 → **强制 C3**（持续 = §5.10 警觉窗口，可叠加延长），
  期间即使远离玩家也保持最高复杂度（与免降格同一套触发）。
- **回落**：C3 结束 → 按距离基线**逐级**回落（无状态残留）。
- **与现有 LOD 参数映射**：C0/C1 ≈ `AGENT_TIER_FAR/MID`；C2/C3 = L3 实体/接敌；
  `THINK_HZ/MOVE_HZ` 继续用；C3 额外"事件驱动立即重掷"（命令变更 / 被击 / 目标死亡）。
- **算力纪律**：复杂度只决定"开多少决策面"（是否分解个体指令 / 是否两级掷），
  **不改变原子集**（原子恒可执行，§5.13）。

---

## 6. AllyBase（友军 · 三种）

### 6.1 三种友军（2026-09-18 用户定调）

| 类型 | 移动 | 典型行为 | 现状对应 |
|---|---|---|---|
| **飞天友军** | 空中悬停（`airborne`），直线 + 避障，不吃地形落差 | 跟随玩家 / 俯冲近战 / 投弹（远期） | 可露希尔的无人机 |
| **陆地跟随友军** | 贴地（地形规则/立面/推挤全开），跟随锚点 + 编队偏移 | 跟随玩家 / 近战或远程 / 归队 | （新增，现无） |
| **陆地站桩友军** | 不位移（基座高度 + 转向/索敌） | 绕自身索敌 / 远程 / 激光 / 挖矿 | 祖宗（悬浮是表现，归属站桩） |

★ 装备/弹药类实体**不在本次重做范围**（保持现状，不动）。

### 6.2 类树与文件

```
entity/ally/
  AllyBase.ts              // 公共：世界端口 / 索敌 / 攻击节奏 / 槽位 / 存活 / 编队兼容位
  AirAlly.ts               // 飞天骨架（airborne 域）
  GroundFollowAlly.ts      // 陆地跟随骨架（贴地 / 跟随锚点）
  GroundStationaryAlly.ts  // 陆地站桩骨架（不位移 / 基座 / 绕自身索敌 / ★ sleep·wake 留存休眠）
  DroneAlly.ts             // 飞天：原 DroneEntity 无人机（跟随 + 近战 + 射线）
  SentinelAlly.ts          // 站桩：原 DroneEntity 祖宗（激光 + 挖矿；表现微悬浮）
```

```
AllyBase extends EntityBase（★ 实施修正：飞天/站桩不需要 CharacterController，见 §6.6）
  ├─ AirAlly（飞天）
  │    └─ DroneAlly
  ├─ GroundFollowAlly（陆地跟随）
  └─ GroundStationaryAlly（陆地站桩）
       └─ SentinelAlly
```

★ **实施修正（2026-09-18 E2 执行时）**：目标形态里 `AllyBase` 位于 `CharacterBase` 之下，
但实测两类现役友军（无人机 / 祖宗）都是 `EntityBase` 直连、**没有物理刚体、也没有角色控制器**；
若为对齐类树而让它们继承 `CharacterBase`，会凭空多出 kinematic 刚体（子弹会被自家无人机挡住）、
角色推挤与贴地规则 —— 违反"纯搬运、行为零变化"。故本轮 `AllyBase extends EntityBase`，
运动层决策见 §6.6。

### 6.3 公共能力（AllyBase）

```ts
// entity/ally/AllyBase.ts —— 世界端口（WorldMode 实现一次、AllySystem 注入所有友军）
export interface AllyWorldPort {
  rangedAttack(from: AllyBase, target: EntityBase): void;
  findAgentTarget(x: number, z: number, radius: number): number;
  agentPosOf(idx: number): { x: number; y: number; z: number } | null;
  rangedAgentAttack(from: AllyBase, idx: number): void;
  mineAttack(from: AllyBase): void;
}
```

★ 实体目标索敌（`findNearestEnemy`）走 `EntityManager.querySphere`（本地调用，不走端口）；
端口只承载"必须问世界"的代理层 / 掉落 / 结算通道。

- **索敌**：实体优先 + 代理兜底；**代理目标绝不跨帧缓存 index**（swap-remove 纪律，从 `DroneEntity` 原样继承）。
- **攻击节奏**：按 `attackType`（§5.3）分支（melee / ranged / laser→远程），冷却与目标切换在基类收口
  （★ 现状仍由子类 think 分支；E2 第二阶段收口，行为不变）。
- **槽位与存活**：`slotIndex / itemId / owner`；损毁即彻底消失（不返还 / 不可维修）；AllyHud 直读 `hp/maxHp`。
- **生命周期**：目标形态走统一 `retire('killed')`（§9.3）——只做表现与槽位回收、**不计入敌人击杀统计**；
  ★ 退役状态机（管线 P2）落地前，仍走 `dispose`（现状）。
- **编队兼容位**：`AllyBase` 预留 `SwarmUnit` 兼容（远期友军编队）；**本轮不实现友军编队**。
- **AISystem**：飞天 / 站桩先用专用骨架；陆地跟随若接行为库，再放宽行为表签名（不在本阶段强求）。

### 6.4 三种骨架的差异（只写差异，公共逻辑上收）

| 维度 | AirAlly | GroundFollowAlly | GroundStationaryAlly |
|---|---|---|---|
| 运动层 | 3D 平滑追踪 + 最小离地（手写） | 直线接近 + 表面高度贴地（手写骨架，§6.6） | 不移动（仅基座高度 + 转向） |
| 目标点 | followTarget（玩家侧上方） | followTarget + 编队偏移 | 自身坐标 |
| 超距处理 | 超距返回玩家 | 超距返回玩家 | 不适用 |
| 攻击 | 近战 / 射线（现无人机） | 近战 / 远程 | 远程 / 激光 / 挖矿（现祖宗） |
| 物理/推挤 | 无（EntityBase） | 无（骨架阶段） | 无（EntityBase） |

### 6.5 E2 验收（分两阶段）

- **第一阶段（类树拆分，已完成）**：
  - `entity/ally/` 六文件落地；`DroneEntity.ts` 删除；`WorldMode.drones` 类型改 `AllyBase`。
  - 无人机 / 祖宗**行为与现状零变化**（槽位回收、损毁消失、代理兜底索敌、激光音效距离门、共享流体步进全部保留）。
  - `npx tsc --noEmit` 零报错、`npm run guard` 通过。
- **第二阶段（收口，已完成）**：
  - ✅ `AllyWorldPort` 统一注入（`AllySystem.setWorldPort`），6 个逐个体回调字段删除；
  - ✅ `AllySystem`（`systems/ally/`）成为友军唯一持有者与统一更新入口，
    `WorldMode` 的友军 `for` 循环 / 跟随锚点计算 / 增删改全部改为 `allySystem.*`；
  - ✅ 登船 / 退出模式走 `allySystem.disposeAll()`，槽位同步走 `allySystem.remove/add`。
- **第三阶段（留存休眠 API，2026-09-19 已完成）**：
  - ✅ `GroundStationaryAlly` 通用 API：`sleep()/wake()/dormant`（休眠闸门收口在
    `updateAI` 覆写——休眠期 `think`/光束/朝向全免，仅维持站桩高度；子类零改动）；
  - ✅ `AllySystem.wakeStationaryNear(x, z, r)`：接触唤醒扫描（WorldMode 每帧一行调用）；
  - ✅ 持久化口径：**只存祖宗**（位置/血量/基座高），重进**休眠入场**、接触 ≈2.2m 唤醒
    并入队友列表（HUD 过滤 `dormant`）；**无人机跟随玩家不入缓存**（旧档 drone 记录丢弃）；
  - ✅ 墙/城墙只保证留存（碰撞/光环本就近身生效）。

### 6.6 运动层决策（为什么 AllyBase 直连 EntityBase）

| 友军 | 需要什么 | 结论 |
|---|---|---|
| 飞天（无人机） | 3D 平滑追踪、不贴地/不推挤 | 手写运动层（AirAlly.moveTo） |
| 陆地站桩（祖宗） | 基座高度、朝向 | 手写（holdStationY） |
| 陆地跟随（新增） | 贴地 / 立面 / 推挤 / 分离 | **接入兵种时再选**：① 迁到 CharacterBase 运动层；② 给 CharacterBase 做 controller 可选化；③ 保持手写 + 局部规则 |

★ 决策留到第一批陆地友军素材/配置进来时做，**现在不为对齐类树而引入物理体/控制器**。

---

## 7. Player（玩家 · 架构独立）

### 7.1 独立性铁律（2026-09-18 用户定调）

- Player **不实现** `SwarmUnit / SwarmCarrier`、**不进** `AISystem`、**不参与**升降格与编队。
- `Player` 只依赖 `CharacterBase` 公共面与 `PlayerServices`；
  **敌 / 友基类怎么改都不影响玩家**（反向亦然：玩家改动不污染敌友）。
- 冻结：`camp='player'`、`sameTeam` 过滤、`lodExempt=true`（动画时钟永不冻结）、
  `dead / controlLocked / preDeathHp / revive` 语义。

### 7.2 模块边界

```
entity/player/
  Player.ts            // 输入驱动 + 载具 + 死亡复活 + 操作锁
systems/player/
  PlayerPipeline.ts    // 玩家专属每帧：效果 tick / statsDirty 聚合 / 复活倒计时
  PlayerServices.ts    // （预留）输入/相机/装备查询接口；本阶段用 deps 闭包承载
```

- WorldMode 只把输入 / 相机 / 装备查询喂进来，**不再在模式层写玩家行为**。
- 载具 / 移动乘数继续走 `VehicleRide`；玩家移动语义（相机相对 + 输入）与敌友完全不同，
  **不做共享驱动基类**，只在 `CharacterBase` 共用运动/地形/表现。

### 7.3 本轮改动范围（已完成 2026-09-18）

- ✅ `entity/Player.ts` → `entity/player/Player.ts`（模块独立，两个引用点同步）。
- ✅ `systems/player/PlayerPipeline.ts`：效果队列 tick / 属性脏刷新 / 复活倒计时与复位
  （`resetRun / onPlayerDeath / setRespawnTimeMul / update`）全部收口；
  WorldMode 只保留 `playerPipeline.update(dt)` 与死亡分支一行 `onPlayerDeath()`。
- ✅ 复活阶梯 / 血量保底常量从 WorldMode 移入 PlayerPipeline。
- ✅ 玩家入水/涉水已下沉：`WaterFx.entry/wade`（`systems/world/`）；WorldMode 只保留调用点。
- 手感 / 数值零变化；`dead / controlLocked / preDeathHp / revive / requireRealLanding` 原样保留。

---

## 8. 非角色实体与未来实体

| 实体 | 基类 | 说明 |
|---|---|---|
| `ItemBase` / `BulletEntity` | EntityBase | 纯物理/逻辑；不带角色语义 |
| `ShipEntity` | EntityBase | 固定/kinematic + 大体积渲染 |
| `NpcEntity` / `VisitorNpcBase` | EntityBase / CharacterBase | 事件 NPC 非战斗；访客走角色管线 |
| **`StructureEntity`（新增）** | EntityBase | v2 §22 工事：固定刚体、可 `applyDamage`、不参与 AI/编队/存档；随波次清理 |
| **`CoverEntity`（掩体，2026-09-19）** | `StructureEntity` | 可摧毁（`COVER_HP`）+ **插值建造**（`buildProgress 0→1` 驱动顶点）+ 灰色砖块程序化材质 + **射击孔**：用 `addFixed.extraColliders`（孔下/孔上/左右立柱）留缝，只有穿缝才命中后方；`owner: enemy/player` + `passFaction`；敌方由杂兵施工、玩家由遗物道具"像祖宗弹"发射落地生成 |
| **陷阱（新增）** | 数据触发器（不建角色实体） | v2 §24：不进寻路代价、不标脏 cluster；可被侦察/引爆 |
| **战壕（2026-09-19）** | 非实体（地形改造） | v2 §22.10：复用挖坑管线改高度（浅沟，非 `pit`）；标脏 cluster；敌我通用减伤；预算 `TRENCH_BUDGET` |

★ 工事/陷阱的**创建与结算**在 swarm/systems，实体侧只提供"可被伤害的静态目标"这一最小面
（`StructureEntity`/`CoverEntity` 实现 `onTakeDamage`，走同一 `applyDamage` 口径）。
★ 掩体的复合碰撞体与舰船分段**共用同一套基建**（`BodyOptions.extraColliders`，2026-09-18 为舰船加入）——
射击孔不是视觉装饰，而是真实弹道/LOS 通道。

★ **掩体口径（2026-09-19 用户定调，覆盖前两版）**：
- **碰撞分组按 owner**：玩家造 = `GROUP_COVER_PLAYER`；敌人造 = `GROUP_WALL`；
  城墙（cover）= 带射击孔复合体（下沿/上沿/左右立柱），墙（wall）= 实心单盒。
- **子弹（终版矩阵，2026-09-19）**：

| 子弹 \ 掩体 | 玩家造 | 敌人造 |
|---|---|---|
| 玩家/友军弹 | 完全无视（自由穿） | 实心，**只能穿射击孔** |
| 敌弹 | **全挡（含射击孔）** | 完全无视（自由穿） |

  实现：玩家/友军弹 filter 剔除 `GROUP_COVER_PLAYER` + `GROUP_SLIT_PLAYER`；敌弹 filter 剔除
  `GROUP_WALL`；**玩家城墙的孔口贴一层"射击孔膜"**（`GROUP_SLIT_PLAYER`，只挡敌弹）。
  玩家不再有"贴墙开枪全无视"（`wallNear` 已移除）。
- **海报**：玩家造的有（城墙=不许笑 / 墙=土木老姐）；**敌人造的没有**（`poster:false`）。
- **萨卡兹大剑手改造为后勤兵**（`role: 'logistics'`）：挖坑、造掩体（行为后续接）。

---

## 9. ★ 管线设计（六相更新 / 生命周期 / 蜂群桥接接口）

### 9.1 现状：顺序只活在 WorldMode 的注释里

实测顺序（2026-09-18，`WorldMode.update`）：

```
① 模式前置（UI/物品/相机上下文）
② aiSystem.updateAll（L3 敌人 think：只写 moveDir/aiAttackTimer，不做积分）
③ swarm.update（代理决策/移动 + 内部 promote 升格）
④ spawner.tickDemote（实体 → 代理；直接 splice + dispose）
⑤ 友军更新（★ 已收口：AllySystem 统一入口 + AllyWorldPort 注入）
⑥ entities.update（全实体 7 段模板：onUpdate→物理→动画→渲染→特效→索引→影子）
⑦ clampCharacter / updateWaterEntry（WorldMode 再 for 一遍 this.enemies）
⑧ 战斗后处理（子弹池/水花/复活/效果）→ renderAll（视锥 + LOD）
```

> ★ 2026-09-18 E2 已完成的收口：友军（⑤）与玩家每帧（效果/属性/复活）已从 WorldMode 移出
> （`systems/ally/AllySystem.ts` + `systems/player/PlayerPipeline.ts`）；②③④⑥⑦ 仍按下表推进。

| 问题 | 证据 | 后果 |
|---|---|---|
| 相位顺序隐式 | 顺序只写在 `WorldMode.update` 的注释与调用行之间 | 新系统只能"插队"，顺序契约无法验证 |
| 升降格桥接分裂 | promote 在 `SwarmSystem.update` 内、demote 在 `spawner.tickDemote` | 可能升格已死代理、降格时点不定；统计靠布尔纠偏 |
| 销毁多路径 | `onDeath→dispose`；`EnemyBase.dispose` 内 emit `enemy_killed`；`killedByCombat/deathReported` | 同一实体可被多路径处理；业务事件与资源释放耦合 |
| 实体集合中途变动 | 升格/降格/死亡发生在遍历期间 | Map 迭代安全靠"碰巧"；无法加批处理/预算 |
| 模式层代替实体层 | `for this.enemies: clampCharacter/updateWaterEntry`；友军 AI 在模式层 | 实体行为拆两处；`enemies` 数组与 `bases` 双账本 |
| 更新无分级 | `EntityManager.update` 全量跑 7 段；子弹池也走完整骨架 | 轻实体浪费；只能靠 `inFrustum` 粗糙跳过表现 |

### 9.2 目标：六相管线（顺序即契约，模式层只装配）

```
Phase 0  Context   （modes）     输入采样、相机帧、UI 状态快照
Phase 1  Brain     （systems）   ★ 蜂群决策 + 个体战斗意图
                                  · Director/编队/攻击槽/警戒场 → 写 SteerIntent
                                  · 代理决策分桶（10/5/2Hz）+ 代理移动积分
                                  · L3 敌人本地战斗决策（攻击节奏/ROE，只产意图）
Phase 2  Simulate  （entity）    消费意图：积分/贴地/立面/推挤 → syncPhysics
Phase 3  Combat    （services）  伤害结算、死亡判定 → retire('killed')
Phase 4  Bridge    （swarm↔spawn）★ 升降格/回收唯一执行相位（唯一改实体集合处）
                                  · promote：AgentPool → EnemyBase（hydrate）
                                  · demote：EnemyBase → AgentPool（drain）→ dispose
                                  · 退役队列 flush（幂等）
Phase 5  Present   （entity）    动画/特效/影子/渲染同步（受视锥/LOD gate）
```

> ★ **P3 实施修正（2026-09-18）**：空间索引移块**留在 Phase 2 Simulate 内**（`base.simulate`
> 末尾 `em.onEntityMoved`）——保证同一帧内后续实体查询能看到新位置；表现相只在末尾做
> 影子调度。相位拆分先做"Simulate/Present 两相 + `update()` 兼容包装"，
> Brain/模式层搬迁按 §9.5 逐步推进。

**相位顺序的五条硬约束（写进契约）**：

1. **实体集合只在 Phase 4 变**：Phase 1~3 只允许标记 `pendingRetire / pendingPromote`，
   禁止 create/dispose（含升格、降格、回收、死亡销毁）。
2. **Brain 与 Simulate 分离**：决策频率（10/5/2Hz）与执行频率（60Hz）解耦；
   `SteerIntent` 是 **hold 语义**——swarm 下发后实体每帧保持执行，直到被覆盖 / 超时 / `clearSteer()`。
3. **Combat 在 Bridge 之前**：本帧死亡的代理/实体先结算 ——
   **绝不升格已死代理、绝不让将死实体回池**。
4. **Present 可跳过**：视锥外 / LOD3 只跳过 Phase 5（+影子），**不跳过 Phase 1~4**（玩法一致）。
5. **模式层不写实体行为**：Phase 0 只准备上下文；`clampCharacter / updateWaterEntry / 友军 AI`
   全部下沉（见 §9.5 清单），WorldMode 只做装配与相位调度。

### 9.3 生命周期：状态机 + 退役原因（✅ P2 已实施）

```ts
// entity/EntityBase.ts
export type EntityLifeState = 'active' | 'retiring' | 'disposed';

/** ★ 退役原因 = 业务语义的唯一来源（取代 killedByCombat/deathReported） */
export type RetireReason =
  | 'killed'        // 战斗致死：emit killed / 掉落 / 死亡动画 / 击杀统计
  | 'demoted'       // 降格回代理池：先 drain 快照，再释放（不算击杀、不掉落）
  | 'recycled'      // 远距/超时回收：不算击杀、不掉落
  | 'despawned'     // 主动移除（导演收手 / 登船收友军）
  | 'mode_cleanup'; // 模式切换 / 场景卸载

retire(reason: RetireReason): void {
  if (this._life !== 'active') return;   // ★ 幂等：任何路径重复调用都安全
  this._life = 'retiring';
  this.retireReason = reason;
  this.onRetire(reason);                 // 子类业务钩子（emit 统计/槽位联动）
  this.dispose();                        // 资源释放（幂等；不再发业务事件）
}
```

**实施记录（P2）**：
- `dispose()` → 幂等纯资源释放；`onDeath` 默认走 `retire('killed')`（Player 覆写不调 super = 不死）。
- `EnemyBase.onRetire('killed')` 发 `enemy_killed`；降格/回收/清场 reason 不同 → **天然不计击杀**。
- `WorldSpawner` 降格改 `retire('demoted')`、无定义清理 `retire('recycled')`；
  `EntityManager.clear` → `retire('mode_cleanup')`；`AllySystem.disposeAll(reason)`。
- `killedByCombat / deathReported` 双布尔**已删除**。
- ⏳ `pendingRetire` 延迟队列与 Phase 4 统一 flush 属 **P3 相位切分**（当前 retire 同步执行，
  行为与旧路径一致；相位化不改业务口径）。

### 9.4 ★ 蜂群桥接接口（`SwarmCarrier` / `SwarmTierPort`）

> §10 定义**字段与快照内容**；本节定义**管线怎么把它们接起来**。
> `SwarmCarrier` 的字段面 = §5.3 `SwarmUnit`（`swarmUid / carrier / activation / isLeader /
> battalionId / squadId / formSlot / corridorIdx / role / isAir / altitude / moveTarget / attackType`），
> **全部非可选、有默认值**。

```ts
// entity/SwarmCarrier.ts —— L3 实体实现（EnemyBase；友军远期可选）
export interface SwarmCarrier extends SwarmUnit {
  controlSource: 'swarm' | 'local';             // ★ 唯一控制权切换点
  applySteer(intent: SteerIntent | null): void; // Phase 1→2：swarm 下发（hold 语义）
  hydrate(snap: AgentSnapshot): void;           // Phase 4 升格：快照灌入实体
  drain(): AgentSnapshot;                       // Phase 4 降格：实体抽干为快照
  retire(reason: RetireReason): void;           // 统一退役入口（§9.3）
}

// systems/swarm/SwarmTierPort —— 唯一桥接点（WorldSpawner 实现，modes 装配）
export interface SwarmTierPort {
  promote(snap: AgentSnapshot): SwarmCarrier | null;  // 建实体 + hydrate
  demote(carrier: SwarmCarrier): void;                // drain + AgentPool 回池 + retire('demoted')
  entityCount(): number;                              // 升格预算 / L3 上限判定
  canPromote(): boolean;                              // PROMOTE_PER_FRAME / L3_CAP
}
```

**控制权铁律**：

| controlSource | 移动来源 | 本地 AI 保留 |
|---|---|---|
| `'swarm'`（v2 F1+） | `applySteer` 下发的 `SteerIntent`（编队槽位 / 走廊车道） | **只保留战斗决策**（攻击节奏/眩晕/ROE），不再自己选路 |
| `'local'`（现状 / 降级） | `EnemyBase` 自身 AI（流场/直线 + 2m 局部避障） | 全部（现状行为） |

- 控制权切换**只允许在 Phase 4（Bridge）发生**；Phase 1~3 内切换 = 未定义行为。
- `steer` 超时（`STEER_TTL`，默认 0.3~0.5s 未刷新）自动回落 `'local'`
  —— **任何情况下不停摆**（对齐 v2 §15.6 降级链）。
- 升格预算 / L3 上限由 `SwarmTierPort.canPromote()` 判定，
  **不在 SwarmSystem 里直接数实体数组**（消灭 `enemies.length` 与 `bases` 双账本）。

### 9.5 模式层瘦身清单（Phase 0 只留装配）

| 现状在 WorldMode | 去向 | 阶段 |
|---|---|---|
| `aiSystem.updateAll` + aiCtx 组装 | 管线 Phase 1（Brain）；ctx 由模式注入一次（顺序已对齐：AI → 实体 Simulate） | 结构 ✅ / 搬迁随 v2 Brain |
| `swarm.update` 内的 promote | Phase 4 统一 `SwarmTierPort` | P4（与 E3 同批） |
| `spawner.tickDemote` 直接 dispose | ✅ 已走 `retire('demoted')`（相位化仍待 P4） | P4 |
| 友军 `updateAI` for 循环 + 6 回调 | `AllyBase/AllyWorldPort`（★ 已完成：`systems/ally/AllySystem`） | ✅ E2 |
| `for enemies: clampCharacter / updateWaterEntry` | ✅ 已完成：`systems/world/CharacterClamp.ts`（贴地/悬停/掉坑结算）+ `systems/world/WaterFx.ts`（入水波动/涉水轨） | ✅ |
| `swarmBulletCheck` / `resolveBulletHit` | ✅ 已完成：`systems/combat/CombatSystem.ts`（命中解析 + 代理线段判定；掉落/友军/水面回调注入） | ✅ P5 |
| 玩家效果 tick / statsDirty / respawn | `systems/player/PlayerPipeline`（★ 已完成） | ✅ E2 |
| `entityPerf` 计时硬编码在热路径 | ✅ 采样开关（`entityPerf.enabled`；HUD/`?perf=1` 时开） | ✅ P1 |

### 9.6 轻实体分级（`pipelineTier`）

```ts
type PipelineTier =
  | 'full'    // 角色：Brain + Simulate + Present 全走
  | 'light'   // 子弹/物品/掉落：Simulate + 运动型 Present（无影子/特效/受击表现）
  | 'static'; // 舰船/工事/NPC：无 Simulate，只在 Bridge/Present 响应
```

- `EntityManager.update` 按 `tier` 分组遍历（Phase 2/5 各扫自己的组），
  不再"全实体 7 段"；`light` 组跳过影子/受击染料/特效槽。
- 现状 `inFrustum` 对表现相的 gate 保留，语义并入 Phase 5。
- **默认值 = `full`**（不声明不改行为）；分级是 P3 之后的优化项，不阻塞 E 阶段。

### 9.7 管线迁移 P0~P5（与 E 阶段并线）

| 管线阶段 | 内容 | 与 E 阶段关系 | 验收 |
|---|---|---|---|
| **P0 顺序冻结** | 实测顺序写进本文档；`entityPerf` 增加 Phase 计时 | 无代码 / 与 E0 同批 | 文档评审 |
| **P1 采样开关（✅ 已完成）** | `entityPerf.enabled` 门控：实体/角色/蜂群热路径 7+5+5 处 `performance.now()` 关闭时全免 | 与 E1 同批 | HUD 关且无 `?perf=1` → 零采样；开 HUD/脚本钩子自动恢复 |
| **P2 退役状态机（✅ 已完成）** | `retire(reason)` 幂等入口 + `onRetire` 业务钩子 + `dispose` 去业务化 | 与 E1/E2 同批 | 击杀统计/掉落/回收与现状一致（`killedByCombat/deathReported` 双布尔已删） |
| **P3 相位切分（✅ 首步完成）** | `EntityManager.simulate/present` 两相 + `EntityBase.simulate/present`（索引留在 Simulate 保证同帧可见）；WorldMode 显式两相；`update()` 兼容包装 | 与 E2 同批 | 行为 A/B 对照一致（实机）；`clampCharacter` 下沉待后续 |
| **P4 桥接统一** | `SwarmTierPort` + Phase 4；promote/demote/回收全走它；steer 接入 | ★ 与 E3 / v2 F0 同批 | 升格/降格快照逐字段断言 |
| **P5 战斗/玩家下沉（✅ 主体完成）** | `CombatSystem` 接管命中解析（`resolveBulletHit/updateAgentHits`）；`PlayerPipeline` 接管效果/复活/涉水 | 与 E4/E6 并线 | 完整一局循环通过（实机） |

★ **P0~P2 行为零变化；P3 起有行为风险，必须逐相位 A/B**（同 seed / 同波次对照）。

---

## 10. ★ 蜂群适配（与《蜂群架构.md》v2 的接口契约）

### 10.1 正交性（v2 §12 铁律的实体侧表述）

```
表现载体（L1 代理 / L2 代理 / L3 实体）  ×  逻辑归属（Battalion / Squad / 个体）  ×  阵营
```

- 一只敌从 L1 走上 L3，**编队归属不变**（`squadId/formSlot` 跨升降格无损）。
- 实体类树**不因 LOD 分层**（不存在 L1Enemy/L3Enemy 两个类），只存在"同一实体 + 代理快照"。

### 10.2 跨 LOD 快照必须无损（v2 §16.3 的实体侧责任）

```ts
// systems/swarm/AgentPool.ts —— ★ E3a 起 extends SwarmSnapshot（entity/SwarmUnit.ts，单一事实源）
export interface AgentSnapshot extends SwarmSnapshot {
  // …现有字段（位置/血量/属性/意图/空中层）…
  /** ★ v2：编队字段（升格 hydrate 灌入实体；降格 drain 从实体读回。缺省 = 散兵） */
  uid?: number;
  /** ★ 大编队副本（权威在 Squad.battalion；仅搬运，不反推） */
  battalionId?: number;
  squadId?: number;
  formSlot?: number;
  corridorIdx?: number;
  /** ★ v2：现有缺失项也要补齐（否则降格靠模式层猜） */
  intent?: number;
  bias?: number;
  aggro?: number;
  wanderSpeed?: number;
  /** ★ 兵种角色（也可经 mobIndex → MobDef.role 取，随 v2 定） */
  role?: UnitRole;
  /** ★ 移动位置（编队/走廊下发并保持的目标点；缺省 = 无目标） */
  moveTargetX?: number;
  moveTargetY?: number;
  moveTargetZ?: number;
  /** ★ 攻击类型（none/melee/ranged/bombard；缺省 = 从 MobDef 推导） */
  attackType?: UnitAttackType;
  // ---- ★ 2026-09-19：单一单位模型（指挥/感知/AI 连续性） ----
  /** 是否本队队长（指挥权标记；dormant 恒 false） */
  isLeader?: boolean;
  /** 感知（逻辑单位状态，跨 LOD 保留） */
  lastSeenX?: number;
  lastSeenZ?: number;
  lastSeenAt?: number;
  aggroFrom?: number;
  /** 休眠 AI 状态（降格抽干 / 升格回灌 → 不失忆、不重置巡逻） */
  aiStateIdx?: number;
  aiTimer?: number;
}
```

**实体侧义务**：
1. `EnemyBase.hydrate(snap)`：构造/升格时灌入全部字段（含编队/意图/感知/AI 状态），一个不漏；
2. `EnemyBase.drain()`：降格时抽干全部字段 → `AgentSnapshot`；
3. 升降格唯一桥接：`SwarmTierPort`（现由 `WorldSpawner` 实现，见 §9.4）；
4. **禁止**在实体上另存一套与 `AgentPool` 冲突的运行时状态（单一事实源）；
5. **激活态不存快照**：`activation` 由载体派生（池=dormant / 实体=active）。

### 10.3 战斗与寻路的分界（v2 铁律 5）

| 事项 | 归属 |
|---|---|
| 编队去哪 / 从哪包抄 / 阵型槽位 | swarm（Battalion/Squad/corridor） |
| 攻击槽 / 攻击令牌 / 集火 | swarm（`AttackSlots`，L3 与代理共用同一套协调） |
| 近战挥击 / 远程弹道 / 攻击节奏 | entity（`executeAttack` + `AIStateMachine`） |
| 伤害结算 | `applyDamage` / `killed`（不变） |

★ `attackType` 只决定走哪条**攻击结算分支**（melee/ranged/bombard）与攻击槽分圈（近战内圈/远程外圈）；
**打谁**仍由攻击槽/编队/警戒场决定，实体不自己选目标类别。
L3 实体**不做全局寻路**（不跑 A*/流场采样）；需要移动时读 swarm 下发的 `SteerIntent`/`moveTarget`。

### 10.4 空中层（v2 §25）

- 基础设施已就绪：`CharacterBase.airborne/airAltitude`、`AgentPool.isAir/altitude`、名册 `isAir`。
- 飞行兵 = `EnemyBase + isAir + role:'flyer'`；**不进地面 corridor / 车道 / 工事代价 / 陷阱**。
- 导航 = 直线 + 避障（高于飞行高度的地形/建筑），由 swarm 空中层负责；实体只执行。

### 10.5 友军与蜂群

- 蜂群引擎本阶段**只服务敌人**；友军编队化（护送、协同包抄）是远期玩法，**不在本次重构范围**。
- `AllyBase` 预留 `SwarmUnit` 兼容位，未来若要友军编队，只加一个友军版 coordinator，不改类树。

### 10.6 性能口径（沿用《性能评估报告》与 v2 预算）

- L3 上限 30、升格预算 ≤2/帧、降格滞回 30/40m —— 实体侧不新增每帧全量遍历。
- `entityPerf` 分项继续统计行为/物理/动画/渲染/影子；E1 搬运不得改变计时口径。

---

## 11. 冻结契约（不得破坏）

1. `applyDamage` / `killed` / `damage` 事件、`sameTeam` 阵营过滤（player/ally 互免）。
2. `EntityManager.update/renderAll` 是实体管线唯一入口；`inFrustum/lodExempt` 语义不变。
3. `RasterMap` 统一空间层（实体索引/查询）；`querySphere/queryRay` 行为不变。
4. `hitAnchorY()`（受击锚点）、`minimapInfo`（含 `hideOnMap`）、`visible` 语义不变。
5. 升格/降格桥接点唯一：`SwarmTierPort`（现由 `WorldSpawner.promoteAgent/demoteFarEnemies` 实现）。
6. 击杀统计口径：远距回收**不算击杀**（P9）；`enemy_killed` 只在真死亡发一次。
   统一读 `retireReason`（P2 已落地，§9.3）。
7. 存档不存任何运行时实体状态/代理/编队（v2 铁律 7）。
8. AI 行为库（`behaviorTable/conditionTable`）敌友共用，加内容=改配置。
9. 玩家 `lodExempt`、无人机 `lodExempt`（动画时钟不得冻结）。
10. 重构期间：E0~E2 只允许"搬运 + 转发"，禁止顺手改数值/时序。
11. ★ 生命周期唯一入口 `retire(reason)`（§9.3）：`dispose` 只释放资源、不发业务事件；
    任何路径不得绕过 / 重复执行。
12. ★ 管线六相顺序（§9.2）为硬契约；**实体集合只在 Phase 4 变动**（Phase 1~3 只标记）。
13. ★ 控制权（`controlSource`）只在 Phase 4 切换；steer 断开自动回 `local`
    —— **任何情况下不能让敌人停摆**（v2 铁律 3）。
14. Phase 4 桥接唯一走 `SwarmTierPort`；禁止在其他相位 create/dispose 敌人实体。

---

## 12. 迁移路线 E0~E6（每阶段可玩、可量、可回滚）

| 阶段 | 内容 | 验收标准 | 回滚点 |
|---|---|---|---|
| **E0 度量与安全网** | 建本文档；`npx tsc --noEmit` 基线；列出旧→新搬运对照表；拍一条"行为对照"跑法（同 seed 同波次截图/日志） | 基线可复现；本文档评审通过 | 无代码改动 |
| **E1 EntityBase 纯搬运（✅ 已完成）** | 影子 → `GroundShadowController`；特效槽 → `EffectSlots`；受击染料 → `CharacterHitDye`；死亡动画 → `CharacterDeathFx` | 行为零变化；`EntityBase` 542→468、`CharacterBase` 568→395；tsc/guard 通过 | 逐文件 revert |
| **E2 ★ 玩家与友军基类重做（✅ 已完成）** | 三种友军骨架（`AirAlly / GroundFollowAlly / GroundStationaryAlly` + `DroneAlly / SentinelAlly`）；`AllyWorldPort` 统一注入 + `AllySystem` 收口；`entity/player/` + `systems/player/PlayerPipeline` 独立收口 | 无人机/祖宗行为零变化；玩家手感/数值零变化；模式层的友军循环与玩家行为循环删除；tsc/guard 通过 | 保留 `DroneEntity` 与旧 Player 路径，双跑对照 |
| **E3 ★ 蜂群接口层（E3a ✅ / E3b 开工准备 ✅ 2026-09-19，清单见 §5.8）** | E3a：`SwarmUnit/SwarmCarrier` 类型 + `EnemyBase` 预留字段（默认值）+ `hydrate/drain/applySteer` + `AgentSnapshot` 基类 + 名册 `role/attackType` 透传；E3b（2026-09-19 定稿）：`AgentPool` 新增列（`isLeader/lastSeen*/aggroFrom/aiStateIdx/aiTimer`）+ `activation` 派生 + **AI/感知状态跨 LOD 连续** + 指挥权转移 + 被击升格（限 L3 范围）+ `squadMode/noDemote` + `SwarmTierPort` 统一桥接（管线 P4）+ **通信/命令载体（§5.11~§5.12：两层命令/队长评级/小队间中转）** + **自主 LOD（§5.10：倾盆而出/动态算力）**，随 v2 做 | E3a：tsc/guard 通过、无编队时行为与现状一致；E3b：升降格往返后 `battalionId/squadId/formSlot/moveTarget/hp/意图/AI 状态/感知/队长` 全部保留 | steer 未接入移动决策（E4 才消费），可随时回滚为纯字段 |
| **E4 ★ 编队移动接入（延后）** | E4a：EnemyBase 消费 `applySteer`（默认 `local`，编队开关打开才生效）；E4b：`swarm` 模式删本地全量危害扫描（保留 2m 局部避障）；与 v2 F1/F2 同步 | 编队成队形推进；跨区能绕障碍找到玩家；帧率不掉 | 切回 `flow/local` 即恢复现状 |
| **E5 ✅ EnemyBase 收口（2026-09-19）** | `EnemyBrain / EnemyLocomotion / EnemyPresentation` 组合件拆分（纯搬运）+ `CombatStats` 可选 | 行为零变化；EnemyBase 收口；子弹/物品不再背战斗字段（若做 CombatStats） | 逐文件 revert |
| **E6 ✅ 清理收口（2026-09-19）** | 死代码审计已清：删 `EnemyBase.aggressive`（全仓只写不读）/ `DIRECTIVE_FIRE_DEFAULT`（无引用）；`moveBy` 保留（仍是 behaviors 的移动 API，E4 接 `applySteer` 后才谈收口）；保留项均为**文档化预留**（`applySteer/hasFreshSteer`=E4、`aggroFrom/corridorIdx/formSlot`=v2、`heading`=评级朝向、`SwarmReportKind` 其余类型=9e 预留） | 全仓未使用项回落到基线；一局完整循环通过 | 阶段前 tag |

**本轮实施顺序（2026-09-18/19 拍板）**：**E2、E1、P2、E3a、P1、P3（首步）、模式层下沉（clamp/水/涉水）、P5（命中解析）已完成**；
**剩余：E3b/P4/E4 随蜂群 v2 启动**（E3b/P4 是 v2 F0 前置，E4 与 F1/F2 同步）；
**E3b 步骤 1/4 已落地、设计定稿（§5.9~§5.12，2026-09-19）；其余按 §5.8 清单推进**；
**战术（F3+）不在本轮**——后续以"小队状态评级"为基本单位消费载体接口；**E5/E6 按需**。
装备/弹药类实体不动；E3b 起 EnemyBase 按 §5 的单一单位模型实施（不再"主体不动"）。
**管线阶段 P0~P5 与 E 阶段并线执行**（详见 §9.7 表）：
P0/P1 随 E0/E1，P2/P3 随 E1/E2，**P4 随 E3b/v2 F0**，P5 随 E4/E6。

---

## 13. 风险与取舍

- **重构期行为漂移**：E0~E2 强制"纯搬运"，禁止顺手改逻辑；每步跑行为对照。
  → 一旦发现差异，先回滚再定位，不"边修边重构"。
- **AllyBase 抽象过度**：飞天 / 陆地跟随 / 陆地站桩三者差异大，先抽"能共享的"
  （世界端口 / 索敌 / 代理目标 / 攻击节奏 / 槽位），移动与站位留在三个子类里，不强行统一状态机。
- **玩家独立后变"孤岛"**：`PlayerPipeline` 收口时容易把装备/载具逻辑切断。
  → `PlayerServices` 接口先冻结（输入/相机/装备查询 + 事件订阅），只搬家不改语义。
- **AISystem 泛化误注册**：类型放宽到 `CharacterBase` 后，玩家/访客可能被注册。
  → 注册只由 `EnemyBase`/`AllyBase` 构造发起；玩家永不注册。
- **E3 快照丢字段**：swap-remove + 升降格是踩过的坑（v2 §16.2/§16.3）。
  → 快照字段清单写进验收用例：升格→改状态→降格→再升格，逐字段断言。
- **不做 ECS**：项目是微信小游戏 + 手写管线，ECS 的包体/心智/改动面都不划算；
  用"继承 + 组合模块"解决 90% 问题，剩下 10% 用接口（`SwarmUnit/AllyWorldPort`）。
- **不动子弹/物品的战斗字段（E5 可选）**：收益小、改动面大；文档标记即可，不为了"干净"而冒险。
- **不引入友军编队**：蜂群是敌人系统；友军协同是另一个玩法课题，等敌人 v2 稳定后再议。
