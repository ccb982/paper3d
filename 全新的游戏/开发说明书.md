# 游戏开发说明书（一步一步怎么做）

> 结合《架构设计.md》最终架构的完整开发路线。
> 每个阶段：目标 → 步骤 → 验证标准 → 完成后进下一阶段。
> 原则：先跑通再完善；每阶段结束都"能玩、能看、能测"。

---

## 架构速览（开发前必读，一页记住整个系统）

### 分层（单向依赖，禁止反向）★ 2026-08-25 按代码现实修订
```
main（入口：路由 + 主循环 + boot 装配；时间经 RenderManager 分发）
  ↓
modes（应用层：ShipMode/WorldMode/[Boss]）
  ↓
entity（实体骨架：EntityBase 统一管线）  systems（玩法：ai/inventory/interaction…）  ui（ship/world 两套）
   三大业务层平级、少横向。已知例外：entity→systems/player（角色控制器）、
   ui→systems/inventory（舰船背包网格展示直读 session；★ 计数/落账/合成全走
   ItemManager/CraftingManager，加工台与背包共享同一图标服务）
  ↓
core（EventBus/Session/SaveSystem/ItemArchetype）←─ services（render/camera/combat/fx/map/physics…）
   并列基础设施层，均可被上方任意层消费；services→core 合法（如 CombatDirector 订阅事件）
  ↓
platform（输入/平台适配：Web/WeChat/Douyin）  vendor（自研 FTX 播放器，仅 main/services 消费）
```
- 上层依赖下层；platform 是唯一碰 wx./tt. 的地方
- ⚠️ entity 层是核心层（旧图漏画）；services 不引 modes/ui/entity 的运行时代码
  （type-only import 例外）；加内容 = 改配置；换平台 = 改 platform

### 核心约定（写代码时始终记住）
```
① 游戏循环：fixedUpdate(物理/战斗) → update(渲染/特效/相机/UI) → render
② 实体：Entity + 组件（Transform/RigidBody/Billboard/Health/AI/...）
   - 需要物理的实体挂 RigidBody（按 kind 配置，非全部）
   - 序列化只存描述（kind/位置/数值），运行时引用（handle/mesh）不存
③ 事件总线：只有"UI/跨模块要听"的才走总线（拾取/抽卡/警报/对话/死亡）
   - 战斗内部（伤害/命中）直接调用，不 emit；只在"表现边界" emit（damage→伤害数字/导演反馈、killed→击杀演出）
④ AI：AIStateMachine 通用解释器 + 行为注册表 + 阵营标签(camp)
   - 敌人/友军共用行为库；玩家单独 PlayerController（输入驱动）
   - 子弹管线统一：谁发射都一样，阵营过滤在碰撞层
⑤ 存档：安全点（本舰）写 Session → storage；读档仅启动一次
⑥ 配置化：内容全在 config/*.json（敌人/遗物/卡池/合成/对话/事件）
⑦ 渲染管线：视锥剔除 → LOD → 分组(图集×LOD) → 批次y排序 → draw
   - 特效=播放器（.scene.zip）；物理=rapier；水/布料=独立 shader/verlet
```

### 模式状态机
```
BOOT → SHIP(日常) → WORLD(战斗) → BOSS(决战) → RESULT → BOOT
保留：舰船上的东西/科技树/主角物品/友军/遗物/卡池计数/day/背包
丢弃：当天地图/地图实体/战斗运行时状态
```

### 物理使用（rapier 分工）
```
游戏代码 → PhysicsWorld 封装（唯一碰 rapier 的地方）→ rapier
 ├ addBody(entity, kind)   # 按配置自动挂刚体
 ├ step(dt)                # 固定 1/60
 ├ applyImpulse            # 爆炸/击退/击飞
 ├ querySphere             # 爆炸范围
 ├ castRay                 # 瞄准/直线判定
 └ onCollision             # 阵营过滤后转伤害事件
特效/布料/水 = 不走物理（播放器/verlet/Gerstner）
```

### 开发时三问（拿不准时自检）
```
Q1: 这个新内容能不能放配置里？→ 能就放 config/，不硬编码
Q2: 这个调用跨模块了吗？→ 跨了就走 EventBus，没跨就直接调
Q3: 这个实体需要物理吗？→ 会被撞/会撞人/被力推才挂 RigidBody
```

### 已实现的多图层复合特效渲染（★ 2026-09-09 按代码现实）
```
背景：特效包（.scene.zip）一帧可以有区域实体（region/maskEffect → VAT 顶点动画）；
      一个包的"多帧"有时不是动画，而是多图层（共享同一画布，如可露希尔的无人机：
      frame_0 主体 / frame_1 左翼 / frame_2 右翼）。播放器正常渲染只画单帧，
      多图层必须拆开各自摆位再同时显示。

复合渲染器（services/render/DroneCompositeRender）：
  - 主体（无区域实体帧）→ FTXQuad 静态贴片
  - 双翼（带区域实体+maskEffect 帧）→ ★ 独立重走播放器单帧渲染管线：
      ① 网格重建：按整块美术 bbox 矩形 ring（标注空间、首尾闭合）重建，
        作者画的小区域多边形常只罩住翅膀下部 → 直接渲染会裁掉大半；
        重建后顶点走 buildDisplacementTextureData + buildEntityMesh（maskEffect
        原样驱动顶点呼吸，矩形随位移自然扑扇）。
      ② 恒等 uv：播放器 base/residual 纹理本身已按 bbox 裁剪（尺度=bbox.w×bbox.h），
        0..1 即整幅翅膀。帧映射须设 offset=(art.x/canvasW, 1-(art.y+art.h)/canvasH)、
        scale=(art.w/canvasW, art.h/canvasH)，得到的 vUv=(x,1-y) 归一化 0..1，
        与直接绘制 FTX 像素级一致。⚠️ 绝不能沿用 Asset 的整画布基准
        texBbox/frameSize 映射，否则采样区间推到纹理边缘 → 翅膀跑到画布顶部/偏移。
      ③ renderFrameData → 离屏 WebGLRenderTarget → 世界 billboard quad 采样
        （与 MoonEffect→SkyDome 同范式；RT 采样 quad 用 ShaderMaterial 原样输出
        sRGB 并翻转 v：RT row0=底，直接采样上下颠倒）。
  - VAT 连续时间 = 实体基类动画播放器（FrameAnimatorBase.localTime，默认一直 play、
    loopMax 无上限 → 连续不封顶），绝不另造控制器/计时器。
  - 主渲染器注入：★ 无人机是悬浮召唤物，由其所在模式（WorldMode）注入 THREE.WebGLRenderer，
    离屏 RT 与主渲染器共享 GL 上下文（WorldMode.render 里 entities.renderAll 在
    renderer.render(scene) 之前执行，实体内可安全做离屏 RT）。
  - ★ 关键实体 LOD/视锥豁免（EntityBase.lodExempt，主角/无人机/子弹为 true）：
    renderAll 对豁免实体绕过 2D 梯形视锥裁剪 + 距离 LOD，始终 applyViewDistance(0)。
    原因：FrameAnimatorBase.update 在 LOD≥2 时冻结动画时间轴（含 VAT 连续时钟
    localTime）——主角/无人机必须全程满档播放，不受距离/视野影响。
  - 兜底：无区域实体数据（纯 ftx3 纹理包）时双翼回退 FTXQuad + 老式 UV 扭曲；
    三图层按 frame 顺序渲染同一帧号（此包必需，勿按动画循环切帧）。
```

---

## 阶段总览

```
阶段 P  播放器集成（★ 首要任务，1~2 周）—— 所有美术进游戏的前提
阶段 0  环境搭建（半天）
阶段 1  M0 平台验证（1~2 天）      —— three.js + rapier + 播放器能跑
阶段 2  M1 最小战斗闭环（3~5 天）   —— 能移动/开枪/打敌人
阶段 3  M2 模式切换（2~3 天）      —— 舰船↔大世界往返
阶段 4  M3 地图生成（5~8 天）      —— chunk + 迷雾 + 小地图
阶段 5  M4 战斗完整（5~8 天）      —— AI + 掉落 + 背包
阶段 6  M5 抽卡+守卫（3~5 天）     —— 抽卡流程 + 舰船袭击
阶段 7  M6 Boss 决战（5~10 天）    —— 四维竞技场
阶段 8  M7 内容填充（持续）        —— 配置表批量加内容
阶段 9  M8 平台发布（2~3 天）      —— 微信/抖音打包
```

**贯穿全程**：每写一个模块，顺手测一个；每阶段结束跑通一个可玩闭环。

---

## 阶段 P：播放器集成（★ 首要任务，其他一切的前提）

### 为什么是首要任务
```
所有美术素材 = 播放器格式（ftx3 纹理包 / .scene.zip 特效包）：
 ├ 角色（主角/友军/敌人）动画 = ftx3
 ├ 技能/命中/环境特效 = .scene.zip
 ├ 道具图标/世界贴片 = ftx3 单帧/帧动画
 └ 物理参数 = 公共库 .phys.json（可注入任意资产）
播放器不集成 → 没有角色、没有特效、游戏无法开始做。
```

### 现状与目标
```
现状：
 ├ 播放器内核已验证（effects-player 原型成熟）
 ├ 已无头移植到 src/vendor/player/（解码/实体渲染/流体/播放）
 └ 但游戏内零调用：没有加载入口、没有特效管理、没有渲染集成

目标（本阶段结束时）：
 ├ 游戏内能加载并播放任何 ftx3 / .scene.zip
 ├ 特效实例可挂到实体、可编辑（物理注入/扭动/换皮）
 ├ 有性能管理（池化/LOD/剔除/实例上限）
 └ 有验证场景：主角模型 + 一个技能特效 + 中毒注入演示
```

### 子任务

**P1 播放器内核入驻（半天）**
```
1. vendor/player 整理为唯一源码：
   - FtxAsset.ts 归位到 core/ftxAsset.ts（与 effects-player 结构一致）
   - 补缺失文件：gl/ftxRenderer.ts（纯纹理 quad 渲染）
   - index.ts 统一导出（Asset/FtxAsset/controller/fluid/渲染器）
2. ★ 帧名解析器 FrameResolver（每帧独立名字，不做去重/片段）：
   - 解析 ftx3 / .scene.zip 全部帧名（decode 已读 name 字段）
   - list() → Array<{index, name}>    # 明确都有啥帧（名字+顺序）
   - names() → string[]
   - resolve(name) → number | null    # 名字 → 帧索引
   - contains(name) → boolean
   - 挂在 Asset/FtxAsset 上：asset.getFrameNames() / asset.goto(name)
3. 验证：现有全部能力编译通过
4. 基准测试页（dev 入口）：加载主角 ftx3 → 列出帧名 → 播放
```

**P2 资源加载层（1 天）—— services/assets/AssetManager 扩展**
```
AssetManager 三种资源类型：
 ├ loadTexturePack(path)  → FtxAsset（ftx3，decodeMultiFrame + 帧纹理）
 ├ loadEffectPack(path)   → Asset（.scene.zip，bundle + 实体网格）
 ├ loadPhysics(id)        → PhysicsConfig（.phys.json 公共参数库，缓存）
 └ 统一：缓存 + 引用计数 + release(path)（chunk 卸载时释放）
路径 = 相对路径字符串（见架构 0.1 assets 目录）
```

**P3 特效实例管理器（2 天）—— services/fx/EffectFx**
```
EffectFx：
 ├ spawn(assetRef, opts) → EffectInstance     # 创建播放实例
 ├ despawn(instance)                           # 销毁（池化回收）
 ├ 实例上限：同时活跃 < 30（超限按 LOD 降级/停最远）
 └ 更新入口：update(dt)（每帧驱动所有实例 + 排序）

EffectInstance（挂到实体）：
 ├ attachTo(entity, attachPoint)  # 挂点：头顶/手部/胸口（billboard 化）
 ├ 播放控制：play/pause/stop/goto/loop/speed
 ├ 生命周期回调：onComplete/onFrameChange/onStateChange
 └ 销毁：实体销毁自动回收

★ FrameState 衔接层（动画管线写 / 渲染管线读，见架构 3.3.3）：
 ├ 每实体一份：{ assetRef, frameIndex, facing/flipX, lodLevel, 渲染句柄 }
 ├ 动画管线（FrameAnimator）每帧写 frameIndex
 ├ 渲染管线（FTXQuad/网格）读 frameIndex 渲染
 └ 本阶段实现基础字段；lodLevel 字段预留（P7 用）

★ 时间轴 = 复用播放器内核（core/time.ts + core/controller.ts，不重写）：
 ├ 每动画实例私有 TimeState（localTime/frameIndex/fps/order/loopCount）
 ├ FrameAnimator 只做语义层：状态 → 帧名列表 → 配置 controller
 ├ 推进：FrameAnimator.update(dt) → controller.advance(dt) → 写 FrameState
 ├ 单次动画回 idle：controller onComplete 回调驱动状态机
 └ LOD 降级：调 fps / 暂停 controller（不 reset 时间轴，见 P7）

★ 朝向与翻转（控制解耦，见架构 3.3.2a）：
 ├ 帧名语义：前=脸朝相机（向下移动用）/ 后=背对相机（向上移动用）
 ├ 输入解耦：InputActions.moveAxis（抽象）→ 移动系统 → 朝向逻辑
 │   moveAxis.y>0→前 / y<0→后 / x<0→flipX=true / x>0→flipX=false
 ├ FrameState 携带 facing+flipX → 渲染层应用（quad.scale.x=±1）
 └ ★ flipX 状态延续：动画状态切换不重置，只由新输入方向修改
```

**P4 渲染集成（2 天）—— 播放器输出进世界（★ 先简单版，LOD 后做）**
```
★ 本阶段只做"能显示"的最简集成，不做 LOD（LOD 单独后续阶段）：
 ├ 世界空间：billboard 化（面相机）+ 实体变换跟随 + y-sort
 ├ 视锥剔除：视锥外不更新/不渲染（简单 AABB 判定）
 ├ 合批：逐实例渲染（先不合并，性能后优化）
 └ 屏幕空间覆盖（UI 特效，后续）
 ├ 数据流：读 FrameState.frameIndex → 取纹理/网格 → 合成 → FXLayer → 排序 → draw

★ 但必须为 LOD 预留控制口（两个管线都要受 LOD 控制）：
 播放管线预留：setLodLevel(level)  # 0=全帧 / 1=减帧 / 2=单帧
   - FrameAnimator 读 LOD 等级 → 帧率/帧数降级
   - 注入的流体：LOD≥1 时停步进（保留最后一帧合成结果）
 渲染管线预留：setLodLevel(level)  # 0=完整 / 1=减细节 / 2=静态单帧
   - FTXQuad/实体网格按 LOD 切换输出档位
   - 与 RenderManager 的 LODManager 联动（评估 → 下发等级）
（接口先定义好，实现放"阶段 P7：LOD 控制"单独做）
```

**P5 运行时编辑 API（2 天）—— 游戏各方调用（架构 9.4）**
```
EffectInstance 可编辑接口：
 ├ injectPhysics(physicsId)     # 注入公共流体参数（中毒/火焰/冰冻）
 ├ clearPhysics()               # 取消注入 → 恢复原样
 ├ setDistort({enabled,amplitude,frequency,speed,rotation})  # 扭动编辑
 ├ setTextureTransform({offset,scale,rotation})              # 底图变换
 ├ setFrameTextures(base,residual)                           # 换皮
 └ injectAt(uv)                 # 交互注入（点击/拖拽 → 流体源）
```

**P6 游戏内验证场景（1 天）**
```
验证场景（ShipMode 或独立 dev 模式）：
 ├ 主角挂载：FtxAsset 播放主角帧动画（idle/walk）
 ├ 技能特效：spawn 一个 .scene.zip 技能特效挂到主角
 ├ 中毒演示：注入 poison.phys.json → 主角纹理毒雾扩散 → 取消恢复
 └ 按钮/快捷键触发（验证 API 各方可调）
```

**P7 LOD 控制（后续单独做，本阶段只留接口）**
```
★ 播放/渲染两个管线都必须受 LOD 控制（接口在 P4 已预留）：
 播放管线：LOD 等级 → 帧率降级/减帧/停流体
 渲染管线：LOD 等级 → 减细节/静态单帧
 联动：RenderManager.LODManager 评估距离 → setLodLevel 下发
（实现放 P7，与 3.6 架构 LOD 章节对齐）
```

### 验证标准
```
✅ 加载主角 ftx3 → 帧动画正常播放（无颠倒/无错位）
✅ getFrameNames() 列出全部帧名（名字+索引，与实际包一致）
✅ resolve(name) 按名字跳帧正确；不存在的名字返回 null
✅ spawn 特效 → 挂实体跟随移动，销毁回收
✅ 注入物理参数 → 纹理流体效果出现；取消 → 恢复
✅ setDistort → 扭动幅度实时变化
✅ 播放/渲染管线预留 setLodLevel 接口（本阶段不实现 LOD 逻辑）
✅ 性能：30 实例稳定 60fps（简单场景）
✅ tsc 无报错，全部通过
```

### API 总览（游戏各方调用的接口面）
```
━━━ 帧名解析器（FrameResolver，P1 内置）━━━
  asset.getFrameNames() → Array<{index, name}>   # 明确都有啥帧
  asset.names() → string[]
  asset.resolve(name) → number | null            # 名字 → 帧索引
  asset.contains(name) → boolean
  asset.goto(name)                                # 按名字跳帧
  （每帧独立名字，无重复；不做去重/片段分组）

━━━ 加载层（services/assets/AssetManager）━━━
  loadTexturePack(path) → Promise<FtxAsset>    # ftx3 纹理包
  loadEffectPack(path)   → Promise<Asset>      # .scene.zip 特效包
  loadPhysics(id)        → Promise<PhysicsConfig>  # 公共参数库 .phys.json
  release(path)          → 引用计数 -1，归零释放

━━━ 实例管理（services/fx/EffectFx）━━━
  spawn(assetRef, {loop, fps, attachPoint, layer}) → EffectInstance
  despawn(instance) / despawnAll()
  EffectInstance:
    attachTo(entity, attachPoint)   # 挂接实体（billboard 化）
    play() / pause() / stop() / goto(frame) / setSpeed(fps) / setLoop(mode)

━━━ 运行时编辑（架构 9.4，游戏内函数调用）━━━
  injectPhysics(physicsId)        # 状态效果：中毒/着火/冰冻
  clearPhysics()                  # 取消注入
  setDistort({enabled, amplitude, frequency, speed, rotation})
  setTextureTransform({offset, scale, rotation})
  setFrameTextures(base, residual)  # 换皮
  injectAt(uv)                    # 交互注入（点击/拖拽）

━━━ 状态/环境系统联动（调用方示例）━━━
  StatusSystem(中毒)   → instance.injectPhysics('physics/fluid/poison')
  StatusSystem(解除)   → instance.clearPhysics()
  WindSystem(风暴)     → instance.setDistort({amplitude: 大, frequency: 高})
  玩家交互(点火焰)     → instance.injectAt(uv)

━━━ 渲染集成（services/render）━━━
  FXLayer：世界空间特效容器（billboard + y-sort + LOD + 剔除）
  游戏侧唯一操作：实例挂到实体后自动渲染，无需手动管理
```

### 产出
游戏可用的特效播放器集成层：加载 → 播放 → 挂接 → 编辑 → 销毁 全链路

---

## 阶段 0：环境搭建

### 目标
开发环境就绪：能编译、能跑空场景。

### 步骤
1. **新建项目**：在 `全新的游戏/` 用 Vite + TypeScript 初始化
   ```
   npm create vite@latest . -- --template vanilla-ts
   ```
2. **装依赖**：three.js、rapier（@dimforge/rapier3d）、类型定义
3. **建目录**：按架构设计 0.1 的目录树（core/entity/systems/services/modes/ui/platform/config/assets）
4. **跑通空场景**：浏览器显示一个旋转的立方体（验证 three.js + Vite 工作）
5. **建最小 WebAdapter**：createCanvas/loadAsset/storage 三个空实现
6. **git 初始化**：提交"环境搭建"

### 验证
- `npm run dev` 能跑，页面显示 3D 场景
- 目录结构符合架构

### 产出
可运行的空壳项目 + git 仓库

---

## 阶段 1：M0 平台验证（最优先！）

### 目标
**验证三个核心依赖在目标平台（微信小游戏）能跑**——这是全项目最大技术风险，先验证，避免做完才发现跑不了。

### 步骤
1. **three.js 最小场景**：在微信小游戏环境跑一个方块（需要微信开发者工具 + 小游戏适配）
2. **rapier 最小验证**：加载 rapier WASM → 创建一个刚体 → 落体/碰撞
3. **播放器真机验证**：用阶段 P 的集成层在微信真机加载 ftx3/.scene.zip → 播放（验证播放器在真机的解码/渲染/WebGL 兼容）
4. **三者合体**：一个场景里同时有 3D 方块 + 刚体掉落 + 特效播放

### 验证
- 微信真机（或开发者工具）能跑，不崩溃
- 记录：WASM 怎么加载、canvas 怎么桥接（这决定 WebAdapter 怎么写）

### 关键产出
- **WeChatAdapter 的核心实现验证**（canvas/资源加载）
- 技术风险清单第一条"微信真机兼容"确认

### ⚠️ 如果验证失败
- 换方案：降低 three.js 版本/换渲染适配库/rapier 换轻量方案——在阶段 1 解决，不拖后

---

## 阶段 2：M1 最小战斗闭环（第一个可玩版本）

### 目标
浏览器里跑通：**移动 billboard 角色 + 攻击发射子弹 + 敌人出现 + 命中伤害**。

### 步骤（按依赖顺序）
1. **GameLoop**：fixedUpdate/update/render 三段循环（core/）
2. **EventBus**：最小实现（core/）
3. **Session**：最小字段（day/player 位置）（core/）
4. **Entity + EntityManager**：Entity 容器 + 组件基础（entity/）
5. **Transform + RigidBody 组件** + **PhysicsWorld 最小封装**：
   - init/addBody/step/syncTransforms（services/physics/）
   - 验证：一个方块受重力掉落、撞地面停住
6. **Billboard 组件 + 渲染**：一个 2D 贴片角色面朝相机（services/render/）
   - 用一个测试贴图（先不用真素材）
7. **PlayerController**：WASD 移动 + 鼠标转向（modes/ + platform/input/）
   - InputActions 最小集：moveAxis/lookAxis/attack
   - 先用 DesktopBinding
8. **BulletSystem**：发射子弹（对象池最小版）→ 物理飞行（services/physics/ + systems/combat/）
9. **Health 组件 + StatsCalculator**：命中减血（systems/combat/）
10. **最小敌人**：一个站桩敌人（无 AI，就是个靶子），能被子弹打死
11. **相机**：越肩跟随（services/camera/）

### 验证
- 能控制角色在场景里走
- 开枪打靶子，靶子掉血死亡
- 帧率 > 60（简单场景应该轻松）

### 产出
**第一个可玩的战斗最小闭环**——角色移动 + 射击 + 敌人死亡

### 完成标准
阶段 2 结束前，代码量应该只有几百行核心——如果超过，说明做复杂了，回头简化

---

## 阶段 3：M2 模式切换

### 目标
舰船（日常）↔ 大世界（战斗）两个场景能切换，Session 数据保留。

### 步骤
1. **IGameMode 接口**：enter/exit/update/render（core/IGameMode.ts）
2. **ShipMode**：实现 IGameMode，舰船场景 + 三面板 UI
3. **WorldMode**：复用阶段 2 的战斗场景
4. **切换流程**：舰船走到"出发"区域 → 加载 WorldMode → 返回按钮 → 回 ShipMode
5. **SaveSystem 最小版**：Session 序列化 → storage.set/get
   - 在舰船存档点测试：存档 → 刷新页面 → 恢复（能回到舰船）

### 验证
- 舰船 ↔ 大世界往返切换不崩
- 存档/读档：刷新页面后能恢复
- 模式切换时实体正确卸载/加载（旧地图实体消失）

### 产出
日常/战斗双场景 + 基本存档

---

## 阶段 4：M3 地图生成

### 目标
大世界有真实地图：chunk 流式生成 + 迷雾 + 小地图。

### 步骤
1. **ChunkGenerator**：噪声高度场 → 网格（services/render/TerrainSystem.ts）
   - 先做"当天地图"：以舰船为原点，生成一块有界地形
2. **地形碰撞**：固定刚体（PhysicsWorld.addBody）
3. **ChunkActorGenerator 最小版**：随机放几个箱子/敌人/资源点
4. **每日新地图**：每天出击 = 新 seed 重新生成（换 seed 测试）
5. **FogOfWar**：黑雾掩码 + 静态地形层 + 实时层（systems/map/）
6. **小地图 UI**：Canvas 2D 画迷雾（ui/）
7. **LOD + 视锥最小版**：远处实体降细节（先做实体 LOD，地形 LOD 后补）

### 验证
- 地图能走、不穿地、有起伏
- 迷雾：探索过的地方亮、没探索的黑、怪物只在附近显示
- 换天/换 seed 地图不同

### 产出
能探索的 3D 地图 + 迷雾小地图

---

## 阶段 5：M4 战斗完整

### 目标
战斗成为"真游戏"：敌人 AI、掉落、拾取、背包。

### 步骤
1. **AIStateMachine**：通用解释器（读配置 → 行为表 → 转移）（systems/combat/）
   - behaviorTable 先注册 5 个行为：wander/moveToTarget/meleeSwing/shootBurst/death
   - conditionTable 先注册：seePlayer/inRange/outOfRange
2. **两种敌人**：近战兵（巡逻→追→砍）+ 远程兵（巡逻→追→射）
   - 用 enemies.json 配置（测试"加敌人=改配置"）
3. **掉落**：敌人死亡 → 掉落物品实体（掉地上，物理落地）
4. **拾取**：走近 → 交互 → 物品进背包
5. **背包网格**：InventoryGrid（addItemToGrid/removeItemFromGrid，★ 2026-09-10 起同层合并：一格一类、数量无上限）（systems/inventory/ + core/Session.ts）
6. **背包 UI**：Canvas 网格界面 + 拖动（ui/）
7. **弹药/食物**：同层合并堆叠，数量无上限

### 验证
- 两种敌人行为正确（会追、会打、会死）
- 打怪掉东西，捡起来进背包
- 背包能拖动、堆叠

### 产出
完整战斗循环：打怪 → 掉落 → 拾取 → 背包

---

## 阶段 6：M5 抽卡 + 守卫

### 目标
抽卡流程 + 舰船守卫，形成"日常→战斗"的核心循环。

### 步骤
1. **GachaPool**：加权随机 + pityCounter（systems/gacha/）
   - 50 抽 +2% 递增逻辑（可单测）
2. **抽卡 UI**：按钮 + 结果展示（先简单）
3. **抽卡分发**：抽到角色 → 友军；道具 → 背包；Boss → 决战占位（先弹提示）
4. **ShipGuardSystem**：袭扰波次（时间驱动）→ 敌人直奔舰船（systems/ship/）
5. **舰船 HP + 警报**：受击 → ShipAlertEvent → HUD 红点
6. **友军最小版**：抽到的角色可部署（站桩守卫舰船）→ 用 AIStateMachine 的 deployGuard 行为

### 验证
- 抽卡保底逻辑正确（50 抽后递增）
- 外出后舰船会被袭，警报提示，回防可解
- 抽到的友军能部署守卫

### 产出
**核心循环可玩**：抽卡 → 出击 → 探索守卫 → 回船

---

## 阶段 7：M6 Boss 决战

### 目标
四维 Boss 战（简化版能打）。

### 步骤
1. **BossRaidMode**：独立模式（模式切换复用阶段 3）
2. **BossArenaGenerator**：一个独立竞技场（不用 chunk 流式）
3. **Boss 实体**：大血条 + 多阶段（先 2 阶段）
4. **机制 1：超立方体投影**（先做视觉版）：
   - 竞技场几何体阶段切换时旋转重组
5. **机制 2：切片**（先做简化版）：
   - Boss 部分时间"不可见/不可命中"→ 玩家切视角才能打
6. **Boss 专属 HUD**：大血条 + 阶段提示
7. **抽到 Boss → 进决战**（接上阶段 6 的占位）

### 验证
- 抽到 Boss 能进决战
- Boss 有多阶段、有至少一个四维机制
- 能打死（平衡可后调）

### 产出
完整一局的核心：抽到 Boss → 决战 → 赢/输 → 结算

---

## 阶段 8：M7 内容填充（持续，边玩边加）

### 目标
批量加内容，调平衡，让游戏"好玩"。

### 步骤（按优先级）
1. **维什戴尔技能**：3 个技能具体化（CD 已就绪，加效果）
2. **敌人扩充**：5 种 → 10 种（多数复用行为，配置加）
3. **友军扩充**：抽到的干员行为（复用行为库）
4. **遗物**：黑冠（每日×1.01）+ 5 个局内遗物（RelicEffectSystem）
5. **合成**：弹药/食物配方（CraftingSystem）
6. **采集**：资源点 + 采集交互（GatheringSystem）
7. **对话/事件**：DialogueSystem + 几个事件（幸存者/残响）
8. **平衡**：敌人曲线 vs 玩家成长（黑冠+遗物+合成）——跑几局调数值

### 验证
- 每加一块内容：跑一局确认不崩、手感好
- 平衡：玩家能在 20~30 天内"够强"决战

### 产出
内容丰富的完整游戏

---

## 阶段 9：M8 平台发布

### 目标
打包到微信/抖音，包体 ≤ 20MB。

### 步骤
1. **WeChatAdapter 完善**：真机全功能（阶段 1 验证过的核心 + 音频/存储）
2. **分包**：主题区/Boss 素材分包
3. **包体瘦身**：WebP 纹理/压缩音频/特效低分辨率档
4. **真机测试**：全流程在真机跑（性能/内存/触摸）
5. **Canvas UI 触屏适配**：TouchBinding 真机验证
6. **发布**：微信后台提审 → 上线

### 验证
- 真机 30fps 保底
- 包体 ≤ 20MB
- 触屏操作顺手

---

## 每个阶段的通用检查清单

```
✅ 这阶段的目标"能玩/能看/能测"了吗？
✅ 代码量合理吗？（超出预期 = 过度设计，简化）
✅ 配置化了吗？（新内容走配置，不硬编码）
✅ 提交 git 了吗？（每阶段一个 commit）
✅ 跑过 tsc 类型检查吗？（无报错再继续）
```

## 开发纪律（重要）

1. **先跑通再优化**：别在阶段 2 优化粒子性能——那不是现在的事
2. **文档是地图，不是行李**：架构文档描述目标，实现时按阶段取用，不要试图一次实现全部
3. **每阶段结束都能玩**：任何一个阶段中断，你都有一份"能跑的东西"
4. **遇到卡点先简化**：如果某个机制做 2 天还没通，砍到最小能跑版本，回头再补
5. **测试一个加一个**：新增模块前，确认旧模块还活着（别等到最后一起炸）

---

## 时间线参考（乐观估计）

```
阶段 P：约 1~2 周  → 播放器集成（首要）
阶段 0-2：约 1 周   → 第一个能打的版本
阶段 3-4：约 2 周   → 有地图有存档
阶段 5-6：约 2 周   → 核心循环可玩
阶段 7：约 1~2 周  → Boss 战
阶段 8：持续 1~2 月 → 内容+平衡
阶段 9：约 1 周    → 发布
```
**总计：约 3 个月能出可玩版本，之后持续迭代内容。**

---

# 附：战斗与掉落管线架构

> 原《战斗与掉落管线架构.md》全文合并（2026-09-10）。本章 §N 指原文编号。

## 战斗与掉落管线架构

> 一行定位：**命中 = rapier 物理碰撞｜伤害 = modifier 结算链｜掉落 = 挂载在静态世界命中回调上的独立管线**
> 范围：瞄准 → 发射 → 飞行 → 碰撞分发 → 命中分类 → 伤害 → 反馈 → 掉落。
> 原则：谁拥有谁负责；战斗内部直接调用，表现边界走 EventBus；组合层回调注入世界细节。

---

### 一、击中检测架构（现状，代码即文档）

#### 1.1 总览
```
输入(KeyJ/左键) → DesktopBinding.pressed.attack
  → WorldMode.consumeAttack()
  → firePlayerBullet()
        ├─ cameraRay() 直射方向（底）
        └─ aimRaycast() 辅助瞄准（命中目标则把弹道转指向目标落点）★预测用，不负责最终判定
  → executeAttack('projectile') → BulletManager.spawn（池 100）
  → BulletEntity 纯物理：球 r=0.08 / gravityScale=0 直线 / CCD 防隧穿 / restitution0.8 反弹 / 寿命2s（零世界认知——不判伤害不判地形）
  → rapier 接触 → PhysicsWorld → EntityManager.dispatchCollision（按 userData=实体id）
        └─ BulletEntity.onCollision → 过滤(仅started/同阵营跳过) + hitFx → onHit(payload)  ★唯一出口
              └─ ★★命中解析层 resolveBulletHit（WorldMode 唯一入口，敌人/装饰物/地块全归口）
                    ├─ other=敌人实体 → applyDamage + emit 'damage' + 反弹
                    └─ other=null（地块/装饰物）→ chunks.resolveImpact → ImpactReport（一次权威判定；跳弹每次反弹各自解算）
                          ├─ playBulletImpact(report)  ◆ 地形扣除（还原老版：任意落点必出坑；报告供坐标）
                          ├─ agitateWaterNear(x, z)    ◆ 水面波动（报告 tile/water）
                          └─ spawnItemDrops(report)    ◆ 掉落：ground/water/prop 全来自报告
  → 反馈：CombatDirector 听 damage/killed → FEEL 表(hitstop/时间缩放/镜头踢/音频钩子)；浮动伤害数字
```

#### 1.2 瞄准（Targeting.ts）
- **双射分离**：`cameraRay` 纯相机方向打底；`aimRaycast`（Targeting.ts:37）做辅助瞄准——实体优先（`queryRay` 空间索引粗筛 + `raySphereHit` 3D 精细球判定）+ 物理兜底（`castRay` 落地面/墙），并有**遮挡校验**（墙后目标不算命中）。
- 命中目标是**预测**，决定发射方向；子弹是否真的打中，交给物理碰撞判，二者不互相替代。弹道直线 → 指即中，手感好。

#### 1.3 发射（Attack.ts：统一攻击意图）
- `executeAttack` 分派三类意图：`projectile`（子弹池发射）/ `melee`（querySphere 范围瞬时 + 高度过滤±2m）/ `aoe`（落点延迟，预留）。
- 玩家与 AI 共用同一入口（`aiCtx.attack` 也是 executeAttack）——"谁发射都一样"。

#### 1.4 飞行 & 碰撞（BulletEntity.ts）
- 纯物理实体（零渲染），绘制由 BulletRenderer 从速度快照完成（数据/视觉解耦）。
- 碰撞体积 = 小球 r=0.08 + CCD（防隧穿）；gravityScale=0 → 直线弹道；restitution=0.8 → **打地面会反弹**。
- 命中实体后**不销毁、继续穿透**（可一路打穿多个敌人，设计如此）。

#### 1.5 碰撞分发（EntityManager.dispatchCollision）
- 关键设计：按 `userData = 实体 id`（非 rapier handle）查 `bases` → **杜绝 handle 复用误伤**。
- 地面/装饰物刚体（`kind:'ground'/'decoration'`，fixed）**没有 EntityBase 实例注册** → 落到 `onCollision(null)` 分支 = "命中静态世界"。

#### 1.6 命中分类（BulletEntity.onCollision → 命中解析层）
- 只在 `started`（接触开始）触发一次；命中过滤唯一真源 = `services/combat/teams.ts sameTeam`（`player`/`ally` 互免：玩家不误伤无人机，友军不互殴；`enemy` 对一切非同阵营生效）。
- **子弹实体零世界认知**：不做伤害、不判地形、不判装饰物——只过滤 + `hitFx` + 回抛载荷 `onHit({self, other, point, damage})`，分类结算全部由解析层（WorldMode.resolveBulletHit）负责。
- 敌人实体（`other≠null`）：解析层 `applyDamage + emit 'damage' + 控制台`（伤害结算归 combat，入口归解析层）。
- 静态世界（`other=null`）：解析层 `resolveImpact(point)` → 细分地块/装饰物 → 地形扣除/水波/掉落。
- ★ **组合层回调注入是本管线核心扩展点**：统一出口 `onHit` 一颗入口吃所有命中，解析层内部再细分——掉落/水波/地形/伤害全在解析层归口。
- ★ **跳弹分别解算（2026-09-08 定调）**：restitution=0.8 反弹 → 每次接触开始（`started`）独立触发一次解析 → 每次反弹各自结算（独立挖坑 / 水波 / 掉落 / 敌人伤害），不做每发合并。

#### 1.7 伤害（DamagePipeline.ts）
- `resolveDamage` modifier 链：闪避 → 格挡 → 护盾 → 防御 → 暴击 → 下限/取整（顺序即语义，加机制=插链，不改核心）。
- `applyDamage`：未闪避且 final>0 → `target.onTakeDamage` → 扣血 → hp≤0 → emit `'killed'` + onDeath。
- 同层已有 CombatDirector（事件消费者）配 FEEL 规则表统一手感强度。

#### 1.8 反馈（表现边界）
- `'damage'`：CombatDirector 按规则表做 hitstop/时间缩放/镜头冲击；WorldMode 投影浮动伤害数字。
- `'killed'`：击杀定格演出。
- 战斗数值逻辑内部直接调用；EventBus 只承载"表现/跨模块听众"——符合《开发说明书》③约定精神。

---

### 二、命中解析层 + 物品掉落管线（重写 2026-09-08）

> 一行定位：**所有子弹命中（敌人 / 装饰物 / 地块）都进唯一入口 `resolveBulletHit`；静态世界再走 `resolveImpact` 一次权威判定产出 ImpactReport，地形修改 / 水面波动 / 掉落全部消费同一份**——敌人伤害结算归 combat、世界细节归解析层，子弹实体零认知。此前"敌人伤害内嵌在 BulletEntity、静态世界各端重复探测"的旧架构整体废除。

#### 2.1 挂点（每次碰撞开始一次；敌人 / 装饰物 / 地块全走这里）
```
BulletEntity.onCollision → onHit(payload)             // ★唯一的出口（跳弹每次反弹各触发一次）
  → WorldMode.resolveBulletHit({ self, other, point, damage })
      ├─ other = 敌人 → applyDamage + emit 'damage'   （伤害归 combat）
      └─ other = null → chunks.resolveImpact(point)   ★命中解析层：一次权威判定
            ├─ chunks.playBulletImpact(impact)         // 地形扣除（还原老版：任意落点必出坑，报告供坐标）
            ├─ agitateWaterNear(x, z)                  // 水面波动（既有，十字采样保留）
            └─ spawnItemDrops(impact)                  // 掉落管线：三键全来自报告
```

#### 2.2 命中解析层（ChunkManager.resolveImpact → ImpactReport）
```
ImpactReport {
  x, y, z;                       // 命中点（世界）
  tile:  { cx,cz, bx,bz, id, role, h }   // ★ 命中地块：blockTypes→ Tiles 表 id + genRole + 4m 面高
  water: 'hit' | 'edge' | 'none'         // 命中块本身是 liquid / 4 邻块是 liquid / 无
  prop:  { key,x,y,z,r,h } | null        // ★ 命中点 ~2.2m 内最近的可碰撞装饰实体（无则 null）
}
```
- **地块属性来自表**（`tileById(id).genRole`）：`blockAt`（4m 世界块）→ `ChunkData.blockTypes[15×15]` → `Tiles 注册表`；高度取 blocks 内 1m cell 的 `heights`（4×4 平整）。无 chunk 数据 → id=0/role=''/h=0（视为不可挖）。
- **水判定**：命中块 role=='liquid' → `hit`；否则查 4 邻块任一 liquid → `edge`；否则 `none`。（水判定随"命中地块"表属性，不再十字采样。）
- **装饰实体**：`queryPropsNear` 查**运行时权威索引** `propRegistry`（见 2.5），返回最近者。

#### 2.3 地形修改（playBulletImpact(impact)）
- **还原老版行为（2026-09-08 回退）**：第一版资格门（role + 陡变带 PATCH_SLOPE_EPS=0.5）导致大片地形打不出坑、worker 空转，体验"远不如之前"。回退后：
- 每次静态命中都出坑——水平圆与 coarse cell AABB 判交 → digCells 逐格 +1（不封顶；同点连打持续加深）→ patchRebuildChunk 重建链路不变。
- 报告 `impact` 仍为**坐标源**（复用 resolveImpact 的命中点），地形修改与掉落/水波共用同一入口，维持"接入解析层"的归口；不做单独的地块资格收敛（后续若要"防水底/陡变带不挖"，须以玩家认可的手感再调，勿一步砍死）。

#### 2.4 环境探测 → 规则表（ItemDropPipeline.ts：纯函数，零依赖）
```
spawnItemDrops(impact):
  env = {
    hasGround:  impact.tile.role ∈ {ground, platform},   // 命中地块是可站地形
    hasWater:   impact.water !== 'none',                 // 命中块或贴邻一格水面
    hasCrystal: impact.prop?.key === 'depleted_crystal', // 2.2m 内耗尽原石晶体
  }
  → rollDrops(env) → [{itemId, count}]
```

| 环境 | 物品 | 概率 | 每次数量 | 堆叠上限 |
|---|---|---|---|---|
| 地面地块 | `raw_rock` 固原岩 | 0.35 | 1~2 | 99 |
| 命中水/贴水 | `ketone` 酮凝集 | 0.40 | 1~2 | 99 |
| 原石晶体 2.2m | `iron_grain` 异铁 | 0.55 | 1~2 | 99 |

#### 2.5 装饰实体权威索引（propRegistry）
- 旧 `hasPropTypeNear` 是"确定性重放规划"——每发命中重算 9 片 chunk 的 `planChunkProps`，是**推演**不是**查实际**。
- **新方案**：`createDecorColliders` 创建碰撞体时把实体登记进 `propRegistry`（key → `{key,cx,cz,x,y,z,r,h}[]`，世界坐标），chunk 视觉替换销毁时同步 `propRegistry.delete(key)`，`disposeAll` 全清——**只索引实际存在的碰撞体**（与 propBodies 同生命周期）。`queryPropsNear(x,z,r)` 查命中片 + 8 邻环取最近；`hasPropTypeNear` 降级为委托。BOSS 四维空间无装饰回归 false。

#### 2.6 背包落账 + UI
- `hasSpace` + `addItem('player', …)`：有空间直接直塞（★ 2026-09-10 起同层合并语义：一格一类、数量无上限，直塞即并入唯一格；不再有 maxStack 分堆）。
- `showPickupResult(itemId, ok, count)`：显示**物品名 × 数量**（"拾取了 固原岩 ×2"），背包满提示失败。
- `flashItemAndRefresh(itemId)`：背包打开时该格闪烁 + 面板实时刷新（bullet 掉落与 E 键拾取同走一格闪烁反馈）。
- 跳弹多次命中 → 每次反弹独立走本管线（各自掷概率 / 落账 / 提示），属特性。

#### 2.7 与既有生态的位置
- 掉落**直塞背包** + 提示 + 格子闪烁，与敌人死亡掉落（ItemBase 掉地上拾取，M4 阶段）是两条并行路径；若后续要"爆炸溅出实体掉落物"，把 rollDrops 的产出改走 ItemBase 生成即可，规则层不动。

---

### 三、架构评价（击中检测层）

#### 合理之处（保留不动）
1. **判定分工清晰**：瞄准则（预测）与物理实碰分离，各司其职；辅助瞄准 + 直线弹道给出手感，物理判定兜底真实性。
2. **userData=实体 id 分发**：规避 rapier handle 复用误伤——这是会真实爆炸的坑，已提前防住。
3. **组合层回调注入（onHit / hitFx）**：子弹实体零认知于世界细节——伤害、地形、装饰物、掉落全是解析层（WorldMode.resolveBulletHit）的事。
4. **伤害 modifier 链 + FEEL 表**：机制与手感都是"加一行/加一个"，核心不动。

#### 隐患与改进（按优先级）
1. **[已修 ✅] 动态 `import('../../core/EventBus')`** —— EventBus 改静态顶层导入（WorldMode.resolveBulletHit 里 emit 'damage'），每发命中不再过 Promise 边界。
2. **[中] DamagePipeline.resolveDamage("纯计算"契约背离)** —— 头注释写"不修改实体状态，纯计算"，但 `modifierShield`（DamagePipeline.ts:61）直接改了 `ctx.target.shield`。当前调用方每次新建 ctx 所以暂未炸，但契约脆弱（重入/复用会同点双扣盾）。改进：护盾吸收挪到 applyDamage 落账阶段，resolve 只读快照。
3. **[已修 ✅] 敌人/装饰物/地块分类归口** —— 统一出口 `onHit` → `resolveBulletHit`：敌人走 applyDamage、静态世界走 `resolveImpact` 细分出 tile（Table 属性）/water/prop，掉落据此分化（打水出酮凝集、打水晶簇异铁）；地形修改还原老版任意落点出坑（资格门过严被回退，见 2.3）。**残余精度项**：装饰物是"命中点 ~2.2m 探测"而非"这次撞的确实是装饰刚体"——要精确区分需 dispatchCollision 回抛静态刚体身份（propByBodyId 反查），留作后续。
4. **[已定 ✅] 反弹 vs 命中语义** —— **跳弹分别解算**（2026-09-08 用户定调）：每次反弹独立走命中解析（独立挖坑/掉落/波动/敌人伤害），不做每发合并。
5. **[低] 掩蔽信息**：瞄准的遮挡校验覆盖"预判"，飞行中撞中间物属正常物理——无问题，不必改。

#### 结论
**命中解析层重构完成（2026-09-08）：所有子弹命中进唯一入口 `resolveBulletHit`（敌人→伤害管线；静态世界→一次 `resolveImpact` 权威 ImpactReport），地形/水/掉落消费同一份判定，权威装饰索引替代确定性重放，子弹实体零认知。** 剩余一项小修：DamagePipeline 纯函数契约（护盾落账挪 applyDamage）。

---

### 沿革
- 2026-09-08 建立本文档；记录：①物品掉落管线落地（ItemDropPipeline.ts / WorldMode.spawnItemDrops / ChunkManager.hasPropTypeNear / toast+格闪）；②击中检测架构审查结论（三修清单）。
- 2026-09-08 **命中解析层重构落地**：新增 `ChunkManager.resolveImpact(x,y,z) → ImpactReport`（tile=Table 属性 / water / prop）；装饰实体运行时权威索引 propRegistry 替代确定性重放（hasPropTypeNear 委托）；掉落三键全来自报告；跳弹分别解算（每次反弹独立结算）。
- 2026-09-08 **敌人/装饰物入解析层**：统一出口 `BulletEntity.onHit → WorldMode.resolveBulletHit`——敌人命中改由解析层走 applyDamage + emit 'damage'（EventBus 改静态导入，删掉每发动态 import）；静态世界细分地块/装饰物。
- 2026-09-08 **地形修改回退**：地块资格门（role/陡变带，PATCH_SLOPE_EPS=0.5）致大片地形打不出坑、worker 空转，按玩家反馈回退为老版任意落点必出坑；`resolveImpact` 仍为坐标源，报告继续供掉落/水波判定。
- 2026-09-08 **地形检测+修改链路提速（优先级：地形修改高 > 地形创建不高）**：
  - **帧间合并**：`playBulletImpact` 的 digCells 仍同步落库（数据即时正确），但视觉重建不再逐发即触——攒入 `pendingPatches: Map<chunkKey,{cx,cz,dirty:Set}>`，每帧开头 `flushPatchRebuilds()` 按 chunk 合并同类项一次性投递。同帧 N 发/多跳弹打同一 chunk → 1 次 worker 重建（dirty 取并集，终态收敛不变）。密集射击下不再出现"后一发重建排队等前一发"的串行堆积。
  - **优先级压缩**：`processQueue` 在 `patchRebuilds.size>0 || pendingPatches.size>0` 时把地形创建预算 BUILD_BUDGET_MS 减半——地形创建（烘焙/新建 chunk）向地形修改让路。
  - **重建入口不变**：`patchRebuildChunk(cx,cz,[...dirty])`（terrainPatch Worker 优先）与在途串行化逻辑原样保留。
- 2026-09-08 **地形破坏链路继续提速（用户实测"不打水依旧卡"）**：真因 = 每次命中即整 chunk 顶/墙全量重建 + 主线程**同步全量装配**（不进 ASSEMBLE_PER_FRAME 预算），连射 ≈ 60 次/秒重建+装配。三处修正：
  - **预算化装配**：`patchRebuildChunk` 不再同步 `assembleTableChunk`，改推入与烘焙共用 `assembleQueue`（每帧 ≤2）——破坏重建完成不再造成主线程瞬时装配尖峰。
  - **跨帧重建节流**：`flushPatchRebuilds` 增加 `PATCH_REBUILD_MIN_MS=120` 窗口 + 在途跳过——连射/多跳弹同一 chunk 改为分批下陷（未到期/在途的 dirty 继续攒缓冲，数据层 levels 已同步落库，视觉分批不丢终态），worker 不再被打满。
  - **[PATCH] 日志 500ms 节流**（console.log 本身即热路径开销）。
  - **破坏几何 3 Worker 并行**（`TerrainPatch.ts`）：`WORKER_COUNT=3`，**选排队最短（pending 最少）的 worker 贪心投递**——多个地形破坏并发时各任务进最闲的 worker，worker 内部串行处理，3 核分摊；同 chunk 已被 120ms 节流串行化不重复。无 hash 亲缘：同 chunk 可能换 worker，其水体增量状态跨 worker 退化为全量重解（确定性不变，仅略慢）。单个 worker 断裂 → 只影响分派到它的任务回退主线程同步同函数（字节一致），不再整 chunk 兜底烘焙。
- 2026-09-08 **破坏重建增量装配（用户原设计：按表只改相关区块）**：
  - **分区函数 `partitionPatch(levels, chunkSize)`**（`FaceTable.ts`）：表驱动的补丁重发分区——`topCells` = 补丁 coarse cell ∪ 外扩 1 圈（涵盖 smoothstep 坡面 + 防 T 结）；`blockSides` = 4m 块边两侧粗格子任一带补丁的边（坑缘壁 / 坑内 flush 壁）。纯函数，worker 内廉价计算。
  - **增量组装 `rebuildTerrainOnly`**（`ChunkManager.ts`）：只为 chunk 重发地形 top/wall/water 网格 + 地形 trimesh；把装饰层（`group.userData.terrainCount` 之后的所有子对象）原样保留复用——装饰物网格、装饰物碰撞体、石围裙/水泥台座刚体不再随每发销毁重建（原来 `assembleTableChunk→replaceChunk` 每发全量拆装整 chunk）。
  - 破坏重建仍走 `assembleQueue` 预算化装配（`decor===null` 分派到 `rebuildTerrainOnly`）。
- 2026-09-08 **首建 + 破坏重建统一延迟装饰（props 浮空修复）**：
  - `rebuildTerrainOnly` 对已有网格的 chunk 通过 `replaceChunk` 统一拆旧（旧地形/装饰视觉 + trimesh + propBodies + propRegistry + 围裙/台座刚体全销毁），只挂地形 group。无网格（首建）同理只挂地形。
  - 坑池延迟装饰队列 `pendingDecorJobs: Map<key, {cx,cz,maps}>`，地形 swap 后入队（key 去重，多坑连射只保留一个任务）。
  - 补挂时（每帧 `DECOR_PER_FRAME=1`）`planDecor` 取**当前** levels 重计划（`surfaceHeightAt` 已含挖坑下探）→ props 落到新坑面，不再浮空。视觉 + 碰撞体/围裙/台座刚体 + propRegistry 一并重建，与 `assembleTableChunk` 同构。
  - 风格切换/dispose 清空该队列。

---

# 附：背包与加工台架构（★ 2026-09-10 模块化落地）

> 一行定位：**背包 = 同层合并（一格一类、数量无上限、跨层可拆分）；计数与落账的唯一入口是 ItemManager（countItem / countTotal）；加工台 = 每个配方一个 CraftModule 模块（内部自管 UI 与数量窗），数据查询 / 消耗 / 新增全部走 ItemManager / CraftingManager，图标与背包共享同一 ItemIconRegistry。**
> 范围：session.inventories 数据模型 → 计数/落账 → 合成 → 图标 → 模块化 UI。
> 原则：UI 不碰 session 网格做业务判断；配置 → 业务（ItemManager/CraftingManager）→ 表现（图标服务/模块）；舰船与战斗两套 UI 体系各自装配。

---

### 一、数据模型：同层合并归一（★ 2026-09-10 重构）

```
旧：同层存在同 itemId 多堆（maxStack 裂格）→ 新增/移动按 maxStack 分堆
新：一格一类、数量无上限；同层插入即合并；跨层允许拆分（同 itemId 可同时在基地/飞船/玩家）
```
- `addItemToGrid`（Session.ts）：已有同类格 → 数量累加；无则占空格；无空格则失败。格子数 = 可容纳的**不同物品类型数**（容量语义保留：player 4×6 / ship 8×10 / base 30×30）。
- `moveItemBetweenGrids`：源拿取 → 目标合并；格子不足回滚。
- `hasSpace` 语义：该层已有该物品 → **恒有空间**；否则需空格。
- `mergeDuplicatesInGrid`（Session.ts）：旧存档迁移，加载时对三层各做一次重复堆合并求和（合并只会减少占用，安全）。

### 二、计数唯一入口（ItemManager）
```
countItem(layer, itemId)          // 单层数量
countTotal(itemId)                // ★ 基地＋飞船＋玩家 总数（加工台显示用）
hasItem(layer,itemId,count)       // = countItem ≥ count
hasSpace(layer,itemId,count)      // 已有同类 → 恒真；否则需空格
```
- 业务层（CraftingManager）与 UI（CraftingOverlay / CraftModule）一律经 ItemManager 计数，**不再直接遍历 session 网格**。
- 消耗 / 新增 / 跨层移动全部走 `ItemManager.addItem / removeItem / moveItem`（唯一碰网格的地方）。

### 三、合成：跨层扣料 + 模块化
```
canCraft(recipeId)         → 各材料 countTotal ≥ 需求
craft(recipeId, src, dst)  → 扣料顺序：基地 → 飞船 → 玩家；产出 addItem(dst)
maxCraftable(recipeId)     → min(⌊countTotal / count⌋)   （UI 不再自己算）
```
- 数量确认窗的"拥有 / 需要"均取**所有背包层总数**。
- 产出目标层已有同类 → 恒有空间；否则需空格，否则不扣料（不破坏数据）。

### 四、图标路径唯一化（★ 2026-09-10）
- `ItemIconRegistry.createIconElement(itemId)`：**唯一挂载出口**——静态（六兄弟/色块兜底）→ 独立 `<img>`(dataURL)；无人机 → 活动画布（翅膀动画经 DroneIconAnimator 播放，断连画布自动回收）。
- 背包与加工台 = 同一 UI 体系：**同一个** `ItemIconRegistry` 实例注入 InventoryGridRenderer 与 CraftModule。
- 战斗 = 另一套 UI 体系：WorldMode 不注入，背包网格内部自建注册表，互不干扰。
- 旧的"加工台 cloneIcon / 背包 toDataURL"双路径废除（`InventoryGridRenderer` 构造改可注入，缺省自建）。

### 五、加工模块 CraftModule（配方的基本单位 ★ 2026-09-10）
```
new CraftModule(recipe, deps: { craftingManager, itemManager, iconRegistry, host, onRequestOpen })
  mount(btn)      // 底图 + 输出图标/名字 + 材料图标(iconRegistry) + 拥有/需求(countTotal)
  refresh()       // 重查各材料拥有量，不足标红
  openQuantity()  // 本模块自管数量确认窗（挂 host；单开由宿主仲裁）
  dispose()       // 拆数量窗 + 清槽位 DOM
```
- **一个模块 = 一个配方**；UI 播放（图标、数量、点击、加工）全部封装在模块内部。
- CraftingOverlay 只做编排：6 槽位装配模块 / 单开仲裁（点 A 先关 B 窗）/ 背景 THREE + resize / 显隐销毁。

### 六、装配
```
ShipMode: 一个 ItemIconRegistry（★ 两处共用）
  ├→ ShipUIManager → InventoryPanel → InventoryGridRenderer(注入)
  └→ CraftingOverlay → CraftModule(注入)
WorldMode: 另一套 UI 体系，InventoryPanel 不注入 → 网格内部自建注册表
```

### 架构评价（现状）
- 合理：计数/落账单一入口；加工台零 session 依赖；图标一条路径；模块内聚、可增删配方即增删模块。
- 已知边界：`InventoryPanel` 网格**展示**仍直读 `session.inventories`（视图层，不做业务判断）；`countTotal` 每次全层遍历（网格小、调用频率低，无需优化）。

### 沿革
- 2026-09-10 背包重构：同层合并（一格一类 / 数量无上限）、`mergeDuplicatesInGrid` 迁移、`hasSpace` 语义更新、`removeItemFromGrid` 简化为唯一格。
- 2026-09-10 合成跨层：`canCraft` 总数判断；`craft` 按 **基地→飞船→玩家** 扣料、产出落玩家；加工台数量显示取所有背包层总数。
- 2026-09-10 强耦合改造：`CraftModule` 模块化（数量窗自管）、`CraftingOverlay` 瘦身为编排器、`ItemIconRegistry.createIconElement` 统一图标路径、背包与加工台共享同一图标服务实例。

---

# 附：战斗道具播放架构（ItemPlayback ★ 2026-09-10 已实现）

> 一行定位：**战斗内"主角相关的道具 UI 播放"按三类分发——弹药类 = 弹匣数值（按子弹处理，不建实体）；友军类 = 召唤实体跟随主角（空中/地面两种态）；防具武器类 = 在主角原有纹理上叠加装备贴片（二级 FTXQuad）。统一由 CombatItemController 装配，`items.json` 配置驱动。**
> 现状锚点：弹药无池无 HUD（`ammo` 效果只有 console.log）；友军只有空中的 DroneEntity（VAT 双翼 + follow/attack AI + 残骸回收）；无装备位/穿戴数据；主角 = FTXQuad billboard 贴片（Player.ts）。

---

### 零、道具分类与播放映射（配置驱动）
```
items.json 每条新增 combat 字段：
  "combat": { "kind": "ammo" | "ally" | "equip", ...(分类专属参数) }
```
- ✅ 已落地：`ammo_pack → combat.kind='ammo'`；`kaltsit_drone → combat.kind='ally'`；`shu_jie_xx → combat.kind='equip'`（`combat:{kind:'equip', slot, asset, offsetX, offsetY, offsetZ, scale}`，offsetZ 把贴片推到角色前方避免与本体共面 z-fighting）。
- 归属判断在表现层查 `combat.kind`；没有 combat 字段的普通道具 = 无战斗播放。
- 表现播放 ≠ 业务逻辑：数值/消耗仍走 ItemManager（计数/落账唯一入口，沿用背包架构）。

### 一、弹药类（按子弹处理）✅
- **弹药池**：`session.player.ammo: Record<ammoTypeId, number>`（跨场保留，安全点随 Session 存；新档种子 150）。
- 使用 `ammo_pack`（effect `ammo`）→ 真正入池 +N。弹药包已可通过加工台制造（craftRecipes：scrap_metal×3 + sugar×2）。
- **开火消耗**：WorldMode 开火钩子 → `combatItems.tryFire()`（AmmoStore 出池）；池空 → 拒发（0.15s 重试）+ HUD 红色脉冲。
- **UI 播放**：`AmmoHud`（右下角，WorldUIManager 持有，画布自绘）——弹药图标 + `×N` 实时数字；归零红色脉冲（复刻 PlayerHud 低血量闪烁手法）。
- `ItemManager.useItem` 放行类型：`consumable / ammo / equip`。
- 特点：纯数值 + HUD，**不建实体**——"按子弹处理"即是最小播放路径。

### 二、友军类（召唤实体播放）✅
- **播放 = 召唤实体跟随主角**。空中型已有：`DroneEntity`（VAT 双翼 + follow→approach→attack→return AI + 血条 + 损毁→残骸槽位回收）。
- **地面跟随型（未来）**：`GroundAllyEntity`——follow offset 贴 `surfaceHeightAt`，简化 AI（近战/远程）。
- **统一注册表** `allyPlaybackRegistry`（`src/systems/itemPlayback/AllyPlayback.ts`）：`itemId → { kind, spawn(ctx) }`，新增友军 = 加一个注册项。
- WorldMode 进战场按 `deployedAllies` 遍历注册表生成（原 `WorldMode.ts:406` drone 特判 → 已改成查注册表分发）。
- 数据流不变：`deployedAllies` 槽位 ↔ `replaceAlly` 残骸 ↔ 舰船维修配方（已闭环，保持）。

### 三、防具武器类（主角纹理叠加 = 装备贴片）✅
- 主角渲染 = FTXQuad billboard。`EquipmentLayer`（`src/systems/itemPlayback/EquipmentLayer.ts`）：
  - 每个装备位（`weapon` / `armor` / `headgear`）= **一张二级 FTXQuad 贴片**，作为子节点挂主角 mesh（`Player.rendererMesh` 宿主）。
  - 每贴片独立素材（.ftx3），帧映射按 bbox 对齐；锚点 offsetX/offsetY / scale 按装备位配置定义；帧时间独立 loop。
  - ★ 子贴片**继承父贴片 billboard 朝向与翻转镜像**（父 scale 取反 → 子局部位置/缩放自动同步镜像），无需自转/手动翻转——不侵入播放器管线。
- **穿戴数据**：`session.player.equips: { weapon?; armor?; headgear? }`（旧存档自动迁移默认空）。
- 穿戴动作 = 背包"使用"装备道具 → `effect 'equip'`（`ItemEffect.ts`）：穿戴到 `combat.slot` 对应装备位，旧装备退回玩家背包。
- 装配：`CombatItemController.syncEquips()` 进入战场时 + 每 0.5s 定期 diff 同步（战斗中使用装备道具 → 贴片及时刷新；无装备图素材时自动跳过）。

### 四、CombatItemController（模块装配）✅
```
WorldMode → CombatItemController（src/systems/itemPlayback/）
  ├─ ammo : AmmoStore（读写 session.player.ammo）→ firePlayerBullet 前置 tryFire + AmmoHud（WorldUIManager）
  ├─ ally : AllyPlaybackRegistry → 进战场按 deployedAllies 分发；drone_summon / ally_deploy·undeploy 事件保持
  └─ equip: EquipmentLayer（挂主角）+ session.player.equips 读写 + effect 'equip'
```
- 方向：表现模块单向调用 ItemManager（计数/落账），不反向；与背包架构约定一致。

### 五、边界与判定
- 弹药/友军影响数值与实体，需 WorldMode 钩子（开火、部署）；装备贴片当前**纯表现**（数值加成留待挂 ItemArchetype）。
- 新增一类 = 加 `combat.kind` 配置 + 对应播放器（HUD / 实体 / 贴片）注册，核心装配不动。
- 弹药池现为单类型统一池（`default`），后续多弹药类型扩为不同 ammoType key（AmmoStore 已按 key 分池）。

### 沿革
- 2026-09-10 设计稿：三类播放（ammo 数值 / ally 实体 / equip 贴片）+ CombatItemController 装配。
- 2026-09-10 实现：AmmoStore / AmmoHud / AllyPlayback / EquipmentLayer / CombatItemController 五件套全落地，compat 迁移（ammo/equips 默认为空），弹药包加工台配方，装备 effect 穿戴闭环，WorldMode 接线（tsc 通过）。
