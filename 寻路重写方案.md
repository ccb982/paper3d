# 寻路重写方案（长寻路 + 短寻路；待裁决 → 逐条实施）

> 状态：**草案（用户定 2026-09-25：先落文档；批准后按 S1→S4 逐步实施）**
> 关联：`RTS架构.md` §4/§6（分层与寻路）、§0.2（禁私自补丁）。本文件只管"寻路怎么实现、怎么用"。
> 用户口径（原文）：
> - 长寻路：**"以确保真的能通行优先"**（正确通过 > 最优/最快）。
> - 短寻路：**"多往安全的地方走（用地形语义表），保证可行性和安全性；不要求非常安全，只要求能正常通过并且别导致死循环"**。
> - 短寻路（补充）：**"先找安全路线，再走可行性校验，但是路千万别走偏"**。
> - 短寻路（更正）：**"走偏是目标点不许走偏，路径无需最短"**——允许绕远的安全路，**终点必须精确**。
> - 上坡（补充）：**"寻路也要显式标明要上坡，我必须把上坡程序化"**——每段必须显式标 `climb`，标了就走程序化爬坡，不靠坡面兜底。
> - 长短分家（补充）：**"长寻路要不还是和短寻路分开，长寻路只用可行性表，标明哪里需要爬坡，必须确保路径可达"**。
> - 节奏：先落文档 → 逐条裁决 → 每步验证。

---

## 1. 现状（两套主链 + 五级兜底）

| 层 | 模块 | 作用 |
|---|---|---|
| 静态图 | `nav/PassTable.ts` | 4m 格：高度 + 四向有向边（可走/净落差/爬坡位/水/致命坑）；**只读底图，保留** |
| 长寻路 | `nav/LongPath.ts`（FeasibilityPath） | 恒权有向 A*；输出**稀疏走廊 ≤8 点**；`walkableLine`（2m LOS） |
| 长寻路 | `nav/Corridor.ts`（SquadPathFinder） | 加权 A*（掩体/兵种亲和折扣） |
| 长寻路 | `HpaPath.ts` | HPA*（簇 + 门户，跨簇长距） |
| 短寻路 | `SquadNavigator.greedyStep` | **10m LOS 贪心** + 惯性（hopDir）+ 4s 脱困（esc） |
| 使用 | `SquadNavigator.ensurePath` | >40m 走长寻路、≤40m 走贪心；失败链：feas→HPA→有界A*→coarse→**直线** |
| 使用 | `Anchor.currentTargetOf` | 走廊前瞻 8m / 末程回落 <15m / 锚点滞回 <6m |
| 使用 | `steerEntities`（10Hz） | 阵型槽位 → moveTarget → applySteer |
| 使用 | `Follow/Formation` + `AtomExecutor` | 成员跟队长；原子 forward/back/strafeL/strafeR/hold |
| 语义表 | `TerrainSemantics.ts` / `TerrainScore.ts` | L1 地形语义 / 有利位评分（**目前未用于短寻路代价**） |
| 爬坡链（现状） | `PassTable.climbAt` → `FeasibilityPath.find` 路点 `climb` → `Anchor.anchorClimb` → `steerEntities` → `moveTarget.climb` → `CharacterBase` 爬坡状态机 | **L3 通；L2 池 `climbOrdered:false`（无标注兜底）= 缺口**；短跳 `greedyStep` 无 climb 标注 |

---

## 2. 实测问题（证据，1× seed 4242）

1. **走廊太粗 → 末段不可执行**：#1 停在件外 **12m 共 45s+**，`los=false、bfs=true、climb=false、atom=forward、blk=false、走廊=3点`——长寻路有路，但执行层最后一段只会"朝目标直走"，撞挡步边原地推 → 25s 被收回（回收 churn 主源）。
2. **两处语义混淆**：`walkableLine`（2m LOS + 斜向禁上坡）既当可达门（已修为 `reachFrom`）又当走廊拉直校验（未修）。
3. **多套并行 + 兜底链**：同目标不同队走法不同（`hpa/astar/coarse/fail` 计数可见）；"绝不停摆→直线"会发不可行直令。
4. **贪心短跳视野 10m**：绕不过更大障碍；振荡靠 4s 计时补丁兜底（不是完备搜索，无终止性证明）。
5. **重算触发靠"位移 12m/6m + 12s TTL"**：走廊与执行脱节 → 目标漂移 → `cmdChanges` churn。

---

## 3. 设计原则（本方案铁律）

1. **通行优先**：长寻路产物必须**逐段可执行**（与执行层同一套判步规则）；**没有可行路就报 blocked，绝不回落直线**。
2. **一个语义、一个实现**：段校验（`canSegment`）唯一；可达查询唯一（`reachFrom`）；方向→原子规则唯一。
3. **有限终止、无死循环**：短寻路是**有限窗口内的完备搜索**；无解 = 返回 null（上层停/改令），禁止"原地重试+惯性补丁"式死循环。
4. **安全只是偏好；路径无需最短，但目标点不许走偏**：短寻路 = ①**先按地形语义找安全路线** → ②**再过可行性校验**（`canSegment`/`canStep` 同执行层规则）；
   允许绕远（不做最短路径要求，**不设直达线走廊/偏离惩罚**）；**终点必须精确到目标点**（容差 `ARRIVE_R`），
   不得以"附近安全点"冒充终点；无解 → null（停/换令），不近似、不停半路（用户定 2026-09-25）。
5. **事件驱动重规划**：不再用 TTL/位移轮询抖动；只在 4 个事件重算。
6. **上坡显式化（用户定 2026-09-25）**：长寻路/短跳的**每一段必须显式标 `climb`**（来源 `PassTable.climbAt`）；
   标了 `climb` 的段，执行层**必须走程序化爬坡**（现有 `CharacterBase` 爬坡状态机）；未标注段**不得**触发爬坡；
   不允许"靠坡面自动兜底"当作没标。**所有层（L2 池 / L3 实体）都要拿到标注**。
7. **只做架构调整**：不叠加特判；旧模块验证绿后才删。

---

## 4. 新架构

### 4.1 L0 静态图（保留）
`PassTable` 不动：`canStep/climbAt/dropAt/heightAt/waterAt` + `FeasibilityPath` 作为 **可达性**（`find`）底座。

### 4.2 L1 长寻路——路径服务（Route Service，通行优先；**与短寻路完全分开**）
- **只读可行性表**（`PassTable` 有向边；用户定 2026-09-25）：算法 = BFS/A*（4m，斜向禁上坡；代价 = 1/1.414 + 上坡加价 + 水加价）。
  **不引入 HPA*/加权有向 A*/coarse 软参考**（全部退出主链；性能优化另行立项）；短寻路（LocalStep）与长寻路各自独立模块、各自契约。
- **可执行化（关键）**：A* 路径先按网格保留；随后做"**增密拉直**"：
  - 用 **`canSegment`（执行层同规则：2m 采样 `canStep` + 斜向禁上坡 + 爬坡位标记）** 校验直连段；
  - 通过 → 合并；不通过 → 保留网格拐点；最终**每段 ≤ `SEG_MAX`（暂定 10m）**、逐段可执行；
  - 每点带 `climb` 标记（沿用 PassTable.climbAt），供执行层走"程序化爬坡"。
- **爬坡显式（硬要求）**：每个路点带 `climb`（含义="进入该点所经段需程序化爬坡"，来源 `PassTable.climbAt` 逐段判定）；
  未显式标注的段不许触发爬坡；标注缺失视为不可执行（宁可不发）。
- **保底**：若 A* 返回 blocked → `route() = 'blocked'`（上层：不发令/换点/等 TTL；**不发直线**）。
- **增量**：缓存 `(startCell, goalCell, tableStamp)`；重算时优先复用旧路线**后缀**（若旧路线当前段仍有效）。
- **预算**：每队每 N 拍最多 1 次全量查询；A* 扩展上限（超出即 blocked/partial，记录探针）。

### 4.3 L1 短寻路——局部绕障（LocalStep，两阶段：安全引导 → 可行性校验）
- **阶段①（安全引导）**：以当前点为中心、半径 `LOCAL_R`（暂定 24m）窗口内跑 **Dijkstra**（同一张表，8 向，有向边；斜向禁上坡），代价：
  `cost = 步长(1/1.414) + up坡×kUp + 语义风险 risk(x,z)×kRisk`。
  - `risk` 来源 = **地形语义表**（`TerrainSemantics` 类别 + `TerrainScore` 有利位）：坑/水/陡崖边/致命格 = **硬禁或极高价**；开阔/掩体后/高地 = 低价（偏好）。
  - **路径无需最短（用户定 2026-09-25）**：允许为安全绕远；**不设直达线走廊、不设偏离惩罚**（不为省距离牺牲安全）。
- **阶段②（可行性校验）**：①的解逐段过 **`canSegment`**（与执行层同规则）；不过 → 取下一个**可达且精确到目标**的解（代价次优）；仍无 → **null**。
- **输出 / 目标点不许走偏（硬门）**：路线**终点必须精确 = 目标点**（容差 `ARRIVE_R≈1.5m`）；搜索只接受"真正到达目标点"的解，
  **不得以附近安全点冒充终点、不得半路停下**；若窗口内无"安全且可达且终点精确"的解 → **null**（上层停/换令/请求长寻路）。
- **终止性**：窗口有限 + Dijkstra → **必终止**；无解即 null（**不得原地打转/反复重试**）。
- **启用条件**：目标在 `LONG_PATH_DIST`（40m）内，且"直线（`canSegment`）不可走"时。

### 4.4 使用契约（引擎 / 队长 / 移动 三层；S3 正题）

#### 4.4.0 使用面审计（2026-09-25；**结论：路已修好，用法是最后的根因**）
| # | 位置 | 现状用法 | 问题 |
|---|---|---|---|
| U1 | `SquadCore.drive` | 每拍：`ensurePath` → `leaderTarget`(=resolveAnchor/currentTargetOf) → `interpretLeader` → 环夹 → 分解派件 | 每拍重取站位锚+重算路线触发条件过松（12m/6m+TTL）→ **锚点/目标翻转**（实测 `170,22↔174,22` 口袋振荡） |
| U2 | `Anchor` | `routeNext` 返回"下一个未到达路点"（S3a 已修不跳点） | 无**目标锁存**：位置/路线每拍变化 → 目标在两点间来回；到位半径与 advance 阈值（2m）耦合 |
| U3 | `SwarmSystem.move`（L2 队长） | `leaderDir(directiveTarget-pos, orderTarget-pos)` → `pickSteer` → `CharacterCore.step` | **没有"按表边逐格择向"**：期望方向撞到被禁边时只能 hold/strafe → 原地推（`stepOk=false`） |
| U4 | `SquadNavigator.steerEntities`（L3） | `currentTargetOf` → 编队槽位 → `applySteer` | 与 L2 两套消费路径，语义漂移；成员锚点/槽位在目标翻转时抖动 |
| U5 | `Follow`（掉队成员） | `corridorAhead(look=4)` | 与队长的 `routeNext` 不是同一语义（前瞻 vs 下一路点） |
| U6 | climb 下发 | L3：`moveTarget.climb`/`applySteer.climb`；L2：**无字段**（`stepAgent` 固定 `climbOrdered:false`） | 坡上坡对 L2 不是"程序化爬坡"；与"上坡显式化"冲突 |
| U7 | 路径源 | `state.corridor ?? state.order.path` | 两个路径源并存（走廊/旧 order.path），归属不清 |
| U8 | 引擎侧 | `OrderValidator ③` 用 `reachFrom`（BFS）；工兵取件门用 `reachFrom` | 用法 OK；但每令一次 BFS + 队长每拍重规划 → 冗余（可随锁存/事件制自然降低） |

#### 4.4.1 执行侧最小契约（用户定 2026-09-25；**仅此而已**）
- **用命令 = 按距离选寻路**：>40m 长=可行性表路线（S2：加密 ≤10m + 逐段 climb）/ ≤40m 短=LocalStep（S1）；
- 拿到路线就**沿路点走**（直线朝锁存路点；S2 已保证每段可执行）→ **到达即止**；
- 失败：冷却 3s 重试；**不发不可保证的路**（无直线/兜底链）；
- **不打断保障（S3b，已实装）**：目标锁存 + 重规划仅 4 事件（目标变 / 净推进停滞 3s / 表代次变 / 到达）；
- **明确不做**（已回滚）：逐格择向（S3c 无条件版）/ climb 强制下发（S3d）/ 原子规则单源
  ——实测回归：回收 18→56、工兵全灭、`built=0`。
- **上坡重写（基类，2026-09-25 实装）**：`CharacterCore` 上坡改为**上坡半径 + 正对坡面**——
  `probe.uphillNormal(x,z,CLIMB_FACE_R=5)` 取半径内最近坡面的法线；想上坡**必须先对准法线**，
  `dot ≥ CLIMB_FACE_DOT=0.8` 才进入程序化爬坡（定速沿法线）；未正对只转向坡面；显式爬坡令（L3）同样按法线。
  审计结论：**L2 的显式 climb 标注此前是死的**（`climbOrdered:false` 固定），`fallLineBlend` 在方案 A 格边模式下被跳过
  → 旧"上坡修正"实际只剩坡度自动爬坡（阈值 0.25 过松，可斜蹭）。旧混合已从 L2 撤除（`EnemyLocomotion` 另路保留）。
- **强跟随修正（试过，已回滚）**：净推进口径误判慢速行进 → 回收暴涨；真冻结口径收益有限。
  结论：**执行器修正治不了根**——根因是"移动机制"与"寻路机制"**数据模型不同步**（见 §4.4.3）。

#### 4.4.3 移动 vs 寻路：两套机制为何不同步（2026-09-25 定位，**本轮核心结论**）
**移动机制（实际怎么走）**
- L2：`SwarmSystem.move` → 方向（`leaderDir`/`followDir`/原子）→ `pickSteer`（16 向 + 分离）→ `AgentPool.stepAgent` → `CharacterCore.step`；
- L3：`steerEntities` → `applySteer` → `CharacterBase` → **同一 `CharacterCore.step`**；
- 本质：**连续向量运动**（任意位置/方向）；阻挡 = 高度差 + `isWeldEdge` 的逐分量清零；爬坡 = 梯度/标注。

**寻路机制（路怎么算）**
- `PassTable`：4m 格 + **有向格边**（E/W/S/N 的 can/drop/climb/weld/lethal）；
- `FeasibilityPath.find` / `LocalStep`：**格图上的 A*/Dijkstra**；`canSegment` = 2m 采样 + `canStep`（格边）+ 斜向禁上坡；
- 本质：**离散格边图运动**（格心↔格心、四/八向、有向约束）。

**不同步的三个结构点（实测证据）**
1. **状态空间不同**：路点是**格心**，单位在**任意精确位置**；"精确位置→格心节点"的直线常常是斜向，需要一条被禁的正交边
   （实测：`freshStatus='ok'` 但 `freshSeg=BLOCK@153.1,68.2`——find 校验的是格心向西，执行第一步要求向南）。
2. **规则集合不同**：寻路判定在**格边**上（`canStep`），移动判定在**高度/坡面**上（`CharacterCore.isWall` + B3 表判墙）；
   同一步可能"表说通、移动说堵"（或反之）；两者从未共用同一个"这一步能不能走"的函数。
3. **执行器不消费格边**：执行器只拿"方向/节点坐标"，从不沿规划器的**格边序列**走；规划器的可达性保证（格心图）因此无法传递到执行。

**结论**：不是"某个修正没加"，而是**移动与寻路是两套数据模型** → **已选方案 A 并实装（2026-09-25）**：
- **方案 A（实装）**：`nav/EdgeFollow.ts`——**移动消费格边图**：路线 → 去重格序列 → 执行步 = 轴对齐单步（`canStep` 校验；
  斜向格分解为先一轴、下一拍再另一轴）；格边模式下**跳过 16 向软转向/坡混合**（避免把格边步掰成斜向/被禁分量），
  分离外推仍保留；L2/L3、队长/成员同款（成员=贪心格边步跟队长）。自检 [5h]。
- **方案 B（未选）**：寻路消费移动内核（闭环模拟），成本高。

#### 4.4.4 硬边"卡上去" + "找不到坡面"（2026-09-25 用户报告；待实施/待定位）

**现象**：飞船在高原（无坡面）；**90% 的敌人靠硬墙"挤/卡"上高原**；寻路不去走坡面。

**A. 位移绕过（已定位，待实施）——"卡上去"的通道**
| # | 位置 | 问题 |
|---|---|---|
| A1 | `SwarmSystem.move`（L2） | 人群分离外推 `p.x+=_sep` **不经地形校验** |
| A2 | `CharacterBase.separateFromOthers/separateFromStatics`（L3） | 互推/障碍推出**只做水平位移**，无台阶校验 |
| A3 | `systems/world/CharacterClamp`（贴地） | 推力把 x/z 推过硬边后，**y 直接被贴到新格高度**（借推力越墙） |
| A4 | `separateFromStatics` 翻越候选（`CLIMB_MAX=3.2m`）+ 上岸 `SHORE_CLIMB_MAX=2.5m` | 显式许可，但若边上放了掩体/贴水，就是"合法阶梯" |

**规则（方案 A 延伸）**：**所有水平位移**（自身走 / 分离 / 推出）统一过同一 `canShift(from,to)`（`heightAt` + 台阶阈值）；
**贴地前置校验**（y 抬升前过 `canShift`，不合格回退水平位移）；翻越/上岸保留为显式许可，且受"正对+半径+上限"约束。

**B. "只挤墙不走坡"（待定位；用户怀疑寻路）**
- 现成本模型（`FeasibilityPath.find` / `LocalStep`）：步长 1/1.414 + 上坡 `0.6/m`；**weld 坡与小台阶硬边（cliff 且 |rise|≤0.6）同价**——**无"坡优先"**；
- 阶梯状 ≤0.6m 硬边在表里**双向可走** → A* 会选**最短的"爬墙"路线**，而不是绕远走坡（90% 现象与此吻合）；
- 单一 >0.6m 硬边在表里**只下不上** → A* 不会走；此时"卡上去"只能是 **A 类位移绕过**（或翻越/上岸）；
- 需定点验证：① 目标高原/目的地在表窗内否（表以**落点**为中心，`tableR≤240`；`TerrainSemantics` 半径 144）；② 高原周边的"坡"在 `finalRuling` 里是否真为 `weld`（**渲染坡 vs 裁决一致性**）；③ A* 从敌人位置到高原是否 `ok`（可达）还是 `blocked/outside`。

**倾向修法（移动质量成本，长/短寻路同口径）**：
- **weld 坡**：低价（正常步 1 + 上坡小幅）；
- **台阶硬边**（cliff 且上升 > 小阈值，靠"卡"上的）：**高惩罚**（例如 +3~5/步）；
- **需爬坡的 weld**（`climb` 标注）：中惩罚；
- 目的：**有明显上升时优先绕坡面**，不再选阶梯硬墙；同时把"是否在表窗内"作为取点前置（否则先机动到表内）。

#### 4.4.5 高精度定位 / 寻路（用户定 2026-09-25；待实施）

**问题**：高台下被当成「已经在高台上」——
- 4m 格里 **(x,z) 相同 = 同一个地方**，**不看 y/层**：`cellOf`（EdgeFollow）/`PassTable.cellAt`/到达判定全按平面格；
- 单位在崖底时若 x/z 越过了崖边（推力/挤出/贴边），下一拍就被判「已在上面格」；或 `heightAt` 取的是**格心高度**（崖顶），与单位脚下（崖底）不一致；
- `find` 的 `sk===gk`（同格）会直接判「到达」；`edgeStepRoute` 也会因同格而认为无需移动。

**根因**：地形是「每块 4m 平面 + 块边界崖」，但**单位是连续位置 + 多层高度**；移动层用 `RasterMap.surfaceHeightAtFor(x,z,y)`（**y 感知选层**），而规划/跟随层是**二维格**——两层不同步。

**设计选项（可组合）**
- **H1 局部 1m 高精度**：长距离仍走 4m 可行性图（粗导引）；**队长附近（≤ `FINE_R`，如 24m）用 1m 栅格**做定位/跟随/局部寻路（高度取 `surfaceHeightAtFor(x,z,y)`；1m 已是高度场原始分辨率，块内平但**边界/到达/让位精度**提升到 1m）。
- **H2 y 感知的「层」（cell+layer 判等）**：所有格相关判等/到达/取点都带层：`layer = surfaceHeightAtFor(x,z,y)`；**同 (cx,cz) 不同层 = 不同位置**；
  · 到达判定：`同层 ∧ 水平距离 ≤ ARRIVE ∧ 高度差 ≤ 台阶`；
  · `heightAt` 查询改为 y 感知（或返回层信息），消除「格心高度」误导；
  · 位移（含推挤）跨层/跨崖的校验用同一函数（与 §4.4.4A 的 `canShift` 合并）。
- **H3 队长定位精度**：队长目标/锚点/汇报位置统一到 1m 语义（取点四舍五入到 1m；探针/UI 显示 1m），避免 4m 格心带来的半格偏差叠加。

**实现归属（用户定 2026-09-25）：全部做进实体基类（唯一实现）**
- `entity/base/TerrainProbe` 增加 **layer 查询**（`layerAt(x,z,y) = surfaceHeightAtFor(x,z,y)`）；
- `entity/base/CharacterCore` 拥有 **跨层位移校验 `canShift`**（自身步进 / 推挤 / 贴地共用一处），
  贴地/立面/爬坡现有语义一并收口到它；
- **L2（`AgentPool.stepAgent`）与 L3（`CharacterBase/EnemyBase`）同内核 → 自动同款**；
- 规划/跟随层（`nav/EdgeFollow`、`squad/Anchor`、`SquadCore`）**不另写一套**：只经端口消费
  同一个 `layerOf(x,z,y)` 做「同格判等/到达/取点」（同一语义，单源）。

**建议**：**H2 必做**（这是「被当成在高台上」的直接解），**H1 按需**（只在队长附近启用，成本可控），H3 随 H2 一起收口。
验收：构造「崖底紧贴崖边」用例——单位在崖底时：路线不判到达、不会被判上层、贴地/推挤不抬升；在高台上时：正常到位。

#### 4.4.6 三张表原则 + 废除 TerrainScore（用户定 2026-09-25）

**只有三张表**（各司其职，单源）：
1. **地形语义表**（`TerrainSemantics`）：偏好/评分的**基础**（高地/关口/遮蔽/迎背坡…）；
2. **可行性表**（`PassTable`）：**硬通行**唯一来源（坑 lethal、水、边规则、爬坡标注）；
3. **战壕掩体表**（`HoleMask/HoleTable`）：**动态工事**——挖战壕/造掩体即更新；**参与地形评分**；
   工兵造完掩体 → 掩体表更新 → 评分重评（现有 `buildCover → HoleTable.rebuild → scoreStamp` 链保留）。

**`TerrainScore` 是越权的"第四套网格"**：自建 4m 网格、自采样，把"硬通行（blockedAt）+ 评分（scoreAt/featsAt/weightsFor/bestTrenchNear/pathMulAt）"混在一起
→ 与可行性表/语义表不一致（实测：平地 h=0.09 被判 `blocked=true` → `pickSteer` hold → 站桩卡死；`x=-96` 接缝差异同族）。

**废除计划（两步，逐条裁决）**
- **① 硬通行归一（小、立竿见影）**：`blockedAt` 与 `pickSteer` 的"硬格"判据 → 读 **可行性表**（`PassTable` lethal/边规则）+ `surfaceHeightAtFor`（与寻路同格同源）。
- **② 评分归一（大，单独立项）**：`scoreAt/featsAt/weightsFor/bestTrenchNear/rangedPost/fortifyNeed/pathMulAt`
  → 重表达为 **语义表 + 战壕掩体表** 的纯函数（不再自建网格/采样）；掩体变更走掩体表事件重评。

#### 4.4.7 统一形式：一切状态 = 目标点 + 同一条寻路链（用户定 2026-09-25）
> 用户口径：「寻路、保护等等状态都是**给目标点，然后往这里短寻路走**的形式，就和工兵查询要造掩体的位置一样。」

- **唯一移动形式**：任何行为（保护/驻守/撤退/远程选位/风筝/掉队/巡逻）= **先产出一个目标点** →
  交统一链（短/长寻路 + 可行性校验：`localStep`/`feas.find`/`canSegment`/`edgeGreedy`）→ 沿路线/格边走。
- **禁止旁路独立实现**：不得为某状态另写"直推/绕掩体/自定义站位"等独立移动代码。
- **已归一**：保护（`interpretLeader` 的 blockCheck 调整点 → 写入令目标 → `ensurePath`）；驻守/巡逻（目标点直走）；
  撤退（引擎给点 → `OrderValidator ③`）；工兵（`targetOf` 查询可行点——**模板**）。
- **本轮归一**：远程选位/风筝（改为"给目标点 + `edgeGreedy` 同链"；不可达/在位 → 站住打）；
  删除锚点层（`resolveAnchor`/锁存/掩体绕行现已不存在）。
- 审计口径：全仓搜"直接 `applySteer` 且目标非路线/非标准链"的分支 = 违规。

**收敛记录（2026-09-25）**：
- ✅ C2：删 `SwarmSystem.move` 的**原子直推分支**（无队长/无指令 → 停；移动只走统一链）；
- ✅ C4：删 `squad/MarchAction.ts` 与 `SquadNav` 端口（长短判据只在 `ensurePath` 一处）；
- ✅ C5：远程选位/风筝 → 目标点 + `edgeGreedy` 同链；
- ○ C1：L3 `EnemyBase` 本地 AI 栈——**受控单位已不自行选路**（`swarmControlled && !applyingSteer → return`）；
  非受控分支（`EnemyBrain/moveBy/EnemyLocomotion`）待收（保留火力/表现，只收移动选路）；
- ○ C3：`Follow.ts` 走同一走廊（近=直线/edge，远=走廊前瞻）——已属统一链，滞回参数后置调优。
- **巡逻**（用户口径）：查询**可行的移动目标点** → 短寻路来回走；实现随 C1/C3 一起落。

#### 4.4.2 下命令方案（发令侧；**S3 的真正主体**，待设计 / 验收口径）
1. **蜂群引擎命令一定要少发**——只在三类时刻介入，其余时间**不下令**（不是 2Hz 轮询）：
   - ① **事态函数变更 且 小队不在事态范围**（环/带外）→ **长寻路**令（回到范围 / 重新部署）；
   - ② **小队危机**（整队血比 < 0.5）→ **回撤**令；
   - ③ **小队扎堆**（同兵种目标 < 40m）→ **引导散开**（切向）。
2. **长寻路 = 远距/跨区移动（引擎令或队长派件都可用）**——例如**工兵被派到防区**时，队长也走长寻路；
   **短寻路 = 近距/自决默认**（队长可执行长寻路，不是引擎专属）。
3. **引擎令与队长令使用相同的执行方式**（同一条 order→路线→移动链、同一原子规则）；
   **非必要不打断**（路点锁存 + 重规划仅 4 事件）。
4. **小队执行完引擎令 → 自主决策**（一般为**短寻路**）；要求：**在无序中向舰船前进**（局部自组织，不要求步调一致）。
5. **行动中扎堆 → 由蜂群引擎负责拉开**（与 ③ 同口径）。
6. 验收（探针）：`cmdChanges` 显著下降（目标 ≤5/min）；引擎令（引擎 vs 自决）与长/短寻路占比合理；`成员空转 <5%`；到达率/末段停滞。

7. **设计清单（8 维；逐维裁决）**：① 引擎触发（三时刻）② 命令生命周期（issued/executing/done/dropped，"令尽"语义）
   ③ 不打断规则 ④ 令尽自决（短寻路向舰）⑤ 危机/扎堆（阈值+迟滞+解除）⑥ 长短归属（**按距离**；远距皆长）
   ⑦ 玩家令（最高优先/TTL 交回）⑧ 验收指标（每维可探针量化）。

**实现含义（逐条裁决后做）**：
- **引擎事件制**：DecisionChain 从"每拍"改为"事件触发"（三时刻）；其余保持现令（同签名去重已在）。
- **令尽自决**：令尽后队长自主（短寻路、在无序中向舰推进）——需立项（当前令尽后引擎下一拍再决策 = "频繁下令"来源）。
- **危机/扎堆**：阈值 + 迟滞 + 解除条件，落成事件而非每拍重发。
- **执行侧不做**：逐格择向 / climb 强制 / 原子单源（S3c/S3d 已回滚）。

---

## 5. 迁移步骤（每步单独裁决 + 四关验证；绿后才删旧）

| 步 | 内容 | 验证口径（探针） |
|---|---|---|
| **S1 ✅** | 短寻路替换：`localStep` 替 `greedyStep`（+hopDir/esc 已删）；自检 [5e] | 已交付：`LocalStep.ts` + 接线 + 契约测试 |
| **S2 ✅（算法层）** | 长寻路=可行性表：`FeasibilityPath.find` 输出**加密 ≤10m + 逐段 climb**；`ensurePath` 删 HPA/加权A*/coarse/直线兜底（表外/无解→不发） | 实测：路点 7~15、最长段 ≤9.9m、爬坡点 0~2、`feasOk=16/fail=0`（无兜底触发） |
| **S3（进行中）** | S3a ✅ 锚点路由推进；S3b ✅ 目标锁存 + 4 事件重规划；S3c/S3d ❌ 回滚（逐格择向/climb 强制导致回收暴涨）；**下命令方案（8 维）待设计** | 探针：空转 0.0%、`cmdChanges 7`、`built 1~4`；`#1` 末段仍待按 bug 查 |
| **S4** | 清理：删 `HpaPath`/`SquadPathFinder`/`coarseCheck`/`greedyStep`/hopDir/esc；更新 `RTS架构.md` §4/§6/§11 与探针 | `npm run check/test/smoke/probe` 全绿 + 指标对比 |

**每步固定流程**：汇报（现象/证据/影响面）→ 裁决 → 实施 → 四关验证 + 量化对比。

---

## 6. 删除清单（S4 执行）
- `SquadNavigator.greedyStep` 及其 `hopDir`/`esc` 状态与"4s 脱困"补丁；
- `coarseCheck` 与 `order.coarse` 软参考（含 `Corridor` 的 coarse 分支）；
- `HpaPath`、`SquadPathFinder`（加权 A*）、`coarseCheck`/`order.coarse` 软参考：**S2 后全删**（长寻路只用可行性表；用户定 2026-09-25）；
- `FeasibilityPath.walkableLine` 的"走廊拉直"职责（保留为可达查询内部工具即可）。

## 7. 参数（集中调参口，暂定）
| 参数 | 值 | 位置 |
|---|---|---|
| 长短寻路分界 | 40m | `NAV.LONG_PATH_DIST`（沿用） |
| 路线段最大长度 | 10m | `SEG_MAX`（新，Route 服务） |
| 前沿可视点 lookahead | 8m | `LOOKAHEAD`（新，执行层） |
| 短寻路窗口半径 | 24m | `LOCAL_R`（新） |
| 短寻路终点精度（不许走偏） | ≤ ARRIVE_R（≈1.5m） | 硬门：终点必须=目标点，不许近似 |
| 上坡/水/风险 权重 | 待标定 | 短寻路代价（`kUp/kWater/kRisk`） |
| 停滞后重规划 | 3s | `STALL_S`（新，替换 TTL 轮询） |

## 8.5 坡/硬边的真相源与消费端（2026-09-25 定调）

> **用户口径**："坡是我地形生成算法决定的，有一个表那是真相源，我们做的是给敌人消费的。"
> 表管线（同《RTS架构.md》§6.0）：**真相源** = Tiles/ChunkGenerator/`Refinements.finalRuling`；
> **消费层** = `PassTable`（可行性收敛）→ 寻路/移动；`TerrainSemantics`（战术偏好，与可行性正交）。
> 即：`weld/cliff`（坡/硬边）由**地形生成表**裁决 —— **我们不动地形裁决**；
> 敌人侧（`PassTable` → 寻路 → 移动）只做**忠实消费**，并按战场语义小心处理。

### 真相源（不动）
- 裁决唯一出口 `Refinements.finalRuling`（`BlockSource.edgeFinal` / `edgeRuling`；含 `smoothDirs`、水全向、30% 大落差产坡、围裙保护）。
- `PassTable` 已经**只经 `finalRuling` 读取**（不自行改判）——这一层不需要"修"，需要的是**消费得忠实、标注得足够**。

### 消费端待办（B1~B4；仅记录，逐条裁决后实施）
| # | 位置 | 待办 | 目的 |
|---|---|---|---|
| B1 | `PassTable` | 每条边**显式标注坡(weld)/硬边(cliff)**（E/S 计算、W/N 镜像，两侧对应边对称；新增 `weldAt(x,z,dx,dz)`） | 寻路/移动不再靠坡度/落差**猜**；"坡这个边特殊标注，标注两个地块的对应边" |
| B2 | `PassTable` | **坑（地块类型 `pit`）一律墙**（weld 也当墙）：**这是地块类型，不是战壕**（战壕=HoleMask 动态破坏）；现状仅致死坑 `pit && h<−1.2` 双向禁，非致死坑待实施 | "深坑全是墙" |
| B3 | `entity/base/CharacterCore` | 立面判墙改为**按表边裁决**（weld=坡允许连续上升；cliff 仅 ≤台阶豁免可走、大落差只下不上）；删除"连续上升即非墙"的隐式坡通道 | 现在大陡面会被当坡往上推（违反"落差大的硬边只能下不能上"） |
| B4 | `nav/LocalStep` / 长寻路 | 消费坡标记：坡上坡 → `climb` 显式（程序化爬坡）；长/短寻路一致；必要时对陡坡加代价/避让 | "寻路的时候要非常注意" |

### 语义共识（现状核对）
- **weld = 坡**：双向可走；**上坡需程序化爬坡**（`climb` 标注）。
- **小落差硬边**（≤ `EDGE_CLIFF_BAND=0.6`）：可无视（可走）——现状正确。
- **大落差硬边**：**只能下、不能上**——`PassTable` 现状正确；不得在消费端被"坡度/延续"启发式绕开（B3）。
- **深坑**：weld 也当墙（B2）。

## 8. 开放问题（实施前需定）
1. `canSegment` 的采样步长：沿用 2m 还是 4m（与表格一致）？斜向禁上坡是否保留（表只 4 向边）？
2. 短寻路窗口边界如何选（不因窗口截断把"可到达的精确终点"误判为 null）？大绕障碍时是扩大 `LOCAL_R`，还是直接交长寻路？
3. 地形语义风险的具体权重（首版建议：硬禁坑/水/致命；其余 0.2~0.5 小权重，宁可走开也不绕远）。
4. 增量复用策略：后缀复用是否必须（首版可先全量重算，事件驱动已足够少）。
5. L2 池的 `climb` 进什么字段（`UnitDirective.climb`？）与 `CharacterBase.climbOrdered` 的对接口径（S3 前定）。
