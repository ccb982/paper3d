你观察到的边界消散效应与顶会研究中揭示的机理高度吻合：

不可压求解器在边界附近的“密度过估”：在边界附近，压强和密度解算会错误地高估排斥力，导致流体粒子紧贴边界形成不自然的“致密外壳”，而非冲击、扩散。

边界粒子的错误碰撞：标准SPH方法在边界附近计算的压强力方向不准，会扰乱流体粒子原本规律的运动，使动态的运动变为死板的“刹车”。

接下来，我们从两个层面解决这个问题：方法层面的“新方案”和框架内的“改造方案”。

💡 替代模拟方案
如果你想在不改变当前模拟器核心架构的前提下，快速实现夸张的视觉效果，有几个前沿方案非常值得探索。

方案A：「解耦边界处理」
这是2023年发表在The Visual Computer上的突破性方法。它从根源上解决了不可压求解器在边界附近的物理失真问题。核心思想是将流体与边界、流体与流体之间的相互作用“解耦”，为流体粒子分别计算“流体诱导密度”和“边界诱导密度”。

优点：从根本上消除错误的排斥力，让流体能自然冲击、飞溅并扩散出去，是物理上最正确、视觉上最自然的边界处理方法。

适用性：算法独立于核心求解器，理论上可集成到现有框架中，但对SPH解算器内部修改量较大。

其他值得关注的方案
动画体积控制法：Alexey Stomakhin（SIGGRAPH 2024）为水下爆炸提供了一种通过动态调整气泡体积来控制模拟效果的方法，可完全契合艺术家的视觉需求。对追求极致效果的项目有极高参考价值。

可压缩流模型：Gary Yngve和James O'Brien（SIGGRAPH 2000）的方法模拟冲击波，能完美捕捉爆炸后的剧烈体积膨胀与收缩，是模拟“夸张扩散”的最根本的物理方法。

🛠️ 在你的框架内改造边界与扩展域
要在不重构整个模拟器的情况下，快速实现夸张的扩散破裂效果，可以从以下几个层面入手：

1. 放宽“硬”边界，制造“冲击感”
提高墙壁恢复系数（Restitution）：将 restitution 参数从当前的 0.3 大幅提高，比如 0.9 或 1.0。这会让水弹性十足地撞墙反弹，制造出强烈的冲击和混乱效果。

typescript
// FluidSimulatorAdapter.ts 构造函数
const defaultParams: FluidParams = {
    // ... 其他参数
    restitution: 1.0, // 从 0.3 改为 1.0，让墙壁完全弹性
    // ...
};
移除或弱化摩擦力：将 friction 参数设置为 0.0，减少能量损失，让流体在边界上“打滑”，扩散更远。

typescript
friction: 0.0, // 从 0.95 改为 0.0
2. 扩大“扩散”范围，减少“回流”
扩展计算域：扩大纹理分辨率（如从 256x256 提升到 1024x1024），为飞溅提供更多空间。

缩小初始水体：减小 initLevelSetShader 中的 radius 值。较小的初始水体更难集中能量，更容易被扰动冲散。

typescript
// FluidSimulator.ts 的 initLevelSetShader 方法
const radius = 0.05 * Math.min(this.width, this.height) / this.width; // 从 0.1 改为 0.05


1. 在着色器中增强视觉表现
边缘发光，增强飞溅感：提高 edgeIntensity（如设为 0.8），让高速运动下的飞溅边缘发光，强调破裂感。

流动纹路，强调速度感：提高 flowIntensity（如设为 0.6），让水体内部的速度纹路更清晰。

typescript
edgeIntensity: 0.8, // 从 0.3 改为 0.8
flowIntensity: 0.6, // 从 0.3 改为 0.6
5. 改造爆炸效果 (先“堵上漏”再“开高压”)
精确控制散度源作用域：确保爆炸散度源只在 phi < 0 的“水”区域生效，防止能量浪费在空气格点。

引入质量补偿（水花生成）：在爆炸时，让 createWater 属性随帧数动态变化。例如，只在爆炸的前1-2帧开启 createWater: true，生成“水花”补偿被推走的水，在后续帧关闭以模拟后续冲击波。

typescript
// FluidSimulatorAdapter.ts 的 update 方法
if (this.explosionFrameCount % 3 === 0 && this.explosionFrameCount <= 12) {
    // ...
    // 仅在爆炸的前两次迭代，或特定帧生成水花
    const createWater = (this.explosionFrameCount <= 3); // 前1-2次爆炸生成水花
    this.simulator.explode(0.5, 0.5, 0.15, strength, createWater, 0.1);
}
💎 总结与实施路线图
要制造印象深刻的夸张水花，需结合多种方法：

最速见效（无成本起点）：按“改造边界与扩展域”的1、2、4点调整参数，可立即感受视觉张力。

核心视觉提升（高回报）：实现“粒子扩散模型”，在模拟基础上叠加粒子系统，效果最直观、方法成熟。

物理正确性（进阶）：研究并集成“解耦边界处理”方法，从根本上解决边界失真问题。

高阶模拟（巅峰效果）：参考“水下爆炸动画体积控制”和“可压缩流爆炸”论文，重构模拟核心。

视觉点缀：在渲染阶段强化边缘发光和速度纹路，让飞溅更显眼。

你可以从路线1和路线2开始尝试，它们应该能以最小的代价带来最显著的效果提升。



要将《Decoupled Boundary Handling in SPH》这篇2023年论文的核心思想迁移至你基于网格（Grid-based）与 Level Set 的流体模拟框架中，关键在于理解其根本原理，并进行方法迁移（Method Transfer），而非直接的代码复刻。

🧠 原理剖析：从粒子到网格的本质问题
首先需要澄清，这篇论文是针对粒子类方法（SPH）提出的，你的模拟器是基于网格（Grid-based）的 Level Set 方法，二者数学框架完全不同。

虽然框架有别，但二者在靠近固壁边界时遇到的“物理失真”问题，其背后的物理直觉是相通的：

粒子视角（SPH）：流体会在边界附近形成一层不自然的“致密外壳”，无法顺畅地冲击、飞溅。根源在于，标准SPH在计算流体粒子的密度时，错误地将边界粒子的贡献也一并计算了进去，导致对密度的高估（Overestimation），进而引发了错误的压力排斥力。

网格视角 (网格法)：类似的现象是你的水体会在边界处被“黏住”或被“推开”，难以形成自然的扩散、破裂和飞溅效果。这源于在边界网格上定义的边界条件（Boundary Conditions）变得不精确或不稳定，导致了压力求解的“失真”。

论文的核心解法：将复杂的相互作用一分为二：

流体诱发密度：仅计算来自邻近流体粒子的密度贡献。

边界诱发密度：单独计算来自边界粒子的密度贡献。

在压力求解时，对这两部分交替处理，从而避免力的过高估计，使流体在边界附近的行为更自然。

🔄 迁移性评估：从“粒子”到“网格”的方法迁移
对于你的网格方法，思路不能是直接计算“密度”，而是要借鉴其解耦思想，对边界处的压力求解过程进行“解耦”：

传统耦合方式：在网格上，复杂的边界形状使得真实的流-固交互非常微妙。直接在边界网格上应用一个简单的单一边界条件，无法准确描述这种交互，从而在求解泊松方程时引入了显著误差。

解耦处理方式：将单一边界条件（如 v=0）拆分为两部分效应：一部分负责流体-流体间的相互作用（维持体积），另一部分专项处理流体-固体的相互作用（准确描述滑移和冲击）。

这种处理方法被称为浸入边界法 (Immersed Boundary Method, IBM) 的一种，IBM的基本思想正是将流体域与固体几何“解耦”，通过在流体运动的控制方程（N-S方程）中引入一个附加的力源项，来模拟固体边界对流体施加的作用力。实现IBM的常见方法是在边界处将速度视为Neumann边界条件，这正是你现有求解器支持的边界类型。

也就是说，将这篇论文的核心思想——解耦（Decoupling）——应用于网格框架的最佳路径，并非直接改写SPH公式，而是将其重新解释为一个针对泊松方程边界条件的修正策略。尤其在你使用了混合边界条件（如Neumann）的求解器中，这种“解耦”的思想可以自然地融入对边界条件更精细的处理中。

📝 设计方案：在你的Level Set框架中实现
现在，我们将这个解耦的核心思想转化为具体的实施步骤，将其融入到你现有的 FluidSimulator.ts 框架中。

步骤 1: 数据准备 - 构建“边界影响图” (Boundary Influence Map)
你需要在预处理阶段构建一个与密度图作用相似的“边界影响图”。

目的: 类似于SPH中的“边界密度映射（Density Maps）”，此图用于量化固壁对流体网格的压力影响权重。

实现方式: 创建一个新纹理 boundaryInfluenceTex。对于模拟域的每个网格点，根据其到最近固壁（SolidMaskTex）的距离，计算一个介于 0 到 1 之间的影响权重。

步骤 2: 方程修改 - “解耦”压力求解
这是最关键的一步，修改 solvePressurePCG 或 Jacobi 迭代中的压力求解过程。

目的: 将边界的压力贡献从总的压力计算中分离出来。

两种方案:

方案A (交替求解)：这是对论文算法的直接映射，效果最佳但实现复杂。

流体压力步: 使用标准泊松方程求解，但将边界影响图中的权重作为Neumann边界条件的调制因子。
边界压力步: 单独求解一个修正压力场，该场仅由边界表面“驱动”，用于抵消过度的排斥力。
最终的 curPressureTex 是两个压力场的加权混合。
方案B (边界权重修正)：更易实现，实时性更好，且保留了视觉冲击力。在压力求解时，直接将 solidMaskTex 视为边界交互的“开关”，并引入 restitution 恢复系数来调制切向速度，允许流体在边界产生自然的“滑动”和“飞溅”。

步骤 3: 速度更新 - 应用解耦后的力
根据解耦后的压力场来更新速度场。

目的: 确保最终施加在流体上的加速度是“解耦”后的结果。

实现方式: 在 velocityCorrectMat 着色器中，使用修正后的压力场来计算速度修正量。这能从根本上消除那种“黏滞”感，让流体能够自然地冲击、反弹并在边界扩张。

步骤 4: 物理参数调优 - 释放视觉张力
本步骤可确保新求解器能配合出最佳的视觉表现。

提高回复系数 (Restitution): 将 restitution 调至 0.8 或 0.9，让流体在冲击边界后能保持动能，产生自然的“飞溅”和“反弹”效果。

降低摩擦系数 (Friction): 将 friction 调至0.1或0.0，让流体在边界上能“打滑”，使其更容易铺展开。

启用质量补偿 (enable CreateWater): 在 FluidSimulatorAdapter.ts 中首次爆炸时，将 createWater 设为 true，通过生成新水来补偿因冲击而散失的质量，维持总体积感。

💎 总结与建议
将粒子法（SPH）的“解耦边界处理”论文应用于网格法（你的Level Set框架），本质上是借鉴其“将复杂交互一分为二”的核心思想，将其重新解释为对压力泊松方程边界条件的精细修正。

总体来看，这项方案的理论价值很高，但直接实现的工程量巨大且性能开销高昂。为了让你的方案顺利落地，我强烈建议你按以下路径操作：

首选路线 (最大视觉张力): 走 《方案A：交替求解》 路线。它是论文思想最忠实的映射，理论上效果最惊艳，但适合有深度定制需求的团队。

推荐路线 (工程最优解): 走 《方案B：边界权重修正》 路线。它以最小的修改代价保留了论文“解耦”的思想精髓，并能立即改善边界行为。将开发出来的 boundaryInfluenceTex 等新纹理接入你现有的 wallCollisionMat（墙碰撞）、externalForcesMat（外力计算）等模块，一套强大且可实时运行的“伪解耦”系统就成型了。