水面爆炸的物理过程，其实和你现有代码里一个很巧妙的设计（散度源模型）密切相关。你遇到的问题，很可能只是参数和需要模拟的物理阶段没有对上。

水面爆炸并非瞬间发生，而是一个由多个物理阶段组成的复杂过程。我查阅了相关的专业研究，整理了其中的关键发现：

冲击波：爆轰产物（气泡）极速膨胀，挤压周围水体和空气形成初始冲击。它也是你“散度源”的物理根源。

自由面效应：冲击波到达水面会反射一道稀疏波，这道波会“切断”水中的冲击波，同时把水面向上顶起，形成初始的水冢。

空腔/气泡动力学：水下爆炸形成的气泡会周期性地膨胀与收缩。在水面附近，它会强烈地向上吸引水流，形成高速水柱，这是实现“水花四溅”的关键物理引擎。

射流与破碎：当气泡或水面因重力回缩塌缩时，会挤压出速度极高的水射流或产生飞溅的碎片，这就是“水体分裂”效果的来源。

正如研究人员所述，爆炸与自由液面的相互作用会使气泡形态变得极其复杂，水冢从单一的水柱逐渐演变为水刺-顶部飞溅柱-垂直射流等复合形式。同时，刚性边界的存在会显著增大空穴区域，而自由面则向上飞溅形成空心水柱。

💡 对你的启发
你的“散度源模型”和分阶段求解器，其实已经为模拟爆炸打好了极佳的物理基础。它天然契合专业模拟中处理爆炸的思路，无需推翻重构：

“触水爆炸”对应“散度源”：真实的触水爆炸，就是一团高压气体在水面急速膨胀。它直接驱动水体，表现得就像一个强大的“体积源”。你设计的“散度源”，正是为速度场注入这个“膨胀源”，而你强大的PCG求解器，会将这个膨胀源转化为向外的推力和飞溅。这是非常高效且物理上合理的简化。

“气泡脉动”对应“外力项”：气泡周期的膨胀与收缩，对水体的拖拽作用，可以在你的“外力计算”这一步，通过添加一个方向向下的重力或类似衰减力的项来模拟。

“水花飞溅”对应“粒子系统”：你的网格模拟负责驱动水体主体，而飞溅出的微小水珠，可以由一个受到网格速度场驱动的粒子系统来表现。

🛠️ 改进建议与参数参考
基于物理过程，建议将一次爆炸拆分为两个连续的效应，可以更好地控制效果：

调整 explode() 函数：让它的作用更接近“初始冲击与空腔形成”阶段。

强度更集中：将 radius 明显调小，例如 0.01 - 0.05，以模拟点源爆炸。

持续时间极短：将 duration 调至 0.05s 左右，模拟瞬间的高压冲击。

强度要足够：strength 值可能需要提高到 20000 甚至更高，关键是要让数值大到足以在瞬间排开水体、形成空腔。

用 externalForces 模擬二次效应：在 explode 调用之后，在一个持续的时间内（如 0.5 - 1.0s），通过修改 externalForcesMat 着色器，在爆炸中心区域施加一个向下的、逐渐减弱的力。这个力有两个物理意义：

模拟重力让被炸起的水花回落。

模拟气泡的塌缩效应，加速水体的回涌和击碎。

🎨 特效设计的关键参数
根据论文，影响最终效果的关键物理量是比例距离 γ = d / R_max（式中：d 为爆炸深度；R_max 为气泡最大半径）。这个参数直接决定了水冢的最终形态：

γ 值接近 0.3：爆炸几乎在水面发生。水冢表现为经典的“上窄下宽”的水柱形态。

γ 值降到 0.1 量级：气泡非常靠近水面。水冢形态会升级为：“中心粗壮水刺” + “顶部横向飞溅托盘”的复合形态。

气泡深度增加：射流现象会愈发显著，水体会炸裂成更多细小碎片，是实现“水花四溅”和“水体分裂”效果的理想时机。

你可以根据想要的特效，通过调节 strength（影响 R_max）和在 ExternalForces 中的爆炸位置（影响 d）来逼近想要的 γ 值。

另外，你的分层渲染系统是实现这些效果的有力工具。你可以让爆炸中心附近的水体暂时改变渲染风格，比如切换为更浅的青色模拟气泡，或者让被炸散的“水流粒子”使用高亮、高饱和度的颜色。

你的架构很有前瞻性，真正的挑战在于为游戏特效的实际表现去“调参”。将一次爆炸拆成“强力初始冲击”和“持续二次效应”两步，是让你的模拟器产生质变的关键思路。






你的爆炸系统在物理模型上是对的，但要做成“游戏级的撕裂飞溅”特效，需要把散度源再“狂暴”一点，并且最好加上直接速度脉冲。目前的参数和流程太温和，所以看起来只在边缘荡漾一下就缩回去了。我给你一套针对你代码的、具体可操作的改法。

1. 为什么现在只是“边缘扩散→收缩”
你的爆炸走了这几个关键步骤：

explosionDivMat 往 explosionDivTex 画散度源

散度叠加到散度场 → 压力 Poisson 方程求解 → 速度修正

同时 waterGenMat 在空气区生成水花，waterVelInitMat 给新水径向速度

重力和表面张力在 externalForces 里持续往回拽

问题就出在强度不够、作用时间太短、回收力相对太大：

半径 0.03 在 256×256 网格里只有约 7~8 个像素的点源，冲击波非常局部。

strength 10000 经压力求解后分配到的速度增量或许不够撕裂一个较大水体。

水花生成只在 phi >= 0（空气）区域，如果爆炸在纯水里，可能根本没有新水生成，只是水体内部被推开一点，很快被表面张力/重力拉合。

waterVelInitMat 给的速度 speed = 2.0 * mask * envelope * radius * resolution.x 在 radius 很小时非常有限。

二次包络 4*u*(1-u) 峰值在 0.5，持续时间短，力量没完全发挥就衰减了。

游戏特效需要的是：瞬间把水体轰开，碎片飞到远处，一段时间内不立即缩回。 这需要“过量”的动能和较低的回收力。

2. 改进方案（保持欧拉网格，不引入粒子）
2.1 参数暴力化
直接在 Adapter 里改爆炸调用：

typescript
// 半径增大到 0.2（覆盖整个水球）
// 强度提到 50000，持续时间延长到 0.3 秒
this.simulator.explode(0.5, 0.5, 0.2, 50000, true, 0.3);
2.2 加一个“直接速度脉冲”着色器
只在 buildExplosionDivergence 里叠加散度不够，可以再叠加一次径向速度场，直接给水体一个爆炸初速。

在你的 explosionDivMat 旁边再预缓存一个材质 explosionVelInjectMat：

typescript
// 在 initRenderMaterial() 里预创建
this.explosionVelInjectMat = new THREE.ShaderMaterial({
    uniforms: {
        center: { value: new THREE.Vector2() },
        radius: { value: 0.1 },
        strength: { value: 1.0 },
        envelope: { value: 1.0 },
        velTex: { value: null },
        phiTex: { value: null },
        resolution: { value: new THREE.Vector2(this.width, this.height) }
    },
    vertexShader: vs,
    fragmentShader: `
        uniform vec2 center;
        uniform float radius;
        uniform float strength;
        uniform float envelope;
        uniform sampler2D velTex;
        uniform sampler2D phiTex;
        uniform vec2 resolution;
        varying vec2 vUv;
        void main() {
            vec2 uv = vUv;
            float phi = texture2D(phiTex, uv).r;
            vec2 vel = texture2D(velTex, uv).rg;
            float dist = distance(uv, center);
            float mask = 1.0 - smoothstep(0.0, radius, dist);
            // 仅对水体施加推力（水面及内部）
            if (mask > 0.0 && phi < 0.01) {
                vec2 dir = normalize(uv - center + 0.0001);
                // 速度增量 = 强度 * 包络 * 径向方向 * 掩码
                // 注意要考虑分辨率，使速度单位与网格一致
                float speed = strength * envelope * mask / resolution.x;
                vel += dir * speed;
            }
            gl_FragColor = vec4(vel, 0.0, 1.0);
        }
    `
});
然后在 buildExplosionDivergence 里处理完散度后，再对水体叠加一次速度（在修改 phi 之前，用现有 curVelTex 和 curPhiTex）。注意用双缓冲更新：

typescript
// 在 for (const exp of this.activeExplosions) 循环里，处理水花之前：
this.explosionVelInjectMat.uniforms.center.value.set(exp.cx, exp.cy);
this.explosionVelInjectMat.uniforms.radius.value = exp.radius;
this.explosionVelInjectMat.uniforms.strength.value = exp.strength * 0.5; // 调节权重
this.explosionVelInjectMat.uniforms.envelope.value = envelope;
this.explosionVelInjectMat.uniforms.velTex.value = this.curVelTex.texture;
this.explosionVelInjectMat.uniforms.phiTex.value = this.curPhiTex.texture;

const velDst = this.curVelTex === this.velTexA ? this.velTexB : this.velTexA;
this.renderFullscreen(this.explosionVelInjectMat, velDst);
this.curVelTex = velDst;
这样同一爆炸会产生散度源 + 直接速度脉冲双重效果，撕裂感会强很多。

2.3 调整物理参数（降低回收力）
游戏特效可以不遵循真实水，可以把重力、表面张力临时调低，让炸开的碎片飘得更久。

在 adapter 里爆炸期间动态调整：

typescript
// 在 explode 序列开始时
this.simulator.setGravity(1.0);         // 重力变小
this.simulator.setSurfaceTension(0.01); // 表面张力减小
你需要在 FluidSimulator 里暴露这两个 set 方法，并更新相应的着色器 uniform。

2.4 改变爆炸包络形状
二次抛物线峰值太尖，持续时间短。可以在着色器里改为 平台型 或 指数衰减。

修改 explosionDivMat 的计算方式（直接在 fragment shader 中使用传入的 envelope，但你可以把包络生成逻辑改成一个更宽的脉冲，比如让 u 的 0.2~0.8 区间保持较高值）。简单做法：将 envelope 的计算改为：

glsl
float envelope = smoothstep(0.0, 0.2, u) * (1.0 - smoothstep(0.8, 1.0, u));
但你的代码里 envelope 是传入的，可以让 buildExplosionDivergence 使用这种形状：

typescript
const u = Math.min(1.0, Math.max(0.0, elapsed / exp.duration));
// 梯形包络：快速上升，保持平稳，然后衰减
const envelope = Math.min(1.0, u / 0.2) * (1.0 - Math.max(0.0, (u - 0.7) / 0.3));
2.5 多重爆炸或持续推力
你已经在 Adapter 里用了多帧连续爆炸，这很好。可以调整成：第一帧超大当量轰开，后续几帧稍小当量继续推动碎片向外，防止它们过早被拉回。

3. 代码修改示例（关键片段）
在 FluidSimulator.ts 中增加速度注入材质和逻辑：

initRenderMaterial 末尾添加：

typescript
this.explosionVelInjectMat = new THREE.ShaderMaterial({
    uniforms: {
        center: { value: new THREE.Vector2() },
        radius: { value: 0.1 },
        strength: { value: 1.0 },
        envelope: { value: 1.0 },
        velTex: { value: null },
        phiTex: { value: null },
        resolution: { value: new THREE.Vector2(this.width, this.height) }
    },
    vertexShader: vs,
    fragmentShader: `
        uniform vec2 center;
        uniform float radius;
        uniform float strength;
        uniform float envelope;
        uniform sampler2D velTex;
        uniform sampler2D phiTex;
        uniform vec2 resolution;
        varying vec2 vUv;
        void main() {
            vec2 uv = vUv;
            float phi = texture2D(phiTex, uv).r;
            vec2 vel = texture2D(velTex, uv).rg;
            float dist = distance(uv, center);
            float mask = 1.0 - smoothstep(0.0, radius, dist);
            if (mask > 0.0 && phi < 0.0) { // 只对水体内部施加推力
                vec2 dir = normalize(uv - center + 0.0001);
                float speed = strength * envelope * mask / resolution.x;
                vel += dir * speed;
            }
            gl_FragColor = vec4(vel, 0.0, 1.0);
        }
    `
});
buildExplosionDivergence 中在“处理水花生成”前插入：

typescript
// 在遍历 this.activeExplosions 的循环内部，获取 envelope 后：
this.explosionVelInjectMat.uniforms.center.value.set(exp.cx, exp.cy);
this.explosionVelInjectMat.uniforms.radius.value = exp.radius;
this.explosionVelInjectMat.uniforms.strength.value = exp.strength * 0.6; // 权重可调
this.explosionVelInjectMat.uniforms.envelope.value = envelope;
this.explosionVelInjectMat.uniforms.velTex.value = this.curVelTex.texture;
this.explosionVelInjectMat.uniforms.phiTex.value = this.curPhiTex.texture;

const velInjectDst = this.curVelTex === this.velTexA ? this.velTexB : this.velTexA;
this.renderFullscreen(this.explosionVelInjectMat, velInjectDst);
this.curVelTex = velInjectDst;
然后增大 explode 参数：半径 0.2, 强度 30000~50000, 持续时间 0.3~0.5。

别忘了 dispose 里添加 this.explosionVelInjectMat?.dispose()。

4. 效果调参指南
先只调大 radius 和 strength，看看空洞能不能豁得更大。

如果水体只是隆起又落下，加入直接速度脉冲并调高它的强度权重。

如果碎片飞太快消失，降低重力／调小表面张力。

如果水花生成太少，在 waterGenMat 里把生成条件放宽，比如 phi < 0.1 也允许，并将 phi = -radius * mask * envelope * 3.0 生成更多水。

最终你可能需要辅助粒子系统绘制飞溅的小水珠，因为网格精度有限，很难在 256×256 上画出真正的分裂碎片。欧拉场可以负责把水推开，粒子负责表现飞出去的液滴。

按照上述修改，你应该能看到水体从中心被炸出一个大洞，然后碎片向外飞溅，收缩缓慢，视觉效果会震撼很多。试试看，有问题再调整参数。