// ============================================================
// RefinementConstants —— 精修层与烘焙共享的三无常量（零依赖）
// ============================================================
// 为打破「Refinements ↔ bakeCompute」ESM 循环依赖而抽取：
// 这两个常数被 烘焙像素循环（bakeCompute）与 断崖墙明暗（精修层墙缓冲）
// 共同使用，且必须逐位一致（墙沿同一太阳方向的明暗才对得上烘焙阴影）。
// 放这里 → Bake 与墙两方都单向依赖本模块，依赖图无环，Worker 可安全加载。
// ============================================================

/** 烘焙太阳水平方向（唯一权威来源；墙明暗要与烘焙阴影对齐，勿手抄） */
export const BAKE_SUN = { hx: -0.342, hz: 0.940, tan: 1.0 };

/** 台阶豁免 / 墙生成门槛阈值（米）：落差 < 此值不判墙、不被视为遮挡 */
export const CAST_MIN_DEPTH = 0.5;
