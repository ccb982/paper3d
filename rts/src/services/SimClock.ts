// ============================================================
// SimClock —— 模拟时钟（唯一真源；秒）
// ============================================================
// 玩法时间一律读这里：行动/命令/免降格/撤退/警觉/工事占用/卡死豁免/命令 TTL……
// 随倍速推进（main 每子步写入 simT）——**10× 时全部同步加速 10×**。
// 禁止在玩法计时里用 performance.now()（那是真实墙钟，只留给性能测量/渲染动画）。
// ============================================================

let t = 0;

/** 每子步写入（main.simStep；单位秒） */
export function setSimNow(v: number): void {
  t = v;
}

/** 当前模拟时间（秒） */
export function simNow(): number {
  return t;
}
